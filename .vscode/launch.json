{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Dot Studio — local review (3135)",
      "type": "node-terminal",
      "request": "launch",
      "command": "corepack pnpm exec next dev --webpack --hostname 127.0.0.1 --port 3135",
      "cwd": "${workspaceFolder}"
    }
  ]
}

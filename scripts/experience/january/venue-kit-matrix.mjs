// Read-only authored collateral has no saved/empty/loading/payment lifecycle.
// Its real access guard supplies the additional restricted state.
export const venueKitMatrix = {
  'studio.page.hq-venue-kit': ['default', 'long-content', 'reduced-motion', 'keyboard-only', 'restricted'],
};

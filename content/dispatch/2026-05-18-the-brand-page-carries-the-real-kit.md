## 2026-05-18 · ships · The brand page carries the real kit

**The brand asset library pointed at eighteen older bespoke marks
while the actual exported kit — seventeen outlined-path masters plus
seventy-six renders across wordmark, lockup, dot, app-icon tile, and
four product wordmarks — sat unused. The page now derives its
catalogue from the kit on disk, so it cannot drift from what
shipped.**

Every family grouped, every asset offering its outlined master, a
one-click raster, and a row of size chips, plus a single
megabyte-scale archive of everything. The mobile loader is no longer
a leftover file — it runs live in a phone frame as its own section,
with a spec table and a download. A construction block fixes the
wordmark to its spec.

export function RevealLoadingShowcase() {
  return (
    <section className="reveal-loading-showcase" aria-label="Signal Studio">
      <p className="slx-wordmark">
        <span className="slx-word">signal studio</span>
        <span className="slx-dot" aria-hidden />
      </p>
    </section>
  );
}

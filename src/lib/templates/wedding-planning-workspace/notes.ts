import type { NoteSeed } from "../types";

/**
 * Notes seeds — 0 to 3 named-note prompts.
 * Wedding template seeds one note: the venue site visit, the highest-value
 * capture moment in the wedge demo. PRODUCT.md §7 refusal stands — no
 * date-scaffolded "today template," no recurring journal structure.
 */
export const notes: NoteSeed[] = [
  {
    title: "Venue site visit",
    body: "Decisions taken, questions still open, follow-up owed. Capture the room as you find it — layout, plug points, parking, who needs to know.",
  },
];

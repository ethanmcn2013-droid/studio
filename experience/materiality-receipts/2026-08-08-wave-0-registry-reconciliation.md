# Wave 0 Studio registry reconciliation

The registry had drifted behind eight source files and omitted three real routes. Its deterministic regeneration also refreshed discovery-derived route metadata for nineteen already-registered surfaces whose source hashes did not change. This receipt accepts that current repository description; it does not convert missing evidence into a pass.

- The two `__design-lab/delight` routes remain founder review previews. They are not production alternatives and have no approved baseline.
- `/hq/asset-curator` is a private founder-operator surface and is registered as live with no visual-certification claim.
- The three exported brand artifacts, `/design`, the product-handoff lab, `/hq/assets`, `/hq/entitlements`, and `/hq/financial-model` retain their prior coverage values. `none` remains `none`; `partial` remains `partial`.
- The nineteen `refreshedDerivedMetadata` entries are recorded explicitly in the machine receipt. Their generated route facts replace stale review pointers or generic route classifications; this is registry bookkeeping, not renewed design approval.
- The next capture/certification work must advance coverage with rendered, accessibility, and fixture evidence. A registry hash update alone is never cited as design approval.

The Wave 0 validator checks this receipt against both the prior and current hashes, which makes silent hash acceptance fail closed.

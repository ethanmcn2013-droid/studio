# Final combined January captures

This run reuses the exact compiled m2n artifact from runtime17305479. The build receipt retains its original command/date and links the original bytes. The capture runs on separate loopback4396 with explicit disposable databases and a synthetic HQ session. Earlier2026-09-04 receipts remain unchanged in their original directories.

All four default-state diagram captures passed; `node scripts/experience/january/capture.mjs --atlas` exited 0. `atlas-manifest.json` retains actual rendered Mermaid labels, valid nested list structure, screenshot hashes and desktop/mobile runtime observations. These four supplemental cases do not replace the page's complete extension coverage or the separate Atlas filter component proof.

The generic eight-entry January attestor was also mistakenly invoked with `--atlas`; it refused the nonexistent `capture-manifest.json`. No receipt was renamed and no registry coverage was credited from that failed command. The correct generated file is `atlas-manifest.json`, and this supplemental capture's own completion gate passed. No provider, human comprehension, council or release approval is inferred.

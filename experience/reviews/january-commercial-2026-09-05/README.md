# Final combined January captures

This run reuses the exact compiled m2n artifact from runtime17305479. The build receipt retains its original command/date and links the original bytes. The capture runs on separate loopback4396 with explicit disposable databases and a synthetic HQ session. Earlier2026-09-04 receipts remain unchanged in their original directories.

All 188 required cases passed against the unchanged compiled artifact. The owning `node scripts/experience/january/attest.mjs --write` verifies source, exact state/breakpoint coverage, fixture, interactions and screenshot bytes. All eight owning entries have complete scripted coverage; their review dates, audit findings and approval fields remain unchanged. The capture process's final exit result was unavailable after session completion; its complete persisted manifest and the subsequently successful owning attestor are the evidence, not an invented process receipt.

The fixture initializes the actual additive 0001/0002 migration ledger in disposable local databases. Original 4 September captures remain history. Axe incompletes and canceled local RSC requests remain explicit in the manifest. No provider, human comprehension, council or release approval is inferred.

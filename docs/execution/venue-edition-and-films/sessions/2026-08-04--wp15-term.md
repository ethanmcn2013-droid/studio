# Session wp15-term — 2026-08-04

Append-only record. Do not rewrite a closed session.

**Objective:** WP-15 · R-015 access-term correctness in the app redemption path
**Opened:** 2026-08-03T16:08:42.007Z
**Closed:** 2026-08-04T08:24:08.360Z

## Summary

WP-15 complete. R-015 fixed and proven: the app redemption path applies max(redemption + 548d, wedding + 90d) instead of a flat multiply, the term cannot move earlier, and a 792-case differential test guards against drift from studio. That test skipped itself with no studio checkout present, making it a workstation guard rather than a CI one, so an absent checkout under CI is now a hard failure. Four mutations bite. NOT CLOSED: no wedding date reaches the rule in production - three independent blockers - so every sponsored couple lands on the 548-day floor and the fix is inert until a date is captured.

## Tasks touched

- `E06.01` Define the public artifact content model and what can never be published from the private workspace. — now **blocked**
- `E06.02` Add per-milestone visibility controls for private, title-and-date, image and short-story publication states. — now **founder_review**
- `E06.03` Complete milestone photograph upload, crop, compression, orientation, alt text and deletion. — now **founder_review**
- `E06.04` Complete milestone navigation and the transition from a Timeline point into its photograph and story. — now **founder_review**
- `E06.05` Implement private-link, password-protected and intentionally public sharing modes. — now **founder_review**
- `E06.06` Allow couples to conceal exact wedding dates, locations and other sensitive milestone information. — now **founder_review**
- `E06.07` Implement anonymous aggregate viewer counts without exposing individual viewer behaviour. — now **founder_review**
- `E06.09` Design and implement the intentional vertical mobile Timeline. — now **founder_review**
- `E06.10` Refine the desktop editorial Timeline shown in the Mara and Finn concept. — now **founder_review**
- `E06.11` Implement the post-wedding read-only Keepsake state and the agreed downloadable export. — now **founder_review**
- `E06.12` Complete unpublish, delete, broken-link, performance, accessibility, social-preview and cross-browser QA. — now **blocked**
- `E12.10` Produce the detailed Venue Edition sales deck. — now **founder_review**
- `E12.11` Redesign the Founding Venue certificate as 01/25 through 25/25 with the €1,000 founding rate. — now **founder_review**
- `E12.12` Produce the pre-booking venue sales kit for brochures, proposals, websites and coordinator conversations. — now **founder_review**
- `E12.13` Produce the post-booking couple welcome kit, approved email wording and printable welcome object. — now **founder_review**

## Status changes this session

- `E06.01` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E06.01` ready → blocked (claude_code) — Built, verified and evidenced in Wave 4 (the Timeline allowlist boundary is the best-built privacy control in the programme), but it cannot enter Founder Review: it depends on E03.01 (the role map, still in internal review with critical findings) and E03.06 (public Timeline terms and analytics disclosure, backlog). Both are founder-lane legal and privacy tasks, and 'what can never be published' is exactly the question the role map answers. No dependency was waived because a waiver is the founder's to give and this one is substantive, not procedural.
- `E06.02` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E06.02` ready → in_progress (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E06.02` in_progress → founder_review (claude_code) — Wave 3 delivery. Verified state: PARTIAL (adversarially verified; the verifier's read overrides the build agent's self-assessment). 3 criterion/criteria NOT met — see the packet. Verifier: CONFIRMED. All six acceptance criteria are met by CODE, not by a document describing an intention, and I reproduced every check myself including the fail-before mutation verbatim. The build agent's claims are accurate and, in two places, understated: it recorded freeze semantics as 'read, not proven' when a probe I wrote proves them behaviourally, and it recorded the image/story guard as 'verified by reading what it asserts' when both halves actually fire under mutation. It also reported two gates red that are now green. That is the honest direction to be wrong in. RECOMMEND: move to Founder Review and approve the two shipped states. Do NOT let the ticks read as four states. Before approval,
- `E06.03` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E06.03` ready → in_progress (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E06.03` in_progress → founder_review (claude_code) — Wave 3 delivery. Verified state: PARTIAL (adversarially verified; the verifier's read overrides the build agent's self-assessment). 4 criterion/criteria NOT met — see the packet. Verifier: PUSH BACK. Do not approve, and do not treat the privacy half as finished. The claimed state ('partial') is directionally honest and every one of the five verification claims is TRUE as stated — I re-ran all of them. But the evidence doc's headline sentence, 'the privacy-critical half of E06.03 is built and tested', is REFUTED. The defect sits inside the part claimed complete. The single line that makes C4 true (copy everything from SOS verbatim, so no pixel is touched) is the same line that makes C1 and C7 false: it also copies every byte after the primary EOI. A phone photograph with an embedded secondary image keeps that image's GPS coordinates and capture time, and both defences miss it t
- `E06.04` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E06.04` ready → in_progress (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E06.04` in_progress → founder_review (claude_code) — Wave 3 delivery. Verified state: PARTIAL (adversarially verified; the verifier's read overrides the build agent's self-assessment). 7 criterion/criteria NOT met — see the packet. Verifier: PARTIAL — CONFIRMED AS PARTIAL, WITH DEFECTS THE SUBMISSION DID NOT DISCLOSE. DO NOT SIGN OFF. The builder's own label is right and the code claims survive adversarial checking. Criteria 1, 2, 3 and 4 are met by code I read and re-derived, not by a document describing an intention: ITEM_KEYS really is four keys with attachments and description on a throwing denylist, the keyboard contract really is intact and really was not rebuilt, and reduced motion is honoured in both the JS props and a CSS media block the evidence document does not even claim credit for. The decision not to build a transition into a frame that cannot exist is the correct call and the correct kind of restraint. Declaring 
- `E06.05` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E06.05` ready → in_progress (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E06.05` in_progress → founder_review (claude_code) — Wave 3 delivery. Verified state: PARTIAL (adversarially verified; the verifier's read overrides the build agent's self-assessment). 7 criterion/criteria NOT met — see the packet. Verifier: CONFIRMED as partial, REFUTED as ready. The build agent's self-report is honest — it named criteria 6 and 7 as unmet before I got there, it recorded the share_links plaintext-token qualification against its own interest, and it recorded the missing test as a gap rather than excusing it. Every evidence path exists, every command reproduces, the diff matches the claims exactly. That is the standard this programme has been trying to reach and this package reaches it. But do NOT put this in front of Ethan yet, for one reason: the change makes something worse. `Disallow: /p` without a trailing slash de-indexes /press, /principles and /privacy — three pages the sitemap declares — and puts /pricing
- `E06.06` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E06.06` ready → in_progress (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E06.06` in_progress → founder_review (claude_code) — Wave 3 delivery. Verified state: PARTIAL (adversarially verified; the verifier's read overrides the build agent's self-assessment). 3 criterion/criteria NOT met — see the packet. Verifier: CONFIRMED, with three defects, none of them a false green. Every evidence path exists, the claimed 11/11 reproduces on my own machine, the fail-before mutation reproduces and isolates exactly the two E06.06 assertions, the adjacent 34/34 reproduces, the new test file IS registered in package.json so it runs in CI, the control has a real production caller, and the location refusal holds under an independent grep of both schema files. The build agent's self-reported limits (source-text assertion for criterion 6, no Playwright click, no EXIF claim) are accurate and were not padding. The three defects are: a scope gap where the concealment controls reach /s but not /p, which D-033 ratified as th
- `E06.07` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E06.07` ready → in_progress (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E06.07` in_progress → founder_review (claude_code) — Wave 3 delivery. Verified state: PARTIAL (adversarially verified; the verifier's read overrides the build agent's self-assessment). 3 criterion/criteria NOT met — see the packet. Verifier: CONFIRMED PARTIAL. The build agent's own honest state is correct and I am not downgrading it. I re-ran every command and reproduced every number except the pnpm test assertion count; both fail-before mutations went red exactly as described and both files restored clean. Criteria 1, 2, 4, 6 and 8 are met by CODE, not by a document describing an intention. The privacy work is real: the D-033 defect at artifact-studio.tsx is genuinely gone, the floor is enforced at the value with a passing unit test, the analytics exclusion holds at all three layers, and I could not find a route to GA4 on any of the four couple-facing surfaces. Three things keep it partial, and the third is new. 1. Criterion 7 
- `E06.09` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E06.09` ready → in_progress (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E06.09` in_progress → founder_review (claude_code) — Wave 3 delivery. Verified state: PARTIAL (adversarially verified; the verifier's read overrides the build agent's self-assessment). 8 criterion/criteria NOT met — see the packet. Verifier: REFUTED as complete. The work is honest, precise and better than its claims in places — every line reference resolves, the prior red-and-unregistered state is exactly as reported, and the evidence document volunteers its own limits and two defects it did not fix. Criteria 1, 2, 3 and 6 are met by CODE. Criterion 7 is corroborated by an on-disk attestation I checked field by field, though worktree contention stopped me re-executing it. But criterion 8 is the task's own thesis and it does not hold: (a) design-quality.yml's paths filter omits src/modules/**, so the axe and overflow gate never fires on a change to the artifact itself — the exact defect class the task was written to close, one di
- `E06.10` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E06.10` ready → in_progress (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E06.10` in_progress → founder_review (claude_code) — Wave 3 delivery. Verified state: PARTIAL (adversarially verified; the verifier's read overrides the build agent's self-assessment). 3 criterion/criteria NOT met — see the packet. Verifier: CONFIRMED - partial, and the build agent's own 'partial' is the honest read. Five of seven criteria are fully met by code and reproduced by me; C3 is met on one owner surface and not the other; C7 is met in form but missed a high-severity contradiction sitting in the two files C3 is about. The three headline claims (13/13 contract, 128/128 experience with axe and overflow at four widths, ds:check clean) are all true and I reproduced them independently - the experience run even reproduced the build agent's canonical evidence hash. Do NOT put this to founder approval until R-WP14-01 is answered: the /app gate blocker recorded as fixed is fixed only at the layout, and both of this task's owner 
- `E06.11` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E06.11` ready → in_progress (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E06.11` in_progress → founder_review (claude_code) — Wave 3 delivery. Verified state: PARTIAL (adversarially verified; the verifier's read overrides the build agent's self-assessment). 11 criterion/criteria NOT met — see the packet. Verifier: VERDICT: PARTIAL — the build agent's own label is CORRECT, and the evidence document is the most honest one I have verified on this programme. I did not have to discover the headline gap; §C11 states it outright: 'isPermittedInKeepsake has ZERO production callers. The policy is declared and tested and enforces nothing.' That is true, I confirmed it independently, and a build agent that names its own worst finding earns a different reading than one that does not. What is genuinely CONFIRMED. Every evidence path exists. All three verification commands re-run by me and green, including the full pnpm test suite at exit 0 with the keepsake assertions visibly executing inside the chain — this is N
- `E06.12` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E06.12` ready → blocked (claude_code) — Built and evidenced, blocked behind E06.01 in the same chain: E06.12 depends on E06.01, which depends on E03.01 and E03.06. Unblock by settling the role map.
- `E12.10` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E12.10` ready → in_progress (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E12.10` in_progress → founder_review (claude_code) — Wave 3 delivery. Verified state: PARTIAL (adversarially verified; the verifier's read overrides the build agent's self-assessment). 2 criterion/criteria NOT met — see the packet. Verifier: CONFIRMED — the 'partial' claim stands and is honest. Twelve of the fourteen criteria I verified myself, by re-running the commands rather than reading the record. Two are genuinely short: five of twenty slides show an evidence document rather than a ratified decision in the trace line, and the A4-landscape print rule has never been printed. The evidence document says both of those itself, which is the behaviour this programme has been trying to get. Do not move E12.10 past Founder Review on this evidence, and not because anything is wrong — because a twenty-slide sales deck that no human has looked at cannot be signed off from a test log. The route builds, the guard holds, the words are rig
- `E12.11` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E12.11` ready → in_progress (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E12.11` in_progress → founder_review (claude_code) — Wave 3 delivery. Verified state: PARTIAL (adversarially verified; the verifier's read overrides the build agent's self-assessment). 1 criterion/criteria NOT met — see the packet. Verifier: CONFIRMED. The claim that E12.11 is partial is honest and correct, and it is understated in one direction and correctly stated in the other. All thirteen criteria are met and I re-ran the proof of each rather than reading it. The refusal-first design is real: the failure mode this task exists to remove — a blank number field somebody writes into by hand — has no code path, no render path, and is asserted absent in both directions. The reason it is partial, not complete, is that the defect it FOUND is the one it did not FIX: /hq/partner-card still tells the founder to write the number by hand at signing, on the physical object a venue keeps. Recommend Ethan approve the built artefact and answ
- `E12.12` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E12.12` ready → in_progress (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E12.12` in_progress → founder_review (claude_code) — Wave 3 delivery. Verified state: PARTIAL (adversarially verified; the verifier's read overrides the build agent's self-assessment). 1 criterion/criteria NOT met — see the packet. Verifier: REFUTED as 'complete'. Hold at Founder Review; do not approve as-is. Thirteen of fourteen criteria are CONFIRMED by my own re-runs, and the work is unusually honest - it self-reports its lint failure, names the concurrent stream's diff so a reviewer does not misattribute it, and records defect C-2 against approved copy rather than quietly fixing it. But criterion 5 is its own stated criterion and it is not met in 6 of 10 blocks, and the test labelled with that criterion was written loosely enough to pass all ten. That is the same defect class the programme has already been burned by twice (R-028, a threshold enforced only in a test; E09.11 section 12.5, a check that passed over the thing it 
- `E12.13` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E12.13` ready → in_progress (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E12.13` in_progress → founder_review (claude_code) — Wave 3 delivery. Verified state: PARTIAL (adversarially verified; the verifier's read overrides the build agent's self-assessment). 1 criterion/criteria NOT met — see the packet. Verifier: Move E12.13 to Founder Review, not Done. The build is real and the verification claims survive independent re-execution — I re-ran every one and none was overstated; the module's self-correction on the Keepsake grep and its refusal to describe the wedding-date capture as happening at redemption are the marks of evidence written honestly. Two things to fix before sign-off, both small: (1) the card is labelled A5 and the print stylesheet emits A4 — either set `@page { size: A5 }` for the card page or change the label to A4; (2) either soften the comment at couple-welcome-kit.ts:226 to 'a file, a route or the decision it was read against', or add the file each of the five non-file evidence fiel

## Evidence added

- `E06.01` studio/docs/execution/venue-edition-and-films/evidence/E06.01-public-artifact-content-model.md — The deliverable and its criterion-by-criterion trace. Section 0 separates /p (Tasks, PublicTask, indexable today) from /s (Timeline, AudienceTimelineDto, token-bound and already noindex) and refuses to write a single 'published surface' abstraction over them. Sections 1 and 2 are the field-by-field models. Section 3 is the ownerDisplayLabel decision with its reasoning. Section 4 is R-017 by mechanism with its stated limit. Section 6 pre-agrees the image and short-story model so E06.02 and E06.03
- `E06.01` _wt-wp14/src/modules/timeline/lib/audience-timeline.ts:76-103 — NEW. COUPLE_DISPLAY_LABEL_RE plus isCoupleDisplayLabel and COUPLE_DISPLAY_LABEL_REFUSAL. Digits are refused on couple publications, which blocks a door number, an Eircode, a phone number and a date; @, / and : fall outside the character class so an email and a URL are refused with them. The doc comment states what the rule cannot do.
- `E06.01` _wt-wp14/src/modules/timeline/lib/audience-timeline.ts:178-187 — NEW. The rule enforced inside validateAudienceTimelineDto, so a publication row written before this rule existed throws at the boundary rather than rendering on a public page. class and module publications keep the older rule, so 'Year 3, 2027' still validates.
- `E06.01` _wt-wp14/src/modules/timeline/server/actions/audience-timeline.ts:193-199 — NEW. The same rule at the form, so the couple gets a plain sentence rather than a stack trace. The pre-existing no-@ check is kept for the non-couple kinds.
- `E06.01` _wt-wp14/src/modules/timeline/server/couple-artifact-boundary.test.ts — NEW, registered in package.json. Three E06.01 assertions: the exact top-level key set on a serialised DTO plus twelve leak assertions; five refused label shapes including a postal address with an Eircode, a phone number, an email and a URL; four accepted couple names including Irish fadas, plus the class regression.
- `E06.01` _wt-wp14/package.json:36 — Registration. couple-artifact-boundary.test.ts appended to the final segment of the test script beside unpublished-timeline-boundary.test.ts. Wave 3 found three privacy tests that had never run in CI; this one runs.
- `E06.01` _wt-wp14/src/lib/public-task.test.ts — Existing and already registered. Asserts the PublicTask key set with deepEqual, so adding a field to the projection fails the suite. Recorded as passing existing evidence, not rebuilt.
- `E06.01` evidence/E06.01-public-artifact-content-model.md — EXISTS, read in full (446 lines). Section 0 genuinely separates /p (Tasks, indexable) from /s (Timeline, token-bound, noindex) and refuses a single 'published surface' abstraction. Section 5 records the D-033 standing refusals. No sentence claims legal review, solicitor approval or GDPR compliance; the header states the opposite. C7 MET.
- `E06.01` _wt-wp14/src/modules/timeline/lib/audience-timeline.ts:178-190 — EXISTS. The couple check is inside validateAudienceTimelineDto at :186-188, gated on dto.audienceKind === 'couple'. C2 verified independently: exactKeys at :109-114 throws on FORBIDDEN_KEYS (:57-74) and on any non-allowlisted key, applied at four levels (:150, :193, :203, :214); lines 155-227 construct a fresh object field by field and never spread the candidate. Both mechanisms are real.
- `E06.01` _wt-wp14/src/modules/timeline/server/actions/audience-timeline.ts:190-199 — EXISTS. Form-layer check present, pre-existing no-@ check kept for non-couple kinds. Claimed as :193-199; off by three lines, immaterial.
- `E06.01` _wt-wp14/src/lib/public-task.ts:8-21 — EXISTS. Field-by-field, seven names, description/assignees/externalContactEmail/cents not among them. C4's Tasks mechanism is real.
- `E06.01` _wt-wp14/src/server/db/queries.ts:184-227 — THE GAP. Returns { id, slug, name, activeDomain, publishedAt, tasks }. Only `tasks` passes through toPublicTask. The other five are selected raw off the workspaces row and handed to the public render. Unmodelled in the document, ungoverned by any rule, unpinned by any test.
- `E06.01` _wt-wp14/src/app/p/[slug]/page.tsx:27-58 — ws.name becomes the <title>, the meta description, the OpenGraph title and description, and the Twitter card. generateMetadata returns no `robots` key - grep count 0 - so the D-033 per-page noindex is still absent from /p. The document is honest about this and assigns it to E06.05; robots.ts in this shared tree now disallows /p, which makes section 0's 'still crawlable' line stale in the builder's own favour.
- `E06.01` _wt-wp14/src/components/published/wedding-theme.tsx:44 — {workspace.name} rendered as the hero of the wedding theme on /p. This is the couple's artifact under D-033 and the field is unguarded free text.
- `E06.02` studio/docs/execution/venue-edition-and-films/evidence/E06.02-milestone-visibility-states.md — Criterion-by-criterion. Opens with an honest four-state table: two ship and are proven, two do not exist at any layer and are named as not built.
- `E06.02` _wt-wp14/src/modules/timeline/server/couple-artifact-boundary.test.ts — Four E06.02 assertions, all green. 'a milestone left out of the selection is never copied' publishes one of four fixture milestones and asserts the DTO item titles are exactly [The ceremony] and that the payload does not contain 'Agree the budget with Dad'. 'a hidden milestone is refused at selection' asserts createAudiencePublication rejects with /not available in this workspace/.
- `E06.02` _wt-wp14/src/modules/timeline/server/audience-timeline.ts:159 — The hidden refusal, unchanged and now proven behaviourally rather than read: if (!node || node.hidden) throw new TypeError(...). Publication is opt-in per milestone, capped at 100 items and one project.
- `E06.02` _wt-wp14/src/modules/timeline/lib/audience-timeline.ts:221 — Title-and-date on the payload: if (item.date !== undefined) mapped.date = ... In one resolved DTO, 'Find the dress' has keys exactly publicId/state/title with 'date' in item false, while 'The ceremony' in the same payload has date/publicId/state/title. A genuine omission, not a blank string or a placeholder. This is also the default for a milestone with no target date, which is the more private default.
- `E06.02` _wt-wp14/src/modules/timeline/server/db/timeline-queries.ts:77-99 — The curation layer behind title-and-date: an explicit inherit | date | undated intent persisted through the __signal_timeline_undated__ sentinel, so a null column no longer doubles as both 'inherit' and 'intentionally undated'. Existing, recorded as candidate evidence.
- `E06.02` _wt-wp14/drizzle-timeline/ — The blocker, verified by listing the directory and reading the runner: one file, 0000_timeline_baseline.sql, plus meta. scripts/db/migrate.mjs:465 reads TASKS_DATABASE_URL only. The receipt-backed runner does not reach the Timeline database. No migration was written.
- `E06.02` evidence/E06.02-milestone-visibility-states.md — EXISTS (9435 bytes). Read in full. Criterion-by-criterion, opens with an honest four-state table naming two states as not built at any layer. No claim of legal approval or GDPR compliance. Two factual notes in it are now STALE in the safe direction: it reports npx tsc --noEmit and pnpm ds:check as red in this worktree; I re-ran both and BOTH ARE GREEN (tsc exit 0 with no output, ds-check 'clean - no drift'). A concurrent Wave 3 package fixed next.config.ts since it was written.
- `E06.02` tasks/E06.02.md — EXISTS. Spec written before assessment, criteria match the six given verbatim. Deliverables list ticks four and leaves 'Image publication. Not built.' and 'Short-story publication. Not built.' explicitly unticked. Founder sign-off block is blank - not pre-filled. Header carries Status: Founder Review, which is a written file, not a project-control write.
- `E06.02` _wt-wp14/package.json — REGISTRATION VERIFIED - the CI failure mode does not apply. git diff package.json shows the 'test' script's final segment changed from '... unpublished-timeline-boundary.test.ts' to '... unpublished-timeline-boundary.test.ts src/modules/timeline/server/couple-artifact-boundary.test.ts'. The new suite runs in CI. Combined run: tests 18, pass 18, fail 0.
- `E06.02` _wt-wp14/src/modules/timeline/app/plan/[projectSlug]/_components/curation-surface.tsx:245 — THE HIDDEN CONTROL HAS A REAL PRODUCTION CALLER - I checked, because 'a control with no production caller' is a listed programme failure mode. upsertNodeOverlayAction(..., { hidden: !node.hidden }) is the owner's curation toggle. Defence in depth confirmed at app/audience/page.tsx:60, which filters !node.hidden out of the selection list before it is offered.
- `E06.02` _wt-wp14/src/modules/timeline/lib/audience-timeline.ts:56 — const ITEM_KEYS = new Set(["publicId", "title", "date", "state"]) - four keys, no image, no story. Line 221: if (item.date !== undefined) mapped.date = calendarDate(...). Genuine key omission, confirmed by the test asserting Object.keys(dress).sort() equals exactly [publicId, state, title].
- `E06.02` _wt-wp14/src/modules/timeline/server/audience-timeline.ts:797-809 — Freeze read path verified by reading it: resolveAudienceTimeline selects exactly publicId, title, calendarDate, state, sortOrder from timeline_publication_items and touches no node, overlay, project or task table. The only writers to that table are createAudiencePublication:228, createNotesAudiencePublication:325 (a Notes projection, audienceKind 'class', never milestone-derived), updateAudiencePublicationItem:613 (owner-scoped, title/date/state only) and the unpublish/divergence updates.
- `E06.03` _wt-wp14/src/lib/image/metadata.ts — Pure-TypeScript, zero-dependency JPEG segment and PNG chunk parser. Reads the TIFF stream for orientation (0x0112), the GPS IFD pointer (0x8825) and three date tags, then rebuilds the file without APP1, APP3-APP15, COM, eXIf, tEXt, zTXt, iTXt or tIME. Keeps APP0/JFIF, APP2/ICC and PNG colour chunks. Copies everything from SOS onward verbatim, so no pixel is touched and no image library is needed.
- `E06.03` _wt-wp14/src/lib/image/policy.ts — The numbers as values: PUBLISHED_MAX_LONG_EDGE_PX 2000, PUBLISHED_MIN_LONG_EDGE_PX 800, PUBLISHED_TARGET_BYTES 512000, PUBLISHED_MAX_BYTES 2097152, ALT_TEXT_MAX_LENGTH 200. decidePhotograph refuses 'metadata-present' when handed un-stripped bytes. resolveAltText has no filename fallback.
- `E06.03` _wt-wp14/src/lib/image/metadata.test.ts — 13 tests. Fixtures are built byte by byte in the test (a JPEG with an EXIF APP1 carrying orientation 6, DateTime 2026:08:03 and a GPS IFD, plus a COM comment; a PNG with eXIf and tEXt) so a reader can see exactly what is being claimed rather than trusting a committed binary.
- `E06.03` studio/docs/execution/venue-edition-and-films/evidence/E06.03-photograph-boundary.md — Criterion-by-criterion, with the pre-task audit table, the full unfiltered face-detection grep, and a section naming the five things not built and why.
- `E06.03` _wt-wp14/src/server/actions/attachments.ts:36 — The reason nothing is wired here: this tree still carries the old BLOCKED_MIME_TYPES denylist checked against the client-supplied file.type. E08.07's byte-checked allowlist (src/lib/upload-validation.ts) is on claude/wp07-engineering and does not exist in this worktree.
- `E06.03` studio/docs/execution/venue-edition-and-films/evidence/infrastructure-blob-and-email-auth-2026-08-03.md — Supersedes the recon map's claim that BLOB_READ_WRITE_TOKEN is unprovisioned. A private EU store (fra1, Frankfurt) is connected to app on Production, Preview and Development. A redeploy is still needed before it takes effect.
- `E06.03` _wt-wp14/package.json — REGISTRATION CONFIRMED, and confirmed as part of THIS diff rather than pre-existing. `git diff package.json` shows src/lib/image/metadata.test.ts added to the `node --import tsx --test` segment. Independently corroborated by its assertions appearing at lines 564 and 573 of the full-suite output, so the 'new test file never runs in CI' failure mode does NOT apply here.
- `E06.03` evidence/E06.03-photograph-boundary.md — EXISTS, 304 lines. Unusually honest: pre-task audit table, the full unfiltered grep, §3 reporting the 12-pass/1-fail first run, and §C10 naming five unbuilt things with reasons. It also proactively corrects the recon map on BLOB_READ_WRITE_TOKEN. Its one overreach is the §Headline sentence 'The privacy-critical half of E06.03 is built and tested', which my probes refute.
- `E06.03` evidence/infrastructure-blob-and-email-auth-2026-08-03.md — EXISTS, 97 lines. Supports the stated correction that the blob store is EU (fra1, Frankfurt), private, and that a redeploy is still required. All six referenced evidence paths exist; no dead path in this package.
- `E06.04` _wt-wp14/src/modules/timeline/components/artifact/timeline-artifact.tsx:320 — The defect and the fix. Two of the three sentences used 'journey' as a noun, which the brand voice bans outright, on a sponsored couple's public wedding page: 'This is the next point on the shared journey.' and 'This milestone comes later on the shared journey.' Now 'This one is already behind you.' / 'This one is next.' / 'This one comes later.' Kept rather than deleted, with a source note saying it is the slot a real milestone story lands in once E06.02 and E06.03 give the DTO something to car
- `E06.04` _wt-wp14/src/modules/timeline/lib/audience-timeline.ts:56 — Why the photograph half cannot start: ITEM_KEYS is exactly [publicId, title, date, state], attachments and description are in FORBIDDEN_KEYS, and exactKeys throws for anything outside the set. A transition built now would animate from a milestone to an empty frame.
- `E06.04` _wt-wp14/src/modules/timeline/components/artifact/timeline-artifact.tsx:56 — useArtifactReducedMotion gates Motion's media-query hook behind React's hydration snapshot so SSR and first hydration choose the same motion props. Honoured in the place most implementations miss: focusPoint:431 passes scroll behaviour 'auto' rather than 'smooth' under reduce, because programmatic smooth scrolling is motion too.
- `E06.04` _wt-wp14/src/modules/timeline/components/artifact/timeline-artifact.tsx:434 — The keyboard contract, recorded as passing and not rebuilt: handleKeyDown 435-450 for Left/Right/Home/End/Escape, Enter and Space left to the native button rather than re-implemented, roving tabIndex at :565, aria-expanded :568, aria-controls :569, polite live region :604-608, instructions bound with aria-describedby at :536.
- `E06.04` evidence/E06.04-milestone-navigation.md — Criterion-by-criterion evidence, stating plainly that criterion 5 is unverified in this session and why.
- `E06.04` _wt-wp14/src/modules/timeline/components/artifact/timeline-artifact.tsx:442 — EXISTS AND CONFIRMED, criterion 2 met — at :442, not the :434/:435 cited. handleKeyDown:442-459 handles ArrowRight/ArrowLeft/Home/End/Escape with preventDefault on each. Enter and Space are left to the native <button> onClick at :582-586, not re-implemented. Roving tabIndex at :573, aria-current at :574, aria-pressed :575, aria-expanded :576, aria-controls :577, per-point label with 'Milestone N of M' at :578, aria-describedby on the <ol> at :544 with instruction text at :497-498, polite atomic 
- `E06.04` _wt-wp14/src/lib/public-analytics-boundary.ts:16 — PRIVACY DEFECT FOUND, outside E06.04 but against the decision this wave implements. The file's own docstring states '`/p`, `/s`, `/share` and `/embed` are excluded from GA4 entirely and unconditionally' per R-032 Option A / D-033. Line 16 is `const ANALYTICS_EXCLUDED_ROOTS = ["/p", "/s", "/share"] as const;` — `/embed` is absent. The only other suppressor, isBareArtifactPath (src/lib/bare-artifact-path.ts:2), is `pathname === "/s" || pathname.startsWith("/s/")`. So GoogleTag (src/components/anal
- `E06.04` _wt-wp14/src/modules/timeline/lib/viewer-count.ts — PRIVACY PROBE, NEGATIVE — I could not construct a viewer count that identifies one person through this path. presentViewerCount is first-party, floors at VIEWER_COUNT_FLOOR = 5, coarsens lastViewedAt to month-and-year via Intl, returns pre-rendered strings so no downstream formatter can reconstruct the integer, throws TypeError on a non-integer count, and renders no bound such as 'fewer than five' below the floor (R-027's mitigation says a bound is itself a disclosure). artifact-studio.tsx:57-68
- `E06.04` _wt-wp14/src/app/robots.ts:29 — PRIVACY PROBE, PARTIAL PASS. disallow now reads ["/app", "/p", "/s", "/share", "/redeem", "/welcome", "/api"] — /p added under D-033 R-031 Option B. The comment records the residual honestly rather than hiding it: a Disallow stops an obedient crawler fetching the page, which also stops it reading a per-page noindex, so a URL discovered from an external link can still be listed URL-only, and closing that needs an X-Robots-Tag response header. Stated, with the follow-on named. Not E06.04's, and no
- `E06.05` _wt-wp14/src/app/robots.ts — /p added to the disallow list under D-033. The file carries the interaction that gets discovered late: a Disallow stops an obedient crawler fetching the page, which also stops it reading a per-page noindex, so a URL discovered from an external link can still be listed URL-only. Closing that residual needs an X-Robots-Tag header, which cannot be set per workspace in next.config because the header rules are static and the wedding test needs a database read.
- `E06.05` _wt-wp14/src/components/app/settings/sections/workspace.tsx — A four-line 'Before you publish' panel above the publish button, persistent rather than a dismissable modal so it cannot be clicked past unread. The load-bearing line is 'Your task titles and tags are on the page. Any names you wrote into them are on the page too.' — task titles and tags are exactly where guests' and suppliers' names live.
- `E06.05` _wt-wp14/src/components/app/share/share-button.tsx — The password refusal, shipped where a couple creates a /share link: 'There is no password on this link. Anyone you send it to can open it, and so can anyone they forward it to. Set an expiry, or revoke the link, when you want it to stop working.' The forwarding clause is there because that is the failure mode a couple does not think about.
- `E06.05` _wt-wp14/src/modules/timeline/server/audience-timeline.ts:447 — Private link, recorded as passing and NOT rebuilt. publishAudiencePublication mints a token, stores only the SHA-256 digest, sets every prior active share to 'rotated' in the same transaction and versions the new one; unpublishAudiencePublication at :565 revokes every active share in one transaction.
- `E06.05` _wt-wp14/src/server/db/schema.ts:737-767 — The qualification on criterion 2: share_links.token is a PLAINTEXT primary key resolved by raw equality, with no hash and no constant-time compare, unlike the Timeline path. Wave 3's fix is on claude/wp07-engineering, which is not in this tree, so it was neither assumed nor touched.
- `E06.05` evidence/E06.05-sharing-modes.md — Criterion-by-criterion evidence, including the verified final state showing that neither the search-visibility column nor its migration exists in this branch.
- `E06.05` _wt-wp14/src/app/robots.ts:28 — EXISTS. Line reads `disallow: ["/app", "/p", "/s", "/share", "/redeem", "/welcome", "/api"]` with a 14-line comment recording the Disallow-versus-noindex interaction verbatim as claimed. git diff confirms /p is newly added by this task; /s pre-existed. DEFECT: "/p" without a trailing slash prefix-matches /press, /principles, /privacy — all three real pages, all three in sitemap.ts, none with an Allow rule.
- `E06.05` _wt-wp14/src/components/app/settings/sections/workspace.tsx:614-645 — EXISTS and matches the claim. The four-line panel sits inside the not-yet-published branch of the ternary, above the Publish button, in normal document flow — a persistent panel, not a dismissable modal, exactly as claimed. Line 627 carries the load-bearing sentence about task titles and tags. The toast at :524 changed to mention search engines. git diff shows +42/-14 on this file and nothing else.
- `E06.05` _wt-wp14/src/components/app/share/share-button.tsx:385-393 — EXISTS and matches the claim word for word, directly above the generate button. I checked the claim it depends on: EXPIRY_OPTIONS at :16, expiryDays state at :56, the "Expires in" field at :362 and revokeShareLinkAction at :152 all exist, so "Set an expiry, or revoke the link" points at controls that are actually on this surface. The component has three real production callers (page-header.tsx:90,279 and view-tools.tsx:193) — not orphaned.
- `E06.05` _wt-wp14/src/modules/timeline/app/audience/audience-manager.tsx — NOT IN THE AGENT'S EVIDENCE LIST, and it should have been. This is the /s audience-link creation surface — the other half of the private-link mode per the evidence's own product table. `grep -n password -i` and `grep -n forward -i` both return zero. The refusal sentence is absent here. Publish, rotate, revoke and link-expiry controls are all present (:489-508), so this is unambiguously the surface a couple uses to mint the Timeline link.
- `E06.05` _wt-wp14/src/modules/timeline/server/db/timeline-schema.ts:563-577 — Criterion 2 verified independently, not taken on trust. `tokenHash: text("token_hash").notNull()` at :564 with the comment "SHA-256 hex digest only. The 256-bit raw token is returned once"; `uniqueIndex("uq_audience_shares_token_hash").on(t.tokenHash)` at :577. The timing-safe compare is real: audience-timeline.ts:243-245 implements hashesEqual over node:crypto timingSafeEqual. The agent's PASS on /s holds.
- `E06.05` _wt-wp14/src/server/db/schema.ts:737 and src/server/db/queries.ts:720 — The qualification the agent recorded is real and I reproduced it. schema.ts:737 declares `token: text("token").primaryKey()` with the comment "URL-safe random token, also the primary key" — no hash column. queries.ts:720 resolves it with `eq(shareLinks.token, token)`, raw equality, no constant-time compare. Correctly recorded, correctly not touched (the fix is on claude/wp07-engineering, absent from this tree).
- `E06.05` _wt-wp14/src/app/p/[slug]/page.tsx — Criterion 6 disproof. generateMetadata returns title, description, openGraph and twitter and NO robots key; `grep -rn robots src/app/p/` exits 1. The file header still reads "Server-rendered for indexing" and "a public, indexable, rarely-changing page hit by crawlers and social unfurls" — now in direct contradiction with robots.ts, and left unamended.
- `E06.05` _wt-wp14/src/app/sitemap.ts:19,21,32,34 — Proof of the headline defect's blast radius. /pricing (0.9), /principles (0.8), /privacy (0.4) and /press (0.5) are all declared crawlable here while robots.ts:28 now forbids the /p prefix. Three of the four have no Allow rule to rescue them. The sitemap and robots.txt actively contradict each other on production.
- `E06.05` _wt-wp14/src/components/analytics/google-tag.tsx:28-29 and src/lib/public-analytics-boundary.ts — GA4 attack refuted. GoogleTag calls isAnalyticsExcludedPath(pathname) and returns null BEFORE the enabled check, so the exclusion cannot be re-enabled by an env flag. public-analytics-boundary.ts hard-codes ["/p","/s","/share","/embed"] with exact-or-prefix matching. src/server/public-surface-analytics-contract.test.mjs is registered in package.json and ran in my pnpm test. No second analytics provider (Vercel Analytics, PostHog, Plausible) appears on layout.tsx, /p, /embed or /share. This is E0
- `E06.05` _wt-wp14/src/app/embed/[slug]/page.tsx and scripts/check-frame-headers.mjs — Criterion 7 disproof. The route resolves any slug via getPublishedWorkspaceBySlug with no token and no workspace-type exclusion. check-frame-headers.mjs:63-91 shows the /embed carve-out is still enforced as a REQUIREMENT — global DENY with a negative lookahead excluding /embed, plus an /embed/:path* entry that must set `frame-ancestors *` and must omit X-Frame-Options. `git status --short scripts/check-frame-headers.mjs` returns blank. Nothing was enforced and nothing was updated.
- `E06.05` tasks/E06.05.md:127-135 — EXISTS. Deliverable 4 is honestly checkboxed [ ] NOT BUILT. But the Risks block asserts the shipped confirmation says "The page is not listed unless you turn search listing on" and must not reach a couple — a string that is not in the code. Also note the spec's Status field already reads "Founder Review"; per the shared brief no task reaches Done without Ethan's explicit approval, and the sign-off block is correctly unfilled.
- `E06.06` studio/docs/execution/venue-edition-and-films/evidence/E06.06-conceal-dates-and-details.md — Criterion-by-criterion, including the written refusal on structured location and an explicit scope note that criterion 6 is proven for /s only and that /p's OpenGraph card belongs to E06.05 and E06.12.
- `E06.06` _wt-wp14/src/modules/timeline/server/audience-timeline.ts:650 — NEW. setAudiencePrimaryDate. Re-checks ownsPublication, writes primaryDateLabel and primaryDate together, and treats a missing half as concealment: const conceal = !input.primaryDate || !input.primaryDateLabel. Returns 'set' or 'concealed'. Before this, primaryDate was written once at :219-220 inside createAudiencePublication and read back at :819-826, and no code path updated it - the only way to take the wedding day off a live page was to unpublish the whole artifact.
- `E06.06` _wt-wp14/src/modules/timeline/server/actions/audience-timeline.ts:363 — NEW. updateAudiencePrimaryDateAction. Owner-gated through ownerWorkspace, demo-read-only guarded, intent=conceal clears both columns, revalidates /app/timeline/audience. Success copy: 'The main date is hidden. The shared page no longer carries it.'
- `E06.06` _wt-wp14/src/modules/timeline/app/audience/audience-manager.tsx — NEW owner control in each publication card: a 'The main date' block with a label field, a date field, Save, and a Hide the date button that appears only when a date is set. The caption states what hiding does - the date is removed from the page itself, not just from view, and the couple's own plan keeps it. When there is no main date the card says 'No main date on the shared page' rather than showing nothing.
- `E06.06` _wt-wp14/src/modules/timeline/server/couple-artifact-boundary.test.ts — Four E06.06 assertions, all green. The concealment test publishes with primaryDate 2027-08-14, deliberately a date carried by no milestone in the publication so that 'the string is gone' means the main date and nothing else, then conceals and ROTATES THE LINK so the per-request cache around resolveAudienceTimeline cannot serve the pre-concealment DTO. Asserts 'primaryDate' in dto is false and that neither the date nor the label survives anywhere in the payload.
- `E06.06` _wt-wp14/src/modules/timeline/server/audience-timeline.ts:599 — Existing, recorded not rebuilt: updateAudiencePublicationItem accepts a null calendarDate on an already published item, so per-milestone date concealment on a live page already worked. This is E06.06 criterion 1's mechanism and it is candidate evidence, not new work.
- `E06.06` _wt-wp14/src/app/s/[token]/page.tsx:23-36 — generateMetadata builds title, description and the OpenGraph card from dto.label and dto.audienceKind only - never primaryDate, never ownerDisplayLabel. Asserted by a source contract test with a positive assertion on the noindex posture so a widening is loud. R-024 is satisfied trivially here because no venue value appears in /s metadata at all.
- `E06.06` _wt-wp14/src/modules/timeline/server/db/timeline-schema.ts — The location refusal, verified across both schema files: no location column on workspaces, tasks, node_overlays, timeline_publications or timeline_publication_items. A location on a couple's page is text the couple typed into a title or a label.
- `E06.06` _wt-wp14/src/modules/timeline/app/audience/audience-manager.tsx:390-437 — EXISTS and is a REAL production caller, not a dead control. useActionState(updateAudiencePrimaryDateAction) at :189-190; the 'The main date' block at :390; Save form at :398 with hidden workspaceSlug + publicationId; the separate 'Hide the date' form at :425-431 carries intent=conceal and renders only when both primaryDate and primaryDateLabel are set; 'No main date on the shared page' at :383. Copy is on voice, no em dashes, no exclamation marks.
- `E06.06` _wt-wp14/src/app/robots.ts:29 — CHECKED for the 'noindex claimed but absent' failure mode. /p, /s, /share, /app, /redeem, /welcome, /api are all in the disallow list, and the file's own comment records the residual honestly (a Disallow blocks the fetch that would have read a per-page noindex, so URL-only listing survives; the X-Robots-Tag follow-on is named). This is E06.05's work, already in the tree, and it does not contradict E06.06.
- `E06.06` _wt-wp14/src/components/published/wedding-theme.tsx:125-133 — THE DEFECT. /p's wedding theme renders `t.due` for every published task via formatDue (:204). /p resolves through getPublishedWorkspaceBySlug - the Tasks workspace publish toggle - with no Timeline audience publication involved and no primaryDate concept. E06.06's concealment controls do not reach this surface, and D-033 ratified /p as where the couple's artifact stays.
- `E06.07` _wt-wp14/src/modules/timeline/lib/viewer-count.ts — New. presentViewerCount returns a discriminated union and never a number: below VIEWER_COUNT_FLOOR (5) it returns {state:"withheld", reason:"below_floor", note} with no count key and no timestamp key; at or above it returns a pre-rendered countLabel plus a month-only lastViewedLabel. Floor and count are both validated and throw on malformed input, so no caller can pass underneath the threshold.
- `E06.07` _wt-wp14/src/modules/timeline/lib/viewer-count.test.ts — 10 tests, registered in package.json's tsx Timeline segment. Asserts counts 0-4 serialise byte-identically, that the withheld note contains no digit and no bound phrasing, that a shown presentation carries no numeric property, and that 3 and 4 are withheld so a future edit adopting D-011's venue number goes red.
- `E06.07` _wt-wp14/src/modules/timeline/app/audience/artifact-studio.tsx — The defect D-033 names is gone. It previously rendered qualifiedViewCount.toLocaleString beside displayDate(lastQualifiedViewAt) formatted {day:"numeric",month:"short",year:"numeric"} — 'Timeline views 1 / Last viewed 3 Aug 2026'. Now one stat cell driven by presentViewerCount, with 'Last opened in August 2026' as a caption above the floor and nothing at all below it.
- `E06.07` _wt-wp14/src/modules/timeline/app/audience/audience-manager.tsx — The second render site, which would have been missed: line 353 rendered the raw count in a dense list row. Now calls viewerCountSummary, which returns null below the same floor and the row omits the clause entirely.
- `E06.07` _wt-wp14/src/lib/public-analytics-boundary.ts — New. isAnalyticsExcludedPath matches /p, /s, /share, /embed exactly or as a path prefix, so /pricing and /sitemap.xml are unaffected. Kept separate from isBareArtifactPath, which governs src/proxy.ts and answers a different question — folding them together would have silently changed /p's runtime.
- `E06.07` _wt-wp14/src/components/analytics/google-tag.tsx — The exclusion is evaluated BEFORE the `enabled` prop, so turning GA on in a new environment cannot turn it on for a couple's page. The contract test asserts that ordering, not just the presence of the check.
- `E06.07` _wt-wp14/next.config.ts — noThirdPartyAnalyticsCsp() emits an ENFORCED Content-Security-Policy for /p and /share, and a frame-ancestors * variant for /embed. Deliberately a partial policy (script-src, connect-src, three hardening directives, no default-src) so it can only restrict execution and connection, never rendering. It throws at config load if a Google host survives an edit.
- `E06.07` _wt-wp14/src/server/public-surface-analytics-contract.test.mjs — New, 8 tests, registered in the package.json .mjs segment. Asserts all three analytics layers plus the two no-raw-count invariants, on source text, with the reason stated in its own header.
- `E06.07` evidence/E06.07-aggregate-viewer-counts.md — Criterion-by-criterion evidence including two fail-before mutations and the concurrency note.
- `E06.07` _wt-wp14/src/modules/timeline/server/qualified-view-store.ts — Criterion 2 independently confirmed, not taken on trust. recordQualifiedViewWith writes only (publicationId, sessionHash, createdAt, expiresAt); the token is compared as a hash with a constant-time equality; expired receipts are deleted opportunistically; the aggregate is a counter increment on the publication row. No IP, user agent, referrer or raw token is durably stored. Correctly kept rather than rebuilt.
- `E06.07` _wt-wp14/src/server/db/schema.ts — Criterion 8 checked at the source. share_link_visits.user_agent_hint exists (schema.ts:791, described :779) and IS written on every /share open (src/server/db/queries.ts:591). Nothing renders it — grep returns only schema.ts and queries.ts, matching the evidence claim exactly. It is raised as Decision 3 in the task spec rather than silently left. /p has no counter of any kind. The absence is stated, not implied.
- `E06.09` _wt-wp14/src/modules/timeline/components/artifact/timeline-artifact.module.css:967-1145 — The vertical tier, verified line for line against the audit's claim. progressGeometry becomes width:2px/height:auto (1053-1058); todayMarker becomes width:2rem/height:1px (1079-1085) against the base rule's width:1px/height:2rem (427-433); milestones position by inset-block-start (1104-1107); completedRail display:none and completedRailVertical display:block (1070-1077); hidden labels become opacity:1/pointer-events:auto (1121-1125).
- `E06.09` _wt-wp14/src/modules/timeline/components/artifact/timeline-artifact.module.css:12-13 — container-name: timeline-artifact; container-type: inline-size. The finding the audit did not record: layout is driven by CONTAINER width, which is why the same artifact is correct in the owner panel and the phone preview. The new test collects every @media (…width…) in the file and asserts the list is empty.
- `E06.09` _wt-wp14/src/modules/timeline/components/artifact/timeline-artifact-contract.test.mjs — Was NOT registered in package.json (grep -c returned 0) and was RED when run by hand: 7 pass, 2 fail. Now 13 pass, 0 fail, registered. Four new tests pin the breakpoint identity by reading the shipped CSS rather than a snapshot of it.
- `E06.09` studio/docs/execution/venue-edition-and-films/evidence/E06.09-E06.10-timeline-layouts.md — Criterion-by-criterion for both tasks, with the audit re-verification table and one correction to the audit's desktop line reference (three container tiers, not two).
- `E06.09` _wt-wp14/experience/browser-contract.json — The four viewport projects: mobile 390x844, tablet 768x1024, desktop 1280x900, wide 1440x960. Also records accessMode 'demo' and reducedMotion 'reduce', which bound what the evidence proves.
- `E06.09` _wt-wp14/src/modules/timeline/components/artifact/timeline-artifact.module.css — EXISTS, 1256 lines. Every line reference in the evidence doc resolves exactly. Vertical tier opens 967, closes 1145. progressGeometry width:2px/height:auto 1053-1058. completedRail display:none 1070-1072, completedRailVertical display:block;width:2px 1074-1077. todayMarker width:2rem/height:1px 1079-1085 against base width:1px/height:2rem at 431-432. milestone inset-block-start:var(--timeline-position) 1104-1107. data-labelled false and extra get opacity:1/pointer-events:auto 1121-1125. todayMar
- `E06.09` _wt-wp14/package.json:36 — Registration CONFIRMED in the test script, in the node --test .mjs segment. ci.yml:58 runs pnpm test with NO path filter, so the contract test runs on every PR. C8 is met for the contract test.
- `E06.09` _wt-wp14/.github/workflows/design-quality.yml:5-44 — DEFECT. The paths filter lists src/app, src/components, src/ds, src/lib, src/server, src/proxy.ts, src/env.ts, src/instrumentation*, src/preview, public, next.config, experience, package.json, pnpm-lock, scripts/ds, scripts/experience. It does NOT list src/modules. The artifact lives at src/modules/timeline/components/artifact/. A PR touching only that CSS never triggers pnpm experience:test (line 89), which is where the axe and overflow assertions live.
- `E06.09` _wt-wp14/experience/output/evidence-attestation.json — EXISTS (gitignored via .gitignore:16). Matches the claimed identifiers byte for byte: runId tasks-playwright-5501ae463b99182192baa7cc, rawArtifactSha256 670f570acf683405c5990991b688bce1f526860e4326f6e3fae887e822ee4617, projects mobile/tablet/desktop/wide 32 tests each, testCount 128, passedCount 128, unexpectedCount 0, startedAt 2026-08-03T21:19:09.930Z.
- `E06.09` _wt-wp14/experience/tests/critical-experiences.spec.ts:124-146,373-374 — Mechanism CONFIRMED. expectNoBlockingAccessibilityIssues filters axe to critical+serious and asserts toEqual([]); expectNoDocumentOverflow asserts scrollWidth-clientWidth <= 1. Both are called from auditCurrentSurface for every fixture case, and the config maps one Playwright project per viewport, so both run 4x. tasks.page.s-by-token has 2 cases; app-timeline and app-timeline-audience-by-publication-id 1 each.
- `E06.09` _wt-wp14/src/modules/timeline/components/artifact/timeline-artifact.tsx — Keyboard contract present and UNCHANGED by this package: ArrowLeft/ArrowRight/Home/End, Escape at :455, roving tabIndex at :573, aria-expanded :576, aria-controls :577, aria-live polite at :262/:301/:612. The only diff in this file is E06.04's detailNote copy rewrite (14 lines), which shifted these by 8 and made the evidence doc's line refs (:568-569, :604-607, :447-450) stale.
- `E06.09` _wt-wp14 git diff — Diff matches the claims. Only three files relevant to E06.09 changed: package.json (registration), timeline-artifact-contract.test.mjs (+82/-3), timeline-artifact.tsx (+14/-7, E06.04 copy). timeline-artifact.module.css diff is empty. No layout code was written; the deliverable is evidence and CI registration, which is what the task spec and the audit-first rule say.
- `E06.10` _wt-wp14/src/modules/timeline/components/artifact/timeline-artifact.module.css:622 — @container timeline-artifact (min-width: 980px) — the widest editorial tier, and the only place milestone[data-labelled="extra"] labels become visible. The mid tier is at 953 and the vertical tier at 967, so three tiers, not the two the audit's line reference implied.
- `E06.10` _wt-wp14/src/modules/timeline/app/plan/[projectSlug]/page.tsx:295 — <TimelineArtifact timeline={timeline} embedded showProductHeader={false} /> — a deliberate improvement that stops the artifact's wordmark stacking under the app shell's identity. A literal-string assertion in the contract test made this correct change look like a regression; the assertion now matches on the embedded prop regardless of order and additionally asserts showProductHeader={false} so the improvement is protected.
- `E06.10` _wt-wp14/src/modules/timeline/components/artifact/timeline-artifact.tsx:70 — The second stale assertion: doesNotMatch(/StudioBar/i) was firing on a JSDoc line reading 'the StudioBar already provides identity, so the owner view suppresses it rather than stacking two wordmarks' — the rule being honoured, described. The assertion now runs against a comment-stripped copy. Neither the component nor the CSS was changed.
- `E06.10` _wt-wp14/src/modules/timeline/components/artifact/timeline-artifact.tsx:279-281 — DEFECT, referred not fixed. A 'timeline' wordmark with an accent dot renders above the couple's names on the shared page, against D-011 point 2 ('One line in the footer. No logo, no badge, no powered by in the viewport'). The correct footer line already exists at :678-681. critical-experiences.spec.ts asserts the wordmark's exact text, so a passing test locks in what the ratified decision forbids.
- `E06.10` _wt-wp14/src/modules/timeline/components/artifact/timeline-artifact.tsx:320-324 — DEFECT, referred. detailNote returns 'This is the next point on the shared journey' and 'This milestone comes later on the shared journey'. Front-facing copy on a couple's wedding page, and 'journey' as a noun is on the banned list.
- `E06.10` _wt-wp14/src/modules/timeline/components/artifact/timeline-artifact.module.css:375 — EXISTS. '.stageViewport { ... overflow-x: auto; }' at :375-377 with scrollbar-width: none at :380 and ::-webkit-scrollbar at :384; overridden to 'overflow-x: hidden' at :1039-1041, inside the max-width:620px block that opens at 967 and closes before 1183. C2 MET as written.
- `E06.10` _wt-wp14/package.json — C6 MET, VERIFIED. 'git show HEAD:package.json | grep -c timeline-artifact-contract.test.mjs' returns 0; the working tree returns 1, in the 'node --test ... .mjs' segment. .github/workflows/ci.yml:58 runs 'pnpm test'. So the file went from never running in CI to running in CI. Also checked all eight new test files in this tree - provision-slug, viewer-count, couple-artifact-boundary, timeline-provisioning-contract, public-surface-analytics-contract, published-surface-revalidation, image/metadata,
- `E06.10` _wt-wp14/src/modules/timeline/components/artifact/timeline-artifact.tsx:56-60 — C4 MET. useArtifactReducedMotion returns 'hydrated && Boolean(prefersReducedMotion)'; useHydrated (src/lib/use-hydrated.ts) is useSyncExternalStore with a server snapshot of false, so SSR and first client render both compute reduceMotion=false. All 30 motion call sites read the single wrapper - the contract test counts useReducedMotion() occurrences and fails at two. No conditional subtree swap; only motion props change. Pre-existing code, newly pinned.
- `E06.10` _wt-wp14/experience/output/evidence-attestation.json — C5 MET, REPRODUCED. My own run: testCount 128, passedCount 128, unexpectedCount 0, projects mobile/tablet/desktop/wide at 32 each, runId tasks-playwright-5501ae463b99182192baa7cc, canonicalEvidenceSha256 5501ae46...4a775 - the SAME run id the build agent reported, so the canonical evidence hash reproduced byte-for-byte on an independent run. browser-contract.json projects are 390x844, 768x1024, 1280x900, 1440x960. Every routeCase runs auditCurrentSurface (spec :373-374), which calls expectNoDocu
- `E06.10` _wt-wp14/src/app/app/timeline/audience/[publicationId]/page.tsx:13 — NEW FINDING, not in the evidence. 'await requireAppAccess()' - the allowlist-only gate. Same at src/app/app/timeline/[projectSlug]/page.tsx:11, timeline/page.tsx:13, timeline/audience/page.tsx:14, notes/page.tsx:21, signal/page.tsx:12, signal/onboarding/page.tsx:8, signal/settings/notifications/page.tsx:11, and components/app/app-access-gate.tsx:22. See R-WP14-01.
- `E06.10` _wt-wp14/src/app/p/[slug]/page.tsx:21-58 — NEW FINDING, contradicts D-033. generateMetadata returns title, description, openGraph and twitter - and no 'robots' key. grep for noindex / 'index: false' / X-Robots-Tag across src and next.config.ts shows /embed, /s and /share all set robots index:false; /p sets nothing, and no header rule covers it. See R-WP14-02.
- `E06.11` _wt-wp14/src/lib/keepsake/lifecycle.ts — PLANNING_DAYS_FROM_REDEMPTION 548 and GRACE_DAYS_AFTER_WEDDING 90 as named constants, endsAt = max of the two, a 'basis' discriminator naming which half won, a three-phase model, a twelve-intent read-only write policy with three stated exceptions, and every couple-facing sentence about the term in one function so one test can police all of them.
- `E06.11` _wt-wp14/src/lib/keepsake/export.ts — buildKeepsakeExport returns keepsake.html (one self-contained document, inline CSS, no script/link/img/iframe/src/absolute-URL/@import/url()) and keepsake.json. findExternalReferences is exported so the no-network property is asserted on the produced document, not on a remembered list.
- `E06.11` _wt-wp14/src/lib/keepsake/keepsake.test.ts — 16 tests. The R-015 scenario is driven concretely: books 2027-03-10, marries 2028-09-16, redeems the day they sign; the term lands on 2028-12-15T23:59:59.999Z, strictly later than redemption + 548 days. Under a flat term this couple loses the product on top of their wedding.
- `E06.11` studio/docs/execution/venue-edition-and-films/evidence/E06.11-keepsake.md — Includes the pre-task grep proving Keepsake did not exist (two matches, both comments saying it does not exist), the criterion-by-criterion record, and section 3 on the ds:check failure and how it was resolved without touching the grandfather manifest.
- `E06.11` _wt-wp14/src/ds/tokens.css:48-67 — The export's palette is the actual light-mode token values materialised as rgb() with each one named in a comment, rather than the beige guess it started as. ds:check went from '2 drift failures' to 'clean — no drift' without adding the first entry to a founder-owned exception manifest.
- `E06.11` _wt-wp14/package.json:36 — CONFIRMED REGISTERED. `src/lib/keepsake/keepsake.test.ts` appears in the `test` script's third `node --import tsx --test` group. NOT the unregistered-test-file failure mode. I additionally confirmed it EXECUTES inside the chain, not just that the path is present — see the verification field.
- `E06.11` evidence/E06.11-keepsake.md — EXISTS, 314 lines, read in full. §0 pre-task grep verified by me independently: `grep -rni keepsake src/ drizzle/ scripts/` returns only billing.tsx:101-102 (two comments saying it does not exist) plus a robots.ts comment this branch added. §3's ds:check narrative verified. §C11 is unusually candid and names the zero-caller gap itself. ONE INACCURACY FOUND: §C9 overstates the export test's banned-word coverage by four terms.
- `E06.11` _wt-wp14 (git) — Branch claude/wp14-timeline, HEAD 88e8c5a on top of 5717b75 (WP-08 E05) as stated. 36 working-tree entries; the three keepsake files are untracked additions and package.json is the only diff attributable to this package (test-script registration). No GENERATED file touched — templates.generated.ts is absent from the diff; only the source src/lib/templates.ts is modified, by other tasks in the branch. Tree left exactly as found (36 entries before and after); both probe scripts removed.
- `E06.11` _wt-wp14/src/components/app/settings/sections/billing.tsx:109 — THE COUNTER-EVIDENCE. The only couple-facing sentence about the access term that actually ships lives here, not in keepsakeCopy, and no test file references it. This is what breaks the 'one function, one test' claim.
- `E06.11` _wt-wp14/src/components/redeem/redeem-result-card.tsx:89 — COUNTER-EVIDENCE for C1 at product level: renders 'until {expiresLabel}' from the minted expiresAt, which is still the flat 548-day value while R-015 is open.
- `E06.12` _wt-wp14/src/server/actions/settings.ts — DEFECT FOUND AND FIXED. /p/[slug] is ISR with revalidate = 60. publishWorkspaceAction and unpublishWorkspaceAction both called revalidatePath('/p/{slug}'); deleteWorkspaceAction did not. A couple who deleted a published wedding workspace kept serving its cached public page — task titles and tags, which carry guests' and suppliers' names — for up to sixty seconds after the delete, with no row left to explain it. Fixed by reading the slug before the delete transaction and revalidating after it.
- `E06.12` _wt-wp14/src/server/published-surface-revalidation.test.mjs — 3 tests, registered in package.json. Asserts publish, unpublish and delete all revalidate /p/{slug}, that delete reads the slug BEFORE the row is deleted, and that /p is still ISR (so the test is measuring something). Asserts against source text on purpose and says so in its own header, because the defect was a missing call and only the wiring can catch a missing call.
- `E06.12` _wt-wp14/src/app/p/[slug]/page.tsx:21-58 — DEFECT, reported not fixed. For a wedding workspace the OpenGraph unfurl reads 'Mara and Finn, Wedding planner workspace' / 'Mara and Finn, a real wedding planner workspace made public on Tasks. Same items, four lenses: board, list, timeline, calendar.' That is product marketing on the couple's WhatsApp unfurl (D-011 point 2); 'made public' will be false once D-033's noindex default lands; and 'Wedding planner' is an internal domain label describing the couple as a planner. Not fixed because thi
- `E06.12` _wt-wp14/src/app/s/[token]/not-found.tsx — Broken-link copy verified correct on the point that matters: it names four possibilities (rotated, revoked, unpublished, ended) and commits to none, so a stranger holding a guessed token learns nothing about whether a timeline exists behind it.
- `E06.12` studio/docs/execution/venue-edition-and-films/evidence/E06.12-shared-timeline-qa.md — Full record. Leads with its two limits rather than burying them: this worktree is shared with a concurrent package so every suite result belongs to the tree and not to one task, and two of the seven named areas were not tested at all.
- `E06.12` _wt-wp14/experience/playwright.config.ts — browserName: 'chromium' on every project. Four viewport sizes, one engine. This is why cross-browser is reported as untested rather than as a pass, and it matters: the artifact leans on container queries, logical inset properties and color-mix, and most wedding guests use Safari.
- `E06.12` _wt-wp14/experience/tests/critical-experiences.spec.ts:308-315 — THE REFUTATION. `if (caseName === "invalid bearer link") { /* Next's streamed notFound boundary is intentionally a soft 404. */ expect(response.status()).toBe(200); }`. The evidence doc cites this very case to prove /s returns 'a real 404'. It proves the opposite.
- `E06.12` _wt-wp14/experience/critical-fixtures.json — expectedStatus audit: /share/review-missing = 404 (real 404, criterion met on that surface); /s/invalid = unset, defaults 200. No fixture anywhere mentions 'embed' — /embed/[slug] has no rendered coverage at any viewport.
- `E06.12` _wt-wp14/src/app/embed/[slug]/page.tsx — Confirms /embed is a published surface: same getPublishedWorkspaceBySlug, same ws.tasks DTO, `dynamic = "force-dynamic"` (so no ISR window defect there), metadata robots index:false. Unmeasured and unnamed by E06.12.
- `E06.12` _wt-wp14/src/modules/timeline/server/audience-timeline.ts:565-597 — Criterion 1 CONFIRMED by reading AND behaviourally. One db.transaction sets state 'unpublished', stamps items, and updates every audience_shares row with state 'active' to 'revoked'. Same transaction, as claimed.
- `E06.12` _wt-wp14/experience/output/evidence-attestation.json — Corroborates the Playwright claim without reproducing it. runId tasks-playwright-5501ae463b99182192baa7cc and rawArtifactSha256 670f570acf683405c5990991b688bce1f526860e4326f6e3fae887e822ee4617 match the claim character-for-character; testCount 128, passedCount 128, unexpectedCount 0, 4 projects x 32; startedAt 2026-08-03T21:19:09.930Z, file written 22:28:02 local — consistent with a 8.9m run. attest-playwright.mjs:96-108 throws unless every outcome is green AND the case/project set exactly match
- `E06.12` _wt-wp14/experience/evidence-runs/tasks-playwright-5501ae463b99182192baa7cc-1bce2067f1691fd8.json — CAVEAT ON THE RUN ID. The same runId already existed with startedAt 2026-07-31T11:03:33.708Z. runId is the CANONICAL digest of outcomes plus input hashes and deliberately excludes timestamps, so it certifies greenness, not recency — and it shows the Playwright suite is insensitive to this package's changes. The evidence doc concedes this for the delete fix; it is true of the run identity generally.
- `E06.12` _wt-wp14/src/lib/public-analytics-boundary.ts + src/components/analytics/google-tag.tsx:29 — PRIVACY PROBE, could not break it. isAnalyticsExcludedPath is the FIRST statement in GoogleTag, before the `enabled` check, covering /p /s /share /embed by exact match or prefix. next.config.ts adds analytics-free CSP header sets on /p/:path*, /share/:path*, /s/:path* and /embed/:path*. I found no path that loads GA4 on a couple-facing surface. (Concurrent package's work, not E06.12's.)
- `E06.12` _wt-wp14/src/modules/timeline/lib/viewer-count.ts — PRIVACY PROBE, could not build an identifying count. Floor 5; below it state 'withheld' with a note carrying no number and no bound; above it lastViewedAt is coarsened to month-and-year only. Callers receive pre-rendered strings, never the integer. artifact-studio.tsx:56-69 now consumes this, so the audit's 'Timeline views 1 / Last viewed 3 Aug 2026' defect is closed in this tree. (Concurrent package's work, not E06.12's.)
- `E06.12` _wt-wp14/src/app/robots.ts — D-033 noindex check: /p IS now in the disallow list. But /p/[slug] generateMetadata sets no robots directive at all, so per-page noindex is absent. robots.ts's own comment states this honestly ('Both controls are needed; this one alone is not enough') and defers the X-Robots-Tag residual to E06.05. Belongs to the concurrent R-031 package, not E06.12 — recorded so it is not lost.
- `E06.12` _wt-wp14/src/lib/domains.ts:366-369 — Confirms the /p unfurl defect the evidence reports is real and accurately quoted: wedding label is literally 'Wedding planner', so generateMetadata yields 'Mara and Finn, Wedding planner workspace' and '...a real wedding planner workspace made public on Tasks. Same items, four lenses: board, list, timeline, calendar.' The D-011 point 2 report is correct.
- `E12.10` _wt-wp16/src/lib/venue-sales-deck.ts — Twenty slides as data. Imports COMMERCIAL_SUMMARY_ROWS, COUPLE_TERM_SENTENCE, ENTITLEMENT_SENTENCE, FOUNDING_MEANING, OFFER_PARAGRAPH, PRICE_FORMS, PRIVACY_BLOCK, SCALE_SENTENCE, STANDARD_RATE_SENTENCE, SURVIVAL_SENTENCE, TEAM_ASK_SENTENCE from venue-invitation/copy.ts. No second copy of any price.
- `E12.10` _wt-wp16/src/lib/venue-sales-deck.test.ts — 21 assertions, all pass. Includes the ladder ordering test (price index > what-you-never-see, how-long and at-launch), the VAT test over rendered prose, and the sweep run against both the filled and the unfilled deck.
- `E12.10` _wt-wp16/src/app/hq/venue-deck/page.tsx — Gated route, ?venue= fills the one slot, robots noindex on its own account. Present in the build route table as dynamic.
- `E12.10` public/brand/collateral/venue/venue-deck-screen.pdf via /hq/venue-kit:20 — AUDIT FINDING. The only pre-existing venue deck. Its own caption reads 'the demo → €1,500 → ... → the pilot ask'. Wrong on the founding price, the retired programme term and an ask the ratified offer does not contain. Recorded as defect C-3, not deleted.
- `E12.10` evidence/E12.10-E12.13-verification-run.txt sections 1, 4, 9 — pnpm exec tsx --test src/lib/venue-sales-deck.test.ts: 21 tests, 21 pass, exit 0. pnpm run build: exit 0 with /hq/venue-deck in the table (after two exit-1 attempts caused by a concurrent session holding the next build lock, recorded rather than re-run quietly).
- `E12.10` _wt-wp16/src/components/venue/venue-sales-deck.css — EXISTS. @media print carries @page { size: A4 landscape; margin: 12mm } and .venue-deck-slide { break-after: page; break-inside: avoid }, with :last-child exempt. The rule is correct as written. It has never been printed or measured, so the print half of criterion 1 is asserted, not proved.
- `E12.10` _wt-wp16/public/brand/collateral/venue/venue-deck-screen.pdf — EXISTS, 210,525 bytes. I read the linking caption myself at src/app/hq/venue-kit/page.tsx:20. It currently reads 'Cover -> the 19,898 problem -> four views -> the couple -> the team -> the demo -> €1,500 -> the Founding 25 -> the standard -> the pilot ask.' The audit finding is real and is recorded as defect C-3. See risksFound for what the fix left behind.
- `E12.10` deck probe run in C:/Users/ethan/signal-studio-workspace/_wt-wp16 (tsx over the built deck) — MY OWN MEASUREMENT, not the builder's. slides: 20. em-dash present: false. exclamation present: false. price lines: 4, and all four carry 'including VAT at the prevailing rate' verbatim (€1,000 founding x2, €1,500 standard x2). bare 18-month lines: 0 — the access term always travels as '18 months from when they open it, or 3 months past the wedding, whichever is later'. Criteria 3 and 9 CONFIRMED.
- `E12.10` _wt-wp16/scripts/check-venue-copy.mjs — EXISTS, registered in the package.json 'test' chain. I ran it: exit 0, '16 rules, 92 patterns, 29 surfaces swept'. I then enumerated its 29 surfaces from prohibited-claims.v1.json: NOT ONE of them is a WP-16 artefact. venue-sales-deck.ts, venue-sales-deck.tsx and hq/venue-deck/page.tsx are all outside it. See risksFound R1.
- `E12.10` _wt-wp16/src/lib/hq/rooms.ts:158 and experience/registry.json:6014 — Both registrations present and read directly. I re-ran pnpm exec tsx --test src/lib/hq/rooms.test.ts: 6 pass, exit 0. I re-ran pnpm run experience:validate -- --product=studio: exit 1 with 8 failures. I checked all 8 by name — two __design-lab/delight routes, hq-venue-proposal, v/[token], v/[token] not-found, venues-privacy, venues-questions, venues-what-you-see. None is a WP-16 route. Criterion 13 CONFIRMED: this work adds zero failures.
- `E12.10` _wt-wp16/package.json — git diff confirms 'src/lib/venue-sales-deck.test.ts' was added to the 'test' script, and 'node scripts/check-venue-copy.mjs' added to the chain. I ran pnpm test end to end: exit 0. Criterion 14 CONFIRMED.
- `E12.10` app/src (Keepsake and export claims, verified against the app repo) — Slide 18 says the Keepsake is not built, adoption figures are not built, and there is no venue login. I checked each. grep -rni keepsake app/src returns exactly one hit, a code comment in timeline-artifact.tsx:800 using the ordinary English word — so there is genuinely no Keepsake mode, route or component. app/src/app/api/account/export/route.ts EXISTS, so the deck's 'what exists today is an account-level export' is true. R-043 independently confirms there is no venue-authenticated route. Criter
- `E12.11` _wt-wp16/src/lib/founding-certificate.ts — buildFoundingCertificate returns {state:'refused', reason} for empty input, missing cleared-payment date, non-founding plan, and any number outside 01-25. It delegates to foundingNumberRefusal in venue-founding-number.ts (E02.13) rather than restating the rules. There is no code path producing a blank number.
- `E12.11` _wt-wp16/src/lib/founding-certificate.test.ts — 19 assertions, all pass. Includes 'there is no code path that renders a blank number' (asserts the refused prose contains neither '__' nor '/25'), a nine-word permanence ban, and the sweep run over every refusal reason as well as the issued certificate.
- `E12.11` src/app/hq/partner-card/page.tsx (defect C-1) — AUDIT FINDING, recorded not fixed. The existing physical exploration states 'the number is still written by hand, in front of the venue, at signing' and 'the number field stays blank until a real venue signs'. Both are assignment at signature, which D-009.6 forbids. A concurrent WP-16 stream corrected that page's programme wording on the same day; the assignment-event defect is a different thing and is untouched by that sweep.
- `E12.11` _wt-wp16/src/components/venue/founding-certificate-sheet.tsx — Renders the refusal with a visual treatment rather than an empty frame, because a blank number field is the failure mode this task exists to remove.
- `E12.11` evidence/E12.10-E12.13-verification-run.txt sections 1, 9 — pnpm exec tsx --test src/lib/founding-certificate.test.ts: 19 tests, 19 pass, exit 0.
- `E12.11` _wt-wp16/src/app/hq/partner-card/page.tsx — DEFECT C-1 CONFIRMED AND STILL PRESENT. Line 82: 'the number field stays blank until a real venue signs.' Line 84: 'The number is still written by hand, in front of the venue, at signing.' Line 25: 'The number on the front is still written by hand at signing.' All three are assignment at signature, which D-009.6 forbids. The evidence entry's account is exactly accurate: the concurrent stream DID correct the programme wording (title is now 'The Founding 25 Card', eyebrow 'Signal HQ · The Founding
- `E12.11` VERIFIER RE-RUN: pnpm exec tsx --test src/lib/founding-certificate.test.ts — 19 tests, 19 pass, 0 fail, exit 0. Claim confirmed exactly.
- `E12.11` VERIFIER RE-RUN: pnpm run typecheck — tsc --noEmit, exit 0. Claim confirmed.
- `E12.11` VERIFIER RE-RUN: pnpm test — exit 0. Full suite green. I confirmed by reading the diff of package.json that `src/lib/founding-certificate.test.ts` and `node scripts/check-venue-copy.mjs` are both registered inside the `test` script, so CI runs them. Criterion 13 confirmed.
- `E12.11` VERIFIER RE-RUN: pnpm run build — exit 0. I then read .next/server/app-paths-manifest.json directly: "/hq/founding-certificate/page" is present. Note the build leaves a stale .next/lock that blocks the next build with 'Another next build process is already running'; I removed it. The evidence file already records the same lock trap at its section on attempts 1-3.
- `E12.11` VERIFIER RE-RUN: pnpm run experience:validate -- --product=studio — exit 1, 8 failures. I read all eight: two __design-lab/delight routes, hq-venue-proposal, v/[token], v/[token] not-found, venues-privacy, venues-questions, venues-what-you-see. `studio.page.hq-founding-certificate` is NOT among them. The route IS registered — src/lib/hq/rooms.ts:192-193 and experience/registry.json:4801-4806. Criterion 12 confirmed: both registries, zero new failures.
- `E12.11` VERIFIER RE-RUN: pnpm run lint — exit 1. ONE error: src/app/hq/account-review/account-review.tsx:86 react-hooks/set-state-in-effect. NOT attributable to E12.11 — that file is unmodified in git status, last touched by commit 90908f3, and `git diff 16df762 -- src/app/hq/account-review/` is empty, so it is inherited from the stacked WP-13 base. The evidence document discloses this failure honestly rather than omitting it.
- `E12.11` VERIFIER BANNED-TERM SWEEP over E12.11's touched files — I wrote and ran my own sweep (19 rules: partner, member, investor, exclusive, guaranteed, certified, for life, forever, in perpetuity, lifetime, countdown, plus/ex VAT, seat count, allotment, GDPR compliant, solicitor, accountant). 10 hits total across founding-certificate.ts, .test.ts, the sheet, the CSS, the route and venue-copy-refusals.ts. EVERY ONE is the ban list existing rather than a use: three in the ban-list definition comment, three in the regex array itself, three quoting the permane
- `E12.11` VERIFIER BANNED-TERM SWEEP over the whole of public/brand and src/app — 148 raw hits, NONE introduced by E12.11. 'founding partner' now survives in ZERO venue-facing HTML or TSX — the market-entry deck and src/app/design/page.tsx are clean, which is R-042's two headline surfaces cleared. Surviving occurrences are CHANGELOG.md, docs/content-truth-audit.md, the RAID entry, the prohibited-claims JSON defining the ban, and the test asserting absence. The remaining src/app 'partner' hits are unrelated (school design-partner pilot, partnership conversations, a 'Northline 
- `E12.11` VERIFIER PLANTED-VIOLATION TEST 1 — the standing repository check — I appended `The founding partner variant, locked in for life, guaranteed.` to public/brand/market-entry-deck-2026.html (pinned at 0) and ran `node scripts/check-venue-copy.mjs`. IT FAILED, exit 1, naming all three: [p1-permanence] "for life", [p1-permanence] "guaranteed", [d033-programme-terms] "partner". I restored the file and re-ran: exit 0. The check is real, it catches the retired term, and it is registered in `pnpm test` which is a blocking CI step.
- `E12.11` VERIFIER PLANTED-VIOLATION TEST 2 — the certificate's own test — I replaced the lock-condition line in TERMS_LINES with `Your founding partner rate is locked in for life, guaranteed forever.` and re-ran the suite. IT FAILED, exit 1, 4 of 19 tests red: the VAT/lock test, the permanence test, the sweep test, and the Founding-25-naming test. I restored the file and re-ran: 19/19, exit 0. Confirmed: this suite does not pass on a planted violation.
- `E12.11` RATIFIED-CLAIM CHECK against the certificate's actual strings — Both prices VAT-INCLUSIVE and in one string: 'The founding rate is €1,000 a year, prepaid, including VAT at the prevailing rate. The standard rate is €1,500 a year on the same basis.' Lock on CONTINUOUS RENEWAL: 'holds for as long as the agreement renews continuously without lapse.' Change of control present in the same block. 01/25 on CLEARED PAYMENT: the statement reads 'assigned when the first payment cleared', the record label is 'Payment cleared', the refusal reads 'never on signature'. NO 
- `E12.11` UNBUILT-CAPABILITY CHECK on E12.11's artefact — The certificate promises nothing the product does not have. It makes exactly one claim of fact (a place was assigned when payment cleared), states the rate with its conditions, and says outright 'This certificate records an event. It is not the agreement, and it does not grant access on its own.' The number is backed by real code, not a document: `pnpm test` ran assignFoundingNumber's 38 assertions green, including 'refuses before the payment clears', 'refuses a venue that is not on the founding
- `E12.11` GIT DIFF CHECK — The diff matches the claims. E12.11 ships five new untracked files (founding-certificate.ts, .test.ts, founding-certificate-sheet.tsx, founding-certificate.css, app/hq/founding-certificate/page.tsx) plus registry edits in src/lib/hq/rooms.ts and experience/registry.json and the test registration in package.json. Nothing outside the declared column. C:/Users/ethan/signal-studio-workspace/studio was not touched by me and no project-control.mjs write command was run. My two planted violations were 
- `E12.12` _wt-wp16/src/lib/venue-prebooking-kit.ts — Five channels, three lengths each, a per-channel never-list, KIT_TEAM_NOTES carrying the launch-scope sentence verbatim, and MARKETING_UNDERTAKING as R-020's mitigation in plain words. PROHIBITED_SHEET points at E09.11-venue-copy-pack.md section 4 and prohibited-claims.v1.json rather than reprinting either.
- `E12.12` _wt-wp16/src/lib/venue-prebooking-kit.test.ts — 16 assertions, all pass. The couple-facing sweep refuses currency, 'free', 'complimentary', 'worth', 'sponsored', 'discount', and separately refuses 'export', 'download', 'read-only', 'keepsake' and 'archive'.
- `E12.12` evidence/E09.11-venue-copy-pack.md section 1, third sentence (defect C-2) — AUDIT FINDING, recorded not fixed. The approved pack tells a venue to say today: 'After the wedding it stays open, read-only, and they can download everything in one click.' E09.11 section 7 sets the rule that forbids it, and none of it is built. This is R-020's exact shape: an approved sentence, repeated by a venue, folding into the venue's own contract with the couple. Replacement supplied and used in this kit.
- `E12.12` _wt-wp16/src/lib/venue-copy-refusals.ts — The refusal set once, 49 absolute expressions plus the conditional P7 bare-term rule, shared by all four artefacts. Counted at runtime, not by eye. The neverHere lists are excluded structurally rather than by a citation heuristic, because a heuristic of that kind once suppressed two real defects on the live venues page (E09.11 section 12.5).
- `E12.12` evidence/E12.10-E12.13-verification-run.txt sections 1, 9 — pnpm exec tsx --test src/lib/venue-prebooking-kit.test.ts: 16 tests, 16 pass, exit 0.
- `E12.12` BANNED-TERM SWEEP I ran myself over public/brand and src/app (all .html/.tsx/.ts/.txt/.md/.json/.css) — 'founding partner': ZERO hits. D-033's retirement holds across both trees for the compound term. Residual single-word hits are all other contexts and none is a Founding 25 programme term: partner 19 (school design-partner pilot, motion brief, lender-pack cohorts, 'Partnership conversations' on /contact, internal HQ route names), investor 7 (all internal HQ, meaning actual lenders), seat 20 (includes the known pre-existing venues/page.tsx:469 'No seats', pinned defect owned by E12.04), 'ex VAT' 1
- `E12.12` _wt-wp16/public/brand/collateral/social/alt-text.txt lines 26-33 — ONE SURVIVING OCCURRENCE, disclosed not hidden, and NOT E12.12's. The four s4-founding-sp01 PNGs were renamed from s4-partner-sp01 but their PIXELS still read 'Founding Limerick Partner' and 'Glen House' rather than Glenmara House. The file says so and says 'Do not publish S.4 until the images are re-authored.' Belongs to the concurrent WP-16 R-042 stream, not this task. It does not refute E12.12, which never claimed the repository-wide sweep.
- `E12.12` _wt-wp16/scripts/check-venue-copy.mjs and package.json — REGISTERED AND IT WORKS. package.json 'test' runs it fifth in the chain, and .github/workflows/ci.yml:53 runs 'pnpm test'. I ran it: exit 0, 16 rules, 92 patterns, 29 surfaces, 30 pinned hits, 21 citations, 6 exceptions. I then PLANTED '> Your founding partner place is guaranteed forever.' into the enforced surface E09.11-venue-copy-pack.md: exit 1, 3 hits reported (p1-permanence forever, p1-permanence guaranteed, d033-programme-terms partner), pinned at 0, owner named. Restored; re-run exit 0 w
- `E12.12` MY RE-RUNS: pnpm exec tsx --test src/lib/venue-prebooking-kit.test.ts -> 16/16, exit 0. pnpm run typecheck -> exit 0. pnpm test -> exit 0 (38 tests in the final suite, 0 fail). pnpm run build -> exit 0, 'Compiled successfully in 14.7s', 51/51 static pages, and the route table carries 'f /hq/venue-prebooking-kit'. pnpm run experience:validate -- --product=studio -> exit 1, 8 failures. — Every claimed command reproduces exactly. The 8 experience failures are: two __design-lab/delight routes, hq-venue-proposal, v/[token], v/[token] not-found, venues-privacy, venues-questions (all unregistered discoveries from the stacked WP-13 base) and venues-what-you-see (changed coverage). NONE names venue-prebooking-kit. E12.12 adds no new failure.
- `E12.12` pnpm run lint -> EXIT 1 (I ran it; the claimed verification omitted it, the evidence document does not) — One error: src/app/hq/account-review/account-review.tsx:86 react-hooks/set-state-in-effect. That file is not in git status and is untouched by WP-16, so the failure is pre-existing on the stacked branch. The evidence document section 7.2 states this correctly and notes CI runs lint as informational and non-blocking. Reported because it is red.
- `E12.12` _wt-wp16/experience/registry.json and src/lib/hq/rooms.ts:175-176 — Registered in BOTH. registry.json carries studio.page.hq-venue-prebooking-kit -> /hq/venue-prebooking-kit -> studio/src/app/hq/venue-prebooking-kit/page.tsx. rooms.ts carries slug venue-prebooking-kit, group 'sell', kind 'artifact', lifecycle 'active'.
- `E12.12` THE KEEPSAKE CHECK (the prompt's highest-value item): git -C app grep -n -i -E 'keepsake|one-click export' -- src — CONFIRMS THE FINDING. Only two hits in the whole of app/src, both CSS/JSX comments in timeline-artifact about a visual 'keepsake' metaphor. There is no Keepsake mode, route, state or export. The kit correctly claims none of it: I verified venuePrebookingCoupleFacingProse() contains no 'export', 'download', 'read-only', 'keepsake', 'archive' or 'one-click'. The companion welcome kit (E12.13, src/lib/couple-welcome-kit.ts) records the same verification in its header at lines 22-31 and its ONBOARDI
- `E12.12` THE CRITERION-5 MEASUREMENT: I ran a throwaway probe importing the module and testing block.text.includes(PREBOOKING_PRIVACY_SENTENCE) per block, then deleted it — brochure short TRUE, brochure full TRUE, website short FALSE, website full FALSE, proposal short FALSE, proposal full FALSE, show-around short TRUE, show-around full TRUE, wedding-fair short FALSE, wedding-fair full FALSE. Website and proposal reword the connector; wedding-fair omits 'We can see that you opened it' altogether. The task spec at tasks/E12.12.md criterion 5 says 'unchanged from the copy pack', and its Inputs section says the pack is 'the source of the privacy sentence used unchange
- `E12.12` git status and git diff in _wt-wp16 — THE DIFF MATCHES THE CLAIMS. package.json adds check-venue-copy.mjs to the test chain, adds the four new test files including venue-prebooking-kit.test.ts, and adds a check:venue-copy script. registry.json adds four new route entries plus ten R-042 re-baselines belonging to the concurrent stream, exactly as section 7.1 of the evidence document discloses. The E12.12 artefacts are untracked new files. WORKING TREE LEFT AS FOUND: 50 status entries before and after, kit SHA256 identical to backup, c
- `E12.13` _wt-wp16/src/lib/couple-welcome-kit.ts — Card, email and an eight-step sequence from one module, four slots, explicit missing-slot reporting. The card's attribution is 'Compliments of <venue>', which is the exact string the product already shows on first open, so paper and screen agree.
- `E12.13` _wt-wp16/src/lib/couple-welcome-kit.test.ts — 23 assertions, all pass. Includes a venue-name-appears-exactly-once test on the card and again on the email, a price sweep over all couple-facing prose, and refusals for keepsake/export/download/read-only/archive.
- `E12.13` KEEPSAKE VERIFICATION, and a correction to my own first reading — grep -rn -i keepsake app/src returns TWO hits, not zero as my first grep in this session reported. Both are code comments in modules/timeline/components/artifact/timeline-artifact.tsx:800 and its stylesheet at :1526, using 'keepsake' as an ordinary English word for a printed timeline. Neither is the Venue Edition Keepsake. Two narrower checks confirm the substantive claim: no Keepsake mode/state/route/component exists, and grep for readOnlyReason|isReadOnly|expiredWorkspace|workspaceExpired retu
- `E12.13` app/src/server/actions/planning.ts:945 recomputeSponsoredAccessTermQuietly — CORRECTS A NATURAL ASSUMPTION. D-022 point 1 says capture the wedding date at redemption. The code does not: the recompute fires on completing the planning onboarding inside the product. The behaviour is right because the rule only moves access later, but a kit that told a venue the date is asked for at the code page would describe a flow that does not exist. Step 6 of the sequence says so.
- `E12.13` evidence/E12.10-E12.13-verification-run.txt sections 1, 7, 8, 9 — pnpm exec tsx --test src/lib/couple-welcome-kit.test.ts: 23 tests, 23 pass, exit 0. Sections 7 and 8 carry the Keepsake greps and the wedding-date trace verbatim.
- `E12.13` PLANTED-VIOLATION TEST — the check is real, not decorative — I edited couple-welcome-kit.ts to replace the after-the-wedding line with 'It is yours forever, and your founding partner venue guaranteed it.' The suite went to pass 20 / fail 3, exit 1, failing on the couple sweep, the unfilled-kit sweep and the after-the-wedding test. File restored byte-for-byte from backup; re-run returned 23/23 pass. I separately planted 'A founding partner card, guaranteed for life.' into public/brand/collateral/social/alt-text.txt and `node scripts/check-venue-copy.mjs` e
- `E12.13` _wt-wp16/package.json:13 — CONFIRMED registered in CI. The `test` script diff adds both `node scripts/check-venue-copy.mjs` and `src/lib/couple-welcome-kit.test.ts`. I ran `pnpm test` end to end: exit 0.
- `E12.13` app/src/components/welcome/venue-welcome-card.tsx:66 — INDEPENDENTLY CONFIRMS THE HARDEST CLAIM. The product's own first-open string is `Compliments of {sponsorName}`. The printed card and the screen say the same four words. This was the one claim most likely to be asserted rather than checked, and it holds.
- `E12.13` KEEPSAKE — I re-ran the greps rather than trusting the correction — `grep -rn -i keepsake app/src` returns exactly two hits, both code comments in the Timeline print renderer (timeline-artifact.tsx:800, timeline-artifact.module.css:1526) using 'keepsake' as ordinary English. `find app/src -iname '*keepsake*'` returns nothing. `grep -rnE 'readOnlyReason|isReadOnly|expiredWorkspace|workspaceExpired' app/src` returns nothing. The only export route is app/src/app/api/account/export/route.ts. The evidence document records its own first wrong reading rather than repla
- `E12.13` app/src/server/actions/planning.ts:945 and app/src/server/db/sponsored-access-term.ts — EXIST. recomputeSponsoredAccessTermQuietly is imported at :58 and called at :945 on completing planning onboarding, not at redemption — the kit's step 6 describes it correctly. I went one step further than the evidence did: planning.ts:875 refuses a wedding_season period without a primaryDate, so the built path always has a date, which is what makes the card's 'It does not switch off the day after the wedding' true rather than conditional.
- `E12.13` evidence/E12.10-E12.13-verification-run.txt — EXISTS in the canonical VEF evidence directory (not in the worktree). Sections 1, 7, 8 and 9 read verbatim. Every number in it matches what I got on my own re-run, including the 8-failure experience:validate baseline and the build that went red twice on a concurrent next-build lock before passing.
- `E12.13` _wt-wp16/src/lib/hq/rooms.ts and experience/registry.json:4088 — BOTH REGISTRIES CONFIRMED. rooms.ts gains a `couple-welcome-kit` room; registry.json carries `studio.page.hq-couple-welcome-kit` with route /hq/couple-welcome-kit. `pnpm run experience:validate -- --product=studio` returns 8 failures and none of them names any of the four new WP-16 routes — all 8 are on committed, pre-existing sources (two __design-lab routes, hq-venue-proposal, v/[token], venues/privacy, venues/questions, venues/what-you-see).
- `E12.13` _wt-wp16/src/components/venue/venue-kit-print.css — DEFECT. The sheet heading and the module comment both say the card is 'A5, one side'. The print stylesheet sets `@page { size: A4 }` for the whole document, and the A5 heading itself is `print:hidden`. A venue printing from the browser gets the card alone on an A4 sheet. Small, but it is the one place the artefact does not do what it says.

## Blockers

- `E06.01` Built, verified and evidenced in Wave 4 (the Timeline allowlist boundary is the best-built privacy control in the programme), but it cannot enter Founder Review: it depends on E03.01 (the role map, still in internal review with critical findings) and E03.06 (public Timeline terms and analytics disclosure, backlog). Both are founder-lane legal and privacy tasks, and 'what can never be published' is exactly the question the role map answers. No dependency was waived because a waiver is the founder's to give and this one is substantive, not procedural.
- `E06.12` Built and evidenced, blocked behind E06.01 in the same chain: E06.12 depends on E06.01, which depends on E03.01 and E03.06. Unblock by settling the role map.

## Awaiting founder review

- `E06.02` Add per-milestone visibility controls for private, title-and-date, image and short-story publication states.
- `E06.03` Complete milestone photograph upload, crop, compression, orientation, alt text and deletion.
- `E06.04` Complete milestone navigation and the transition from a Timeline point into its photograph and story.
- `E06.05` Implement private-link, password-protected and intentionally public sharing modes.
- `E06.06` Allow couples to conceal exact wedding dates, locations and other sensitive milestone information.
- `E06.07` Implement anonymous aggregate viewer counts without exposing individual viewer behaviour.
- `E06.09` Design and implement the intentional vertical mobile Timeline.
- `E06.10` Refine the desktop editorial Timeline shown in the Mara and Finn concept.
- `E06.11` Implement the post-wedding read-only Keepsake state and the agreed downloadable export.
- `E12.10` Produce the detailed Venue Edition sales deck.
- `E12.11` Redesign the Founding Venue certificate as 01/25 through 25/25 with the €1,000 founding rate.
- `E12.12` Produce the pre-booking venue sales kit for brochures, proposals, websites and coordinator conversations.
- `E12.13` Produce the post-booking couple welcome kit, approved email wording and printable welcome object.

## Next action

`E03.02` Draft the annual Venue Edition agreement and commercial order form.

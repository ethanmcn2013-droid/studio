# Session wp08-couple — 2026-08-03

Append-only record. Do not rewrite a closed session.

**Objective:** WP-08 · E05 couple planning experience, audit-first per D-015 Q2
**Opened:** 2026-08-03T06:31:14.751Z
**Closed:** 2026-08-03T15:29:31.876Z

## Summary

WP-08 complete. Nine E05 tasks audited against shipped code per D-015 Q2, criteria written first, all nine verified PARTIAL and in Founder Review, none Done. Both tasks claimed complete were corrected down by their verifiers. Production blocker 1 CLOSED: the /app gate called allowlist-only requireAppAccess and bounced every invited and redeemed user to /waitlist after their token was spent; it now calls requireAppAccessTasks, and redemption is covered because comp.ts:206 calls ensureUserProvisioned which writes the workspace_members row the fallback reads. Three privacy tests written this wave had never been registered in package.json and had never run in CI - deleting the publish gate would have shipped green; all three registered plus a wiring contract test. The couple billing card carried four ratified-position violations and is corrected. pnpm typecheck exit 0, pnpm test exit 0. Blocker 2 left open deliberately: wiring Timeline workspace creation decides what a couple's Timeline is seeded with and would pre-empt R-031. Branch claude/wp08-couple pushed, not merged.

## Tasks touched

- `E05.01` Map the complete couple journey from venue invitation through first useful action, active planning, wedding day and Keepsake mode. — now **founder_review**
- `E05.03` Build the default wedding workspace template with restrained milestones, decisions, tasks and example Notes. — now **founder_review**
- `E05.04` Complete the wedding-specific Notes experience, including voice capture, manual capture and high-signal structured output. — now **founder_review**
- `E05.05` Complete the Notes-to-Tasks promotion flow using ordinary wedding-planning language. — now **founder_review**
- `E05.06` Complete the wedding Tasks experience for ownership, due dates, status, tags, priorities and clear next actions. — now **founder_review**
- `E05.07` Complete the task-detail experience for comments, attachments, subtasks, decisions and private collaboration. — now **founder_review**
- `E05.08` Complete the authenticated Timeline planning experience and its relationship with tasks and milestones. — now **founder_review**
- `E05.09` Complete the wedding-specific Signal briefing showing only what needs the couple’s attention today. — now **founder_review**
- `E05.10` Complete spouse, planner, family-member and collaborator invitations with appropriate role boundaries. — now **founder_review**
- `E08.01` Implement annual prepaid billing for standard and founding agreements. — now **founder_review**
- `E08.02` Implement an immutable founding-rate flag and historical price record for each qualifying venue. — now **founder_review**
- `E08.03` Implement renewal invoices, renewal reminders, failed-payment handling, grace periods and lapse behaviour. — now **founder_review**
- `E08.04` Complete multi-tenant data isolation and database row-level security for venues and couple workspaces. — now **founder_review**
- `E08.05` Complete role-based authentication and authorisation across venue owners, managers, viewers and couples. — now **founder_review**
- `E08.06` Secure invitation tokens against guessing, reuse, unintended forwarding, replay and duplicate redemption. — now **founder_review**
- `E08.07` Secure photograph and attachment uploads with file validation, malware controls, size limits and private storage. — now **founder_review**
- `E08.08` Complete audit logging, operational logging, error monitoring and alert escalation. — now **founder_review**
- `E08.09` Complete backups, restore testing, disaster-recovery procedures and data-integrity verification. — now **founder_review**
- `E08.10` Set and test performance budgets across portal, couple workspace, Timeline images and public artifact. — now **founder_review**
- `E08.11` Complete unit, integration, end-to-end, browser, responsive and device test coverage for the entire sponsored journey. — now **founder_review**
- `E08.12` Complete security review, production-readiness review, release checklist, rollback process and incident-response runbook. — now **founder_review**
- `E12.01` Build the public Venue Edition landing page around the sponsored couple experience. — now **founder_review**
- `E12.02` Build the private Founding 25 invitation page. — now **founder_review**
- `E12.03` Build a parameterised private proposal page for each venue. — now **founder_review**
- `E12.05` Add the branded couple-experience preview. — now **blocked**
- `E12.06` Add the Venue Portal trust-and-renewal preview. — now **founder_review**
- `E12.07` Add the privacy explanation covering exactly what the venue sees and never sees. — now **founder_review**
- `E12.08` Add the complete commercial, product, support, entitlement, Keepsake and renewal FAQ. — now **founder_review**
- `E12.09` Produce the concise one-page commercial proposal. — now **founder_review**
- `E12.14` Complete analytics, conversion, responsive, accessibility, performance, copy and visual QA across every commercial page. — now **blocked**

## Status changes this session

- `E05.01` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E05.01` ready → in_progress (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E05.01` in_progress → founder_review (claude_code) — Wave 3 delivery. Verified state: PARTIAL (adversarially verified; the verifier's read overrides the build agent's self-assessment). 8 criterion/criteria NOT met — see the packet. Verifier: PARTIALLY CONFIRMED — not complete. I set out to refute this and largely failed on the substance. Ten of the twelve evidence refs are exact, all twelve resolve, and every one of the three headline findings (D-022 implemented and unreachable, the /app allowlist gate bouncing redeemed couples, the wedding template declaring zero milestones so even a provisioned Timeline is empty) survived independent re-derivation. The two suites and the one gate reproduce byte-for-byte. THE PRIVACY BOUNDARY IS THE CLAIM I ATTACKED HARDEST AND IT HOLDS: beyond re-running the 23-line grep, I widened to all of src, read partner-stats in full, read the sponsor DTO allowlist field by field, read the sponsored-use 
- `E05.03` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E05.03` ready → in_progress (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E05.03` in_progress → founder_review (claude_code) — Wave 3 delivery. Verified state: PARTIAL (adversarially verified; the verifier's read overrides the build agent's self-assessment). 7 criterion/criteria NOT met — see the packet. Verifier: VERDICT: PARTIAL CONFIRMED, BUT NARROWER THAN CLAIMED. The build agent's own honest state of 'partial' is right, and I am not upgrading it - I am widening it from two unmet criteria to five. WHAT SURVIVED MY ATTACK, AND IT IS SUBSTANTIAL. Every evidence path exists. All three test-suite results reproduced to the exact count. The fail-before run reproduced exactly, three named failures out of six, so the R-017 guard is genuinely non-tautological and is wired into the CI test chain rather than existing only as a local artifact. The generated file was not touched. R-017 was handled correctly: not removed, not redesigned, no dietary field invented, four decisions put to the founder in the spec. 
- `E05.04` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E05.04` ready → in_progress (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E05.04` in_progress → founder_review (claude_code) — Wave 3 delivery. Verified state: PARTIAL (adversarially verified; the verifier's read overrides the build agent's self-assessment). 9 criterion/criteria NOT met — see the packet. Verifier: PARTIAL, and the build agent's own state word is right, but its evidence is overstated on four of the ten criteria it marks 'met' and it introduces one defect that must be fixed before this goes anywhere near Founder Review. What genuinely holds, and I re-ran every check: typecheck clean, pnpm test 413/413 EXIT 0, ds:check clean, brand-voice clean on the new files, the eleven guards are non-tautological and are inside the CI chain, voice capture really does pre-exist, the seam really is free (no route, no field, no migration), and the wedding signal really is presentation-only. This is careful work and the audit's correction about contextType versus active_domain is right and non-obvious. Th
- `E05.05` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E05.05` ready → in_progress (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E05.05` in_progress → founder_review (claude_code) — Wave 3 delivery. Verified state: PARTIAL (adversarially verified; the verifier's read overrides the build agent's self-assessment). 7 criterion/criteria NOT met — see the packet. Verifier: REFUTED as complete. Correct state is PARTIAL. Do not put this in front of the founder as a finished task. What survives scrutiny is substantial and I want to be fair about it. The typecheck, the full test run, the registration in the test chain, the fail-before, and the brand-voice result all reproduce exactly as claimed — I ran every one. The copy module is genuinely good work: two registers with enforced key parity, a documented ban list with a reasoned omission, and guards that fail for the right reasons when I mutate the strings back. The wedding register is wired to production, not stranded in a test. And on the question the brief said matters most, I went looking for a path where a ve
- `E05.06` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E05.06` ready → in_progress (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E05.06` in_progress → founder_review (claude_code) — Wave 3 delivery. Verified state: PARTIAL (adversarially verified; the verifier's read overrides the build agent's self-assessment). 5 criterion/criteria NOT met — see the packet. Verifier: CONFIRMED AS PARTIAL, with the criterion-level accounting corrected downward. I could not refute the engineering. Every path exists, every command reproduces to the digit, and the fail-before is real — I mutated the file myself and got exactly one failure on exactly the right test, then restored the tree. The two things actually built are sound: the seed-persona roster is genuinely gone from a panel that mounts on every /app page, and anchor-due.ts refuses a malformed date rather than guessing. This agent also volunteered three findings against its own interest (the dead fixtures.test.ts, the concurrent typecheck failure, the demo-mode gap). That is the behaviour this programme has been shor
- `E05.07` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E05.07` ready → in_progress (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E05.07` in_progress → founder_review (claude_code) — Wave 3 delivery. Verified state: PARTIAL (adversarially verified; the verifier's read overrides the build agent's self-assessment). 4 criterion/criteria NOT met — see the packet. Verifier: PARTIAL — the state label is right, but for a different reason than the build agent gives, and the difference is the point of the task. What I confirm without reservation: the audit-first half is honest and unusually well evidenced. Sixteen of sixteen evidence paths exist, every cited line number lands within a line or two of where it says, pnpm test reproduces to the exact ten group totals, typecheck and ds:check are green, and the fail-before proof survives a real file swap rather than a reading. I attacked the privacy boundary specifically and could not break it: toPublicTask is provably the only projection on /p, /embed and /share, the venue egress is four scalars behind a bearer token, 
- `E05.08` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E05.08` ready → in_progress (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E05.08` in_progress → founder_review (claude_code) — Wave 3 delivery. Verified state: PARTIAL (adversarially verified; the verifier's read overrides the build agent's self-assessment). 6 criterion/criteria NOT met — see the packet. Verifier: CONFIRMED ON THE PART THAT MATTERS, WITH THREE CORRECTIONS REQUIRED BEFORE FOUNDER REVIEW IS MEANINGFUL. I set out to refute the privacy boundary and could not. Criteria 1, 2, 3 and 4 are met by CODE, not prose, and I proved it two ways the builder did not: I re-ran their publish-gate mutation (5 pass / 2 fail, exactly as claimed) and then ran my own independent mutation, leaking sourceRelation into the emitted DTO, which turned the projection test red. That is the test that decides whether criterion 4 is real, and it is. I then hunted for a second bearer path into publication content and found none: resolveAudienceTimeline is the single reader, reachable only from /s/[token], and the only o
- `E05.09` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E05.09` ready → in_progress (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E05.09` in_progress → founder_review (claude_code) — Wave 3 delivery. Verified state: PARTIAL (adversarially verified; the verifier's read overrides the build agent's self-assessment). 4 criterion/criteria NOT met — see the packet. Verifier: VERDICT: CONFIRMED — with one concrete defect to fix. I could not prove this overstated, and on substance it is UNDER-claimed rather than over-claimed. I set out to refute and failed on every material point. All eight evidence paths exist. The 9/9 test result is real and I pasted it. Most importantly, I did not take the agent's word on the two measurements that carry the whole honest finding — I wrote my own probes and got byte-identical results: 18 tasks / 0 offsets / 0 milestones / lanes {done:2,doing:4,review:3,todo:9}, and readCount 18 with exactly one row, synthetic:overload 'Seven items open at once', zero due-soon, zero crowded-week. ds:check and typecheck are clean because I ran them
- `E05.10` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E05.10` ready → in_progress (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E05.10` in_progress → founder_review (claude_code) — Wave 3 delivery. Verified state: PARTIAL (adversarially verified; the verifier's read overrides the build agent's self-assessment). 8 criterion/criteria NOT met — see the packet. Verifier: VERDICT: PARTLY REFUTED. The honest state label 'partial' is right, but the criteria breakdown behind it is overstated — two of the nine criteria the build agent marked MET are refuted by shipped code it did not open, and the one control it produced is not wired into CI. What holds, and I confirmed it personally. Every one of the twelve evidence paths exists. Every cited line number is accurate — I checked all of them rather than sampling. Criteria 3, 4, 5 and 8 are CONFIRMED by code, not by prose: six role values including venue-shaped ones drive through the real accept SQL against a real in-memory database and every one lands as member; three sponsor references all fail as tokens; wedding 
- `E08.01` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E08.01` ready → in_progress (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E08.01` in_progress → founder_review (claude_code) — Wave 3 delivery. Verified state: PARTIAL (adversarially verified; the verifier's read overrides the build agent's self-assessment). 12 criterion/criteria NOT met — see the packet. Verifier: PLAUSIBLE, trending REFUTED on three of the ten criteria. Do not let this reach Founder Review as 'partial with two known gaps' - there are five more, and two of them are the exact failure classes this programme keeps repeating. What is genuinely real and good: criteria 1, 4, 6, 7, 8, 9 and 10 are met by running code I read and tests I watched pass. venue-money.ts is a well-built constructor (gross-only, null-not-zero, net recomputed on read). prepaymentRefusal refuses both wrong-price directions. The mark-venue-paid.ts rewrite is a real improvement and the mirror now fails loudly. The contract-check edit is a strengthening, not a weakening. The runbook is honest about what is not built. The
- `E08.02` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E08.02` ready → in_progress (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E08.02` in_progress → founder_review (claude_code) — Wave 3 delivery. Verified state: PARTIAL (adversarially verified; the verifier's read overrides the build agent's self-assessment). 6 criterion/criteria NOT met — see the packet. Verifier: VERDICT: CONFIRMED on everything the builder claimed, and the builder's own honesty holds up under adversarial re-run — but the unmet list is materially incomplete, so 'partial' is right for the wrong reasons as much as the right ones. What is genuinely true. I re-ran all five verification commands and both mutation runs myself. Every number the builder reported reproduced to the digit, including the exact assertion message on mutation 2. All six cited evidence paths exist and contain what they are said to contain. planChangeRefusal is not decorative; the price of record comes from the plan constant rather than the caller; the append-only triggers are probed for firing rather than inferred f
- `E08.03` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E08.03` ready → in_progress (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E08.03` in_progress → founder_review (claude_code) — Wave 3 delivery. Verified state: PARTIAL (adversarially verified; the verifier's read overrides the build agent's self-assessment). 8 criterion/criteria NOT met — see the packet. Verifier: PLAUSIBLE — not CONFIRMED, and not REFUTED. The verification record is honest and survives attack on its strongest point: every evidence path exists, every command I re-ran returned what was claimed, and the couple-access invariant is genuinely enforced, which I proved by mutation rather than by reading. That is better discipline than this programme has usually seen and the build agent deserves credit for it. It is also correctly self-reported as 'partial' with five true UNMET items. What stops it being CONFIRMED is that the unmet list is incomplete in exactly the direction that flatters the work. Three criteria the agent counted as met are not met by running code. Criterion 2 requires the s
- `E08.04` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E08.04` ready → in_progress (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E08.04` in_progress → founder_review (claude_code) — Wave 3 delivery. Verified state: PARTIAL (adversarially verified; the verifier's read overrides the build agent's self-assessment). 8 criterion/criteria NOT met — see the packet. Verifier: REFUTED on 'all criteria met'. The build agent's own honest state of 'partial' is the right label, but it is paired with ALL CRITERIA MET: true and UNMET: [], which is internally contradictory and, more importantly, wrong on the headline criterion. What genuinely holds up, and it is substantial: every evidence path exists, all four claimed commands reproduce exactly (25/25, 5/5, tsc exit 0, 402/402), both new suites really are wired into pnpm test and pnpm test really does run in CI, the studio-side proofs for criterion 7 pass, the no-RLS position is stated plainly and repeatedly rather than papered over, the negative suite is real behavioural testing against libSQL with a non-vacuous contro
- `E08.05` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E08.05` ready → in_progress (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E08.05` in_progress → founder_review (claude_code) — Wave 3 delivery. Verified state: PARTIAL (adversarially verified; the verifier's read overrides the build agent's self-assessment). 8 criterion/criteria NOT met — see the packet. Verifier: VERDICT: PLAUSIBLE — not CONFIRMED, and the claim is overstated on one acceptance criterion. What survives adversarial checking, and it is the majority: every evidence path exists, three of the four verification commands reproduce EXACTLY as claimed (21/21, 5/5, tsc exit 0), and the load-bearing mutation probe is real — I re-ran it myself and the suite goes red on 'declares no capability that names couple content' with the mutation present and green with it removed. The 'the guard had never run in CI' finding is verifiable in the package.json diff. The auth.ts v2 token is genuinely sound and proxy.ts genuinely enforces it at the edge. This agent found and fixed a real defect in its predecess
- `E08.06` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E08.06` ready → in_progress (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E08.06` in_progress → founder_review (claude_code) — Wave 3 delivery. Verified state: PARTIAL (adversarially verified; the verifier's read overrides the build agent's self-assessment). 7 criterion/criteria NOT met — see the packet. Verifier: CONFIRMED, with three narrowings. I re-ran every claimed command in the correct worktrees and the results reproduce almost exactly: app typecheck exit 0, app test exit 0 with 1066 tests across 10 segments and 0 failures, app db:contract 19/19 with the '28 SQL files, 28 receipts' line verbatim, studio typecheck exit 0, studio test exit 0, invitation-code.test.ts 11/11, share-token-security 23/23, invitation-code-security 13/13. Every one of the nine evidence paths exists. The one numeric drift is 75 lint warnings against a claimed 76, and the one red gate (studio lint) was disclosed by the builder before I found it, with an attribution I independently verified as correct (the offending file i
- `E08.07` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E08.07` ready → in_progress (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E08.07` in_progress → founder_review (claude_code) — Wave 3 delivery. Verified state: PARTIAL (adversarially verified; the verifier's read overrides the build agent's self-assessment). 7 criterion/criteria NOT met — see the packet. Verifier: VERDICT: PLAUSIBLE — not REFUTED, and not CONFIRMED. What survives adversarial reading, and it is most of it. Every evidence path exists. Every command re-ran green with numbers matching the claim (typecheck exit 0; 1066/1066 tests; the 16-test suite 16/16 and genuinely wired into `pnpm test`). The diff is what it says it is: a client-trusting 8-entry denylist replaced by a byte-checked allowlist, and a 302 to a private blob URL replaced by an authenticated server-side stream. I checked the SDK type declarations myself and the static case against the redirect is correct — that was a real latent production defect and finding it is the substantive value here. The builder also volunteered the Z
- `E08.08` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E08.08` ready → in_progress (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E08.08` in_progress → founder_review (claude_code) — Wave 3 delivery. Verified state: PARTIAL (adversarially verified; the verifier's read overrides the build agent's self-assessment). 8 criterion/criteria NOT met — see the packet. Verifier: VERDICT: PLAUSIBLE — the work is real, substantially better evidenced than this programme's average, and its one red gate was disclosed honestly and verified by me as genuinely pre-existing. But it is NOT fully CONFIRMED, because one acceptance criterion is refuted by my own probe and the 'partial' label understates which criteria are unmet. WHAT IS GENUINELY TRUE. Every one of the ten evidence paths exists. Every verification command reproduced: both typechecks exit 0, both full suites exit 0, 8/8 and 9/9 on the two new tests, and I confirmed both are actually wired into `pnpm test` rather than merely runnable standalone — the studio one genuinely never ran before. Criteria 1, 4, 5, 6 and 7
- `E08.09` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E08.09` ready → in_progress (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E08.09` in_progress → founder_review (claude_code) — Wave 3 delivery. Verified state: PARTIAL (adversarially verified; the verifier's read overrides the build agent's self-assessment). 11 criterion/criteria NOT met — see the packet. Verifier: PLAUSIBLE, not CONFIRMED. Every command the builder claimed, I re-ran, and every one reproduced: 27/27, 21/21, 7/7, 402/402, 56/56, drill PASSED, backup-all EXIT=1 on partial coverage, lint EXIT=1 on a genuinely pre-existing error in another lane's file. All fourteen evidence paths exist. The deleted predecessor drill really is deleted, and the builder's description of why it was worthless (a recovery_probe table compared to itself) is accurate. The four unmet items the builder self-declared are all real and I found no grounds to soften any of them. This is honest work and the studio-side artefacts in particular are the strongest in the set. But the claim is overstated in one specific and ch
- `E08.10` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E08.10` ready → in_progress (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E08.10` in_progress → founder_review (claude_code) — Wave 3 delivery. Verified state: PARTIAL (adversarially verified; the verifier's read overrides the build agent's self-assessment). 9 criterion/criteria NOT met — see the packet. Verifier: VERDICT: CONFIRMED, and the builder's honest state of 'partial' is correct - but I am adding four defects the builder did not list, so it stays partial for more reasons than it claimed. What I personally re-ran and can vouch for: every evidence path exists; the app numbers reproduce from a fresh pnpm build I ran myself (exit 0); the studio numbers reproduce exactly against the production build on disk; the ratchet genuinely fails the build (I lowered the ceiling to 100, got EXIT=2 and the FAILED banner, restored, got exit 0 and a byte-identical file); all six exit-3 refusal paths fire, proven in a sandbox rather than by reading; --self-test runs with no build and no contract present. This is
- `E08.11` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E08.11` ready → in_progress (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E08.11` in_progress → founder_review (claude_code) — Wave 3 delivery. Verified state: PARTIAL (adversarially verified; the verifier's read overrides the build agent's self-assessment). 8 criterion/criteria NOT met — see the packet. Verifier: VERDICT: PLAUSIBLE, not CONFIRMED, and not REFUTED. I could not prove this overstated in the way the programme usually gets burned — there is no green gate here that is secretly red, no control with no production caller, no privacy claim the docs invent. The opposite: this builder reported a red studio lint nobody asked about, reported a flake against their own interest, and declared four gaps rather than papering them. The 1,066-test app figure reproduces exactly, the CI wiring is real in both repositories, and the orphan claim is corroborated by the package.json diff rather than asserted. That is the strongest evidence discipline I have verified in this programme. But two criteria the buil
- `E08.12` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E08.12` ready → in_progress (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E08.12` in_progress → founder_review (claude_code) — Wave 3 delivery. Verified state: PARTIAL (adversarially verified; the verifier's read overrides the build agent's self-assessment). 10 criterion/criteria NOT met — see the packet. Verifier: VERDICT: PLAUSIBLE — the work is real and largely excellent, but it is NOT confirmable as-is, and I found material defects the build agent missed. Corrected state stays 'partial', for MORE reasons than they gave. What survived adversarial verification, and it is a lot. All seven evidence paths exist. All twelve verification commands re-ran and reproduced, two of them to the exact test count (1,066 and 604) and one to the exact file and line of a claimed failure. The migration-0027 segment proof reproduced independently against a real libSQL database. Every code finding I chased was accurate by file and line. The banned-claims sweep is clean and I confirmed the adjacent traps too — no VAT-exc
- `E12.01` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E12.01` ready → in_progress (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E12.01` in_progress → founder_review (claude_code) — Wave 3 delivery. Verified state: DOCUMENTED-ONLY (adversarially verified; the verifier's read overrides the build agent's self-assessment). 6 criterion/criteria NOT met — see the packet. Verifier: VERDICT: honestState CONFIRMED as documented-only. The audit's completeness is REFUTED. The state claim is not overstated — it is one of the more honest submissions I have verified. I tried to break it and failed on every dimension the brief asked me to attack: no source file was changed (git-verified), no gate was claimed, typecheck and tests pass, the banned-term sweep is genuinely clean at zero hits, there are no em dashes or exclamation marks, no R-031 violation, every ratified commercial claim is correct in the page body, and the three reasons given for not editing (I-014, the WP-06 pin, R-025) are all real — I confirmed the branch sweep script genuinely cannot read the pin file. Criter
- `E12.02` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E12.02` ready → in_progress (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E12.02` in_progress → founder_review (claude_code) — Wave 3 delivery. Verified state: PARTIAL (adversarially verified; the verifier's read overrides the build agent's self-assessment). 8 criterion/criteria NOT met — see the packet. Verifier: PLAUSIBLE, NOT CONFIRMED — and the engineering is the strongest I have verified on this programme, while the copy is not. Every mechanical claim the build agent made is TRUE. I re-ran typecheck, test, build, lint, the contract check, ds-check and experience:validate, and each result matched to the number, including the 502 tests, the single pre-existing lint error with zero warnings, and the exact seven-item validate failure list. I reproduced the live proof independently: the GA4 differential, the byte-identical 404s after nonce strip, the four events with none from an unresolvable token, the served robots.txt, the noindex head with no venue name in it, and one action per state. The agent u
- `E12.03` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E12.03` ready → in_progress (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E12.03` in_progress → founder_review (claude_code) — Wave 3 delivery. Verified state: PARTIAL (adversarially verified; the verifier's read overrides the build agent's self-assessment). 10 criterion/criteria NOT met — see the packet. Verifier: REFUTED as 'complete'. Correct state is PARTIAL, and it should go to Founder Review with the defects named rather than to Done. What I could not break, and this matters: every gate the build agent claimed, I re-ran and every number matched. Typecheck 0, test 38/38, build 0, lint 1 pre-existing error in a file this branch does not touch, experience:validate exit 1 with exactly two failures attributable to E12.03. The banned-term sweep is clean. The rendered page carries every ratified claim in the ratified words, VAT-inclusive on both prices, the lock on continuous renewal, 01/25 through 25/25 on cleared payment and explicitly not on signature, the couple term never a bare eighteen months, no
- `E12.05` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E12.05` ready → blocked (claude_code) — Built, verified and evidenced in Wave 3, but it cannot enter Founder Review: it depends on E05.12, the world-class design-system review and visual-regression baselines, which is FOUNDER-CREATIVE and was deliberately excluded from Wave 3. The tool refuses the transition and a dependency waiver would be inventing founder consent, so it is recorded as blocked instead. Unblock by taking E05.12 to founder review, or by an explicit founder waiver recorded with --waive-deps.
- `E12.06` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E12.06` ready → in_progress (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E12.06` in_progress → founder_review (claude_code) — Wave 3 delivery. Verified state: PARTIAL (adversarially verified; the verifier's read overrides the build agent's self-assessment). 5 criterion/criteria NOT met — see the packet. Verifier: PARTIALLY REFUTED. Keep at partial; do not advance to Founder Review yet. What survives adversarial verification, and it is a lot: every gate reproduced, including the exact 502/502/0 test figure and the 15-rule zero-hit sweep. I attacked the copy from three directions — the project's own banned-term list, all 15 prohibited-claims rules run by hand, and a manual read against E09.11 sections 3.1, 4.1, 5.1 and 5.1a — and could not find a single wording defect. The launch-scope sentence, both privacy halves, the R12 asymmetry and the survival sentence are verbatim, not paraphrased. The two red gates were reported honestly and correctly attributed to other lanes. This is careful work. What is ov
- `E12.07` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E12.07` ready → in_progress (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E12.07` in_progress → founder_review (claude_code) — Wave 3 delivery. Verified state: PARTIAL (adversarially verified; the verifier's read overrides the build agent's self-assessment). 2 criterion/criteria NOT met — see the packet. Verifier: REFUTED IN PART — do not approve as-is; push back on one defect and one evidence correction, then approve. The agent's self-report of 'partial' is honest, and this is genuinely strong work: I re-ran every check and every green claim it made is green, every red claim it made is red, both attributions are correct, and its disclosure of its own broken first sweep (§8.0) is the kind of thing that raises confidence rather than lowering it. The negative half of the page is exemplary — I traced all six 'never visible' claims into project-venue-access.ts myself and every one holds, and I deliberately tried to break the 'Descriptions are not visible' claim and could not, because toPublicTask is a str
- `E12.08` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E12.08` ready → in_progress (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E12.08` in_progress → founder_review (claude_code) — Wave 3 delivery. Verified state: PARTIAL (adversarially verified; the verifier's read overrides the build agent's self-assessment). 3 criterion/criteria NOT met — see the packet. Verifier: REFUTED IN PART — the honest state 'partial' is right, but for three reasons the builder did not record. What is claimed and true: every gate I re-ran matches (contract ok/0, typecheck 0, build 0 with the route in the table, test 0/38 pass, lint 1 unrelated error, experience:validate red on this route). All five evidence paths exist. My independent banned-term sweep reproduces theirs exactly — zero hits across every prohibited class, zero em dashes, zero exclamation marks. The price, entitlement, access-term, privacy, branding, insurance and data-location answers all state the ratified position correctly and consistently with the Done /venues page, and the access-term promise is backed by re
- `E12.09` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E12.09` ready → in_progress (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E12.09` in_progress → founder_review (claude_code) — Wave 3 delivery. Verified state: PARTIAL (adversarially verified; the verifier's read overrides the build agent's self-assessment). 11 criterion/criteria NOT met — see the packet. Verifier: REFUTED on three of the twelve criteria, and two of the three were asserted as met. The build is real and well made: everything I could re-run (typecheck, test with all three named contract lines and the 7498cee02c07 digest, build with the route in the table, ds-check, and the lint failure with its pre-existing attribution) matched the claim exactly, and criteria 3, 5, 6, 7, 8, 9 and 10 survived my own independent sweeps. What does not survive is this. (1) The standard rate is not stated as VAT-inclusive: 'The standard rate is EUR 1,500.' carries no VAT sentence, the test does not assert one, and the evidence document states the opposite as a checked fact. That is a defect in the one documen
- `E12.14` backlog → ready (claude_code) — Wave 3 execution, audit-first per D-015 Q2.
- `E12.14` ready → blocked (claude_code) — Built, verified and evidenced in Wave 3, but blocked behind the same chain as E12.05: E12.14 depends on E12.05, which depends on E05.12 (founder-creative, excluded from Wave 3). No dependency was waived because a waiver is the founder's to give. Unblock by clearing E05.12 then E12.05, or by an explicit founder waiver.

## Evidence added

- `E05.01` studio/docs/execution/venue-edition-and-films/evidence/E05.01-couple-journey-map.md — The deliverable. Section 9 traces all eight acceptance criteria to their proof.
- `E05.01` src/app/sign-up/[[...sign-up]]/page.tsx:33-34,72-73 — welcomeUrl = buildWelcomeUrl(preselected); both fallbackRedirectUrl and forceRedirectUrl are set to it. sp.redirect_url is read only at :24-31 to render a sponsor card. The redeem path is never handed to Clerk.
- `E05.01` src/app/welcome/page.tsx:58 with src/server/db/venue-welcome.ts:39-55 — /welcome has no redeem fallback. detectVenueWelcome queries entitlements for a row redemption would have created; with no redemption it is null and the venue branch at :69-86 never runs.
- `E05.01` src/server/actions/comp.ts:215-217 — expiresAt = new Date(Date.now() + row.durationDays * 24*60*60*1000). Flat. This is the shipped path. D-022 is not applied.
- `E05.01` studio/src/lib/entitlements-db/codes.ts:29-34,252-267,285 — D-022 is correctly implemented here and the header itself says the public redeem write lives in the Tasks repo. grep for redeemLicenseCode|setCoupleWeddingDate across studio/src and studio/scripts returns only codes.test.ts and one re-export at index.ts:101.
- `E05.01` studio/scripts/issue-codes.ts:121-129 — Mint still fails on any venue_edition duration other than exactly 548. D-022 point 4 said to relax this guard. Not done.
- `E05.01` src/app/app/layout.tsx:11,16 with src/server/require-app-access.ts:21-34 and src/server/app-access.ts:29-62 — Blocker 1 re-confirmed. The outer allowlist-only gate fires before the membership-aware requireAppAccessTasks called at src/components/app/tasks-runtime-shell.tsx:67. A redeemed couple already holds a workspace_members owner row from comp.ts:206-207, so the inner gate would admit them.
- `E05.01` src/modules/timeline/server/actions/workspaces.ts:93 — createWorkspaceAction has zero callers across src, e2e, scripts and experience. Blocker 2 re-confirmed. Empty state at src/modules/timeline/app/page.tsx:48-71 advises 'Create a workspace in Tasks first', which does nothing.
- `E05.01` src/modules/timeline/server/sync/tasks-milestone-source.ts:120-141 and src/modules/signal/lib/briefing/tasks-db-source.ts:114 — NEW second Timeline blocker not in the prior audit. Milestones require is_milestone=1 and order by due_at; Signal reads due_at. The wedding template sets neither, so even a provisioned Timeline renders empty.
- `E05.01` src/components/app/settings/sections/billing.tsx:90-104 — NEW. Four defects on one couple-facing card: price 'Via venue partner' (banned term, R-042), 'Twelve months' (wrong term, contradicts 548 days and D-022), 'Up to 6 collaborators' (a seat count against D-020, and fiction since getMemberCapacity returns unlimited), '12 months of editing for the planner' (wrong term, planner voice).
- `E05.01` src/server/db/membership.ts:95-115 with src/components/app/settings/sections/members.tsx:96 — D-020 verified HELD in the runtime: wedding tier returns max:null and the seat counter is suppressed. The violation is copy-only.
- `E05.01` src/server/db/schema.ts:308 with src/components/welcome/contextual-onboarding.tsx:383 and src/lib/planning/flags.ts:33 — The wedding date is already asked for and already has a column, on a path whose flag defaults to NODE_ENV !== production, so it is off in production and wired to nothing.
- `E05.01` studio/docs/execution/venue-edition-and-films/tasks/E05.01.md — EXISTS. Well-formed against the template, status founder_review, sign-off block empty and unfilled. Eight acceptance criteria match the claimed set verbatim.
- `E05.01` _wt-wp08/src/server/actions/comp.ts:215-217 — CONFIRMED verbatim: 'const expiresAt = new Date(Date.now() + row.durationDays * 24 * 60 * 60 * 1000,);'. Flat multiply, shipped path, D-022 not applied. Also confirmed the atomic conditional UPDATE at :227-233 as the real over-issue guard, and ensureUserProvisioned at :206.
- `E05.01` _wt-wp08/src/server/db/ensure-user.ts:108-109 — CONFIRMED the map's load-bearing assumption for the blocker-1 fix: 'INSERT OR IGNORE INTO workspace_members (workspace_id, user_id, role) VALUES (..., 'owner')'. A redeemed couple does hold a membership row, so requireAppAccessTasks would admit them.
- `E05.01` _wt-wp08/src/app/app/layout.tsx:11,16 + src/server/require-app-access.ts:21-34 + src/server/app-access.ts:29-62 + src/components/app/tasks-runtime-shell.tsx:67 — BLOCKER 1 CONFIRMED at every cited line. Both gates are production-only (isProductionMode() === getAccessMode() === 'production'), so the bounce is a production behaviour, not a local artefact. The outer SharedAppGate wraps the shell that calls the membership-aware gate.
- `E05.01` studio/src/lib/entitlements-db/codes.ts:29-34,252-267,285 + studio/src/lib/venue-edition.ts:100-121 + studio/scripts/issue-codes.ts:121-129 — D-022 IMPLEMENTED-AND-UNREACHABLE CONFIRMED at every cited line. The header at :30-31 does say the public redeem write lives in the Tasks repo. grep for redeemLicenseCode|setCoupleWeddingDate across studio/src and studio/scripts returns only codes.ts itself, codes.test.ts, and the index.ts:101 re-export. venue-lifecycle.ts's only importer is venue-lifecycle.test.ts. issue-codes.ts:124-128 still fails any venue_edition duration other than 548 — D-022 point 4 is not done.
- `E05.01` _wt-wp08/src/app/api/internal/partner-stats/route.ts — PRIVACY: read in full. Bearer PARTNER_STATS_SECRET with timingSafeEqual, reads comp_codes by json_extract sponsor_slug and entitlements by notes IN (...), returns exactly four scalars. No title, tag, name, date or workspace identifier. The map's 'no small-cell floor' gap is real and correctly attributed elsewhere.
- `E05.01` _wt-wp08/src/server/planning/queries.ts:225-245 — PRIVACY: listSponsorActivationDTOs allowlist verified field by field — activationId, sponsorReference, status, metadata, metadataVersion, consentReceiptId, consentedAt, revokedAt. No workspace name, task, note, member or link. Zero production callers; the only other references are assertions inside venue-never-a-collaborator.test.ts.
- `E05.01` _wt-wp08/src/lib/account/instrumentation/event-schema.ts:7,64-79 — PRIVACY, attacked independently: the sponsored-use event schema carries a denylist covering title, name, body, content, note, filename and states 'It carries no content, no identity, and no destination'. The sink at sink.ts:36 is a noop. No content egress via instrumentation.
- `E05.01` studio/src/lib/hq/product-analytics.ts:93,103-199 + studio/src/server/today/aggregate.ts:80,274-300 — PRIVACY, attacked independently: these are the only studio paths that open TASKS_DATABASE_URL outside issue-codes and the migration script. I read the SQL. Every statement is COUNT(*) / COUNT(DISTINCT) or a timezone/cadence preference. No task title, note body or workspace name crosses. They are also not sponsor-scoped. No refutation found.
- `E05.01` _wt-wp08/src/server/venue-never-a-collaborator.test.ts — 8 tests, 8 pass under npx tsx --test. Test at :225 'the sponsor-facing read path carries no membership field' pins the DTO allowlist against regression. This CONTRADICTS section 7's closing claim that no such test exists, and it strengthens the privacy verdict rather than weakening it.
- `E05.01` _wt-wp08/src/lib/wedding-template-contract.test.ts — 6 tests, 6 pass. 'audit pin (E05.03): the shipped wedding template declares ZERO milestones' and 'no task carries a wedding-relative due offset' both pass, so the map's second Timeline blocker is CONFIRMED and pinned. Cross-checked: git diff shows apply-template.ts now writes dueAt and isMilestone, but templates.generated.ts contains zero occurrences of either 'milestone' or 'dueOffsetDays', so the mechanism is live and the data is still inert. The claim holds.
- `E05.01` _wt-wp08/src/components/app/settings/sections/billing.tsx:93,94,97,98,175 — All four copy defects CONFIRMED present, but two of the four cited line numbers are wrong (:93 not :92, :97 not :96). Line 175 is an unenumerated second call to redeemCompCodeAction in the same file the map audits.
- `E05.01` studio/scripts/partner-digest.ts:16-20,60-103 — UNENUMERATED VENUE-FACING EGRESS. Usage text: prints a summary 'suitable for forwarding to the venue contact'. buildDigest emits ungated small-cell counts in prose: ':82 N couples have redeemed so far', ':94 N couples havent reached the workspace yet'. Counts only, no content, so the boundary holds, but criterion 5's enumeration is incomplete.
- `E05.01` _wt-wp08 git diff src/app/invite/[token]/page.tsx — MISATTRIBUTION PROOF. The diff adds a comment beginning 'E05.10: this page is the first thing a couple's spouse, planner or family member sees'. E05.10 is WP-08's own task and its evidence doc was written by this session at 08:18. The experience:validate failure is this package's, not a sibling's.
- `E05.01` studio/docs/execution/venue-edition-and-films/evidence/E05-production-blocker-app-gate.md — The first of the two production blockers the audit found, now fixed and verified: the shared /app gate no longer bounces invited and redeemed users to /waitlist. Full suite exit 0. Also records three privacy tests that had never run in CI, now registered.
- `E05.03` studio/docs/execution/venue-edition-and-films/evidence/E05.03-wedding-template-audit.md — The audit. Section 1 the source/artifact trap, section 2 what ships, section 2a the restraint verdict, section 3 the build, section 4 R-017, section 6 criterion-to-proof, section 7 verification.
- `E05.03` scripts/sync-templates.ts:20 — resolve(__dirname, '../../studio/src/lib/templates') resolves from _wt-wp08/scripts to the LIVE studio working tree, which this package may not edit and which carries another lane's uncommitted work. This is why template CONTENT could not be changed here.
- `E05.03` src/lib/templates.generated.ts:1 vs scripts/sync-templates.ts:62 — The committed artifact says '// AUTO-GENERATED, do not edit by hand.' while the generator emits '// AUTO-GENERATED — do not edit by hand.' with an em dash. Proof that the artifact has been hand-edited and that re-running the generator would revert hand edits.
- `E05.03` _wt-wp08/src/lib/template-anchor.ts — New pure module. resolveTemplateDueAt returns null with no anchor, deliberately, rather than falling back to days-from-now.
- `E05.03` _wt-wp08/src/server/db/apply-template.ts — Now reads workspaces.primary_date as the anchor and writes dueAt: resolveTemplateDueAt(...) and isMilestone: t.milestone === true. Optional options.anchorDate lets the redemption path supply the wedding date directly once it is captured.
- `E05.03` _wt-wp08/src/lib/wedding-template-contract.test.ts — R-017 forward guard plus four audit pins. The ledger holds one entry, marked open, with a comment stating that adding a title to clear the test is not the fix.
- `E05.03` src/lib/templates.generated.ts:108 — The ONLY production occurrence of 'Collect final dietary notes' in the whole app repo. Canonical source is studio/src/lib/templates/wedding-planning-workspace/tasks.ts:69.
- `E05.03` src/server/suite-navigation-contract.test.mjs:464,495 — CORRECTS the prior audit. This asserts 'Confirm the final dietary list before the venue team locks the service notes', which is Notes/Tasks DEMO FIXTURE copy, not the template string. The two were conflated. Removing the template task breaks no test.
- `E05.03` studio/src/lib/templates/wedding-planning-workspace/meta.ts:15 — audience: 'wedding-planner'. The template was written for the planner, not the sponsored couple.
- `E05.03` studio/src/lib/templates/wedding-planning-workspace/notes.ts:9-14 with scripts/sync-templates.ts:51-58 — Criterion 9 UNMET. The canonical note seed exists; the generator copies only id, name, description, icon, domain and tasks, so it is dropped at the repo boundary. grep for NoteSeed or template.notes across src/ returns zero.
- `E05.03` _wt-wp08/src/server/db/schema.ts — Criterion 10 UNMET. No decision table or object exists anywhere. A 'decision' tag on three template tasks is the whole of it.
- `E05.03` evidence/E05.03-wedding-template-audit.md — EXISTS, 18217 bytes. All 12 evidence paths in the claim exist; not one dead path. I read the whole document and the task spec.
- `E05.03` _wt-wp08/src/lib/templates.generated.ts — CONFIRMS the generated file was NOT hand-edited by this package. Absent from git status; git diff --stat empty; md5 37f9b3c35dccd0c240c7af207accf0de before my mutation and byte-identical after restore. The recurring failure mode 'an edit to a GENERATED file a build step will overwrite' does not apply.
- `E05.03` _wt-wp08/src/app/welcome/page.tsx:73 — THE DEFECT. applyTemplateToWorkspace(VENUE_TEMPLATE_ID, ws) is called with no options, and the UPDATE at :74-81 does not set primary_date. VENUE_TEMPLATE_ID is 'wedding-planning-workspace' (:23), so the guard does target the right template, but the anchor is null at apply time.
- `E05.03` _wt-wp08/src/app/welcome/page.tsx:60-61 — COMPOUNDS THE DEFECT. When planningFlags.contextualOnboarding is on, a venue arrival redirects to /welcome/plan and line 73 is never reached, so no template is applied at all. src/lib/planning/flags.ts:33 sets defaultEnabled = env.NODE_ENV !== 'production', so that is the default everywhere except production.
- `E05.03` _wt-wp08/src/app/welcome/plan/page.tsx — grep -c for 'applyTemplate|TEMPLATES' returns 0. The only branch that captures the wedding date applies no template; the only branch that applies the template captures no date. The two halves of the mechanism never meet.
- `E05.03` _wt-wp08/src/server/actions/comp.ts:259 — Second applying caller, same shape: no options.anchorDate, and the UPDATE at :260-265 sets only template_id and active_domain.
- `E05.03` _wt-wp08/src/server/db/apply-template.ts:43-49,77-81 — The code is correct and reads workspaces.primary_date as the anchor, writing dueAt and isMilestone. Verified in the diff. The implementation is sound; only its reachability and its test coverage are not.
- `E05.03` _wt-wp08/src/server/suite-navigation-contract.test.mjs:464,495 — THE BUILD AGENT'S CORRECTION OF THE PRIOR AUDIT IS CORRECT. Those two lines assert 'Confirm the final dietary list before the venue team locks the service notes' - demo fixture copy. The prior audit cited :428 and :459, which assert an IdentityCell render and a sourceNoteId respectively. Repo-wide, 'Collect final dietary notes' appears in exactly two places: templates.generated.ts:108 and the new ledger at wedding-template-contract.test.ts:54. The prior audit's 'nine touchpoints, removing it fai
- `E05.03` _wt-wp08/package.json — GOOD. The diff adds src/lib/template-anchor.test.ts and src/lib/wedding-template-contract.test.ts to the pnpm test chain, so the R-017 guard runs in CI rather than being a control that exists only ad hoc.
- `E05.03` _wt-wp08/scripts/sync-templates.ts:20,57-62 — Criterion 3 verified. resolve(__dirname, '../../studio/src/lib/templates') is real at :20. The slice at :57-62 copies id, name, description, icon, domain, tasks - dropping notes, which is exactly why criterion 9 fails.
- `E05.03` _wt-wp08/src/modules/timeline/server/sync/tasks-milestone-source.ts:133 — WHERE t.is_milestone = 1 confirmed, and src/modules/signal/lib/briefing/tasks-db-source.ts:114 selects t.due_at. Both consumer claims that motivate the whole build are true, so the mechanism is the right thing to have built.
- `E05.03` _wt-wp08/src/modules/timeline/lib/templates.generated.ts — Trap 4 confirmed independently: it carries the AUTO-GENERATED banner and grep over scripts/ finds no generator that writes it. Hand-maintained under a generated file's banner.
- `E05.04` evidence/E05.04-wedding-notes-and-voice-capture.md — Full criterion-to-proof evidence document with real command output, the fail-before run, and an explicit 'what is NOT done' section.
- `E05.04` src/modules/notes/app/notebook/hooks.ts:177 — useVoiceCapture — voice capture genuinely exists via the Web Speech API. This is the finding that most changed the task: the brief warned against claiming it or faking it, and it turned out to be real.
- `E05.04` src/modules/notes/app/page.tsx:50-51 — hybridNotebookEnabled = process.env.NOTES_LEGACY_NOTEBOOK_ENABLED !== '1'. HybridNotebook is the default; the legacy notebook needs an explicit opt-out. Establishes which surface 'shipped' means.
- `E05.04` src/server/db/ensure-user.ts:79-99 with src/server/actions/comp.ts:259-264 — CORRECTS a plausible wrong turn. A redeemed couple's workspace is provisioned with planning period context_type='general' and workspace context_type='project'; redemption then sets ONLY template_id and active_domain='wedding'. So contextType is NOT the wedding signal despite already reaching Notes in the workspace DTO. active_domain is the only signal.
- `E05.04` src/app/api/internal/workspace-personalization/route.ts:98 — The route already returns activeDomain. Notes' fetchTasksPersonalization had zero callers and dropped it. This is why criterion 5 (no new field/route/migration) is satisfiable at all.
- `E05.04` _wt-wp08/src/modules/notes/lib/notes-copy.ts — New. Two-register copy source. voice.disclosure is the string that closes the dictation privacy gap.
- `E05.04` _wt-wp08/src/modules/notes/lib/notes-copy.test.ts — New. 11 guards including 'dictation discloses that spoken words leave the device' and 'every dictation failure has its own plain-English message'.
- `E05.04` grep -rni 'speech|dictat|microphone' src/app/privacy/page.tsx src/app/terms docs — Zero disclosure anywhere before this task. The only hit was a code comment about brand 'voice'. This is the evidence for the new RAID candidate.
- `E05.04` _wt-wp08/src/modules/notes/app/notebook/hooks.ts — EXISTS, 305 lines. useVoiceCapture now at line 190 (HEAD 177). Diff adds only the VoiceEngineState tri-state and its plumbing. Voice capture is genuinely pre-existing, not fabricated.
- `E05.04` _wt-wp08/src/modules/notes/lib/notes-copy.ts:268 — DEFECT SOURCE. Wedding handoff.boundary ends 'Your venue never sees any of it.' That absolute promise is rendered above the control that moves the couple's words into Tasks, and approved words can reach an indexable public page. See failure_scenario.
- `E05.04` _wt-wp08/src/lib/public-task.ts:11 — toPublicTask publishes task.title on the unauthenticated allowlist. Combined with src/server/db/queries.ts:218 and src/app/p/[slug]/page.tsx ('Server-rendered for indexing', revalidate 60, /p absent from src/app/robots.ts disallow, no noindex), the approved wording is crawlable.
- `E05.04` _wt-wp08/src/modules/notes/app/Notebook.tsx:1144,1278,1300-1310 — The rollback notebook still calls useVoiceCapture, still gates the mic on `voice.supported &&` so it silently vanishes, still carries only three inline failure messages, shows no disclosure, and never receives activeDomain (page.tsx:174). Its whole diff is 4 lines of aria-label/copy.
- `E05.04` _wt-wp08/experience/feature-tests/notes-hybrid.spec.ts:88,152,168,170,172,175,254,259,262,307,309,312,315 — Eleven-plus assertions now reference strings this diff deleted. Not run by pnpm test, and not run by CI either: its config experience/notes-hybrid.playwright.config.ts has no npm script and no workflow step.
- `E05.04` _wt-wp08/src/app/api/internal/workspace-personalization/route.ts:75-82 — The wedding signal is read from a `.limit(1)` select with no ORDER BY. Pre-existing, but this task makes it load-bearing for which register a couple reads.
- `E05.04` _wt-wp08/package.json — notes-copy.test.ts IS registered in the `test` script's final node --import tsx segment. Verified in the diff and by the 413-test count. The guard is inside CI, not beside it.
- `E05.05` evidence/E05.05-notes-to-tasks-language.md — Criterion-to-proof document including a full before/after table of every rewritten string, the direct grep sweep, the fail-before run, and an explicit 'what is NOT done' section.
- `E05.05` _wt-wp08/src/modules/notes/lib/notes-copy.ts — New. Single source for every user-visible capture and hand-off string, in a generic and a wedding register, with BANNED_IN_COPY documenting why each term is forbidden and which of them actually shipped.
- `E05.05` _wt-wp08/src/modules/notes/lib/notes-copy.test.ts — New, 11 guards. Includes key-parity between registers so a wedding account can never fall through to a missing string, plus assertions that no string names a price or promises access forever (D-001 §5, D-009 pt 3, R-008).
- `E05.05` _wt-wp08/src/modules/notes/app/hybrid/HybridNotebook.tsx — The shipped surface. All hand-off strings now resolve through copy.*. A grep for the fourteen retired strings returns nothing; a broader sweep for approv|extract|receipt|payload|promot returns only the code comment explaining where the task id went.
- `E05.05` package.json — SHARED FILE, one line. Added src/modules/notes/lib/notes-copy.test.ts to the test chain so the guard runs in CI. Final test segment went 402 → 413. Declared for collision visibility.
- `E05.05` _wt-wp08/src/modules/notes/app/Notebook.tsx — Legacy rollback path. Only its two genuinely wrong strings fixed: aria-label='Promote note' and 'Signal Tasks', the latter violating docs/SUITE_URL_AND_NAMING_CONTRACT.md. Its remaining wording was already close and it is reachable only via NOTES_LEGACY_NOTEBOOK_ENABLED=1.
- `E05.05` _wt-wp08/package.json — EXISTS. One line changed, as declared. notes-copy.test.ts is in the final test segment. The same line also carries three sibling agents' additions (anchor-due, template-anchor, wedding-template-contract, assignee-roster-contract) — the declared collision surface is real.
- `E05.05` _wt-wp08/src/modules/notes/app/page.tsx:87-111,169 and src/app/api/internal/workspace-personalization/route.ts:78,90,98 — Checked specifically for the 'control that exists only in a test' failure mode. It is NOT one. The route genuinely returns activeDomain, page.tsx fetches it via fetchNotesWorkspaceDomain and passes it to the component. The wedding register is reachable in production.
- `E05.05` _wt-wp08/src/modules/notes/server/notes-cross-product-assertion.ts:64-65, 98-99 — noteId and workspaceId are claims in both the v1 and v2 signed envelopes, so they still cross to Tasks. The retired privacy sentence named them; the replacement does not.
- `E05.06` evidence/E05.06-wedding-tasks-experience.md — Full criterion-to-proof trace with verbatim command output, the fail-before mutation result, and the scope correction.
- `E05.06` _wt-wp08/src/components/app/detail-panel/field-rows.tsx — AC1/AC2/AC4. Was line 26: the five-name seed-persona constant, rendered at line 291 as the whole assign menu. Now reads useWorkspaceMembers() and branches on members.length === 0. DueRow now renders the anchor relation.
- `E05.06` _wt-wp08/src/lib/members.ts:1-6 — AC1. The rule was already written in prose — 'the assign menu ... Resolved server-side from workspace_members ⋈ users' — and nothing enforced it. src/lib/domain-context.tsx says the same: 'never from design-lab fixtures'.
- `E05.06` _wt-wp08/src/components/hybrid/options/a/board-view.tsx:241 — AC1. The board already did this correctly via listPeople(); only the task detail was missed. This is why the fix is a wiring change, not a new system.
- `E05.06` _wt-wp08/src/components/hybrid/shared/task-inspector.tsx:108 — AC1, extension to the prior audit. Same class of defect (imports LAB_PEOPLE directly, bypassing listPeople) but it is NOT live: repo-wide grep finds no importer except suite-navigation-contract.test.mjs:35. Dead code. Recorded, not fixed.
- `E05.06` _wt-wp08/src/server/db/schema.ts:308-309 — AC3. workspaces.primary_date / primary_date_label — 'Calendar date, never an instant.' The wedding date IS a first-class field.
- `E05.06` _wt-wp08/src/components/welcome/contextual-onboarding.tsx:383 — AC3. The field is literally labelled 'Wedding date' and marked required when the context is a wedding; src/server/actions/planning.ts:875 refuses a wedding_season period without one.
- `E05.06` _wt-wp08/src/app/welcome/page.tsx:69-85 — AC3, the finding. The production branch applies the template and sets template_id, active_domain, primary_use_case and onboarding_completed_at — and never writes primary_date. The branch that DOES capture the date (:59-67) is gated on a flag that src/lib/planning/flags.ts:33 defaults OFF in production.
- `E05.06` _wt-wp08/src/server/actions/comp.ts — AC3. redeemCompCodeAction applies the wedding template and short-circuits to /app/tasks. It never writes primary_date. Combined with the above: a redeemed couple's workspace has primary_date = NULL in production, always.
- `E05.06` _wt-wp08/src/components/app/settings/sections/workspace.tsx — AC3. Zero matches for primaryDate, primary_date, 'Wedding date' or anchor. There is no field anywhere in the app to set or change the wedding date after the workspace exists.
- `E05.06` _wt-wp08/src/lib/tasks/anchor-due.ts — AC4/AC5/AC7. Pure module. relateDueToAnchor returns null for a missing or malformed date rather than guessing; counts are exact calendar days; the noun comes from primary_date_label so 'Examinations' works, and a label already carrying an article is not doubled.
- `E05.06` _wt-wp08/src/lib/tasks/anchor-due.test.ts — AC4/AC5/AC7/AC8. 14 tests: DST boundary, leap day, 2027-02-30 refused, 12/06/2027 refused, singular/plural, three tenses, and an explicit assertion that no generated line contains an em dash or an exclamation mark.
- `E05.06` _wt-wp08/src/components/app/detail-panel/assignee-roster-contract.test.mjs — AC1/AC2/AC8. Source-scan regression guard. Fail-before proven: restoring the ALL_USERS constant produced 'ℹ tests 4 / ℹ pass 3 / ℹ fail 1' on exactly the seed-persona test, then the mutation was reverted.
- `E05.06` _wt-wp08/src/lib/domain-context.tsx — AC4. anchorDate/anchorLabel added to DomainCtx with useWorkspaceAnchor(); both props optional and defaulted to null, so the single existing DomainProvider caller was the only one that had to change.
- `E05.06` _wt-wp08/src/lib/template-anchor.ts — AC3, independent corroboration. The concurrent E05.03 package in this same worktree reached workspaces.primary_date from the write side and made the same refusal (no anchor means no due date, never days-from-now). Two packages arriving at the same field independently.
- `E05.06` _wt-wp08/src/components/hybrid/fixtures.test.ts:83 — AC2, and a finding. The 'empty roster reads as empty, never eight fixture people' property was already asserted here — but grep -c 'hybrid/fixtures.test.ts' package.json returns 0. It was tested and never run. The new contract test IS in the chain.
- `E05.06` _wt-wp08 — all 20 claimed evidence paths — Checked every one with a file-existence loop. 20 of 20 EXIST. No dead paths.
- `E05.06` pnpm test in C:/Users/ethan/signal-studio-workspace/_wt-wp08 — PNPM_TEST_EXIT=0. Group totals extracted from the log: 13/50/70/20/31/20/90/45/239/413, every group 'ℹ fail 0'. This is an EXACT match to the claimed totals.
- `E05.06` node --import tsx --test src/lib/tasks/anchor-due.test.ts — ℹ tests 14 / ℹ pass 14 / ℹ fail 0. Reproduces the claim exactly. Includes the DST-boundary, leap-day, 2027-02-30-refused and 'no em dash or exclamation mark' assertions.
- `E05.06` node --test src/components/app/detail-panel/assignee-roster-contract.test.mjs — ℹ tests 4 / ℹ pass 4 / ℹ fail 0. Reproduces exactly.
- `E05.06` FAIL-BEFORE, re-run independently by me — I re-inserted `const ALL_USERS: UserId[] = ["chloe","david","alex","ada","marcus"]` into field-rows.tsx myself. Result: '✖ no seed persona id is hard-coded as an assignable person', AssertionError naming persona "chloe", exit 1 — exactly one failing test, the right one. I then restored from backup; git diff --stat returns to '68 +++, 60 insertions(+), 8 deletions(-)' and the test is 4/4 green. The contract test is NOT tautological.
- `E05.06` pnpm typecheck — TYPECHECK_EXIT=0. The disclosed mid-session TS2540 NODE_ENV failure in the concurrent package's unpublished-timeline-boundary.test.ts is genuinely resolved.
- `E05.06` pnpm ds:check — 'ds-check: clean — no drift.' DS_EXIT=0.
- `E05.06` npx eslint on all nine changed files — ESLINT_EXIT=0, zero problems. I included conversation-feed.tsx and task-detail.tsx, which the evidence table omits.
- `E05.06` _wt-wp08/package.json (git diff) — AC8. Both new test files are genuinely in the chain: assignee-roster-contract.test.mjs added to the node --test group, src/lib/tasks/anchor-due.test.ts added to the tsx group. Verified in the diff, not asserted.
- `E05.06` _wt-wp08/src/components/app/task-detail/metadata-rail.tsx:114-128 — AC6 DEFECT. Tags render as read-only <span> chips with one uniform neutral style (bg-bg-sunken/60, text-ink-soft), inside a `task.tags && task.tags.length > 0` guard. No button, no onClick, no updateTask. There is no per-tag colour lookup in this file at all.
- `E05.06` _wt-wp08 repo-wide grep for tag-setting controls — AC6 DEFECT, decisive. `grep -rn tags src/components/app/ --include=*.tsx | grep -E 'updateTask|setTags|onChange|onClick'` returns EMPTY. `grep -rln 'TagRow|TagPicker|TagEditor' src/` returns EMPTY. Tags cannot be added or removed from the task detail, or from anywhere in src/components/app/.
- `E05.06` _wt-wp08/src/lib/domain-context.tsx:171 useTagDefs() — AC6 DEFECT. useTagDefs() exists and tagDefs are threaded from the server through DomainProvider, but it has ZERO consumers in src/components/app/. The colour source is delivered to the client and never read, which is why the 'live colours' claim is false.
- `E05.06` _wt-wp08/src/app/app/your-work/page.tsx:13 and your-work-view.tsx:562-563,627,631 — AC3/AC4, extension the build agent did not record. The ONE surface in the app with a primaryDate input is the Your Work bulk-create form. Its route redirects to /app/tasks when the planningPeriods flag is off, and flags.ts:33 defaults it OFF in production, exactly like contextualOnboarding.
- `E05.06` _wt-wp08 grep for `primary_date/s*=` across src/ — AC3, my finding. Returns NOTHING. There is no UPDATE path for primary_date anywhere. Every write is an INSERT at workspace creation. The field is not merely unset for redeemed couples, it is unsettable after creation by any surface.
- `E05.06` _wt-wp08/src/components/app/detail-panel/field-rows.tsx nameFor() — AC1 DEFECT, narrow. `nameFor = (id) => members.find(m => m.id === id)?.name ?? USERS[id].name`. USERS (src/lib/data.ts:150) is a Proxy over SEEDED returning the canonical persona row for a known seed id, so an assigned-but-non-member seed id still renders a persona name in the avatar strip. Latent rather than live: apply-template.ts:74 writes `assignees: []` and the wedding template carries no persona ids. The contract test cannot catch it — it scans for quoted ids only.
- `E05.06` _wt-wp08 git status / git diff --stat — UNDECLARED SCOPE. conversation-feed.tsx and task-detail.tsx are modified and belong to this change (the mention pool now reads useWorkspaceMembers, and task-detail passes assigneeIds). `git show HEAD:...conversation-feed.tsx | grep useWorkspaceMembers` returns nothing, so the change is new. The evidence doc's Files touched table lists 8 files and omits both.
- `E05.06` PRIVACY BOUNDARY — checked, clean — No new wedding-date egress. src/lib/public-task.ts carries no primaryDate or anchor field. The only route exposing primaryDate, src/app/api/internal/workspaces/route.ts, is HMAC-signed with timingSafeEqual, jti replay tracking and membership-scoped rows, and was NOT touched by this change. The anchor renders only inside the authenticated app shell to workspace members.
- `E05.06` PROGRAMME FAILURE MODES — checked, clean — Grepped all added lines across the six changed source files for forever / for life / in perpetuity / seat / allotment / dietary / 18 month / eighteen month / guaranteed: zero hits. No generated-file edit (no 'generated' or 'do not edit' header on any E05.06 file). No invented dietary field, so R-017 is not touched. No flat eighteen-month access term and no seat count.
- `E05.06` _wt-wp08 grep -c 'hybrid/fixtures.test.ts' package.json → 0 — AC2. Independently confirms the build agent's own self-reported finding that fixtures.test.ts was written and never run. Honest disclosure, verified true.
- `E05.07` evidence/E05.07-task-detail-experience.md — Full criterion-to-proof trace, including the three-ships / one-broken / one-absent verdict table.
- `E05.07` _wt-wp08/src/server/db/schema.ts:479-516 — AC1. comments table with cascade, author FK and both hot indexes, plus the compile-time contract that keeps the client Comment type covered.
- `E05.07` _wt-wp08/src/server/actions/attachments.ts:35-44 — AC2/AC7. BLOCKED_MIME_TYPES refuses text/html, image/svg+xml, xml and the executable shapes, with an honest comment: 'Not a full antivirus pass; enough to block the obvious executable + stored-XSS shapes.'
- `E05.07` _wt-wp08/src/server/actions/attachments.ts:182-235 — AC2. Active-workspace ownership check before any write, then claim-then-verify: the metadata row is inserted before the bytes, the usage SUM includes the claim, and an over-quota or failed byte write deletes the claim.
- `E05.07` _wt-wp08/src/app/api/attachments/[id]/route.ts — AC2. Authenticated download re-checks the workspace and returns 404 (not 403) for mismatched, unknown and missing; sends X-Content-Type-Options: nosniff, default-src 'none'; sandbox; frame-ancestors 'none', and Cache-Control: private, no-store.
- `E05.07` _wt-wp08/src/server/db/schema.ts:73-83 — AC3. parentTaskId with one level of nesting; getTasks filters parent_task_id IS NULL so subtasks stay out of board, list, timeline and calendar.
- `E05.07` _wt-wp08/src/components/app/detail-panel/subtasks-section.tsx — AC3. 372 lines: getSubtasksAction reads, addTaskAction/updateTaskAction write, ReorderList drag, and clicking a subtask swaps the panel's active id with a working back button.
- `E05.07` _wt-wp08/src/components/app/detail-panel/conversation-feed.tsx — AC4. Was: mention pool = assignees (never passed) plus past commenters. Now reads useWorkspaceMembers() and merges members, assignees and past speakers, with member names winning over the seed USERS proxy.
- `E05.07` _wt-wp08/src/components/app/task-detail/task-detail.tsx — AC4. PrimaryContent rendered ConversationFeed with no assigneeIds prop at all; it now passes task.assignees.
- `E05.07` _wt-wp08/src/lib/templates.generated.ts — AC5. The ONLY trace of decisions anywhere is a 'decision' tag on three wedding-template tasks. No decisions table in src/server/db/schema.ts, no decision route under src/app/app/.
- `E05.07` _wt-wp08/src/server/db/schema.ts:99-110 — AC5. The near-miss re-confirmed: tasks.source_note_extract_body already stores 'exact creator-approved wording' with a SHA-256 alongside. grep -rn sourceNoteExtractBody across src/components and src/app returns ZERO. The one field that already records a decision someone made is read by no component.
- `E05.07` _wt-wp08/src/lib/public-task.ts — AC6. Field-by-field allowlist with its reason written in: 'so adding a private column to Task cannot silently publish it through /p, /embed, or /share'. Comments, attachments, subtasks, assignees, description, contact and amount are all absent.
- `E05.07` _wt-wp08/src/lib/public-task.test.ts:33 — AC6. 'public task projection is a stable allowlist' — and it IS in the pnpm test chain, so the boundary is machine-enforced rather than documented.
- `E05.07` _wt-wp08/src/app/api/internal/partner-stats/route.ts:25-28 — AC6. The only venue-facing egress from the app returns four scalars — codesRedeemed, redeemed30d, reachedBoard, mostRecentRedemptionAt. No task, comment, attachment, subtask, name or title.
- `E05.07` _wt-wp08/src/components/app/detail-panel/assignee-roster-contract.test.mjs — AC4/AC8. Covers conversation-feed.tsx as well as field-rows.tsx: both must call useWorkspaceMembers(), neither may name a seed persona, neither may import the design-lab fixture roster.
- `E05.07` _wt-wp08/src/server/actions/comments.ts:38-50 — THE DEFECT. VALID_USER_IDS = new Set(Object.keys(USERS)) resolves to the six seed persona ids because USERS is a Proxy over SEEDED with no ownKeys trap. extractMentions fires notify() only for those. Real Clerk-backed members can never match.
- `E05.07` _wt-wp08/src/components/ui/mention-field.tsx:204 — const mention = `@${person.name}` — the composer inserts the display name, not an id and not a handle. Nothing downstream resolves a display name.
- `E05.07` _wt-wp08/src/server/db/schema.ts:181-183 — users.handle is declared as 'Mention slug, what @<handle> matches in the comment composer' and is read by no mention code anywhere. The intended design is documented in the schema and unbuilt.
- `E05.07` _wt-wp08/src/server/db/notifications.ts:22-56 — notify() inherits the workspace from the task but never checks that userId is a member of it, so a mention misrouted to a seed persona id writes a live but unreachable row inside the couple's workspace.
- `E05.07` evidence/E05.07-task-detail-experience.md:114-116 — The false load-bearing sentence: 'The downstream mechanism was already correct and was not touched.' Refuted by the reproduction above. The same claim is duplicated as a code comment in conversation-feed.tsx.
- `E05.07` _wt-wp08/src/lib/public-task.ts + src/server/db/queries.ts:218,744-745 — AC6 UPHELD after an attempt to break it. toPublicTask is the sole projection on all four unauthenticated route families (/p, /p opengraph, /embed, /share). No private field escapes.
- `E05.07` _wt-wp08/src/app/api/internal/partner-stats/route.ts:33-56 — AC6 UPHELD. The only venue-facing egress: bearer + length-guarded timingSafeEqual, returns four scalars. No task, comment, attachment, subtask or name.
- `E05.07` _wt-wp08/src/server/db/queries.ts:103,136,331,831 — AC3 UPHELD. isNull(tasks.parentTaskId) at all four read sites, so subtasks are genuinely absent from board, list, calendar and timeline.
- `E05.07` _wt-wp08/src/app/api/attachments/[id]/route.ts:34-35,67-70 — AC2 UPHELD. 404-not-403 on mismatch, plus nosniff, sandbox CSP and private/no-store, verbatim as claimed.
- `E05.07` git status --porcelain in C:/Users/ethan/signal-studio-workspace/_wt-wp08 — AC7 UPHELD. No file under src/server/actions/attachments.ts, src/server/storage.ts, src/app/api/attachments/** or resources-section.tsx is modified. No upload path added or widened.
- `E05.08` _wt-wp08/src/modules/timeline/server/unpublished-timeline-boundary.test.ts — 7/7 pass. Criteria 1-4 and 6 asserted behaviourally against a real database and the real resolver, not against source text.
- `E05.08` app/src/modules/timeline/server/audience-timeline.ts:755 — The publish gate. Criterion 1 and 2.
- `E05.08` app/src/modules/timeline/server/audience-timeline.ts:759-769 — The explicit five-column public projection. Criterion 4, asserted on a real serialised DTO and on the item key set.
- `E05.08` app/src/modules/timeline/server/audience-timeline.ts:587-595 — Unpublish revokes every active share transactionally. Criterion 2 and 3.
- `E05.08` app/src/modules/timeline/server/sync/tasks-milestone-source.ts:135 — A Tasks task becomes a Timeline milestone only when is_milestone = 1, ordered by due_at. Criterion 5.
- `E05.08` app/src/server/db/apply-template.ts:74-81 — CORRECTS THE RECON MAP. apply-template does set dueAt (via resolveTemplateDueAt) and isMilestone. The recon said it never sets dueAt. The mechanism is built; the template declares neither.
- `E05.08` app/src/lib/templates.generated.ts (wedding-planning-workspace) — Measured 2026-08-03T07:13:31Z: 18 tasks, 0 dueOffsetDays declared, 0 milestones declared. So the couple's Timeline would render empty even once reachable. Criterion 5.
- `E05.08` app/src/app/api/internal/partner-stats/route.ts — The only venue-facing egress. Asserted to contain none of timeline/Timeline/publication/audienceShares/workspaceSlug/tasks and all four returned scalars. Criterion 6.
- `E05.08` app/src/modules/timeline/server/actions/workspaces.ts:93 — createWorkspaceAction re-verified as having zero callers across src, e2e, scripts, experience. One definition, one internal call, one comment. Criterion 7.
- `E05.08` app/src/modules/timeline/server/audience-timeline.ts:49 — NEW, not in the prior audit: the entire audience publication surface is behind SIGNAL_AUDIENCE_TIMELINE_ENABLED. If unset in production, publishing is unavailable regardless of everything else.
- `E05.08` studio/docs/execution/venue-edition-and-films/evidence/E05.08-timeline-planning-boundary.md — Criterion-to-proof trace, the fail-before mutation output verbatim, and the R-031 statement that does not paper over /p.
- `E05.08` _wt-wp08/src/modules/timeline/server/audience-timeline.ts:755 — VERIFIED IN CODE. `if (!publication || publication.state !== "published") return { kind: "unpublished" };`. Criteria 1 and 2 hold.
- `E05.08` _wt-wp08/src/modules/timeline/server/audience-timeline.ts:759-769 — VERIFIED IN CODE. Five-column select (publicId, title, calendarDate, state, sortOrder) and the DTO mapping at :794-799 narrows further to four emitted keys. Criterion 4 holds.
- `E05.08` _wt-wp08/src/modules/timeline/server/audience-timeline.ts:587-595 — VERIFIED IN CODE. unpublishAudiencePublication sets state='unpublished' and, in the same db.transaction, updates every audienceShares row with state='active' to 'revoked'. Criteria 2 and 3 hold.
- `E05.08` MY OWN MUTATION A — delete the publish-gate term at :755 — MY RESULT, not the builder's: 'ℹ tests 7 / ℹ pass 5 / ℹ fail 2', failing exactly 'an unpublished planning Timeline never resolves' and 'unpublishing closes it again for the same token'. Matches the claim exactly. File restored; git diff on it empty.
- `E05.08` MY OWN MUTATION B — leak sourceRelation into the emitted DTO item (independent, not the builder's mutation) — MY RESULT: 'ℹ tests 7 / ℹ pass 4 / ℹ fail 3', red on 'the published projection carries presentation fields only' plus two others via the DTO validator. This is the check the builder did not run and it is the one that matters: criterion 4's test is NOT tautological — widening the projection is genuinely caught.
- `E05.08` _wt-wp08/src/lib/templates.generated.ts (wedding-planning-workspace, lines 15-175) — MEASUREMENT INDEPENDENTLY REPRODUCED AND EXACT: 18 "title" keys, 0 dueOffsetDays, 0 milestone. Four "due" string literals only (Today, Tomorrow, Fri x2). The file is UNMODIFIED in the worktree — no generated-file edit, so the recurring failure mode is clear.
- `E05.08` _wt-wp08/src/server/db/apply-template.ts — REFUTES THE EVIDENCE NOTE. Working tree sets dueAt/isMilestone; HEAD does not. `git show HEAD:` grep for dueAt|isMilestone|resolveTemplateDueAt returns nothing. src/lib/template-anchor.ts is untracked (another package's work).
- `E05.08` _wt-wp08/src/modules/timeline/server/actions/workspaces.ts:422,616,653 — THE PATH THE ENUMERATION MISSED. Three Timeline actions call recordSponsoredUse into the venue-facing instrumentation layer, passing workspaceSlug as objectKey and the Clerk id as subjectId.
- `E05.08` _wt-wp08/src/lib/account/instrumentation/emitter.ts:100-101 and sink.ts:23-36 — WHY IT IS NOT A LIVE LEAK. subjectId and workspaceId are salted SHA-256 hashed before the payload is built; the payload has no content field; sponsoredUseSink is a noop that increments a counter and posts nowhere. The omission is an enumeration defect, not a disclosure.
- `E05.08` _wt-wp08/src/modules/timeline/server/actions/workspaces.ts:93 — CRITERION 7 RE-DERIVED BY ME. Repo-wide grep for createWorkspaceAction returns exactly three lines: the definition at :93, a rate-limit comment at :183, and a doc comment at timeline-queries.ts:228. Zero call sites. The prior audit's blocker holds.
- `E05.08` Repo-wide grep for timelinePublications|timelinePublicationItems|audienceShares (7 files) — MY OWN SEARCH FOR A SECOND BEARER PATH — none exists. resolveAudienceTimeline is called only from src/app/s/[token]/page.tsx:16,45. The other readers are timeline-gdpr.ts (scoped to workspaces.ownerUserId = the couple's own clerkId, and it omits tokenHash), qualified-view-store.ts, the schema, and two tests. There is no route that reads publication content without the publish gate.
- `E05.08` _wt-wp08/src/app/s/[token]/page.tsx:29 — VERIFIED. `robots: { index: false, follow: false, noarchive: true, nosnippet: true }`. src/app/robots.ts disallows /app, /s, /share, /redeem, /welcome, /api — and, as R-031 says and this evidence correctly refuses to paper over, NOT /p.
- `E05.08` evidence/E05.08-timeline-planning-boundary.md — EXISTS (11,936 bytes), read in full. Honest in register: states criterion 7 as PROVEN NOT FIXED, states the partner-stats small-cell gap against itself, states the typecheck caveat, and does not describe the token-bound model as covering /p.
- `E05.08` tasks/E05.08.md — EXISTS. Founder sign-off block correctly left unticked. Deliverable 5 (Timeline provisioning) correctly marked unbuilt.
- `E05.08` RECURRING FAILURE-MODE SWEEP over both E05.08 documents — CLEAN. Regex sweep for '18 month|eighteen month|548|seat|forever|for life|in perpetuity|permanen|dietary|allerg|guaranteed|exclusive' returns ONE hit across both files: evidence line 160, 'which is null forever', describing a null return value. No flat 18-month term, no seat count, no permanence promise, no invented dietary field, no green gate that is red.
- `E05.09` _wt-wp08/src/modules/signal/lib/briefing/wedding-briefing-selectivity.test.ts — 9/9 pass. Criteria 1-5 and 7-8.
- `E05.09` app/src/modules/signal/lib/briefing/build.ts:19,80-93,185,196 — BUCKET_CAP, per-task dedupe, readCount denominator, pre-cap triggeredCount. Criteria 1-4.
- `E05.09` app/src/modules/signal/lib/planning-periods/scope.ts:105-111 — CORRECTS THE RECON MAP. The catalog query joins workspace_members, so an invited spouse's briefing does cover the shared wedding. The recon recorded Signal as per-user; that is true only of the analytics_users preference row (signal-build-for-user.ts:208), not of the data. Criterion 6.
- `E05.09` app/src/modules/signal/lib/briefing/tasks-db-source.ts:126-128 — The DB source filters on workspace_id IN (SELECT workspace_id FROM workspace_members WHERE user_id = ?). Criterion 6.
- `E05.09` app/src/lib/template-anchor.ts:50-58 — resolveTemplateDueAt returns null when either the offset or the anchor is absent. Its header states the refusal deliberately: a fabricated deadline is worse than an absent one. Correct behaviour, recorded as such. Criterion 8.
- `E05.09` app/src/server/actions/comp.ts:259 — Redemption calls applyTemplateToWorkspace with no anchorDate, so it reads workspaces.primary_date, which redemption never sets. This is the missing input.
- `E05.09` app/src/lib/templates.generated.ts (wedding-planning-workspace) — Measured 2026-08-03T07:13:31Z: 18 tasks, 0 offsets, 0 milestones, lanes {done:2, doing:4, review:3, todo:9}. Fed through the real engine: zero due-soon, zero crowded-week, and exactly one row, synthetic:overload 'Seven items open at once'. Criterion 7.
- `E05.09` studio/docs/execution/venue-edition-and-films/evidence/E05.09-wedding-briefing-selectivity.md — Criterion-to-proof trace, the measurement command, and the note on why the template's counts are deliberately NOT pinned as test equalities while E05.03 rewrites it in the same worktree.
- `E05.09` _wt-wp08 — all 8 claimed evidence paths checked individually — NO DEAD PATHS. wedding-briefing-selectivity.test.ts, build.ts, planning-periods/scope.ts, tasks-db-source.ts, template-anchor.ts, comp.ts, templates.generated.ts, triggers.ts — all EXIST. The VEF evidence doc E05.09-wedding-briefing-selectivity.md exists (8237 bytes, Aug 3 08:17).
- `E05.09` node --import tsx --import ./src/test/register-server-only.mjs --test src/modules/signal/lib/briefing/wedding-briefing-selectivity.test.ts — RE-RAN MYSELF. Real output: 9 checkmarks then 'tests 9 / suites 0 / pass 9 / fail 0 / duration_ms 553.7056'. Matches the claim exactly.
- `E05.09` Independent probe of src/lib/templates.generated.ts (my own script, not the agent's) — REPRODUCES THE MEASUREMENT EXACTLY: 'tasks: 18 / offsets: 0 / milestones: 0 / lanes: {"done":2,"doing":4,"review":3,"todo":9}'. Identical to the claimed 2026-08-03T07:13:31Z measurement.
- `E05.09` Independent probe feeding the real template through buildBriefing at NOW=2026-08-03T09:00Z — REPRODUCES THE ENGINE RESULT EXACTLY: 'readCount: 18 triggeredCount: 0 isEmpty: false / needsAttention: [{id: synthetic:overload, trigger: overload, text: Seven items open at once}] / quietRisks: 0 movingWell: 0'. Zero due-soon, zero crowded-week, exactly one synthetic row. Criterion 7 is genuinely measured, not asserted.
- `E05.09` studio/src/lib/templates/wedding-planning-workspace/tasks.ts (canonical source) — THE '0 OFFSETS' MEASUREMENT IS NOT STALE — I checked the risk. grep -c dueOffsetDays = 0 and grep -c milestone = 0 in the CANONICAL studio source too, so running `pnpm sync:templates` would not change the generated file. The measurement survives regeneration.
- `E05.09` _wt-wp08 git status --short — NO GENERATED-FILE EDIT. src/lib/templates.generated.ts (header: 'AUTO-GENERATED, do not edit by hand') is NOT in the modified list. The worktree's template changes are confined to the hand-written scripts/sync-templates.ts and src/lib/templates.ts. The programme's 'edit to a generated file a build step will overwrite' failure mode is absent.
- `E05.09` grep -c wedding-briefing-selectivity _wt-wp08/package.json -> 0; siblings anchor-due/template-anchor/wedding-template-contract/notes-copy/assignee-roster all -> 1 — THE DEFECT. The new test is absent from the CI chain while five sibling new tests from the same worktree were added to it in the same diff. .github/workflows/ci.yml:58 and verify.yml:30 both run `pnpm test`; package.json has no glob-based discovery.
- `E05.09` src/modules/signal/lib/planning-periods/scope.ts:105-111 and src/modules/signal/lib/briefing/tasks-db-source.ts:126-128 — CRITERION 6 CORRECTION IS REAL — I read both queries. scope.ts: 'INNER JOIN workspaces w ON w.owner_user_id = cu.id OR EXISTS (SELECT 1 FROM workspace_members wm WHERE wm.workspace_id = w.id AND wm.user_id = cu.id)'. tasks-db-source.ts: 'WHERE t.workspace_id IN (SELECT workspace_id FROM workspace_members WHERE user_id = ?)'. The recon map's 'Signal is per-user' read is genuinely wrong about the data.
- `E05.09` src/server/db/ensure-user.ts:108-110 and src/modules/signal/lib/briefing/tasks-db-source.ts:78-93 — I CLOSED AN ASYMMETRY THE BUILD AGENT DID NOT MENTION. scope.ts admits owner-OR-member; tasks-db-source.ts admits member-only. An owner without a workspace_members row would be listed in the catalog yet read zero tasks. ensure-user.ts:108 saves it: 'INSERT OR IGNORE INTO workspace_members (workspace_id, user_id, role) VALUES (workspaceId, clerkUserId, owner)'. Id types also line up — tasksUserId resolves clerk_id -> users.id, and app-access.ts:55 records that ensureUserProvisioned writes users.i
- `E05.09` src/server/actions/comp.ts:259 plus grep for primary_date across comp.ts — THE CLAIMED GAP IS REAL. Line 259 is `await applyTemplateToWorkspace(VENUE_TEMPLATE_ID, ws);` with no options argument, and comp.ts contains ZERO references to primary_date/primaryDate. The sibling diff to src/server/db/apply-template.ts makes the no-options path read workspaces.primary_date, which redemption never sets. Missing input confirmed.
- `E05.09` pnpm ds:check and pnpm typecheck in _wt-wp08 — RAN BOTH MYSELF. ds:check -> 'ds-check: clean — no drift.' typecheck (tsc --noEmit --incremental false) -> no output, exit clean. Both claims true.
- `E05.09` node --import tsx --import ./src/test/register-server-only.mjs --test over six Signal briefing suites (triggers, build, signal-build-for-user, tasks-db-source, voice, calendar-time) — RAN MYSELF: 'tests 127 / suites 28 / pass 127 / fail 0'. Green. My set differs from the agent's claimed 117 (different suite selection), so the numbers are not comparable, but no regression exists either way.
- `E05.09` Failure-mode sweep: grep -niE '18 month|eighteen month|seat|forever|for life|in perpetuity|permanen|dietary|allerg|guaranteed' over the E05.09 evidence doc and the new test file — NO HITS on either file. No flat eighteen-month term, no seat count, no permanence wording, no invented dietary field (R-017 respected). No commercial claim is made anywhere in this task's artifacts.
- `E05.09` src/modules/signal/lib/analytics/ledger-adapters.ts:107 with voice.ts:96-112 — CRITERION 1's SECOND HALF VERIFIED IN CODE, not just in the data object. healthyEmptyState.body = readCountSentence(briefing.readCount, briefing.triggeredCount) ?? emptyStateBody ?? fallback — so the denominator sentence WINS over the wedding segment phrasing. The empty state does state its denominator.
- `E05.10` _wt-wp08/src/server/venue-never-a-collaborator.test.ts — 8/8 pass. Criteria 3-8. Six role values (venue, sponsor, coordinator, planner, admin, empty string) driven through the real accept SQL all land as member; three sponsor references all fail as tokens.
- `E05.10` app/src/app/invite/[token]/page.tsx — THE FIX, and the FOURTH price surface in the product. The E05-E06 audit found three (Notes, billing settings, Timeline cap message) and did not reach the invitation path. Criterion 7 and 9.
- `E05.10` app/src/server/db/schema.ts:467,933 — workspace_members.role and pending_invites.role are typed to exactly owner|member. Criterion 4.
- `E05.10` app/src/server/actions/settings.ts:348-349,585 — Two independent clamps, on write and on accept. The second is why criterion 4 passes even against a hand-written database row. Criterion 4.
- `E05.10` app/src/server/planning/queries.ts:225-245 — listSponsorActivationDTOs asserted to reach none of workspaceMembers, pendingInvites, tasks, notes, comments, attachments, shareLinks. It has zero production callers, which is exactly why pinning it now is worth more than pinning it later. Criterion 6.
- `E05.10` app/src/server/db/membership.ts:105-114 — wedding is in the unlimited set and unlimited is max: null, never a large number. D-020 satisfied structurally. Criterion 8.
- `E05.10` app/src/server/actions/comp.ts:235-244 — Redemption writes workspaceId: ws. Had it written a user-level entitlement, getWorkspaceTier would return free and a sponsored couple would be capped at four and shown an upgrade prompt. One line from being false.
- `E05.10` app/src/modules/notes/server/actions/notes.ts:257 and app/src/modules/timeline/server/db/timeline-queries.ts:167 — Notes are per user and Timeline workspaces per owner. An invited spouse gets Tasks and Signal and NOTHING in Notes or Timeline, whatever role they are given. That is what 'invite your spouse' actually buys today.
- `E05.10` studio/docs/execution/venue-edition-and-films/evidence/E05.10-collaborator-access-matrix.md — The six-row matrix with verdicts, the cross-product delivery table, the fail-before output for the copy assertion, and the raised risk about the missing database constraint.
- `E05.10` app/src/server/email.ts:79-82 — THE REFUTATION, and it was not found by the build agent. inviteEmailHtml still renders the identical banned sentence. I applied the build agent's own forbidden list to the template body: 'VIOLATION: the invite email carries "editing guests" / "on Free" / "on Team" / "No card" / "trial" / total violations: 5'. Five hits versus four on the landing page, because the email's line wrapping makes 'editing guests' contiguous. `git diff --stat src/server/email.ts` returns nothing. Criterion 7 is refuted
- `E05.10` app/src/server/require-app-access.ts:31-33 with app/src/app/app/layout.tsx:16 and app/src/app/invite/[token]/accept-button.tsx:24,26 — THE SECOND REFUTATION. layout.tsx:16 awaits requireAppAccess() inside SharedAppGate, which wraps children at :45-47. require-app-access.ts:31-33 is `if (!isEmailAllowed(email)) redirect("/waitlist")`, and access-allowlist.ts:38-41 fails closed on any address not in SIGNAL_ALLOWLIST. accept-button.tsx toasts 'Open the board, you're in.' and pushes /app/tasks. requireAppAccessTasks (app-access.ts:29), the membership-aware gate built precisely for this, is wired at tasks-runtime-shell.tsx:67, insid
- `E05.10` app/src/app/api/internal/partner-stats/route.ts — The live sponsor-facing read path the evidence never opened. Consumed by studio/src/lib/partners/stats.ts:104 for /hq/partners. Returns codesRedeemed, redeemed30d, reachedBoard and mostRecentRedemptionAt per sponsor slug, with no suppression floor of any kind. Reaches comp_codes and entitlements only, so criterion 6's literal wording survives, but reachedBoard is an unsuppressed behavioural count and mostRecentRedemptionAt is an exact per-couple timestamp. R-027's shape, on a shipped path, in th
- `E05.10` app/src/server/db/schema.ts:467 and :933 — CONFIRMED at the exact cited lines. :467 `role: text("role").$type<"owner" | "member">().notNull().default("member")`. :933 `role: text("role").$type<"member" | "owner">().notNull().default("member")`. Criterion 4's structural half holds.
- `E05.10` app/src/server/actions/settings.ts:349, :584-585 — CONFIRMED. Both clamps read as claimed. Also found at :357-358, inside the same invite action, the string 'Free workspaces include three editing guests. Upgrade to Workspace to invite more.' It is gated by canAddMember and cannot fire for a wedding workspace (max is null), so it is not a live violation — but it is a fifth seat-count string on the invitation path that the evidence does not record, and the same test would not catch it if the gate ever changed.
- `E05.10` app/src/server/db/membership.ts:105-112 — CONFIRMED. `const isUnlimited = tier === "workspace" || tier === "studio" || tier === "wedding" || tier === "event"` and `max: isUnlimited ? null : FREE_WORKSPACE_MEMBER_CAP`. Unlimited is an absent cap, never a large number. Criterion 8 CONFIRMED, and the comp.ts:236 `workspaceId: ws` dependency the evidence flags is real and correctly identified.
- `E05.10` package.json (test script) — Programmatic check of the full chain: 'NOT IN CHAIN venue-never-a-collaborator' while 'IN CHAIN assignee-roster-contract / anchor-due / template-anchor / wedding-template-contract / notes-copy'. The same diff wired five sibling tests in and left this one out.
- `E08.01` _wt-wp07s/src/lib/venue-money.ts — vatInclusive() is the only constructor and takes gross. net = round(gross*10000/(10000+rate)), vat = gross - net, so net+vat===gross by construction. No constructor accepts a net amount. Null rate means 'not determined', never zero-rated.
- `E08.01` _wt-wp07s/src/lib/entitlements-db/venue-billing.ts — recordAnnualPrepayment(): the single guarded writer. Gross comes from the plan and the caller's figure is checked then discarded, so an operator typo cannot become the price of record. One transaction writes the append-only term, refreshes the sponsors cache and appends the audit line.
- `E08.01` _wt-wp07s/scripts/mark-venue-paid.ts — REWRITTEN THIS SESSION. Previously wrote five mutable columns itself, kept no history, wrote no audit line, was not idempotent, never assigned the founding number, and swallowed a failed shared-DB mirror in a console.warn while exiting 0. Now records through recordAnnualPrepayment and exits non-zero if the HQ Traction mirror fails.
- `E08.01` _wt-wp07s/src/lib/venue-money.test.ts — Includes 'gross is the input and net is derived, never the other way round', 'net plus VAT equals gross exactly, for every amount and rate', 'no code path reconstructs gross from net', 'no surface built here claims a confirmed VAT position'.
- `E08.01` _wt-wp07s/docs/strategy/VENUE_RENEWAL_AND_LAPSE_RUNBOOK.md — WRITTEN THIS SESSION. The checkpoint's code referenced this path and the file did not exist. Its first table lists ten process steps with five marked 'Not built': invoicing, payment capture, both sends, and dunning.
- `E08.01` _wt-wp07s/scripts/check-venue-edition-contract.mjs — Updated: the requirement that mark-venue-paid.ts contains the literal line 'const founding = plan === "founding";' would have forced the weaker pre-rewrite version back, so it now requires the recordAnnualPrepayment call by name. The runbook was added to currentCommercialSources so it is string-checked.
- `E08.01` evidence/E08-venue-billing-and-renewal.md — Evidence document, criterion to proof, including the six checkpoint defects with the commands that proved each was real.
- `E08.01` _wt-wp07s/src/lib/entitlements-db/venues.ts — REFUTES CRITERION 5. onboardVenue() lines 115-177 writes paidAt, termStartsAt, termEndsAt, annualAmountCents and foundingLocked directly on `sponsors`, on both the insert and the update branch (`paidAt: isPaidPlan ? now : null`). `grep -n 'prepaymentRefusal|recordAnnualPrepayment|venue-billing' src/app/hq/entitlements/actions.ts src/lib/entitlements-db/venues.ts` returns NOTHING. No sponsor_price_agreements row, no payment audit line, no founding-number assignment, no amount check.
- `E08.01` _wt-wp07s/src/app/hq/entitlements/actions.ts — Proves the second writer is LIVE, not dead code. Line 19 imports onboardVenue; line 264 `export async function onboardVenueAction` in a 'use server' file; line 295 calls onboardVenue. OnboardVenueForm.tsx line 4 binds the action and line 67 sets `defaultValue="founding"` on the plan select, so the DEFAULT submission of the live HQ onboarding form stamps paidAt=now, foundingLocked=1 and annualAmountCents=100000 with no money received.
- `E08.01` _wt-wp07s/src/lib/entitlements-db/founding-numbers.ts — Consequence of the second writer. foundingNumberRefusal() at line 92 gates only on `sponsor.paidAt` being non-null. Because onboardVenue sets paidAt on plan selection, a console-onboarded venue that never paid satisfies the 'assigned on cleared payment' guard. D-009 point 6 ('on payment, not on signature') is defeated on that path.
- `E08.01` _wt-wp07s/src/app/venues/page.tsx — REFUTES CRITERION 2. Quotes euro prices at lines 11, 102, 468, 473, 495 and 509 ("€1,500 per venue, per year, prepaid", "€1,000", "€1,500"). `grep -rn 'inclusive of VAT' src/app src/emails` returns ZERO hits across the entire app and email trees. src/emails/registry.tsx:186 quotes "€1,500 a year" the same way. No venue-facing surface renders through formatVenuePrice().
- `E08.01` _wt-wp07s/src/lib/venue-edition.ts — Criterion 1 GENUINELY MET. venueEditionAnnualAmountCents() at line 34 is the single resolver; VENUE_EDITION_FOUNDING_ANNUAL_PRICE_CENTS = 100000, VENUE_EDITION_ANNUAL_PRICE_CENTS = 150000. The drift test is real: src/lib/venue-money.test.ts:245-259 asserts the resolver equals COMMERCIAL_TERMS.plans.venue.founding.annualAmountCents and .annualAmountCents, which src/lib/commercial-terms.ts:1 loads from contracts/commercial-terms.v2.json (verified 100000 / 150000 at lines 52 and 56 of the JSON).
- `E08.01` _wt-wp07s/scripts/venue-billing-ddl.mjs — No statement-breakpoint trap. The DDL is a list of discrete statements executed one at a time (migrate-venue-billing.mjs lines 98, 115, 124), never a .sql file split on '--> statement-breakpoint'. `git status` shows no .sql or drizzle migration file touched. vat_rate_basis_points is present in CREATE_VENUE_BILLING_TABLE, and only one CREATE TABLE for this table exists in the repo.
- `E08.02` _wt-wp07s/src/lib/venue-billing.ts — planChangeRefusal() ADDED THIS SESSION — the guard that actually makes the rate immutable. Append-only rows protect the past; nothing stopped the NEXT term being recorded as 'paid' at EUR 1,500 for a venue holding 01/25, with every historical row intact, every amount matching its plan, and the lock ended with no lapse on record. Also foundingRateOnReturn(), which returns an explicit unsettled marker rather than inventing what a returning venue is offered.
- `E08.02` _wt-wp07s/src/lib/entitlements-db/venue-billing.test.ts — 'refuses to renew a held founding lock onto the standard plan' uses its own seeded venue on purpose: the preceding fixture leaves v-founding carrying a directly-inserted future standard term, which is exactly the state that would have made this test pass for the wrong reason. Also 'survives a price change, per term' asserting history [100000, 100000, 180000].
- `E08.02` _wt-wp07s/scripts/venue-billing-ddl.mjs — CREATED THIS SESSION. The ledger table was defined three times by hand — in the migration, in schema.ts, and inline in the test file behind a comment claiming they were 'kept identical on purpose'. The migration and the tests now read the same strings from here.
- `E08.02` _wt-wp07s/scripts/venue-billing-ddl.test.mjs — CREATED THIS SESSION. Parses schema.ts as text and compares its column set to the DDL the migration executes, both directions. Also asserts the VAT columns stay nullable (null means not determined; NOT NULL would force a zero, which is a claim) and that no DDL statement carries a semicolon outside a trigger body.
- `E08.02` _wt-wp07s/scripts/migrate-venue-billing.mjs — probeAppendOnly() writes a real probe row, attempts an UPDATE and a DELETE, and rolls back — it proves the triggers FIRE rather than checking sqlite_master and assuming. A trigger that exists and does not fire is worse than none, because the table name says append-only and a reviewer stops looking.
- `E08.02` _wt-wp07s/src/lib/entitlements-db/founding-numbers.ts — assignFoundingNumber / withdrawFoundingNumber. Pre-existing and genuinely correct: idempotent, payment-gated, audit-writing, with a partial UNIQUE index. The gap was that nothing called it; recordAnnualPrepayment now does.
- `E08.02` _wt-wp07s/src/lib/entitlements-db/venue-billing.ts — EXISTS. The guard is wired at :176-194, before any write. Verified genuinely load-bearing: the price of record comes from the plan constant at :200 ('gross = venueEditionAnnualAmountCents(input.venuePlan)') and the caller's amount is checked then discarded, so an operator typo cannot become the rate. assignFoundingNumber is called at :299 for founding plans only. No other code path in src/ or scripts/ writes sponsors.annualAmountCents, venuePlan or foundingLocked.
- `E08.02` _wt-wp07s/scripts/mark-venue-paid.ts — Verified independently as the ONLY caller of recordAnnualPrepayment in the entire repo (:122-146). This makes criterion 3 genuinely met — the thing that records a cleared venue payment does call the assigner. It also means the whole payment path is one operator CLI; there is no Stripe webhook in studio.
- `E08.02` _wt-wp07s/drizzle-entitlements/0000_init.sql — The only checked-in migration. Contains none of the columns or tables this task depends on. This is the load-bearing defect.
- `E08.03` _wt-wp07s/src/lib/venue-billing.ts — deriveVenueBillingState() — seven states, with never_paid, term_end_unknown and not_a_paying_plan all distinguished from a lapse so absence of data is never read as one. VENUE_RENEWAL_POLICY carries ratified:false and renewalPolicyPublicationRefusal() names agreements, order forms, renewal emails and the portal. The machine takes the notice and grace windows as parameters, so ratifying them is a one-line change and no rework.
- `E08.03` _wt-wp07s/src/lib/entitlements-db/venue-billing.ts — recordVenueLapse() is defined by what it refuses to touch: it does not write to entitlements, does not shorten any expiry and does not withdraw a founding number. It refuses a lapse the dates do not support, and refuses without a reason. It writes one audit line recording entitlementsTouched: 0.
- `E08.03` _wt-wp07s/src/lib/entitlements-db/venue-billing.test.ts — 'does not shorten one couple's access, and does not take the founding number' inserts a sponsored entitlement expiring 900 days past term end, lapses the venue, then asserts expires_at is unchanged and status is still active, the founding number is still 01/25, and the audit line says entitlementsTouched: 0. This is the single most important assertion in the package.
- `E08.03` _wt-wp07s/src/lib/venue-billing.test.ts — 'walks current, renewal due, grace, lapsed at the right boundaries' pins the exact instants, including that the instant the term ends is grace and not lapse. 'a lapse stops new issuance and moves no couple's access' exercises accessAfterSponsorshipEvent from venue-lifecycle.ts rather than a local copy, so D-020 has one source.
- `E08.03` _wt-wp07s/docs/strategy/VENUE_RENEWAL_AND_LAPSE_RUNBOOK.md — WRITTEN THIS SESSION — the checkpoint's code pointed at this path and no such file existed. Opens with a ten-row table of which process steps are built and which are not, five marked Not built. Includes the weekly worklist procedure, the state-to-action table, and the five settled consequences of a lapse.
- `E08.03` _wt-wp07s/src/lib/venue-lifecycle.ts — Pre-existing and the strongest asset for this task: accessAfterSponsorshipEvent returns access unchanged for every sponsorship event, canIssueNewSponsorship is false once ended, BRANDING_REMOVAL_DEADLINE_MS is 24h. Linked, never reimplemented.
- `E08.03` _wt-wp07s/scripts/check-venue-edition-contract.mjs — VERIFIED: the runbook is at line 44 of currentCommercialSources, so it is now genuinely covered by the retired-price, retired-cohort and permanence pattern checks. The script exits 0. Note the checks it applies are pricing, cohort size and permanence only — it enforces nothing about the unratified grace window, so criterion 8 gains no protection from it.
- `E08.03` _wt-wp07s/package.json — VERIFIED the 'absent at the checkpoint' claim by parsing `git show HEAD:package.json`: src/lib/venue-billing.test.ts present at HEAD = false, entitlements-db/venue-billing.test.ts present at HEAD = false. Both were genuinely added to the suite this session. This claim was true and it is the kind of claim worth checking, since a test that runs nowhere is the failure mode this programme keeps hitting.
- `E08.03` _wt-wp07/src/server/stripe.ts — VERIFIED no venue price id. PRICE_IDS at line 35-39 carries workspace, studio, wedding, event. ANNUAL_PRICE_IDS at 63-64 carries workspace only. Webhook at app/src/app/api/webhooks/stripe/route.ts handles exactly three events: checkout.session.completed (113), customer.subscription.updated (134), customer.subscription.deleted (159). The build agent's description was accurate.
- `E08.04` _wt-wp07/src/server/tenant-scope-rules.mjs — NEW. Detection rules extracted as pure functions so their sensitivity can be tested. Closes the verified false negative by splitting scope tokens by strength: strong tokens (workspaceId, userId, sponsorId, workspaceSlug) count anywhere after .from(; weak bare-column tokens (.id, .token, .slug, .code, .email) count only inside .where(.
- `E08.04` _wt-wp07/src/server/tenant-scope-rules.test.mjs — NEW, 16 cases. Proves the detector actually detects, over synthetic statements with known answers. Includes the join-ON-clause false negative pinned as a permanent regression test, and an explicit test recording the accepted substring-matching limitation.
- `E08.04` _wt-wp07/src/server/cross-tenant-negative.test.ts — NEW, 5 tests. The first BEHAVIOURAL isolation test in the repo — real in-memory libSQL, two workspaces, nine sensitive tables. Asserts a scoped read returns only the caller's rows, and returns nothing when handed the other tenant's primary key. Carries a control case asserting the unscoped read returns both tenants' rows, so the negatives cannot pass vacuously.
- `E08.04` _wt-wp07/src/server/tenant-scope.test.mjs — Rewritten to consume the rules module. Keeps its three invariants; MIN_EXPECTED_READS raised 170 to 200 against a measured 247 reads across four surfaces.
- `E08.04` _wt-wp07/src/server/db/tenant.ts — Pre-existing and correct, kept unchanged. byWorkspace() throws on an empty workspace id and states the no-RLS position plainly. Now also asserted behaviourally in cross-tenant-negative.test.ts.
- `E08.04` _wt-wp07/package.json — Both new suites wired into `pnpm test`. Verified they execute in the full run.
- `E08.04` evidence/E08.04-E08.05-tenancy-and-access-control.md — Evidence document: criterion-to-proof table, the before/after mutation table, the four known limitations of the control, and the full enforcement file list.
- `E08.04` tasks/E08.04.md — Task specification written to the TASK.md template.
- `E08.04` _wt-wp07/src/server/db/daily-digest.ts:90-104 — REFUTING. Live production read of the tenant-keyed activities table with NO tenant predicate in its WHERE (only activities.kind and activities.createdAt), limit 50, then filtered in JavaScript by whether the snippet contains @handle. I ran the shipped classifier against this exact statement: it returns scoped=true, evidence=userId - GREEN purely because userId appears in the leftJoin ON clause at line 96. Lines 113-116 then read a task title by eq(tasks.id, a.taskId) with no workspace predicate,
- `E08.04` _wt-wp07/src/app/app/inbox/page.tsx:82 — REACHABILITY. compileDailyDigest(me, ws) renders the signed-in inbox page, so the unscoped read above is on a live user-facing surface. Also reachable at src/app/api/cron/digest/route.ts:130, which emails the digest. This is not dead code.
- `E08.04` _wt-wp07/src/server/tenant-scope-rules.mjs:58-89 — ROOT CAUSE. STRONG_SCOPE_TOKENS is commented 'Tokens that name a tenant column outright' but contains taskId, task_id, parentTaskId, noteId, publicationId and clerkId, none of which is a tenant column. Strong tokens count anywhere after .from(, joins included. So the fix closed one instance of the join-ON false negative (the weak .id case) and left the class open.
- `E08.04` _wt-wp07/src/server/db/tenant.ts:14-18 — OVERSTATED. The docstring claims 'Routing every workspace-scoped collection read through it gives ... a RUNTIME guard'. byWorkspace() has exactly 6 production call sites, all in src/server/db/queries.ts, against 239-247 governed reads. The two other files that mention the name (src/modules/signal/lib/data/source.ts, src/server/actions/project-overview.ts) do so as a local Map variable and a comment, not calls. The guard is real and throws; it covers roughly 2.5% of scoped reads. Not listed among
- `E08.05` _wt-wp07s/src/lib/account/roles.ts — FIXED. AccountCapability was a bare union type, which cannot be enumerated at runtime — the root cause of the guard failure below. Now ACCOUNT_CAPABILITIES is a runtime `as const` array with the type derived from it, plus ACCOUNT_ROLES derived from ROLE_CAPABILITIES' keys (which TypeScript already forces to be exhaustive).
- `E08.05` _wt-wp07s/src/lib/account/role-matrix.test.ts — FIXED on two counts. (1) It was NEVER WIRED INTO `pnpm test` — it had not run in CI once, while roles.ts cited it in its own header as the enforcement. (2) It iterated its own hand-written copy of the capability list, so it could not see a new capability at all. Now reads ACCOUNT_CAPABILITIES/ACCOUNT_ROLES from the shipped module and adds a suite asserting the recorded matrix matches the module in both directions.
- `E08.05` _wt-wp07s/package.json — role-matrix.test.ts wired into the studio test script. Confirmed it now executes in the full run.
- `E08.05` _wt-wp07s/src/lib/hq/auth.ts — AUDITED AND KEPT unchanged — the predecessor's v2 rewrite is correct. v1 cookie was sha256('signal-hq-session:v1:'+password): derivable offline, an offline oracle for the password itself, and no server-side expiry. v2 is `v2.<issuedAt>.<HMAC>` over a PBKDF2-150k-derived key. Its header states plainly that this is still a shared password and that a venue must never be given it.
- `E08.05` _wt-wp07s/src/lib/hq/auth.test.ts — AUDITED AND KEPT. Twelve malformed-input cases fail closed; each property test is written to fail against v1 and does. Wired into CI by the checkpoint.
- `E08.05` _wt-wp07s/src/lib/hq/hq-authorization.test.mjs — AUDITED AND KEPT, 5 tests passing. Pins that proxy.ts verifies rather than recomputing, that every /hq page and route handler is gated or waived in writing, that only auth.ts may mint a token, that no venue-facing module may reach TASKS_DATABASE_URL, and that the venue projector cannot fetch.
- `E08.05` evidence/E08.04-E08.05-tenancy-and-access-control.md — Section 3 carries the full role and permission matrix: venue owner/manager/viewer with the enforcing file per capability, plus couple, collaborator, guest (a token, not a role), venue (no identity exists) and HQ operator. Also records the unreconciled four-role-doc vs three-role-code disagreement.
- `E08.05` tasks/E08.05.md — Task specification written to the TASK.md template.
- `E08.05` _wt-wp07s/src/app/hq/account-review/actions.ts — DEFECT I FOUND, not in the agent's evidence. `recordMoreAccessRequestAction` (lines 33-55) is a real server action that writes a row via createSponsorRequest. Its only guard is requireHqAccountReview() -> verifyHqToken, i.e. the shared HQ password. It performs NO AccountRole check. The `request_access` capability it implements is gated only client-side.
- `E08.05` _wt-wp07s/src/app/hq/account-review/account-review.tsx — DEFECT I FOUND. Line 60: `const [role, setRole] = useState<AccountRole>("owner")`. The role is client state chosen from a picker. There is no server-side notion of which role a caller holds anywhere in the product, so no venue role check can run server-side by construction.
- `E08.06` evidence/E08.06-invitation-and-share-token-security.md — Full criterion-to-proof trace, the audit of the interrupted checkpoint, the two-live-redemption-writers finding, and the real command output including the two failures seen and how they were attributed.
- `E08.06` _wt-wp07s/src/lib/invitation-code.ts — The single studio generator. 31-symbol alphabet, 10-character body = 31^10 ≈ 2^49.5, drawn with crypto randomInt (unbiased). Replaces generateCode(prefix,5) in scripts/issue-codes.ts (31^5 ≈ 2^24.8) and genLicenseCode() in hq/entitlements/actions.ts (31^8 ≈ 2^39.6).
- `E08.06` _wt-wp07/src/lib/comp-code.ts — The app-side generator, extracted out of the 'use server' comp.ts so a test can load it. Replaces newCode(), which was 7 hex chars off a UUID = 2^28 with a Math.random() fallback. Rejection sampling; throws rather than falling back when no CSPRNG exists.
- `E08.06` _wt-wp07/src/server/invitation-code-security.test.ts — 13 tests. The uniformity test carries its own control: it runs the old randomBytes%31 draw in the same run and asserts it EXCEEDS the bound the new draw must sit under, so a bound too loose to detect the defect cannot pass as green.
- `E08.06` _wt-wp07/src/server/share-token-security.test.ts — 23 tests, all now passing. Before this session it had NEVER RUN: it imported resolveShareLinkRowWith from @/server/db/queries (server-only, and no such export) so it threw on load, and it was not registered in pnpm test.
- `E08.06` _wt-wp07/src/server/db/share-link-resolver.ts — Rewired to import nothing server-only and take its database as an argument (the Timeline recordQualifiedViewWith pattern). The checkpoint's version imported db from ./index, which pulled server-only back in transitively and defeated its own stated purpose.
- `E08.06` _wt-wp07/src/server/actions/comp.ts — redeemCompCodeImpl now resolves identity and spends redeemWithinAttemptLimits BEFORE the comp_codes lookup. The limiter's fail-open behaviour and the unprovisioned Upstash dependency are stated in the file and asserted by a test.
- `E08.06` _wt-wp07/src/server/share-revocation-contract.test.mjs — Was RED on main: it asserts the archived-workspace guard inside queries.ts, and the checkpoint moved that guard out. Repointed at the resolver and strengthened to count both branches of the dual read.
- `E08.06` _wt-wp07s/scripts/issue-codes.ts — The script whose own comments call the Tasks comp_codes row 'runtime redemption' and the studio license_codes row 'sponsor audit'. This is the proof that the guarded path is the audit copy and the unguarded one is what couples reach.
- `E08.06` tasks/E08.06.md — EXISTS, 7,560 bytes. Status 'Founder Review', sign-off block empty. Non-goals section names recipient locking, revocation and the unmerged writers explicitly. Scanned for banned wording: no VAT statement, no seat count, no permanence/'for life'/'forever', no 'partner'/'guaranteed'/'certified', no legal-approval implication. Clean against D-009/D-016/D-020/D-021.
- `E08.06` _wt-wp07s/src/app/hq/entitlements/actions.ts — EXISTS. Line 25 imports generateInvitationCode; line 235 is its only mint site. genLicenseCode is gone (grepped both repos: zero occurrences outside the assertion that it is absent).
- `E08.06` _wt-wp07/src/server/db/queries.ts — PRODUCTION CALLER CONFIRMED. Line 27 imports resolveShareLinkRowWith; line 715 binds it to the live `db`. Grepped: this is the only non-test caller, so the tested control is the one that runs. This is the failure mode the programme keeps hitting (a control tested but never called) and it does NOT apply here.
- `E08.06` _wt-wp07/drizzle/0027_share_link_token_hash.sql — EXISTS. I checked the breakpoint trap specifically: the first segment before `--> statement-breakpoint` contains the header comment AND `ALTER TABLE share_links ADD COLUMN token_hash TEXT;`. It is NOT a comment-only first segment. All three statements run. Journal entry idx 27 present; ledger carries sha256 a4d68de2… bound to receipt drizzle/receipts/share-link-token-hash-2026-08-03.json.
- `E08.06` _wt-wp07/src/server/actions/share.ts — MINT PATH VERIFIED. createShareLinkAction writes token=generateSharePublicId(), tokenHash=hashShareToken(secret), tokenScheme='sha256', and returns the secret once. I also chased the UX regression the spec flags: share-button.tsx builds `/share/${token}` only from the freshly-minted secret (line 116); the manage-links rows use link.token solely as a revoke id, so no dead share URL is rendered to owners.
- `E08.06` _wt-wp07/src/lib/ratelimit.ts — FAIL-OPEN CONFIRMED IN CODE, not just in prose: `if (!url || !token) return true;` plus `if (!res.ok) return true;` plus `catch { return true; }`. Matches what comp.ts tells the reader.
- `E08.07` evidence/E08.07-attachment-upload-security.md — Criterion-to-proof trace, the four-row audit of what the task title claims against what existed, the malware-scanning cost table with a recommendation, and an explicit list of what the evidence does NOT claim.
- `E08.07` _wt-wp07/src/lib/upload-validation.ts — The allowlist with byte signatures. Replaces an 8-entry DENYLIST checked against the client-supplied file.type string — two compounded failures: anything not on the list passed, and the label could simply be changed. SVG/HTML/XML/executables are refused by ABSENCE, which is the point.
- `E08.07` _wt-wp07/src/server/attachment-upload-security.test.ts — 16 tests. Includes the exact old bypass — HTML content declared image/png — plus MZ-executable-as-PDF, SVG-as-PNG, JPEG-as-PNG and RIFF/AVI-as-WebP (which checks the WebP wildcard bytes have not made every RIFF file a WebP).
- `E08.07` _wt-wp07/src/server/storage.ts — chooseBackend() returns 'vercel-no-token' and putBytes THROWS when BLOB_READ_WRITE_TOKEN is absent. New openBlobStream() reads via get(url, {access:'private'}) with the store token, because a private blob URL is not fetchable by a browser.
- `E08.07` _wt-wp07/src/app/api/attachments/[id]/route.ts — Response.redirect(url, 302) to a PRIVATE blob is removed. That redirect sent the browser to a URL requiring an Authorization header it does not have; it had never fired only because the store is unprovisioned, so it would have fired on the day the token was added. Security headers now cover both backends from one block.
- `E08.07` studio/content/hq/operator-todos/premium-blob-storage.md — status: open, priority: P1, blocking: true. This is the proof that BLOB_READ_WRITE_TOKEN is unprovisioned and therefore that attachment upload is broken in production today.
- `E08.07` _wt-wp07/node_modules/.pnpm/@vercel+blob@2.6.1/node_modules/@vercel/blob/dist/index.d.ts — The SDK's own type declarations, lines 128-132 and 202-212: "'private': The blob requires authentication to access" and get()'s note that "The authorization header is automatically set". This is the static proof that the 302 could not have worked.
- `E08.07` _wt-wp07/src/server/actions/attachments.ts — Not in the submitted evidence list but it is where the change lands. `validateUpload(` at line 181, `.insert(attachments)` at line 214 — C4 verified by reading the file, not only by the offset test. `const mimeType = verdict.mimeType` at line 195 verifies C3. `verdict.sniffed` is never read.
- `E08.07` upload-path completeness check (my own, not the builder's) — I grepped for every byte-writing path: `putBytes`, `arrayBuffer()`, `instanceof File`, `insert(attachments)`, `insert(resources)`. `uploadAttachmentAction` is the ONLY one. `addLinkResourceAction` inserts links, never bytes. So there is no second, unvalidated upload route — the coverage claim is genuine.
- `E08.08` _wt-wp07/src/server/operational-log.ts — NEW. The written distinction plus the only sanctioned console sink. opLog takes scalars only, so spreading a database row into a log call does not type-check. Exports redactForLog/redactEmail/safeError, reusing sentry-scrub's redactSensitiveUrl so the console path applies the same rule Sentry already applied.
- `E08.08` _wt-wp07/src/server/operational-log-contract.test.mjs — NEW, 8/8 pass, wired into pnpm test. RULE 1 ratchet over 51 recorded logging sites across 31 files (unrecorded file fails, grown count fails, stale entry fails). RULE 2 denylist proven to fire on document-uri, args.to, args.html, task.title, guest list, row.dietary, distinctId, and proven not to fire on four ordinary error logs.
- `E08.08` _wt-wp07/src/app/api/csp-report/route.ts — LEAK CLOSED. logViolation logged the browser-supplied document-uri verbatim; on a shared workspace that URL is /share/<token>, a bearer credential, written into the platform function log. sentry-scrub already redacted exactly that shape, so the same string was safe when thrown and unsafe when logged.
- `E08.08` _wt-wp07/src/server/email.ts — LEAK CLOSED. sendEmail logged recipient address, subject and the first 120 chars of the body whenever RESEND_API_KEY was unset. That is a configuration state, not an environment, so a production deploy that lost the key would have logged every couple's address and a slice of every wedding email. Full payload now gated on NODE_ENV development with an op-log-safe waiver; every other environment gets the recipient domain only.
- `E08.08` _wt-wp07s/scripts/apply-audit-ledger-triggers.test.mjs — 9/9 pass, now wired into pnpm test (it never ran before). Includes the case 'DEFECT: a database built from the baseline lets an audit line be edited and deleted', which passes because that is the true state of a fresh database. I added the ninth case: a source-contract test that fails if audit.ts re-states the retired enforcement claim.
- `E08.08` _wt-wp07s/src/lib/entitlements-db/audit.ts — FALSE CLAIM CORRECTED. Header said the physical append-only guarantee 'is enforced by SQLite triggers from migrate-access.mjs'. True of one hand-run database, false of every database built since the 2026-07-31 reset: grep -c 'CREATE TRIGGER' drizzle-entitlements/0000_init.sql returns 0 because drizzle cannot express a trigger. Now states plainly that the hash chain DETECTS and the triggers PREVENT, and names the two triggers and the commands.
- `E08.08` _wt-wp07/src/instrumentation.ts — MONITORING FACT. Line 17 returns early when SENTRY_DSN is unset. studio/docs/INFRASTRUCTURE.md records Sentry, PostHog, Upstash and production Anthropic as 'integrated in app code, none provisioned'. An unhandled production error is currently observed by nobody, and there is no alerting mechanism of any kind in either repository.
- `E08.08` _wt-wp07/src/server/db/schema.ts — AUDIT GAP, STATED NOT CLOSED. workspace_events (line 988) is written from exactly two sites in src/server/actions/settings.ts, kinds inviteSent and inviteAccepted. Member removal, role change, share-link create/revoke, publish and account deletion are unaudited. Those writers sit in files other groups are editing concurrently this wave.
- `E08.08` studio/docs/execution/venue-edition-and-films/evidence/E08.08-E08.09-observability-and-continuity.md — Evidence document, criterion to proof, covering both tasks, including the five checkpoint defects and the pre-existing studio lint red.
- `E08.08` _wt-wp07s/scripts/apply-audit-ledger-triggers.mjs — EXISTS (8620 bytes). Read in full. Not a drizzle migration, so the `--> statement-breakpoint` comment-first-segment trap does not apply; DDL is executed statement by statement via client.execute. --verify path exits 1 on missing triggers. I ran all three modes against a real database.
- `E08.08` evidence/E08.08-E08.09-observability-and-continuity.md — EXISTS (17398 bytes). Criterion-to-proof structure is genuine. Section C3 is titled 'A new logging site cannot appear unreviewed', which is the overstatement my probe refutes, and it repeats the incorrect 51-sites figure. Sections C4-C7 are accurate. Em dashes present (21) but this is an internal evidence doc, not front-facing copy, so the brand-voice rule does not bind it.
- `E08.09` _wt-wp07/scripts/db/restore-verify.mjs — AUDITED AND KEPT from the interrupted checkpoint. Correct in the ways that matter: it re-reads the restored database rather than the dump, sorts rows before hashing so the hash describes a set not an engine's return order, installs triggers LAST (an append-only trigger installed before the rows would refuse its own restore), and throws on any non-file: target (lines 45-50). I fixed one doc reference that pointed at studio/docs/RECOVERY.md as if it were in the app repo.
- `E08.09` _wt-wp07s/scripts/recovery-drill.mjs — NEW, replacing scripts/run-recovery-drill.mjs which I DELETED. The old one could not fail: it created a two-row table called recovery_probe that exists nowhere in this product, JSON.stringify'd it, compared the string to itself, then wrote {"release":"v1"} to a file and read it back. It was wired to pnpm recovery:drill while docs/RECOVERY.md listed 'rehearse one restore' as the open Critical action, so a green drill read like that action was closed. The new one builds the real entitlements schem
- `E08.09` _wt-wp07s/scripts/recovery-drill.test.mjs — NEW, 7/7 pass, wired into pnpm test. Four mutation cases prove the drill can fail: a lost row (count differs), an altered value at the same count (hash differs - the mutation is deliberately a founding venue's annual_amount_cents rewritten 100000 to 150000), a lost index, and the important one: every row replayed with the triggers dropped, where the data compares byte-identical and the drill still fails because the restored ledger accepts an UPDATE.
- `E08.09` _wt-wp07s/scripts/check-entitlements-integrity.test.mjs — WAS RED, NOW 21/21. The checkpoint's version shared ONE database across every case, so paid-venue-without-term reported 4 violations against 1 expected and paid-venue-without-price-agreement reported 2 against 1. It had never been run. Rewritten with a fresh database per case and a strictly stronger assertion: the planted violation must be the ONLY check that fires. Also stopped hand-copying the sponsor_price_agreements DDL (a fourth copy of a money table) and now imports it from Group A's scrip
- `E08.09` _wt-wp07/scripts/db/backup-all.mjs — NEW. Backs up every module from studio/docs/INFRASTRUCTURE.md in one command, restore-verifies each, writes into the existing workspace db-archive/<date>/ convention, and EXITS NON-ZERO on partial coverage. Exercised: with one module credentialed it reported '1 backed up, 5 skipped' and exit 1; with --modules=tasks it reported full coverage and exit 0.
- `E08.09` _wt-wp07/scripts/db/integrity-check.test.mjs — AUDITED AND KEPT, 27/27, now wired into pnpm test (it never ran before). Builds its fixture from the real drizzle migration SQL from baseline 0014 forward, splitting on '--> statement-breakpoint' and never on semicolons - the migration trap this workspace has paid for twice, handled correctly by the predecessor.
- `E08.09` _wt-wp07/.gitignore — DEFECT FIXED. The checkpoint committed a 307KB database dump (.db-backups/tasks-local-2026-08-03T07-30-04-141Z.jsonl, 462 rows) and .db-backups/ was not ignored. Content was synthetic (person0@example.test) so nothing personal leaked, but the next run against a real database would have committed real couple data. Removed from index and tree; directory now ignored. The bytes remain in the checkpoint commit's history.
- `E08.09` _wt-wp07s/docs/RECOVERY.md — REWRITTEN against 2026-08-03 truth. Turso account ethan387 is FREE tier so PITR (the 2026-06-19 runbook's top recommendation) is unavailable and the budget is zero (D-015 Q5); nothing takes a backup on a schedule; the only backup of production is db-archive/2026-07-31/, a manual single copy on one laptop covering the PREVIOUS generation of databases, never restored; the data-loss window is four days.
- `E08.09` _wt-wp07/scripts/db/backup.mjs — EXISTS. tableHash at lines 96-101 DOES sort before hashing. I proved it: same row set in reversed order hashes equal. This is the correct implementation and the studio drill's copy of it is not (see unmetCriteria).
- `E08.09` _wt-wp07/scripts/db/integrity-check.mjs — EXISTS. 16 checks, SELECT-only guard real at lines 226-236. I ran it against the real tasks.db: 16 checks, 0 failing, 0 warning, 0 rows implicated, exit 0 - matches the claim. BUT it has no skip concept whatsoever: against a database missing the schema it dies on the FIRST check with 'db:integrity: SQLITE_ERROR: no such table: workspaces' and the other 15 never run.
- `E08.09` _wt-wp07s/scripts/check-entitlements-integrity.mjs — EXISTS. Skip logic is real (lines 243-253 push {skipped: 'table(s) absent: ...'}). Against an empty database it printed 16 [SKIP] lines and '0 checks run, 16 skipped, 0 failing, 0 warning' - but EXIT=0. Count is 16 checks, not the 17 RECOVERY.md states twice.
- `E08.10` _wt-wp07/scripts/check-performance-budgets.mjs — Rewritten from the checkpoint version. The ratchet logic is extracted into a pure evaluate(contract, measured, populations) so --self-test can prove it without a build. Every number now carries its population; population 0 exits 3 unless the contract declares it, and then prints 'no data (declared)', never 'ok'. A missing imageRoot now exits 3 instead of walking to zero.
- `E08.10` _wt-wp07/contracts/venue-surface-performance-budgets.v1.json — App budgets. repo_images now carries emptyPopulation:{expected:true,reason:...} stating that app/public holds five SVGs and no raster image at all. The checkpoint's basis text ('no image exceeds 200 KB') was true of an empty set and read as a passing measurement.
- `E08.10` _wt-wp07s/contracts/venue-surface-performance-budgets.v1.json — Studio budgets. repo_images ceiling 657 KB against a 200 KB target: public/brand/collateral/venue/venue-deck-preview.png is 656.2 KB and is venue-facing.
- `E08.10` _wt-wp07s/scripts/measure-performance-baseline.mjs — Retired. It walked five sibling repositories, four merged into `app` on 2026-07-31, printed built:false/gzipBytes:0 for each and exited 0. Now prints why, names pnpm perf:budgets, and exits 3. Verified exit 3 at close. NOTE: this file was briefly reverted by a concurrent lane mid-session.
- `E08.10` _wt-wp07/.github/workflows/verify.yml — Both repositories now run perf:budgets:self-test then perf:budgets after pnpm build, plus the two journey-coverage steps.
- `E08.10` evidence/E08.10-performance-budgets.md — Evidence: the measured numbers from real builds in both repositories, the breach proof with exit codes, the self-test output, the unmeasured list, and the statement that the E08.10 gate criterion cannot be satisfied as written.
- `E08.10` _wt-wp07s/scripts/check-performance-budgets.mjs — EXISTS, tracked. diff --strip-trailing-cr against the app copy returns IDENTICAL, so 'same checker' is true and not two drifting forks.
- `E08.10` _wt-wp07s/.github/workflows/verify.yml — EXISTS, tracked. perf steps at lines 44 and 46 after pnpm build at line 28. Triggers: pull_request and push to [main, feat/remediation-program]. grep confirms budgets:check and performance:measure appear in NO workflow in either repo.
- `E08.10` tasks/E08.10.md — EXISTS, 8293 bytes. This is the stale artefact: deliverable 6 unchecked and the Risks section asserts performance:measure 'is still live and still exits 0 measuring nothing', which my run refutes.
- `E08.11` _wt-wp07/scripts/check-journey-coverage.mjs — Expands `pnpm test` — following pretest and any chained `pnpm <script>` — into the set of files CI actually runs, then fails when a file named as covering a segment is missing OR is not in that set. --self-test proves 11 rules including 'an ORPHANED covering test fails'.
- `E08.11` _wt-wp07/contracts/sponsored-journey-coverage.v1.json — App segment map: 6 covered (couple_redeems, couple_workspace_is_private, couple_plans, couple_publishes, venue_reads_metrics, couple_leaves), 2 declared uncovered (browser_responsive_device, attribution_is_correct).
- `E08.11` _wt-wp07s/contracts/sponsored-journey-coverage.v1.json — Studio segment map: 7 covered, 2 declared uncovered (renewal_and_lapse_are_operated — nothing exists to test; browser_responsive_device).
- `E08.11` _wt-wp07/src/app/api/internal/partner-stats/partner-stats-boundary.test.mjs — THE ORPHAN I WIRED. 193 lines, 6/6 passing, executed by nothing until this session. It pins that the only venue-directed read in the app projects four aggregate numbers, reaches no couple-content table, fails closed when the shared secret is unset, and that its missing D-011 floor is acknowledged in the route rather than forgotten.
- `E08.11` _wt-wp07/src/server/share-token-security.test.ts — THE ORPHAN THAT COULD NOT RUN. 444 lines. Imported resolveShareLinkRowWith from @/server/db/queries, which begins `import "server-only"` — unresolvable by the Node test runner: 'Error: Cannot find module server-only'. Its source-contract block also sliced queries.ts from a function that does not live there, yielding an empty string every assertion would match against. Repaired by its owning group; now runs.
- `E08.11` evidence/E08.11-sponsored-journey-test-coverage.md — Evidence: the red-suite finding, the seven orphans with line counts and fates, real suite counts, the segment maps, the observed flake, and the four declared gaps.
- `E08.11` _wt-wp07/.github/workflows/verify.yml — Triggers on pull_request and push to main. Journey-coverage self-test and check both present — but positioned after `pnpm build`.
- `E08.11` _wt-wp07s/.github/workflows/verify.yml — Triggers on pull_request. Both coverage steps present, after remediation:check, recovery:drill, typecheck, test and build. Note studio's verify.yml runs no lint at all, and ci.yml runs lint with continue-on-error: true — so the builder's statement that the lint error 'gates nothing today' is verified true.
- `E08.11` _wt-wp07/src/app/api/internal/partner-stats/route.ts — Read in full. Carries the R-027 acknowledgement and the 'not venue-facing' wording the test pins. The projection really is two timestamps and the response really is four numbers TODAY — the defect is that the test would not stop that changing.
- `E08.11` C:/Users/ethan/AppData/Local/Temp/.../scratchpad/studio-test.log — My run 1 of studio pnpm test: STUDIO TEST EXIT: 1, group 3 `tests 57 / pass 56 / fail 1`, founding-numbers.test.ts file-level failure with all 16 subtests green.
- `E08.11` C:/Users/ethan/AppData/Local/Temp/.../scratchpad/mut/ — Mutation harness — a scratchpad COPY of route.ts + the boundary test. Both worktrees verified untouched afterwards (git status on the partner-stats directory is empty).
- `E08.12` evidence/E08.12-security-and-production-readiness.md — Ten findings F-01 to F-10, each verified in code by file and line, each with a concrete failure scenario and a state. Verdict: NOT READY to contact a venue, with four named conditions. Includes a table restating every open register entry against its verified state — R-027 and R-028 would read as closed to anyone checking the branch and are recorded as not closed, because nothing is closed until it is in production.
- `E08.12` evidence/E08.12-release-checklist.md — T-7 preconditions (four open founder decisions plus the two gate items), T-1 verification with the exact package.json commands and expected counts, the receipt-backed db:migrate sequence from DEPLOY.md §4 with the backup-and-restore-verify step first, the both-repositories redeploy requirement from the launch operator-todo, and an outside-in smoke test from a phone in incognito.
- `E08.12` evidence/E08.12-rollback-procedure.md — Opens on the one thing that matters — code rolls back in a minute, data does not roll back at all — then a two-minute decision tree, vercel promote for code, a configuration table (four variables, each needing a redeploy), and the schema section stating there is no down migration and why the answer is a forward fix. §6 records that the rollback has never been executed against a staging deploy.
- `E08.12` evidence/E08.12-incident-response-runbook.md — Five scenarios, each with person, channel and clock. Article 33's 72 hours and dataprotection.ie recorded. §0 states plainly that there is one person, no rota, no paid on-call and no second contact, as an accepted limitation of D-015 Q5. §6 covers the wrongly issued or wrongly expired entitlement including the rule that a couple's access is never shortened to fix an administrative error.
- `E08.12` _wt-wp07/drizzle/0027_share_link_token_hash.sql — F-04 verification. The `--> statement-breakpoint` trap did NOT recur: applying the file as the runner does (split on the marker, never on semicolons) produced 3 segments and all three objects — columns token_hash and token_scheme present, uq_share_links_token_hash unique. Proven against a real libSQL database with pragma_table_info and pragma_index_list, not reasoned about.
- `E08.12` _wt-wp07/src/lib/ratelimit.ts — F-06. Lines 14-21 state the design: with UPSTASH_REDIS_REST_URL/_TOKEN unset the limiter is a no-op that ALLOWS, and on a Redis error it ALLOWS rather than 500s. Bounded by the code entropy arithmetic: 10 chars over a 31-symbol alphabet = 49.5 bits, with a 48-bit floor asserted in a test that fails the build.
- `E08.12` _wt-wp07/next.config.ts — F-03 and F-07. Line 20: enforceCsp is gated on SIGNAL_ENFORCE_CSP === 'true', so the CSP is Report-Only by default. Lines 136-149: /embed is excluded from the global X-Frame-Options DENY by negative lookahead and gets frame-ancestors * — R-033's redistribution exposure confirmed at the header level, not just the route.
- `E08.12` BRANCH-STATE CHECK (my own, not claimed by the build agent) — git log confirms commit 7cb50d5 (R-027/R-028 suppression floor and branded rate value, and the hq/auth.ts v2 rewrite) is contained ONLY by claude/wp07-engineering-studio, not by main. So the review's 'closed in the studio branch, not deployed' framing for R-027/R-028 is factually correct. Criterion 2 MET.
- `E08.12` CONTROL-HAS-A-PRODUCTION-CALLER CHECK (my own) — Chased the programme's recurring failure mode. R-028's fix is genuine: presentRate has real production callers at daily-metrics.ts:241 and retention.ts:99, formatRateValue is consumed in 8 production UI files, and metricRateLabel is deleted (surviving only in a comment and in a test asserting it must not exist). R-027's two-sided rule reaches production through isSmallCell at daily-metrics.ts:108 and :151. NOTE: presentBehavioural itself has ZERO production callers (test-only), the same shape as
- `E08.12` BANNED-CLAIMS SWEEP (my own regex, all four docs plus the task spec) — Criterion 7 MET. Four hits total, all negations or the criterion text itself: 'No solicitor reviewed them (D-016)', 'It is not a legal opinion. No solicitor reviewed it. No accountant verified it', and the two occurrences inside the acceptance criteria. Separately swept for the programme's other traps: no VAT-exclusive statement anywhere (the only VAT line is 'inclusive of VAT at the prevailing rate', correct under D-021); no founding-lock permanence wording ('for life'/'forever'/'in perpetuity'
- `E12.01` evidence/E12.01-public-landing-audit.md — The full audit. Criterion by criterion against the live page, with the approved replacement text for each of the five failures quoted from E09.11 §3, §3.1, §4.1, §5.1 and §7.2 rather than invented.
- `E12.01` tasks/E12.01.md — The ten acceptance criteria, written for this task because none were on record, plus the non-goals that explain why no source file was touched.
- `E12.01` _wt-wp13/src/app/venues/page.tsx — The live page, read in full, 676 lines. NOT MODIFIED. Six of ten criteria pass on it; E12.04's audit of it is confirmed rather than contradicted.
- `E12.01` _wt-wp13/src/lib/venue-invitation/copy.ts — The single-source copy module built for E12.02/E12.03. It already carries every block the audit quotes, verbatim, and it is swept by check-venue-edition-contract.mjs. Offered as the third option in the recommendation: adopting it on /venues converts 'four sentences are wrong' into 'the page cannot state an unapproved claim'.
- `E12.01` scratchpad/p7check.mjs — independent reimplementation of copy/prohibited-claims.v1.json against src/app/venues/page.tsx — Output: 8 ungraced hits, exactly matching the pin `expected: 8`. p7-bare-term fires on lines 15, 55, 61, 67, 73 — FIVE, against the ONE the audit reports. This is the single highest-value finding of the verification.
- `E12.01` evidence/E09.11-copy-system.md §13 — Defect #1 pre-records lines 55/61/67/73 as 'four times, in the portal mock rows a prospect reads on screen'. Six defects are listed against this file (#1-#6); the audit surfaces three cleanly, discloses two without folding them into remediation, and mishandles one.
- `E12.01` evidence/E09.11-copy-system.md §5.1a — CONFIRMS the audit's criterion 6 finding verbatim: the table row for 'The live public Venue Edition page' reads 'Does not carry it, and is worse than silent.' The audit represented this source accurately.
- `E12.01` _wt-wp13 — banned-term sweep over src/app/venues/page.tsx — ZERO HITS for partner, member, investor, exclusive, guaranteed, certified, 'for life', forever, 'in perpetuity', lifetime, countdown, 'only N left', 'plus VAT', 'ex VAT', VAT-exclusive, 'GDPR compliant', 'legally approved', solicitor, accountant, 'reviewed by our lawyers'. Criteria 5 and 10 CONFIRMED pass. Zero em dashes (U+2014) and zero exclamation marks, verified by node scan not grep.
- `E12.01` _wt-wp13/src/app/venues/page.tsx lines 101,102,106,468-481,495-513,599-600 — Every ratified commercial claim traced and CORRECT in the body: EUR 1,500 and EUR 1,000 both present, 'Both prices include VAT at the prevailing rate' plus 'VAT included' on both cards, 'EUR 500 a year less', lock 'holds while the agreement keeps renewing' with no expiry clause, '01/25 to 25/25 ... assigned when your payment clears, not when you sign', 'held for fourteen days', 'Every couple with a booking gets the full suite'. Only the metadata block (lines 10-11, 15) is defective, as the audit
- `E12.01` _wt-wp13 — R-031 privacy check — CLEAN. Lines 361-364 read 'A couple's Notes, Tasks and unpublished Timeline remain inaccessible' — 'unpublished' is correctly scoped and the page makes no claim that the token-bound model covers /p. No R-031 violation.
- `E12.01` _wt-wp13/src/lib/venue-allotment.ts, src/lib/entitlements-db/codes.ts:180, src/lib/account/live/project-venue-access.ts:48,143,195 — COUNTER-FINDING, in the build agent's favour. I expected 'No seats. No per-couple maths.' (line 469) to be an unbuilt-capability claim per R-016. It is not: allotmentMode 'unlimited' now exists, the mint accepts it, and headroom counts and warnings are suppressed for unlimited sponsors with D-020 cited in the source. The audit's deferral to OQ-6 was correct and I do not raise it as a defect.
- `E12.02` evidence/E12.02-03-private-venue-page.md — Claim by claim against all twelve E12.02 criteria and the four E13.16 criteria that are WP-13's (8, 9, 10, 11) plus criterion 21. Includes the copy source table tracing every visible sentence to an approved document.
- `E12.02` evidence/E12.02-03-verification-run.txt — The measured before and after: the baseline gate readings taken before any file was touched, the final readings, the served robots.txt, the route probe table, and the real emitted event lines.
- `E12.02` _wt-wp13/src/app/v/[token]/page.tsx — The route. Unknown, malformed and revoked all reach one notFound() call, so there is no second template that could drift into being a distinguishable answer.
- `E12.02` _wt-wp13/src/lib/venue-invitation/links.ts — The registry. Fails closed on any refusal, resolves with no early exit, refuses a film without a poster or a .vtt track, and refuses a venue name inside any media URL.
- `E12.02` _wt-wp13/src/lib/venue-invitation/event.ts — Seven fields and a builder that throws on an eighth. buildVenuePageViewedEvent rejects rather than trims, which is what E13.16 criterion 8 requires by name.
- `E12.02` _wt-wp13/src/lib/venue-invitation/copy.ts — Every visible sentence, quoted from E09.11, E11.11 and E13.16. Nothing is reworded at a render site.
- `E12.02` _wt-wp13/src/lib/venue-invitation/paths.ts — The single predicate E13.16 §7 requirement 1 demands. The concurrent session working this worktree adopted it rather than writing a second one.
- `E12.02` _wt-wp13/src/app/v/[token]/states.tsx — EXISTS, 12572 bytes. NOT listed in the build agent's own evidence array despite carrying every visible sentence and the seven inline strings.
- `E12.02` _wt-wp13/src/app/v/[token]/not-found.tsx — EXISTS. One link, to /venues. Names nothing.
- `E12.02` _wt-wp13/src/lib/venue-invitation/paths.test.ts — EXISTS. Proves the robots disallow and walks every sitemap entry. Does NOT test the noindex metadata — the gap behind unmet criterion 2.
- `E12.03` evidence/E12.02-03-private-venue-page.md — Section 3a is the criterion table for E12.03. Section 5 traces every sentence of the proposal to E11.11 §2, §2.3 and §3, and records the two deliberate departures from E11.11 with their reasons.
- `E12.03` evidence/E12.02-03-verification-run.txt — The rendered proposal state at 96,361 bytes, 200, zero third-party references, zero marketing nav links, one action.
- `E12.03` _wt-wp13/src/app/v/[token]/states.tsx — ProposalState. The commercial summary renders all seventeen rows of E11.11 §3 as a table with th scope=row, inside an overflow-x-auto container so the page body never scrolls sideways.
- `E12.03` _wt-wp13/src/lib/venue-invitation/links.test.ts — The slot tests: every one of the six proposal slots deleted in turn refuses the document; a founding number outside 01/25 to 25/25 refuses; 2026-02-31 refuses rather than rolling forward to 3 March.
- `E12.03` _wt-wp13/src/lib/venue-invitation/copy.ts — Line 164 is the shortened commercial summary row. Lines 61-70 carry both VAT-inclusive prices and the continuous-renewal lock verbatim. Line 89 carries the present-tense 'Adoption figures across your couples as a group' that line 96's launch-scope sentence contradicts.
- `E12.03` _wt-wp13/src/lib/account/instrumentation/suppression.ts — Lines 34-41. presentBehavioural tests eligibleWorkspaces < 3 and returns {state:'value', value} otherwise. R-027 is unfixed in this worktree, which is what makes the page's 'small numbers are held back' sentence a promise the code does not keep.
- `E12.05` _wt-wp13/src/app/venues/couple-preview/page.tsx — The page. Renders the four real venue-name moments as words, not screenshots, plus the name-only limits, the term, the survival sentence, the verbatim privacy and collaboration blocks, the launch-scope sentence, and a three-item not-built section.
- `E12.05` evidence/E12.05-couple-experience-preview.md — Claim-by-claim trace. Every line on the page mapped to a source file and line or to a ratified decision. Includes the audit of what already existed, the five defects found and returned, the registry method, and the shared-worktree concurrency finding.
- `E12.05` studio/src/app/redeem/[code]/page.tsx:314-317 — Moment 1 verified. Line 314 sets the eyebrow to the venue name, 315 the headline 'X is sponsoring your access to Signal Studio.' Line 317 carries cta: 'Claim your seat', which is on E09.11 section 8's banned CTA set by name. Returned as a defect, not reproduced on the preview.
- `E12.05` app/src/components/welcome/venue-welcome-card.tsx — Moment 3 verified. 'Compliments of {sponsorName}'. It is fixed to the BOTTOM of the Tasks board, shown once, and permanently dismissible via localStorage keyed by sponsor slug.
- `E12.05` app/src/components/settings/plan/plan-view.tsx:77-81 — Moment 4 verified, and two defects found on the same lines: 'A year of Signal Studio' and 'When the year is up' both state a flat year against D-010.2 and D-022, and 'moved to the Free tier' substitutes for Keepsake.
- `E12.05` app/src/server/db/venue-welcome.ts — Proves the name-only claim mechanically. The sponsor payload is exactly three fields (sponsor_name, sponsor_slug, source_type). There is no logo path, no colour and no theme field anywhere, so D-027 point 3 is enforced by the data shape, not only by policy.
- `E12.05` studio/src/app/venues/page.tsx:180-189 — The finding that changed the page. The live /venues hero mock renders a persistent 'Sponsored by Glenmara Estate' bar across the top of the couple's workspace. grep -rn 'Sponsored by' app/src returns nothing. That bar does not exist. Returned to E12.04 as a boundary request.
- `E12.05` _wt-wp13/experience/registry.json — studio.page.venues-couple-preview registered by hand, hash d812165a05a8a0b3, coverage none/none/none. Diff is 88 insertions, 0 deletions, no pre-existing entry moved or changed.
- `E12.05` _wt-wp13/scripts/check-venue-edition-contract.mjs:55-60 — Route added to currentCommercialSources in the same change that created it, so retired pricing, the retired 15-venue cohort and the four permanence patterns are checked on it.
- `E12.05` app/src/app/sign-up/[[...sign-up]]/page.tsx:47-61 — Moment 2 VERIFIED by me, and it was the one moment the manifest evidence list omitted. :50 renders sponsor.name above the form, :53 'Almost there. {name} is covering your year.', :56-59 the Code label and the code beneath. The preview's description is accurate. The 'your year' line is a real term defect and the agent returned it as defect 2.
- `E12.05` app/src/components/welcome/venue-welcome-card.tsx:54,66 — Moment 3 VERIFIED by me. :66 'Compliments of {sponsorName}'. :54 className 'fixed inset-x-0 bottom-6' confirms it is anchored to the BOTTOM, and :23-48 confirm permanent localStorage dismissal keyed by sponsor slug. The page's 'not a banner that follows them around' is accurate.
- `E12.05` app/src/components/settings/plan/plan-view.tsx:75-84 — Moment 4 VERIFIED by me, including the part I expected to fail. :77-78 renders the sponsor name and `Expires {longDate(expiresAt)}`, so the preview's 'beside the date their term ends' is true. :76 'A year of Signal Studio' and :81 'When the year is up' are the flat-year defects the agent reported. :82-84 'moved to the Free tier' is the Keepsake substitution.
- `E12.05` app/src/server/db/venue-welcome.ts:15-19 — The name-only claim is mechanically enforced, as claimed. CompNotes is exactly three fields: sponsor_slug, sponsor_name, source_type. No logo path, no colour, no theme anywhere in the sponsor payload. D-027 point 3 is enforced by data shape.
- `E12.05` _wt-wp13/src/app/venues/page.tsx:187 — The boundary request is REAL and I confirmed both halves. :187 renders 'Sponsored by Glenmara Estate' inside the hero workspace mock. `grep -rn 'Sponsored by' C:/Users/ethan/signal-studio-workspace/app/src` returns nothing. E12.04 is Done and founder-approved and its page depicts a bar that does not exist.
- `E12.05` _wt-wp13/src/lib/venue-invitation/paths.ts:32-35,79-84 — Publication precondition 2 is now SATISFIED, which the evidence document could not yet claim. VENUE_EDITION_COMMERCIAL_PREFIXES includes '/venues' and matchesPrefix matches '/venues/couple-preview'. src/proxy.ts:166-168 sets NO_THIRD_PARTY_HEADER and src/app/layout.tsx:183 gates GoogleTag on it. Rendered HTML carries no gtag, googletagmanager or G- measurement id.
- `E12.05` evidence/copy/prohibited-claims.v1.json — All 15 rules applied by me to the page source. One raw regex hit: p7-bare-term on line 251 'eighteen months'. The rule's own graceWithin:200 and gracePattern '(?:3|three) months past|whichever is later' clear it, because the sentence continues 'or three months past the wedding, whichever is later'. Net 0 hits, as claimed.
- `E12.05` app/src (Keepsake absence) — THE ONE SUBSTANTIVE OVERSTATEMENT. `grep -rni keepsake app/src` returns 0 hits. `grep -rni keepsake studio/src` returns 30, all in venue-lifecycle.ts and its test file, which is a state-machine model in the marketing repo. No read-only mode, no indefinite tail, no Keepsake export exists in the running product.
- `E12.06` _wt-wp13/src/app/venues/what-you-see/page.tsx — The page. Previews what a venue RECEIVES under D-032 point 1 Option C: the launch-scope sentence in its own block, both halves of the privacy block at equal weight, the suppression floors with the reason, the batch-and-record rhythm, the four permitted renewal forms, the survival sentence, Keepsake, and a not-built section naming the login, the adoption figures and the Keepsake screen.
- `E12.06` evidence/E12.06-venue-record-preview.md — Decision-by-decision trace, opening with the finding that the task title's premise had to be re-read against the record before anything could be built.
- `E12.06` studio/docs/execution/venue-edition-and-films/DECISIONS.md D-032 point 1 — The decision that makes this task buildable and defines what it may show: 'Venue identity at launch is Option C. Signal HQ administers invitations on the venue's behalf and the venue receives evidence rather than a login. No venue-authenticated route is built before 1 September.' This decision is NOT in the DECISIONS-slice.md I was handed, which stops at D-027.
- `E12.06` studio/docs/execution/venue-edition-and-films/evidence/E07.16-venue-identity-lab.md — E07.16's own specPath, and the tiebreaker on its status. Header: 'options for founder decision. Nothing here is built. Nothing here is approved.' Audit score 3 of 13 criteria satisfied, 2 partial, 8 not satisfied, while PROJECT_STATE records the task as done.
- `E12.06` project-control.mjs task E07.16 (read-only) — status: done, but its nine recorded evidence entries each record a FAILURE (hq/auth.ts:15-25 one shared password; project-venue-access.ts:326 members: [] unconditional; account-panel.tsx hardcoded support history; actions.ts:22-27 any slug accepted with no per-venue authorisation). The approval note resolves the contradiction in its own words: 'the E07 audit and the invitation-administration build'.
- `E12.06` studio/src/lib/account/instrumentation/suppression.ts:33-41 — Read to confirm the gap the page must not describe. presentBehavioural tests only the population, so presentBehavioural(1, 40) returns a naked 1. R-027. The page therefore states the ratified RULE, per the brief, and section 5 of the evidence records that this makes the page a promise WP-07 must land.
- `E12.06` studio/src/lib/account/format.ts:44-71 — metricRateLabel divides two exact values with no threshold, and presentRate() has no production caller. R-028. Same consequence.
- `E12.06` _wt-wp13/experience/registry.json — studio.page.venues-what-you-see registered by hand, hash 818c9db90d863298, coverage none/none/none. Merged with three asserted invariants and a refuse-to-write guard, never with experience:discover --write.
- `E12.06` _wt-wp13/src/lib/account/instrumentation/suppression.ts:33-40 — R-027 RE-VERIFIED BY ME in this worktree, not taken on trust. presentBehavioural() tests only `eligibleWorkspaces < BEHAVIOURAL_MIN_WORKSPACES` and returns {state:"value", value} otherwise, so presentBehavioural(1, 40) returns a naked 1. src/lib/account/format.ts:44-71 metricRateLabel divides two exact values and returns Math.round(n/d*100) with no floor (R-028). The page asserts both protections in the present indicative.
- `E12.06` _wt-wp13/src/lib/redeem/lookup.ts + src/lib/entitlements-db/schema.ts:387-428 + src/lib/entitlements-db/codes.ts — THE FINDING THE BUILD AGENT MISSED. lookupRedemption() is a single SELECT with no write, so loading /redeem/{code} records nothing. grep for opened_at/openedAt across all of studio/src returns zero rows — the "opened" fact has no column. license_codes has deliveredAt but no writer anywhere. codes.ts exports only mintLicenseCodes (138), redeemLicenseCode (285), reconcileCodes (448), setCoupleWeddingDate (588) — no revoke, no reissue. So the two access figures the page calls "exact at any size", a
- `E12.06` _wt-wp13/scripts/check-venue-edition-contract.mjs — VERIFIED. "src/app/venues/what-you-see/page.tsx" is present in currentCommercialSources with a comment naming E12.05 and E12.06. The diff adds nine sibling routes in the same hunk, so this file is shared across the WP-13 lane's tasks rather than owned by this one.
- `E12.06` evidence/copy/prohibited-claims.v1.json — EXISTS, 15 rules. I executed all 15 myself over the page with the p7 grace-window logic honoured: TOTAL HITS 0. Independent banned-term sweep (partner, member, investor, exclusive, guaranteed, certified, for life, forever, in perpetuity, lifetime, countdown, only N left, plus/ex VAT, seat, GDPR compliant, legally approved, solicitor, accountant, reviewed by our lawyers): 0 hits, grep exit 1. Em dash count 0. Exclamation 0. "fewer than"/"less than N"/"below three": 0. CTA "Ask me a question" is t
- `E12.07` _wt-wp13/src/app/venues/privacy/page.tsx — The page. Route /venues/privacy. E09.11 §5.1 reproduced verbatim in all three parts including the D-032 R4 launch-scope sentence; the three permitted things numbered with an explicit 'there is no fourth' close; the definition of 'opened' as a first-party load of the redemption page and not an email open; a six-item negative list; the suppression rule with D-032 R12's access-count asymmetry; sharing and publishing using only E09.11 §6.4's permitted sentences; the venue name rules; and the four re
- `E12.07` studio/docs/execution/venue-edition-and-films/evidence/E12.07-venue-privacy-page.md — Claim-by-claim audit. §2 traces every positive claim to a line number in src/lib/account/live/project-venue-access.ts. §3 traces the negative half the same way. §6 records the two deliberate omissions (no token-bound language because R-031 is undecided; no artifact described as existing because E03.04/E03.05 are backlog). §8.0 records that my first forbidden-word sweep was BROKEN and reported false zeros, with the demonstration.
- `E12.07` studio/docs/execution/venue-edition-and-films/evidence/E12.07-E12.08-verification-run.txt — Real command output for every gate, including the two that fail and their attribution.
- `E12.07` _wt-wp13/src/app/sitemap.ts — /venues/privacy added.
- `E12.07` _wt-wp13/scripts/check-venue-edition-contract.mjs — src/app/venues/privacy/page.tsx added to currentCommercialSources in the same change that created the route, so it is swept from day one rather than after the first regression.
- `E12.07` evidence/E12.07-venue-privacy-page.md — EXISTS (19,283 bytes). TWO INTEGRITY DEFECTS FOUND. (a) §2 row 1 maps 'sent, opened, redeemed, expired or withdrawn' onto 'mapCodeState() returns exactly revoked / redeemed / expired / issued / available' and marks it PASS, explaining only the withdrawn-to-revoked rename. sent-to-issued is defensible; 'opened' has NO counterpart in that list and the document does not say so. A green tick over a red trace — the exact failure class this programme has already paid for twice. (b) The document quotes
- `E12.07` evidence/E12.07-E12.08-verification-run.txt — EXISTS (3,913 bytes). Command output is real and I reproduced it. The self-reported reds are genuine and correctly attributed. §8.0's account of the broken 'grep -i -F' sweep is a real Git Bash defect and the disclosure is the right call.
- `E12.07` _wt-wp13/src/lib/account/types.ts:18 — PRIMARY EVIDENCE FOR THE HEADLINE DEFECT. AccessCodeState = 'available' | 'issued' | 'redeemed' | 'revoked' | 'expired'. No 'opened' member exists in the type the venue read path returns.
- `E12.07` _wt-wp13/src/lib/account/live/project-venue-access.ts:82 — mapCodeState() returns revoked/redeemed/expired/issued/available and nothing else. grep -i opened over this file returns zero hits. Also verified here: members: [] unconditionally at line 326, privacyReceipt.neverIncludes at 333-339 naming 'Recipient identities', 'Emails or Clerk ids', 'Note, task, or Timeline content', 'Comments and attachments', 'Plaintext license codes' — every negative claim on the page traces correctly, and maskLicenseCode is applied at line 230.
- `E12.07` app/src/lib/public-task.ts — Independently verified a positive claim I expected to fail and it holds. Page line 125: 'Titles and tags are visible. Descriptions are not.' PublicTask (app/src/lib/data.ts:292) is Pick<Task, 'id'|'title'|'lane'|'priority'|'due'|'tags'> and toPublicTask is an explicit field-by-field allowlist. Descriptions are excluded structurally. TRUE.
- `E12.08` _wt-wp13/src/app/venues/questions/page.tsx — The page. Route /venues/questions. Thirteen answers grouped under seven headings, in E09.11 §10.2's original order, plus one support answer inserted between answers 10 and 11. Both prices carry 'including VAT at the prevailing rate'. Insurance and data location are named as answered by a person and carry no claim.
- `E12.08` studio/docs/execution/venue-edition-and-films/evidence/E12.08-venue-faq-page.md — Answer-by-answer trace. §2 maps every load-bearing fact to its ratifying decision. §3 records the GDPR substitution in full, with the four conditions of use checked one by one against BACKLOG.md and RAID.md. §4 records the new support answer clause by clause. §5 records renewal as a gap rather than filling it. §10 is a boundary request to E09.11.
- `E12.08` studio/docs/execution/venue-edition-and-films/evidence/E12.07-E12.08-verification-run.txt — Shared verification log, real output.
- `E12.08` _wt-wp13/src/app/sitemap.ts — /venues/questions added.
- `E12.08` _wt-wp13/scripts/check-venue-edition-contract.mjs — src/app/venues/questions/page.tsx added to currentCommercialSources. This page carries a price, so being unswept would have been the worse half of the recon map's warning.
- `E12.08` evidence/E12.08-venue-faq-page.md — EXISTS, 13,569 bytes. Read in full. §2 mapping table is accurate; §3 GDPR substitution is genuine and the four conditions of use were really checked; §7's no-rewording claim is FALSE (see unmet criterion 2).
- `E12.08` evidence/E12.07-E12.08-verification-run.txt — EXISTS, 3,913 bytes. Honest log — it records the lint exit 1 and the experience:validate exit 1 rather than hiding them. My independent re-runs match it apart from two counts that moved (see verification).
- `E12.08` app (whole repo) — DEFECT SOURCE. `grep -rni keepsake` returns 0 hits across .ts/.tsx/.md. Sanity-checked the grep against a control term: 277 files match 'workspace', so the search is sound. Keepsake mode is not built.
- `E12.08` _wt-wp13/src/app/venues/couple-preview/page.tsx:336 — The same package's sibling page DOES disclose it: 'Keepsake. When a couple's planning term ends their workspace becomes read-only and stays open ... That is what is being bought. The screen that shows it is not written yet.' Same wording at what-you-see/page.tsx:325. The builder knew the disclosure was required and omitted it from the FAQ.
- `E12.08` app/src/lib/venue-edition-term.ts:61 and app/src/server/db/sponsored-access-term.ts:112 — CLEARS the access-term answer. `coupleAccessExpiryMs` implements max(redemption+548d, wedding+90d) and `recomputeSponsoredAccessTerm` implements the postponement recompute, so 'if they postpone, the term moves with them' is backed by real code, not a promise.
- `E12.08` app/src/app/api/account/export/route.ts — CLEARS the export half of answer 9. `/api/account/export` and `src/server/account-unified-export.ts` exist. It is the Keepsake read-only state, not the export, that is missing.
- `E12.08` _wt-wp13/src/app/venues/page.tsx:479 — E12.04 reference page carries 'Both prices include VAT at the prevailing rate. The number you see is the number you pay.' verbatim. This MITIGATES the price departure — the wording is already founder-approved elsewhere — but does not excuse it going unrecorded.
- `E12.08` evidence/E09.11-copy-system.md §10.2 — Source of record. I diffed all twelve answers against it line by line. Eleven match exactly (the solicitor answer is split into two paragraphs, formatting only). Answer 4 does not.
- `E12.08` evidence/E09.10-copy-hierarchy.md §6 Answer 1 — Confirms the GDPR interim form is quoted verbatim ('The paperwork is being written now and you will get all of it before you sign. I would rather show you a finished document than describe one.') and that three of its four conditions of use are genuinely unmet. §3 of the evidence doc is sound.
- `E12.09` studio/docs/execution/venue-edition-and-films/evidence/E12.09-one-page-proposal.md — Claim-by-claim trace of all fifteen ratified commercial facts against the literal strings in the module, plus the two-sweep forbidden-wording result, the one deliberate correction to E11.11 section 2, and what the audit does not cover.
- `E12.09` _wt-wp13/src/lib/venue-proposal.ts — The proposal as data, 314 lines. The only copy of the proposal words in the repository. Twelve blocks, six slots, pure function, no clock and no network so a test can pin it.
- `E12.09` _wt-wp13/src/lib/venue-proposal.test.ts — 16 tests, 16 pass. 38 refusal regexes run against BOTH the filled and the unfilled proposal so a placeholder cannot smuggle in what a paragraph may not say. Also pins block order structurally.
- `E12.09` _wt-wp13/src/components/venue/venue-proposal-sheet.tsx — Server component, no use client, so nothing on the sheet can measure a reader anywhere it renders. Now carries a headingLevel prop, which is the E12.14 h1 fix.
- `E12.09` _wt-wp13/src/components/venue/venue-proposal.css — A4 print rules. Chrome hidden two ways: a :has() rule that only fires on a page containing a sheet, plus print:hidden at the form's own call site.
- `E12.09` _wt-wp13/src/app/hq/venue-proposal/page.tsx — The founder tool, behind the HQ password gate, with robots index:false follow:false on its own account rather than relying on the /hq/ disallow.
- `E12.09` studio/docs/execution/venue-edition-and-films/evidence/E12.09-E12.14-verification-run.txt — Raw captured output: axe results, third-party request counts, the copy sweep, robots.txt and every gate.
- `E12.09` _wt-wp13/src/app/hq/venue-proposal/composer.tsx — EXISTS, client component, imports the module and adds no proposal copy. Not listed in the manifest's evidence refs although it is a rendering surface.
- `E12.09` evidence/E12.09-one-page-proposal.md — EXISTS, read in full. Unusually candid in places (admits the A4 fit was never printed, admits the order form does not exist, admits a second copy of the price). But its D-021 row claims both prices carry the VAT sentence, which is false, and its section 5 claims the module is the only copy of the proposal, which a 14-hit exact-string scan refutes.
- `E12.09` evidence/E12.09-E12.14-verification-run.txt — EXISTS. Its two third-party tables (lines 32-55) do not contain /hq/venue-proposal at all. Line 163 records studio.page.hq-venue-proposal failing the experience gate.
- `E12.09` _wt-wp13/src/lib/account/instrumentation/suppression.ts — R-027 unfixed here: presentBehavioural tests only the population floor. R-028 confirmed: presentRate has zero callers outside its own test.
- `E12.09` _wt-wp13/src/lib/venue-invitation/paths.ts — VENUE_EDITION_COMMERCIAL_PREFIXES = ['/v','/venues']. The root cause of the GA4 leak on /hq/venue-proposal.
- `E12.14` studio/docs/execution/venue-edition-and-films/evidence/E12.14-commercial-qa-sweep.md — The full sweep: analytics proved at the network layer, the conversion pass, real axe numbers per route per breakpoint, the copy sweep with measured pin values, the banned-term sweep by category with file and line, and the two process failures that nearly produced a false green.
- `E12.14` studio/docs/execution/venue-edition-and-films/evidence/E12.09-E12.14-verification-run.txt — Raw captured output for every measurement claimed, so the numbers can be checked rather than trusted.
- `E12.14` _wt-wp13/src/app/venues/privacy/page.tsx — CONVERSION FIX. The page closed on 'If something here is not clear, ask.' and offered only two navigation links. There was no way to ask. Now carries 'Ask me a question' to /contact as the primary action, same destination and subject as the two sibling preview pages.
- `E12.14` _wt-wp13/src/app/venues/questions/page.tsx — CONVERSION FIX, and the worse of the two. The FAQ carries E09.10 section 6's governing rule twice ('Ask, and you get the current answer', 'I do not know, and I would rather find out and tell you than guess') and offered no way to ask. The FAQ is exactly the surface that rule exists for.
- `E12.14` _wt-wp13/src/app/v/[token]/states.tsx — ACCESSIBILITY FIX. axe scrollable-region-focusable (serious) at 375px: the commercial summary table is min-w-[420px] inside overflow-x-auto, so the container scrolls and could not be reached by keyboard at all. WCAG 2.1 SC 2.1.1. Fixed with tabIndex, role=region and an aria-label. Re-measured: 0 violations, tab stops 2 to 3.
- `E12.14` _wt-wp13/src/components/venue/venue-proposal-sheet.tsx — ACCESSIBILITY FIX. /hq/venue-proposal rendered two h1 elements. The sheet now takes headingLevel, keeping h1 where it is the page and h2 where embedded, with body headings following one level down so the outline stays ordered. Re-measured: h1=1, skips=0.
- `E12.14` _wt-wp13/src/lib/venue-proposal.ts — COPY FIX. The module header named the rules it obeys using the words those rules forbid, giving two real prohibited-claims hits (P5 'allotment', D-027.3 'venue-written'). Relabelled by rule id, because prohibited-claims.v1.json has no naming-not-using heuristic by design.
- `E12.14` _wt-wp13/src/lib/account/instrumentation/suppression.ts — REFUTATION EVIDENCE. presentBehavioural (:33-41) tests only eligibleWorkspaces < 3; presentRate (:44-51) tests only < 5. grep -rn over all of src/ returns ONLY suppression.test.ts for both. Neither has a production caller.
- `E12.14` _wt-wp13/src/lib/account/format.ts — REFUTATION EVIDENCE. metricRateLabel (:44-71) returns Math.round((numerator.value / denominator.value) * 100) + '%' from two exact values with no threshold, confirming R-028 is unfixed and extends to behavioural counts too.
- `E12.14` _wt-wp13/src/proxy.ts — ROOT CAUSE of the analytics gap. :166-177 sets x-signal-no-third-party as a REQUEST header on matching pathnames. :162-165 documents fail-open but never considers client-side navigation.
- `E12.14` _wt-wp13/src/app/layout.tsx — ROOT CAUSE, second half. :160 reads the header, :183 renders {!noThirdParty && <GoogleTag />}. The App Router root layout does not re-render on client-side navigation, so the tag persists across a Link nav into /venues.
- `E12.14` _wt-wp13/src/components/landing/site-footer.tsx — REACHABILITY PROOF. :99 carries { href: '/venues', label: 'Venues' }. SiteFooter is rendered by src/app/page.tsx:51 and src/app/pricing/page.tsx:1191 - both GA-firing control routes - so the exposed path is the ordinary one.
- `E12.14` _wt-wp13/src/app/robots.ts — CONFIRMED. Disallow list includes VENUE_INVITATION_ROBOTS_DISALLOW, which paths.ts:86 defines as '/v/'.
- `E12.14` _wt-wp13/src/app/sitemap.ts — CONFIRMED and better than claimed: :52-53 filters through isVenueInvitationPath as a predicate rather than a hand-maintained list, with sitemap.test.ts asserting the same predicate against rendered output.

## Blockers

- `E12.05` Built, verified and evidenced in Wave 3, but it cannot enter Founder Review: it depends on E05.12, the world-class design-system review and visual-regression baselines, which is FOUNDER-CREATIVE and was deliberately excluded from Wave 3. The tool refuses the transition and a dependency waiver would be inventing founder consent, so it is recorded as blocked instead. Unblock by taking E05.12 to founder review, or by an explicit founder waiver recorded with --waive-deps.
- `E12.14` Built, verified and evidenced in Wave 3, but blocked behind the same chain as E12.05: E12.14 depends on E12.05, which depends on E05.12 (founder-creative, excluded from Wave 3). No dependency was waived because a waiver is the founder's to give. Unblock by clearing E05.12 then E12.05, or by an explicit founder waiver.

## Awaiting founder review

- `E05.01` Map the complete couple journey from venue invitation through first useful action, active planning, wedding day and Keepsake mode.
- `E05.03` Build the default wedding workspace template with restrained milestones, decisions, tasks and example Notes.
- `E05.04` Complete the wedding-specific Notes experience, including voice capture, manual capture and high-signal structured output.
- `E05.05` Complete the Notes-to-Tasks promotion flow using ordinary wedding-planning language.
- `E05.06` Complete the wedding Tasks experience for ownership, due dates, status, tags, priorities and clear next actions.
- `E05.07` Complete the task-detail experience for comments, attachments, subtasks, decisions and private collaboration.
- `E05.08` Complete the authenticated Timeline planning experience and its relationship with tasks and milestones.
- `E05.09` Complete the wedding-specific Signal briefing showing only what needs the couple’s attention today.
- `E05.10` Complete spouse, planner, family-member and collaborator invitations with appropriate role boundaries.
- `E08.01` Implement annual prepaid billing for standard and founding agreements.
- `E08.02` Implement an immutable founding-rate flag and historical price record for each qualifying venue.
- `E08.03` Implement renewal invoices, renewal reminders, failed-payment handling, grace periods and lapse behaviour.
- `E08.04` Complete multi-tenant data isolation and database row-level security for venues and couple workspaces.
- `E08.05` Complete role-based authentication and authorisation across venue owners, managers, viewers and couples.
- `E08.06` Secure invitation tokens against guessing, reuse, unintended forwarding, replay and duplicate redemption.
- `E08.07` Secure photograph and attachment uploads with file validation, malware controls, size limits and private storage.
- `E08.08` Complete audit logging, operational logging, error monitoring and alert escalation.
- `E08.09` Complete backups, restore testing, disaster-recovery procedures and data-integrity verification.
- `E08.10` Set and test performance budgets across portal, couple workspace, Timeline images and public artifact.
- `E08.11` Complete unit, integration, end-to-end, browser, responsive and device test coverage for the entire sponsored journey.
- `E08.12` Complete security review, production-readiness review, release checklist, rollback process and incident-response runbook.
- `E12.01` Build the public Venue Edition landing page around the sponsored couple experience.
- `E12.02` Build the private Founding 25 invitation page.
- `E12.03` Build a parameterised private proposal page for each venue.
- `E12.06` Add the Venue Portal trust-and-renewal preview.
- `E12.07` Add the privacy explanation covering exactly what the venue sees and never sees.
- `E12.08` Add the complete commercial, product, support, entitlement, Keepsake and renewal FAQ.
- `E12.09` Produce the concise one-page commercial proposal.

## Next action

`E03.02` Draft the annual Venue Edition agreement and commercial order form.

# Founder review packet — E13.03, E13.04, E13.15, E13.16

Generated 2026-08-03T05:15:32.071Z from PROJECT_STATE.json. 4 task(s).

**4 ready to approve · 0 not ready.**

Approving sets these tasks to Done and is the only thing that can. Nothing here is Done yet.

## Ready to approve

### `E13.03` Build the stylised Greater Limerick map geometry, River Shannon path and 15-, 30- and 45-minute rings.

Executor: claude_code · in review since 2026-08-03

**Acceptance criteria**
1. projection.ts is a pure equirectangular projection anchored at the ANCHOR latitude, with project(), projectToViewport(), an inverse for tests, KM_PER_DEGREE_LAT, and haversine great-circle distance. No magic numbers without a named constant and a derivation comment.
2. The projection is correct for a ~120 km extent and the error is stated rather than claimed.
3. LIMERICK_ANCHOR is a literal whose provenance is recorded in words only; no http(s):// substring anywhere in src.
4. REFERENCE_TOWNS re-declared as a literal array in signal-motion (name, lat, lon, measured drive minutes, road km), copied not imported.
5. DRIVE_TIME_RINGS radii are DERIVED from the measured towns by a least-squares fit through the origin, stated in km, with the fit coefficient, per-town residuals and R-squared exported. The deck SVG is not reused.
6. SHANNON_PATH is a stylised polyline of real, checkable lat/lon waypoints ordered from Lough Derg through Killaloe and the city out past Foynes toward Loop Head, each recording the settlement or feature it sits at, with SHANNON_ESTUARY_WIDTH_KM per waypoint.
7. CLUSTERS re-declared with human labels and bounding boxes.
8. assertGeometryInvariants() is exported and throws on: a ring radius not strictly increasing; R-squared below 0.8; a Shannon waypoint outside frame bounds; a town whose measured ring classification disagrees with ringFor(); a projection not invertible to within 1 metre. Nothing throws at module scope.
9. A test file in the house style covering the anchor, the round trip, ring monotonicity and fit, every town projecting inside the frame, the Shannon path monotonic west and south past the city, and assertGeometryInvariants() passing.
10. docs/video-production/LIMERICK-MAP-GEOMETRY.md records the anchor and source, the drive-time method (Valhalla as the number, OSRM as cross-check, the 38-52 band), the ring-fit method with coefficients and R-squared, every Shannon waypoint with its feature, and states plainly that the map is stylised and that 01-GEOGRAPHY.md:148-151 forbids publishing the 45-minute figure or the map as a promise of eligibility.
11. src/system/geo is scanned by scripts/check-motion-source.mts.
12. Typecheck and the new test file pass, with real output reported.
13. No commit, no push, no full render.
14. The fixture sits on the real geometry rather than on invented positions.
15. The ring radii are derived from measured drive times, not copied from the decoy deck SVG, and reproduce exactly from an independent computation.
16. The two named circle-vs-measurement disagreements are real and correctly quantified.
17. Nothing throws at module scope; assertGeometryInvariants is exported and only called by the check.

**Evidence**
- signal-motion/src/system/geo/projection.ts — Pure isotropic equirectangular projection with named, derived constants, an exact inverse and a great-circle distance. No wall-clock read, no randomness, no network, no colour.
- signal-motion/src/system/geo/limerick-geometry.ts — The baked geography: 16 towns, 3 derived rings with fit coefficient / residuals / R-squared, 18 Shannon waypoints with per-waypoint width, 12 cluster boxes. Nothing imported from the studio tree.
- signal-motion/src/system/geo/geo-spec.ts — assertGeometryInvariants() exported, never run at module scope, covering all five required conditions plus the derivation identity and the locked disagreement list.
- signal-motion/src/tests/limerick-geometry.test.ts — 10/10 pass. Independently re-derives k from the town table so the module is not the only witness to its own arithmetic, and pins k to six decimals and each radius to three.
- signal-motion/docs/video-production/LIMERICK-MAP-GEOMETRY.md — Provenance record with the full residual table, all 18 waypoints, and the publication limit stated plainly. The only place URLs appear.
- corepack pnpm verify (signal-motion) — tests 71 pass 71 fail 0; Motion source check OK · 71 production files; Composition contract OK; 24 compositions listed — The whole gate is green with the new module in the tree, and the new files are inside the check:motion scan.
- RING_FIT_KM_PER_MINUTE = 0.8326843115484087; rings 12.490 / 24.981 / 37.471 km; R-squared uncentred 0.9803, centred 0.8590; RMS residual 5.495 km — The ring radii are a defensible derivation from the 16 measured towns, not the deck SVG's fabricated 92/180/265 px.
- RING_BAND_DISAGREEMENTS = ["Tipperary town", "Mallow, Co. Cork"], locked in geo-spec.ts — The two places where the drawn circle contradicts the measured drive time are named, exported and build-locked rather than smoothed away.
- studio/public/brand/assets/deck-v2/limerick-45-minute-decision-map.svg — contains r="92", r="180", r="265" and the strings "SCHEMATIC" and "NOT FOR ROUTING" — The decoy asset was inspected and rejected on evidence, not on the handoff's say-so. No value from it entered the module.
- studio/docs/execution/venue-edition-and-films/venue-universe/05-COHORTS.md:119-126 — consent_public_naming and consent_map_publication are unknown for all 219 accounts. Verified at those exact lines; the handoff cited 122-126, the field rows are at 119-120. Recorded in the doc as the constraint on ever naming a venue on this map.
- signal-motion/src/system/geo/ (peer lane, untracked) — The geometry E13.03 asks for already exists in the working tree and was consumed, not duplicated. My schema imports its frame bounds, cluster ids and ring bands.
- Independent recomputation (node, from the 16-town table only): k=0.8326843115484087, radii 12.490/24.981/37.471, R2 0.9803/0.8590, Tipperary +0.433 km, Mallow +8.118 km — Every geometry figure in limerick-geometry.ts reproduces exactly from first principles. The ring radii are derived, not copied.
- Specification: `signal-motion/docs/video-production/LIMERICK-MAP-GEOMETRY.md`

### `E13.04` Build the data-driven map composition using verified venue coordinates and cohort metadata.

Executor: claude_code · in review since 2026-08-03

**Acceptance criteria**
1. signal-motion carries no committed venue dataset; real cohort geometry reaches the render only as a payload passed at render time.
2. A pin carries a name only where that account's consent_map_publication is the literal string 'granted'; every other value, including blank, means no name.
3. Today every pin is anonymous, and that is asserted rather than assumed.
4. pinId is a stable opaque id derived from account_id, never a venue name, and does not carry the account-number ordering.
5. Pin coordinates are rounded to four decimal places.
6. A fatal guard refuses the write on an email address, a phone number, a forbidden contact column name, a name on an anonymous pin, a missing coordinate, or a cohort that does not exist.
7. The stdout summary is counts only and never prints a venue name.
8. The tool refuses cohort 4 with a clear message and warns loudly that cohort 2 is short and cohort 3 holds two accounts.
9. The ring and cluster on each pin are re-derived from the source rather than trusted from a column.
10. A strict schema in signal-motion refuses a payload where named === false but name !== null, or where a pin's coordinates fall outside the frame bounds.
11. A fictional fixture of twenty-five invented pins, parsed against the schema at import, with fictional: true and one named recipient.
12. The change does not break the signal-motion build.
13. mapSpec.ts owns every frame number and every coordinate; beats are derived by running sum over seed durations, ranges are half-open, curves are closed-form and pure, and nothing else in the film hard-codes a frame or a coordinate.
14. assertLimerickMapTimeline() is exported, carries the invariants, and nothing throws at module scope.
15. schema.ts is z.object({...}).strict() with fixtureId literal, format enum, debug, reducedMotion, revision positive int, recipient {pinId, venueName, town}, cohort and the payload; a superRefine refuses a recipient pinId absent from the payload's pins.
16. Anonymous pins are the DEFAULT. An anonymous pin is a dot with no label. Only the recipient's pin carries a name and it comes from --props, never from the fixture.
17. Paper/ink/one earned indigo per MOTION-SYSTEM.md; all colour from VIDEO_TOKENS.
18. Format floors in src/system/layout/formats.ts are respected and assertInsideSafeArea() is used; 16x9 supported at minimum.
19. 9x16 is supported and the layout tables make it cheap.
20. reducedMotion renders a legible static frame, not a blank one.
21. Root.tsx carries a Folder and Compositions with ids as literal double-quoted JSX attributes, not generated from a .map(), with literal defaultProps, schema= and durationInFrames from the spec module.
22. src/composition-registry.ts is APPENDED, not prepended.
23. scripts/check-compositions.mts calls assertLimerickMapTimeline().
24. scripts/check-motion-source.mts:9-16 has the new composition root added, so the determinism bans genuinely apply.
25. pnpm --dir signal-motion verify passes and the composition enumerates via remotion compositions.
26. Do not commit, do not push, do not run a full video render.
27. The composition is data-driven: one registered composition serves a cohort of any size through --props, with no venue name committed to signal-motion.
28. The creative half — motion language, script, storyboard, voiceover, music, the final motion pass — is complete.
29. The composition enumerates with the right duration and dimensions in all four registered rows.
30. Pins render where their coordinates say, at the correct isotropic scale, with rings at the derived radii.
31. The recipient's pin is the only named one and the plate does not cover it.
32. The recipient plate does not obscure other venues on the map.
33. The map is legible as a map of the Limerick region on the delivery channel.
34. No banned pattern in the new composition source, and check-motion-source actually scans the new directory.

**Evidence**
- studio/docs/execution/venue-edition-and-films/tools/venue-map-export.mjs — The consent-safe transport exists: consent gate, opaque pin ids, coordinate rounding, fatal PII guard, cohort 3/4 handling, counts-only summary.
- studio/docs/execution/venue-edition-and-films/private/map-payload.json — The tool runs clean against the real cohort 1 data and produces 25 anonymous pins. Gitignored; regenerate rather than read into any committed artefact.
- signal-motion/src/fixtures/schemas/limerick-map.ts — The render side independently refuses a payload that would publish a name without consent, draw a pin outside the frame, carry an over-precise coordinate, move the anchor, or name a cohort that does not exist.
- signal-motion/src/fixtures/limerick-map-v1/data.ts — CI and every still render have a fictional 25-pin field to compose against, so no real venue is needed to build or review the map.
- signal-motion/src/tests/limerick-map-fixture.test.ts — 15 passing tests, twelve of them proving the schema REFUSES the payloads it must refuse rather than only accepting the one it should.
- studio git commit 05974d1 (feat/homepage-reduction-relay, 2026-08-03 04:22:40) — A concurrent session committed venue-map-export.mjs mid-flight. I did not commit. Flagged as an open question, not resolved.
- signal-motion/src/compositions/limerick-first-map/mapSpec.ts — The spec module owns every frame and coordinate, derives all beats and staged events, and carries assertLimerickMapTimeline() with seven sections of invariants including the locked cumulative boundaries [0,60,90,180,300,360,420], the locked indigo frame 300 and rest frame 360.
- signal-motion/src/compositions/limerick-first-map/schema.ts — Strict props schema with the required superRefine and the parse-time view-radius contract that keeps safe-area containment a construction rather than a clamp.
- signal-motion/src/tests/limerick-map-timeline.test.ts — 21 tests covering consent, the one earned indigo across all 420 frames, the plate-covers-its-dot regression, cohort-size staging, order totality and per-format safe area. Suite total went 86 to 107, all passing.
- corepack pnpm verify (signal-motion) — Dependency contract OK · 6 aligned Remotion packages; DS drift check OK · signal-ds 2.0.1 · 12 semantic values; Asset check OK · 2 local fonts; Copy-fit check OK · 9 lines; Composition contract OK (28 ids); Motion source check OK · 82 production files; Documentation check OK · 6 authorities.
- corepack pnpm exec remotion compositions src/index.ts — LimerickFirstMap-16x9 30 1920x1080 420 (14.00 sec); LimerickFirstMap-9x16 30 1080x1920 420; LimerickFirstMap-1x1 30 1080x1080 420; LimerickFirstMapAnimatic 30 1920x1080 420.
- signal-motion/out/stills/limerick-first-map/wide-360.png — 16x9 at rest: the market centred, the Shannon reading as a river out to the estuary mouth, 24 anonymous ink dots, one indigo dot with its name plate, and the ratified region caption. Inspected.
- signal-motion/out/stills/limerick-first-map/tall-360.png — 9x16 composes natively with the estuary cropped at the frame edge and the caption below the map. Inspected.
- signal-motion/out/stills/limerick-first-map/reduced-0.png — reducedMotion at frame 0 is a complete, legible map, not a blank card. Inspected.
- signal-motion/out/stills/limerick-first-map/animatic-330.png — The debug overlay renders the beat, timecode, cohort shape, opaque recipient pin id, units/km and the review-only ring figures (45 min · 37.5 km), in ink and faint ink with no accent. Inspected.
- signal-motion/out/stills/limerick-first-map/longname-360.png — The plate-covers-its-dot defect is fixed: a 31-character venue name drops the plate below its dot with the leader running from the indigo mark to the plate's top edge, and the dot stays fully visible. Inspected before and after the fix.
- signal-motion/out/stills/limerick-first-map/wide-55.png and wide-175.png — Beat staging is correct in isolation: the water beat completes with the river drawn and nothing else on screen; the rings beat completes with water, anchor and three settled circles and no venue yet.
- Negative test on scripts/check-motion-source.mts — Appending Date.now() to VenuePin.tsx produced 'WALL_CLOCK_NOW: src\compositions\limerick-first-map\components\VenuePin.tsx'. The determinism bans genuinely cover the new directory, and the trap named in the brief is closed.
- signal-motion/src/tests/period-timeline.test.ts (one assertion changed) — The-period's registry test asserted itself as the last row, which fails for any composition added after it. Recorded as a trap fixed, not a silent edit — the replacement asserts the same intent (contiguous block, not prepended) in a form that survives the next film.
- C:/Users/ethan/signal-studio-workspace/signal-motion/src/compositions/limerick-first-map/LimerickFirstMap.tsx:84-137 — The font-measurement gate that makes plate frames non-reproducible: delayRender/continueRender around a fontsReady promise whose setFontReady(true) races React's commit against Remotion's screenshot, while measureText decides the plate box. Frames 30/100/200 are bit-identical across repeated runs; frame 360 alternates between two outputs 20 anti-aliased pixels apart.
- C:/Users/ethan/AppData/Local/Temp/claude/C--Users-ethan-signal-studio-workspace/9b00cbfc-8d47-4963-b57f-a7b4fd24073e/scratchpad/verify.log — Full 10-step `corepack pnpm verify` chain, exit 0. 131 tests / 131 pass / 0 fail. check:compositions lists all 26 production ids including the four Limerick rows; check:motion 82 files; `remotion compositions` output at lines 203-233.
- C:/Users/ethan/signal-studio-workspace/signal-motion/out/stills/verify-limerick-16x9-f400.png — The held state. One indigo dot, one name plate, three unlabelled rings, the D-012 region caption — and four peer dots hidden under the plate.
- C:/Users/ethan/signal-studio-workspace/signal-motion/out/stills/verify-f060-water.png — The entire water beat: 97.6% pure white, 0.66% real ink, the Shannon at 1.099:1 contrast.
- C:/Users/ethan/signal-studio-workspace/signal-motion/src/compositions/limerick-first-map/mapSpec.ts (limerickMapRecipientPlate, the `coversDot` predicate) — The placement search's occlusion test closes over the recipient's own `dot` only. No peer position is in scope, so no placement can avoid one.

### `E13.15` Build the parameterised rendering pipeline using venue name, coordinates, cohort and private CTA data.

Executor: claude_code · in review since 2026-08-03

**Acceptance criteria**
1. Personalisation travels as data on one composition rather than as many registered compositions.
2. Render pipeline and batch personalisation.
3. Input is a signal-map-payload/v1 JSON file plus --cohort and --profile, and one deliverable is rendered per pin in the cohort with that pin as the recipient.
4. One registered composition, N prop sets. Never 25 registered compositions.
5. Output filenames come from makeRenderFilename(); no name is hand-built.
6. Render profiles come from src/render/profiles.ts via renderProfileCliArgs(); no flags inlined; a profile fitting a personalised invitation exists in the registry.
7. Resumable: an existing output is skipped unless --force.
8. Content-addressed: a changed prop set re-renders, an unchanged one does not.
9. out/limerick-first/<cohort>/render-index.json is written with pinId, composition, profile, revision, output path, output sha256, input hash, render seconds and payload schemaVersion per item.
10. The index never carries a venue name.
11. A --stills mode renders one frame per pin and is the default.
12. Windows: the in-process Remotion API is used in preference to spawning pnpm, and the Node major is pinned.
13. limerick:render:smoke exists in signal-motion/package.json: 2 pins, stills, the fictional fixture.
14. The smoke was actually run and its real output reported.
15. The repository's own gates still pass with these changes.
16. No commit, no push, no full video render, no task marked Done.
17. One registered composition and N prop sets produce N distinct deliverables.
18. Content-addressed rerun reuses unchanged items.
19. The composition is byte-reproducible on plate frames.
20. The render index carries no venue name.
21. A subset rerun flag (--only, taking a comma-separated pin id list) rerenders exactly the named pins and refuses an unknown one.

**Evidence**
- C:/Users/ethan/signal-studio-workspace/signal-motion/scripts/limerick-first-render.mts — The batch pipeline: bundle-once/render-many, one shared browser, per-item selectComposition, resume, --only, --force, index write, distinct-bytes guard, no spawned child process.
- C:/Users/ethan/signal-studio-workspace/signal-motion/src/render/limerick-first-plan.ts — The testable half: recipients schema, per-recipient projection, plan validation and ordering, content addressing, profile-to-encode derivation, index schema, no-venue-name assertion.
- C:/Users/ethan/signal-studio-workspace/signal-motion/src/tests/limerick-first-plan.test.ts — 18 passing cases covering the plan, the address, the filename grammar, the encode each profile implies, seven refusal paths and both privacy guards.
- C:/Users/ethan/signal-studio-workspace/signal-motion/out/limerick-first/1/render-index.json — The receipt the pipeline writes. 2046 bytes for the 2-pin smoke; the 25-item version was 19007 bytes with 25 distinct output sha256, 25 distinct input hashes and zero venue names.
- C:/Users/ethan/signal-studio-workspace/signal-motion/out/limerick-first/1/signal-motion__limerick-map-v1__pin-609931a194c1__limerickfirstmap-16x9__invitation-16x9-v1__r001__f0360.png — A rendered personalised still: one indigo dot, the plate to the right of it reading Fixture Venue 01 / Limerick City, the unlabelled rings and the D-012 region caption. 46243 bytes.
- C:/Users/ethan/signal-studio-workspace/signal-motion/out/limerick-first/1/signal-motion__limerick-map-v1__pin-3d73e958f172__limerickfirstmap-16x9__invitation-16x9-v1__r001__f0360.png — The second item of the same batch, personalised differently: a different dot in indigo, the 34-character name at the derived cap, and the plate flipped below the dot by the four-sided placement. 45789 bytes — the pair that proves the resolved-props bug is fixed.
- C:/Users/ethan/signal-studio-workspace/signal-motion/render-props/limerick-map-v1.recipients.json — The committed fictional recipients companion that makes the smoke reproducible with no real venue data.
- C:/Users/ethan/signal-studio-workspace/signal-motion/docs/video-production/RENDER-AND-HANDOFF.md — The invitation profile family, the extended filename grammar and the personalised-batch procedure recorded in the repository's own render authority.
- C:/Users/ethan/signal-studio-workspace/signal-motion/out/limerick-first/1/ — Three recipients, three distinct sha256 digests, correct opaque `pin-…` filename segments, index written.

### `E13.16` Generate unique tracked links, thumbnails and landing destinations for every personalised render.

Executor: claude_code · in review since 2026-08-03

**Acceptance criteria**
1. Tracked links, thumbnails and landing destinations for personalised renders.
2. Render pipeline follow-on.
3. Every account in a cohort has exactly one link, and the file's declared size matches its body.
4. Re-running the generator mints nothing for an account that already has a token, and the file is byte-identical when no input changed.
5. A token is 24 characters from a URL-safe alphabet, starts with a letter, and is not derived from the account id, venue name, cohort or clock.
6. A token that has already been issued is never invalidated by a later run.
7. No venue name appears in any URL, path or filename the generator writes, checked against the real names in the operational file.
8. No token appears in a render manifest, in render props, in a fixture, or anywhere inside signal-motion.
9. The link file is written only inside private/, and private/ is gitignored.
10. Every pinId in the link file is present in the cohort's map payload, and every payload pin has a link.
11. The generator carries the same fatal PII guard as venue-export.mjs.
12. The tool prints counts only and never a venue name or a token.
13. The landing-destination contract states the URL shape, what the page must and must not contain, that the click is the only event, exactly what is recorded, retention, unknown/revoked behaviour, and the no-pixel/no-third-party/no-consent-tracker rule.
14. The contract records that GA4 runs on every studio public surface with no consent gate and that the destination route needs an explicit carve-out.
15. Each render produces a poster frame at a frame number chosen in the spec module, written next to the video with the same filename grammar.
16. The link tool resolves each account's poster by pin id in the form the render filename actually carries.
17. A test covers the token generator's stability property, in the style of tools/venue-universe.test.mjs.
18. signal-motion's definition of done holds after the change.
19. No real venue name, account id or contact data has entered any committed or commit-eligible file.
20. Nothing was committed or pushed.

**Evidence**
- studio/docs/execution/venue-edition-and-films/tools/venue-map-links.mjs — The token generator: minted once, never re-minted, additive, guarded, counts-only, private-only output.
- studio/docs/execution/venue-edition-and-films/tools/venue-map-links.test.mjs — 26 tests, stability property first. node --test -> pass 26, fail 0.
- studio/docs/execution/venue-edition-and-films/evidence/E13.16-link-and-destination-contract.md — The landing-destination contract handed to WP-13, including the GA4 carve-out and the two recorded conflicts.
- studio/docs/execution/venue-edition-and-films/private/film-links-cohort-1.json — 25 links minted for the real cohort 1, gitignored, guard clean. Not quotable in a packet: it maps tokens to accounts.
- signal-motion/src/render/poster.ts — The poster frame is declared per composition from the spec module, and the poster's name is derived from its video's.
- signal-motion/scripts/venue-film-poster.mts — A poster is produced beside an existing video, with an exported entry point for the batch pipeline.
- signal-motion/src/tests/render-poster.test.ts — 6 tests on the poster frame, the naming, the image format, the refusals and the pin-id filename form.
- signal-motion/out/limerick-first/1/signal-motion__limerick-map-v1__pin-78864023a9a4__limerickfirstmap-16x9__review-16x9-v1__r001__f0360.jpeg — A rendered poster at frame 360 from the committed fictional fixture: map complete, one earned indigo on the recipient dot, name plate fully open. Gitignored (signal-motion/.gitignore:2).
- studio/src/app/layout.tsx:144 — gtag.js is rendered from the studio root layout on every production page with no consent gate, so /v/[token] inherits it. Section 7 of the contract.
- studio/src/app/robots.ts — /v/ is not in the disallow list today. Requirement 2 of the carve-out.
- studio git ls-tree -r HEAD | grep venue-map -> docs/execution/venue-edition-and-films/tools/venue-map-export.mjs — A WP-12 artefact is already in studio's commit history at 05974d1, dated 2026-08-03 04:22:40.
- Specification: `docs/execution/venue-edition-and-films/evidence/E13.16-link-and-destination-contract.md`

## Your decision

Approve all of the above in one command:

```bash
node studio/docs/execution/venue-edition-and-films/tools/project-control.mjs approve-batch "your approval note" E13.03 E13.04 E13.15 E13.16
```

Or push back on any single one:

```bash
node studio/docs/execution/venue-edition-and-films/tools/project-control.mjs reject E13.03 "what is wrong"
```


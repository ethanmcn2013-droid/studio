<!-- honest-state-banner -->
> ## Read this before approving
>
> The heading below says "ready to approve". That is the tool's way of saying every task
> has acceptance criteria and evidence recorded. **It does not mean the work is complete.**
>
> **11 of 11 tasks in this packet are PARTIAL.** Every one was
> independently re-verified by an adversarial agent whose default was to refute, and the
> verifier's read overrides the build agent's self-assessment wherever they disagreed. Where
> a task was claimed complete and the verifier disagreed, it is recorded below as partial.
>
> | Task | Verified state | Criteria | **Not met** | Evidence |
> |---|---|---|---|---|
> | `E06.01` | **PARTIAL** | 7 | 4 | 14 |
> | `E06.02` | **PARTIAL** | 6 | 3 | 12 |
> | `E06.06` | **PARTIAL** | 7 | 3 | 11 |
> | `E06.07` | **PARTIAL** | 8 | 3 | 11 |
> | `E06.05` | **PARTIAL** | 8 | 7 | 17 |
> | `E06.04` | **PARTIAL** | 6 | 7 | 9 |
> | `E06.03` | **PARTIAL** | 10 | 4 | 9 |
> | `E06.09` | **PARTIAL** | 8 | 8 | 12 |
> | `E06.10` | **PARTIAL** | 7 | 3 | 11 |
> | `E06.11` | **PARTIAL** | 11 | 11 | 10 |
> | `E06.12` | **PARTIAL** | 7 | 6 | 16 |
>
> Approving a partial task is a legitimate choice — it means you accept the gap as recorded
> and want the task closed anyway. It should be a decision, not an accident. The unmet
> criteria for each task are in its own section, and the reasoning is in the session record.

# Founder review packet — E06

Generated 2026-08-04T08:23:02.803Z from PROJECT_STATE.json. 12 task(s).

**9 ready to approve · 3 not ready.**

Approving sets these tasks to Done and is the only thing that can. Nothing here is Done yet.

## Ready to approve

### `E06.02` Add per-milestone visibility controls for private, title-and-date, image and short-story publication states.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. Private is proven twice, because two mechanisms produce it: a milestone omitted from the selection is never copied, and a milestone with overlay hidden = true is refused at selection. Both proved on behaviour, and the refusal proved by a fail-before mutation.
2. Title-and-date is proven on a serialised DTO: an undated milestone carries no date key at all, while a dated one in the same payload carries exactly date, publicId, state, title.
3. Freeze semantics are stated with their evidence: publication items are a copy taken at create, so the published page does not follow the private source. Where this is proved and where it is only read is distinguished.
4. Image and short-story are recorded as not built, with the exact schema, validator and migration work each needs, and the migration blocker named.
5. A regression guards the half-built case: a test fails if ITEM_KEYS gains an image or story key, or timeline_publication_items gains an image or story column, without the content model being updated.
6. Nothing in the spec or the evidence describes image or short-story as available, and no user-visible string offers them (D-016).

**Evidence**
- studio/docs/execution/venue-edition-and-films/evidence/E06.02-milestone-visibility-states.md — Criterion-by-criterion. Opens with an honest four-state table: two ship and are proven, two do not exist at any layer and are named as not built.
- _wt-wp14/src/modules/timeline/server/couple-artifact-boundary.test.ts — Four E06.02 assertions, all green. 'a milestone left out of the selection is never copied' publishes one of four fixture milestones and asserts the DTO item titles are exactly [The ceremony] and that the payload does not contain 'Agree the budget with Dad'. 'a hidden milestone is refused at selection' asserts createAudiencePublication rejects with /not available in this workspace/.
- _wt-wp14/src/modules/timeline/server/audience-timeline.ts:159 — The hidden refusal, unchanged and now proven behaviourally rather than read: if (!node || node.hidden) throw new TypeError(...). Publication is opt-in per milestone, capped at 100 items and one project.
- _wt-wp14/src/modules/timeline/lib/audience-timeline.ts:221 — Title-and-date on the payload: if (item.date !== undefined) mapped.date = ... In one resolved DTO, 'Find the dress' has keys exactly publicId/state/title with 'date' in item false, while 'The ceremony' in the same payload has date/publicId/state/title. A genuine omission, not a blank string or a placeholder. This is also the default for a milestone with no target date, which is the more private default.
- _wt-wp14/src/modules/timeline/server/db/timeline-queries.ts:77-99 — The curation layer behind title-and-date: an explicit inherit | date | undated intent persisted through the __signal_timeline_undated__ sentinel, so a null column no longer doubles as both 'inherit' and 'intentionally undated'. Existing, recorded as candidate evidence.
- _wt-wp14/drizzle-timeline/ — The blocker, verified by listing the directory and reading the runner: one file, 0000_timeline_baseline.sql, plus meta. scripts/db/migrate.mjs:465 reads TASKS_DATABASE_URL only. The receipt-backed runner does not reach the Timeline database. No migration was written.
- evidence/E06.02-milestone-visibility-states.md — EXISTS (9435 bytes). Read in full. Criterion-by-criterion, opens with an honest four-state table naming two states as not built at any layer. No claim of legal approval or GDPR compliance. Two factual notes in it are now STALE in the safe direction: it reports npx tsc --noEmit and pnpm ds:check as red in this worktree; I re-ran both and BOTH ARE GREEN (tsc exit 0 with no output, ds-check 'clean - no drift'). A concurrent Wave 3 package fixed next.config.ts since it was written.
- tasks/E06.02.md — EXISTS. Spec written before assessment, criteria match the six given verbatim. Deliverables list ticks four and leaves 'Image publication. Not built.' and 'Short-story publication. Not built.' explicitly unticked. Founder sign-off block is blank - not pre-filled. Header carries Status: Founder Review, which is a written file, not a project-control write.
- _wt-wp14/package.json — REGISTRATION VERIFIED - the CI failure mode does not apply. git diff package.json shows the 'test' script's final segment changed from '... unpublished-timeline-boundary.test.ts' to '... unpublished-timeline-boundary.test.ts src/modules/timeline/server/couple-artifact-boundary.test.ts'. The new suite runs in CI. Combined run: tests 18, pass 18, fail 0.
- _wt-wp14/src/modules/timeline/app/plan/[projectSlug]/_components/curation-surface.tsx:245 — THE HIDDEN CONTROL HAS A REAL PRODUCTION CALLER - I checked, because 'a control with no production caller' is a listed programme failure mode. upsertNodeOverlayAction(..., { hidden: !node.hidden }) is the owner's curation toggle. Defence in depth confirmed at app/audience/page.tsx:60, which filters !node.hidden out of the selection list before it is offered.
- _wt-wp14/src/modules/timeline/lib/audience-timeline.ts:56 — const ITEM_KEYS = new Set(["publicId", "title", "date", "state"]) - four keys, no image, no story. Line 221: if (item.date !== undefined) mapped.date = calendarDate(...). Genuine key omission, confirmed by the test asserting Object.keys(dress).sort() equals exactly [publicId, state, title].
- _wt-wp14/src/modules/timeline/server/audience-timeline.ts:797-809 — Freeze read path verified by reading it: resolveAudienceTimeline selects exactly publicId, title, calendarDate, state, sortOrder from timeline_publication_items and touches no node, overlay, project or task table. The only writers to that table are createAudiencePublication:228, createNotesAudiencePublication:325 (a Notes projection, audienceKind 'class', never milestone-derived), updateAudiencePublicationItem:613 (owner-scoped, title/date/state only) and the unpublish/divergence updates.
- Specification: `studio/docs/execution/venue-edition-and-films/tasks/E06.02.md`

### `E06.03` Complete milestone photograph upload, crop, compression, orientation, alt text and deletion.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. A photograph carrying GPS coordinates, a capture time and an orientation is read for all three and then published with none of the three present, proven on a real byte round trip that also asserts the literal ASCII of the removed data is absent from the output.
2. The EXIF orientation is returned to the caller as a value after the segment carrying it has been removed, together with the CSS transform and the axis-swap flag needed to put a portrait photograph the right way up.
3. Format support is an allowlist and a format outside it is refused rather than passed through; a truncated or malformed file is refused, not half-parsed.
4. Stripping never touches a pixel: intrinsic dimensions and the entropy-coded image data are byte-identical before and after.
5. The maximum long edge, the minimum long edge, the target byte size and the refusal ceiling exist as exported values with numbers, not as comments.
6. Alt text has exactly three outcomes (described, decorative, refused) and never falls back to a filename; a decorative photograph gets alt="", never a missing alt attribute.
7. A caller that forgets to strip is refused publication: bytes still carrying metadata return metadata-present rather than being published.
8. No face detection, no auto-tagging, no face grouping and no smart crop appears anywhere in the module, asserted by reading the source (D-033).
9. Every criterion above is covered by a test registered in package.json's test script, so it runs in CI.
10. What is not built is stated as not built: the upload wiring, the public image path, crop, and re-compression.

**Evidence**
- _wt-wp14/src/lib/image/metadata.ts — Pure-TypeScript, zero-dependency JPEG segment and PNG chunk parser. Reads the TIFF stream for orientation (0x0112), the GPS IFD pointer (0x8825) and three date tags, then rebuilds the file without APP1, APP3-APP15, COM, eXIf, tEXt, zTXt, iTXt or tIME. Keeps APP0/JFIF, APP2/ICC and PNG colour chunks. Copies everything from SOS onward verbatim, so no pixel is touched and no image library is needed.
- _wt-wp14/src/lib/image/policy.ts — The numbers as values: PUBLISHED_MAX_LONG_EDGE_PX 2000, PUBLISHED_MIN_LONG_EDGE_PX 800, PUBLISHED_TARGET_BYTES 512000, PUBLISHED_MAX_BYTES 2097152, ALT_TEXT_MAX_LENGTH 200. decidePhotograph refuses 'metadata-present' when handed un-stripped bytes. resolveAltText has no filename fallback.
- _wt-wp14/src/lib/image/metadata.test.ts — 13 tests. Fixtures are built byte by byte in the test (a JPEG with an EXIF APP1 carrying orientation 6, DateTime 2026:08:03 and a GPS IFD, plus a COM comment; a PNG with eXIf and tEXt) so a reader can see exactly what is being claimed rather than trusting a committed binary.
- studio/docs/execution/venue-edition-and-films/evidence/E06.03-photograph-boundary.md — Criterion-by-criterion, with the pre-task audit table, the full unfiltered face-detection grep, and a section naming the five things not built and why.
- _wt-wp14/src/server/actions/attachments.ts:36 — The reason nothing is wired here: this tree still carries the old BLOCKED_MIME_TYPES denylist checked against the client-supplied file.type. E08.07's byte-checked allowlist (src/lib/upload-validation.ts) is on claude/wp07-engineering and does not exist in this worktree.
- studio/docs/execution/venue-edition-and-films/evidence/infrastructure-blob-and-email-auth-2026-08-03.md — Supersedes the recon map's claim that BLOB_READ_WRITE_TOKEN is unprovisioned. A private EU store (fra1, Frankfurt) is connected to app on Production, Preview and Development. A redeploy is still needed before it takes effect.
- _wt-wp14/package.json — REGISTRATION CONFIRMED, and confirmed as part of THIS diff rather than pre-existing. `git diff package.json` shows src/lib/image/metadata.test.ts added to the `node --import tsx --test` segment. Independently corroborated by its assertions appearing at lines 564 and 573 of the full-suite output, so the 'new test file never runs in CI' failure mode does NOT apply here.
- evidence/E06.03-photograph-boundary.md — EXISTS, 304 lines. Unusually honest: pre-task audit table, the full unfiltered grep, §3 reporting the 12-pass/1-fail first run, and §C10 naming five unbuilt things with reasons. It also proactively corrects the recon map on BLOB_READ_WRITE_TOKEN. Its one overreach is the §Headline sentence 'The privacy-critical half of E06.03 is built and tested', which my probes refute.
- evidence/infrastructure-blob-and-email-auth-2026-08-03.md — EXISTS, 97 lines. Supports the stated correction that the blob store is EU (fra1, Frankfurt), private, and that a redeploy is still required. All six referenced evidence paths exist; no dead path in this package.
- Specification: `studio/docs/execution/venue-edition-and-films/tasks/E06.03.md`

### `E06.04` Complete milestone navigation and the transition from a Timeline point into its photograph and story.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. The dependency is stated rather than worked around: there is no photograph and no story to transition into until E06.03 and E06.02 land, and no placeholder frame ships.
2. The existing keyboard contract survives unchanged: Left and Right between milestones, Home and End to jump, Enter or Space to select, Escape to close, with aria-expanded and aria-controls on the selected point and a polite live region announcing open and closed.
3. prefers-reduced-motion is honoured, and the component reads it through a hydration-safe hook so server and first client render agree.
4. A milestone with no photograph and no story degrades to today's detail panel with no empty frame and no layout shift.
5. The axe gate stays clean: zero critical and zero serious violations at 390, 768, 1280 and 1440.
6. detailNote's three generated sentences are either replaced or kept deliberately, and they pass the brand voice. They are front-facing copy on a couple's wedding page.

**Evidence**
- _wt-wp14/src/modules/timeline/components/artifact/timeline-artifact.tsx:320 — The defect and the fix. Two of the three sentences used 'journey' as a noun, which the brand voice bans outright, on a sponsored couple's public wedding page: 'This is the next point on the shared journey.' and 'This milestone comes later on the shared journey.' Now 'This one is already behind you.' / 'This one is next.' / 'This one comes later.' Kept rather than deleted, with a source note saying it is the slot a real milestone story lands in once E06.02 and E06.03 give the DTO something to car
- _wt-wp14/src/modules/timeline/lib/audience-timeline.ts:56 — Why the photograph half cannot start: ITEM_KEYS is exactly [publicId, title, date, state], attachments and description are in FORBIDDEN_KEYS, and exactKeys throws for anything outside the set. A transition built now would animate from a milestone to an empty frame.
- _wt-wp14/src/modules/timeline/components/artifact/timeline-artifact.tsx:56 — useArtifactReducedMotion gates Motion's media-query hook behind React's hydration snapshot so SSR and first hydration choose the same motion props. Honoured in the place most implementations miss: focusPoint:431 passes scroll behaviour 'auto' rather than 'smooth' under reduce, because programmatic smooth scrolling is motion too.
- _wt-wp14/src/modules/timeline/components/artifact/timeline-artifact.tsx:434 — The keyboard contract, recorded as passing and not rebuilt: handleKeyDown 435-450 for Left/Right/Home/End/Escape, Enter and Space left to the native button rather than re-implemented, roving tabIndex at :565, aria-expanded :568, aria-controls :569, polite live region :604-608, instructions bound with aria-describedby at :536.
- evidence/E06.04-milestone-navigation.md — Criterion-by-criterion evidence, stating plainly that criterion 5 is unverified in this session and why.
- _wt-wp14/src/modules/timeline/components/artifact/timeline-artifact.tsx:442 — EXISTS AND CONFIRMED, criterion 2 met — at :442, not the :434/:435 cited. handleKeyDown:442-459 handles ArrowRight/ArrowLeft/Home/End/Escape with preventDefault on each. Enter and Space are left to the native <button> onClick at :582-586, not re-implemented. Roving tabIndex at :573, aria-current at :574, aria-pressed :575, aria-expanded :576, aria-controls :577, per-point label with 'Milestone N of M' at :578, aria-describedby on the <ol> at :544 with instruction text at :497-498, polite atomic 
- _wt-wp14/src/lib/public-analytics-boundary.ts:16 — PRIVACY DEFECT FOUND, outside E06.04 but against the decision this wave implements. The file's own docstring states '`/p`, `/s`, `/share` and `/embed` are excluded from GA4 entirely and unconditionally' per R-032 Option A / D-033. Line 16 is `const ANALYTICS_EXCLUDED_ROOTS = ["/p", "/s", "/share"] as const;` — `/embed` is absent. The only other suppressor, isBareArtifactPath (src/lib/bare-artifact-path.ts:2), is `pathname === "/s" || pathname.startsWith("/s/")`. So GoogleTag (src/components/anal
- _wt-wp14/src/modules/timeline/lib/viewer-count.ts — PRIVACY PROBE, NEGATIVE — I could not construct a viewer count that identifies one person through this path. presentViewerCount is first-party, floors at VIEWER_COUNT_FLOOR = 5, coarsens lastViewedAt to month-and-year via Intl, returns pre-rendered strings so no downstream formatter can reconstruct the integer, throws TypeError on a non-integer count, and renders no bound such as 'fewer than five' below the floor (R-027's mitigation says a bound is itself a disclosure). artifact-studio.tsx:57-68
- _wt-wp14/src/app/robots.ts:29 — PRIVACY PROBE, PARTIAL PASS. disallow now reads ["/app", "/p", "/s", "/share", "/redeem", "/welcome", "/api"] — /p added under D-033 R-031 Option B. The comment records the residual honestly rather than hiding it: a Disallow stops an obedient crawler fetching the page, which also stops it reading a per-page noindex, so a URL discovered from an external link can still be listed URL-only, and closing that needs an X-Robots-Tag response header. Stated, with the follow-on named. Not E06.04's, and no
- Specification: `studio/docs/execution/venue-edition-and-films/tasks/E06.04.md`

### `E06.05` Implement private-link, password-protected and intentionally public sharing modes.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. The three modes are defined against a named product. Where a mode spans two products, the criteria say which control belongs to which.
2. Private link is assessed and recorded as existing evidence: SHA-256 token hash, a unique index on the hash, a timing-safe compare, an explicit published-state gate, optional expiry, rotate and revoke, share versioning and a rate limit.
3. Password mode is recorded as deliberately not built, and the exact sentence a couple is shown in its place ships in the product rather than being implied by an absent control.
4. /p is in the robots.txt disallow list, and the known interaction between a Disallow and a per-page noindex is written down rather than papered over.
5. The publish confirmation states plainly what publishing does, including that task titles and tags are on the page. Before this task it was one button whose caption never mentioned either.
6. Wedding workspaces are noindex on /p by default, and search visibility is an explicit couple opt-in.
7. R-033 is answered: a decision is recorded on whether wedding workspaces are embeddable at all, and if the answer is no, the exclusion is enforced in the route and scripts/check-frame-headers.mjs is updated in the same change.
8. Nothing in the shipped copy states or implies legal approval, solicitor review or GDPR compliance (D-016).

**Evidence**
- _wt-wp14/src/app/robots.ts — /p added to the disallow list under D-033. The file carries the interaction that gets discovered late: a Disallow stops an obedient crawler fetching the page, which also stops it reading a per-page noindex, so a URL discovered from an external link can still be listed URL-only. Closing that residual needs an X-Robots-Tag header, which cannot be set per workspace in next.config because the header rules are static and the wedding test needs a database read.
- _wt-wp14/src/components/app/settings/sections/workspace.tsx — A four-line 'Before you publish' panel above the publish button, persistent rather than a dismissable modal so it cannot be clicked past unread. The load-bearing line is 'Your task titles and tags are on the page. Any names you wrote into them are on the page too.' — task titles and tags are exactly where guests' and suppliers' names live.
- _wt-wp14/src/components/app/share/share-button.tsx — The password refusal, shipped where a couple creates a /share link: 'There is no password on this link. Anyone you send it to can open it, and so can anyone they forward it to. Set an expiry, or revoke the link, when you want it to stop working.' The forwarding clause is there because that is the failure mode a couple does not think about.
- _wt-wp14/src/modules/timeline/server/audience-timeline.ts:447 — Private link, recorded as passing and NOT rebuilt. publishAudiencePublication mints a token, stores only the SHA-256 digest, sets every prior active share to 'rotated' in the same transaction and versions the new one; unpublishAudiencePublication at :565 revokes every active share in one transaction.
- _wt-wp14/src/server/db/schema.ts:737-767 — The qualification on criterion 2: share_links.token is a PLAINTEXT primary key resolved by raw equality, with no hash and no constant-time compare, unlike the Timeline path. Wave 3's fix is on claude/wp07-engineering, which is not in this tree, so it was neither assumed nor touched.
- evidence/E06.05-sharing-modes.md — Criterion-by-criterion evidence, including the verified final state showing that neither the search-visibility column nor its migration exists in this branch.
- _wt-wp14/src/app/robots.ts:28 — EXISTS. Line reads `disallow: ["/app", "/p", "/s", "/share", "/redeem", "/welcome", "/api"]` with a 14-line comment recording the Disallow-versus-noindex interaction verbatim as claimed. git diff confirms /p is newly added by this task; /s pre-existed. DEFECT: "/p" without a trailing slash prefix-matches /press, /principles, /privacy — all three real pages, all three in sitemap.ts, none with an Allow rule.
- _wt-wp14/src/components/app/settings/sections/workspace.tsx:614-645 — EXISTS and matches the claim. The four-line panel sits inside the not-yet-published branch of the ternary, above the Publish button, in normal document flow — a persistent panel, not a dismissable modal, exactly as claimed. Line 627 carries the load-bearing sentence about task titles and tags. The toast at :524 changed to mention search engines. git diff shows +42/-14 on this file and nothing else.
- _wt-wp14/src/components/app/share/share-button.tsx:385-393 — EXISTS and matches the claim word for word, directly above the generate button. I checked the claim it depends on: EXPIRY_OPTIONS at :16, expiryDays state at :56, the "Expires in" field at :362 and revokeShareLinkAction at :152 all exist, so "Set an expiry, or revoke the link" points at controls that are actually on this surface. The component has three real production callers (page-header.tsx:90,279 and view-tools.tsx:193) — not orphaned.
- _wt-wp14/src/modules/timeline/app/audience/audience-manager.tsx — NOT IN THE AGENT'S EVIDENCE LIST, and it should have been. This is the /s audience-link creation surface — the other half of the private-link mode per the evidence's own product table. `grep -n password -i` and `grep -n forward -i` both return zero. The refusal sentence is absent here. Publish, rotate, revoke and link-expiry controls are all present (:489-508), so this is unambiguously the surface a couple uses to mint the Timeline link.
- _wt-wp14/src/modules/timeline/server/db/timeline-schema.ts:563-577 — Criterion 2 verified independently, not taken on trust. `tokenHash: text("token_hash").notNull()` at :564 with the comment "SHA-256 hex digest only. The 256-bit raw token is returned once"; `uniqueIndex("uq_audience_shares_token_hash").on(t.tokenHash)` at :577. The timing-safe compare is real: audience-timeline.ts:243-245 implements hashesEqual over node:crypto timingSafeEqual. The agent's PASS on /s holds.
- _wt-wp14/src/server/db/schema.ts:737 and src/server/db/queries.ts:720 — The qualification the agent recorded is real and I reproduced it. schema.ts:737 declares `token: text("token").primaryKey()` with the comment "URL-safe random token, also the primary key" — no hash column. queries.ts:720 resolves it with `eq(shareLinks.token, token)`, raw equality, no constant-time compare. Correctly recorded, correctly not touched (the fix is on claude/wp07-engineering, absent from this tree).
- _wt-wp14/src/app/p/[slug]/page.tsx — Criterion 6 disproof. generateMetadata returns title, description, openGraph and twitter and NO robots key; `grep -rn robots src/app/p/` exits 1. The file header still reads "Server-rendered for indexing" and "a public, indexable, rarely-changing page hit by crawlers and social unfurls" — now in direct contradiction with robots.ts, and left unamended.
- _wt-wp14/src/app/sitemap.ts:19,21,32,34 — Proof of the headline defect's blast radius. /pricing (0.9), /principles (0.8), /privacy (0.4) and /press (0.5) are all declared crawlable here while robots.ts:28 now forbids the /p prefix. Three of the four have no Allow rule to rescue them. The sitemap and robots.txt actively contradict each other on production.
- _wt-wp14/src/components/analytics/google-tag.tsx:28-29 and src/lib/public-analytics-boundary.ts — GA4 attack refuted. GoogleTag calls isAnalyticsExcludedPath(pathname) and returns null BEFORE the enabled check, so the exclusion cannot be re-enabled by an env flag. public-analytics-boundary.ts hard-codes ["/p","/s","/share","/embed"] with exact-or-prefix matching. src/server/public-surface-analytics-contract.test.mjs is registered in package.json and ran in my pnpm test. No second analytics provider (Vercel Analytics, PostHog, Plausible) appears on layout.tsx, /p, /embed or /share. This is E0
- _wt-wp14/src/app/embed/[slug]/page.tsx and scripts/check-frame-headers.mjs — Criterion 7 disproof. The route resolves any slug via getPublishedWorkspaceBySlug with no token and no workspace-type exclusion. check-frame-headers.mjs:63-91 shows the /embed carve-out is still enforced as a REQUIREMENT — global DENY with a negative lookahead excluding /embed, plus an /embed/:path* entry that must set `frame-ancestors *` and must omit X-Frame-Options. `git status --short scripts/check-frame-headers.mjs` returns blank. Nothing was enforced and nothing was updated.
- tasks/E06.05.md:127-135 — EXISTS. Deliverable 4 is honestly checkboxed [ ] NOT BUILT. But the Risks block asserts the shipped confirmation says "The page is not listed unless you turn search listing on" and must not reach a couple — a string that is not in the code. Also note the spec's Status field already reads "Founder Review"; per the shared brief no task reaches Done without Ethan's explicit approval, and the sign-off block is correctly unfilled.
- Specification: `studio/docs/execution/venue-edition-and-films/tasks/E06.05.md`

### `E06.06` Allow couples to conceal exact wedding dates, locations and other sensitive milestone information.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. Concealing a milestone date is proven on the serialised public DTO: the item carries no date key at all, rather than a blanked or placeholder string.
2. The wedding day itself has its own concealment control that works on an already published artifact, and concealing removes primaryDate from the payload entirely.
3. A half-set pair - a label with no date, or a date with no label - never publishes a partial main date.
4. Concealment is owner-scoped: the publication ownership is re-checked server-side and another workspace cannot clear someone else's date.
5. Location is stated plainly. There is no structured location field anywhere in the published model, so either a structured field is added or the refusal is written down. The criteria must not imply a control that does not exist.
6. A concealed value never appears in the OpenGraph card or the page title of the surface it was concealed on, asserted rather than assumed.
7. EXIF GPS is cross-referenced to E06.03, and the current position stated: no photograph reaches any public surface today, so there is no metadata path to leak through and no claim is made that one is handled.

**Evidence**
- studio/docs/execution/venue-edition-and-films/evidence/E06.06-conceal-dates-and-details.md — Criterion-by-criterion, including the written refusal on structured location and an explicit scope note that criterion 6 is proven for /s only and that /p's OpenGraph card belongs to E06.05 and E06.12.
- _wt-wp14/src/modules/timeline/server/audience-timeline.ts:650 — NEW. setAudiencePrimaryDate. Re-checks ownsPublication, writes primaryDateLabel and primaryDate together, and treats a missing half as concealment: const conceal = !input.primaryDate || !input.primaryDateLabel. Returns 'set' or 'concealed'. Before this, primaryDate was written once at :219-220 inside createAudiencePublication and read back at :819-826, and no code path updated it - the only way to take the wedding day off a live page was to unpublish the whole artifact.
- _wt-wp14/src/modules/timeline/server/actions/audience-timeline.ts:363 — NEW. updateAudiencePrimaryDateAction. Owner-gated through ownerWorkspace, demo-read-only guarded, intent=conceal clears both columns, revalidates /app/timeline/audience. Success copy: 'The main date is hidden. The shared page no longer carries it.'
- _wt-wp14/src/modules/timeline/app/audience/audience-manager.tsx — NEW owner control in each publication card: a 'The main date' block with a label field, a date field, Save, and a Hide the date button that appears only when a date is set. The caption states what hiding does - the date is removed from the page itself, not just from view, and the couple's own plan keeps it. When there is no main date the card says 'No main date on the shared page' rather than showing nothing.
- _wt-wp14/src/modules/timeline/server/couple-artifact-boundary.test.ts — Four E06.06 assertions, all green. The concealment test publishes with primaryDate 2027-08-14, deliberately a date carried by no milestone in the publication so that 'the string is gone' means the main date and nothing else, then conceals and ROTATES THE LINK so the per-request cache around resolveAudienceTimeline cannot serve the pre-concealment DTO. Asserts 'primaryDate' in dto is false and that neither the date nor the label survives anywhere in the payload.
- _wt-wp14/src/modules/timeline/server/audience-timeline.ts:599 — Existing, recorded not rebuilt: updateAudiencePublicationItem accepts a null calendarDate on an already published item, so per-milestone date concealment on a live page already worked. This is E06.06 criterion 1's mechanism and it is candidate evidence, not new work.
- _wt-wp14/src/app/s/[token]/page.tsx:23-36 — generateMetadata builds title, description and the OpenGraph card from dto.label and dto.audienceKind only - never primaryDate, never ownerDisplayLabel. Asserted by a source contract test with a positive assertion on the noindex posture so a widening is loud. R-024 is satisfied trivially here because no venue value appears in /s metadata at all.
- _wt-wp14/src/modules/timeline/server/db/timeline-schema.ts — The location refusal, verified across both schema files: no location column on workspaces, tasks, node_overlays, timeline_publications or timeline_publication_items. A location on a couple's page is text the couple typed into a title or a label.
- _wt-wp14/src/modules/timeline/app/audience/audience-manager.tsx:390-437 — EXISTS and is a REAL production caller, not a dead control. useActionState(updateAudiencePrimaryDateAction) at :189-190; the 'The main date' block at :390; Save form at :398 with hidden workspaceSlug + publicationId; the separate 'Hide the date' form at :425-431 carries intent=conceal and renders only when both primaryDate and primaryDateLabel are set; 'No main date on the shared page' at :383. Copy is on voice, no em dashes, no exclamation marks.
- _wt-wp14/src/app/robots.ts:29 — CHECKED for the 'noindex claimed but absent' failure mode. /p, /s, /share, /app, /redeem, /welcome, /api are all in the disallow list, and the file's own comment records the residual honestly (a Disallow blocks the fetch that would have read a per-page noindex, so URL-only listing survives; the X-Robots-Tag follow-on is named). This is E06.05's work, already in the tree, and it does not contradict E06.06.
- _wt-wp14/src/components/published/wedding-theme.tsx:125-133 — THE DEFECT. /p's wedding theme renders `t.due` for every published task via formatDue (:204). /p resolves through getPublishedWorkspaceBySlug - the Tasks workspace publish toggle - with no Timeline audience publication involved and no primaryDate concept. E06.06's concealment controls do not reach this surface, and D-033 ratified /p as where the couple's artifact stays.
- Specification: `studio/docs/execution/venue-edition-and-films/tasks/E06.06.md`

### `E06.07` Implement anonymous aggregate viewer counts without exposing individual viewer behaviour.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. No third-party analytics loads on /p, /s, /share or /embed, enforced at the component seam and at the Content-Security-Policy, and asserted by a source-text contract test registered in the pnpm test chain. A Playwright assertion cannot carry this: the e2e suite runs with VERCEL_ENV=preview and GoogleTag renders only when VERCEL_ENV === "production", so a browser assertion that GA is absent passes today and would keep passing if the exclusion were deleted.
2. The count is first-party and aggregate, with no IP, user-agent, referrer or raw token durably stored. The existing /s mechanism already satisfies this and is recorded as passing evidence rather than rebuilt.
3. A suppression rule lives in a library function with its own unit test: the threshold is a property of the value, not of the caller, and no formatter can build a displayed count from a raw integer.
4. Below the floor the surface says nothing about the data. It never renders "fewer than five" or any other bound, because a bound is itself a disclosure. Counts of 0, 1, 2, 3 and 4 produce byte-identical output.
5. lastQualifiedViewAt is removed from the surface at day precision and coarsened to a month, shown only above the same floor. artifact-studio.tsx no longer renders a count and a last-viewed timestamp side by side.
6. Every surface that renders the count goes through the same presenter, including the dense list row in audience-manager.tsx, so a list cannot become the leak the detail page closed.
7. The floor number is recorded as a founder decision and is explicitly not D-011's venue-cohort thresholds of 3 and 5.
8. If the couple's artifact is /p or /share, the absence of an equivalent first-party counter there is stated as not built, and share_link_visits.user_agent_hint is dealt with explicitly.

**Evidence**
- _wt-wp14/src/modules/timeline/lib/viewer-count.ts — New. presentViewerCount returns a discriminated union and never a number: below VIEWER_COUNT_FLOOR (5) it returns {state:"withheld", reason:"below_floor", note} with no count key and no timestamp key; at or above it returns a pre-rendered countLabel plus a month-only lastViewedLabel. Floor and count are both validated and throw on malformed input, so no caller can pass underneath the threshold.
- _wt-wp14/src/modules/timeline/lib/viewer-count.test.ts — 10 tests, registered in package.json's tsx Timeline segment. Asserts counts 0-4 serialise byte-identically, that the withheld note contains no digit and no bound phrasing, that a shown presentation carries no numeric property, and that 3 and 4 are withheld so a future edit adopting D-011's venue number goes red.
- _wt-wp14/src/modules/timeline/app/audience/artifact-studio.tsx — The defect D-033 names is gone. It previously rendered qualifiedViewCount.toLocaleString beside displayDate(lastQualifiedViewAt) formatted {day:"numeric",month:"short",year:"numeric"} — 'Timeline views 1 / Last viewed 3 Aug 2026'. Now one stat cell driven by presentViewerCount, with 'Last opened in August 2026' as a caption above the floor and nothing at all below it.
- _wt-wp14/src/modules/timeline/app/audience/audience-manager.tsx — The second render site, which would have been missed: line 353 rendered the raw count in a dense list row. Now calls viewerCountSummary, which returns null below the same floor and the row omits the clause entirely.
- _wt-wp14/src/lib/public-analytics-boundary.ts — New. isAnalyticsExcludedPath matches /p, /s, /share, /embed exactly or as a path prefix, so /pricing and /sitemap.xml are unaffected. Kept separate from isBareArtifactPath, which governs src/proxy.ts and answers a different question — folding them together would have silently changed /p's runtime.
- _wt-wp14/src/components/analytics/google-tag.tsx — The exclusion is evaluated BEFORE the `enabled` prop, so turning GA on in a new environment cannot turn it on for a couple's page. The contract test asserts that ordering, not just the presence of the check.
- _wt-wp14/next.config.ts — noThirdPartyAnalyticsCsp() emits an ENFORCED Content-Security-Policy for /p and /share, and a frame-ancestors * variant for /embed. Deliberately a partial policy (script-src, connect-src, three hardening directives, no default-src) so it can only restrict execution and connection, never rendering. It throws at config load if a Google host survives an edit.
- _wt-wp14/src/server/public-surface-analytics-contract.test.mjs — New, 8 tests, registered in the package.json .mjs segment. Asserts all three analytics layers plus the two no-raw-count invariants, on source text, with the reason stated in its own header.
- evidence/E06.07-aggregate-viewer-counts.md — Criterion-by-criterion evidence including two fail-before mutations and the concurrency note.
- _wt-wp14/src/modules/timeline/server/qualified-view-store.ts — Criterion 2 independently confirmed, not taken on trust. recordQualifiedViewWith writes only (publicationId, sessionHash, createdAt, expiresAt); the token is compared as a hash with a constant-time equality; expired receipts are deleted opportunistically; the aggregate is a counter increment on the publication row. No IP, user agent, referrer or raw token is durably stored. Correctly kept rather than rebuilt.
- _wt-wp14/src/server/db/schema.ts — Criterion 8 checked at the source. share_link_visits.user_agent_hint exists (schema.ts:791, described :779) and IS written on every /share open (src/server/db/queries.ts:591). Nothing renders it — grep returns only schema.ts and queries.ts, matching the evidence claim exactly. It is raised as Decision 3 in the task spec rather than silently left. /p has no counter of any kind. The absence is stated, not implied.
- Specification: `studio/docs/execution/venue-edition-and-films/tasks/E06.07.md`

### `E06.09` Design and implement the intentional vertical mobile Timeline.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. The rail flips its coordinate system below the breakpoint rather than rewrapping: horizontal geometry becomes vertical geometry, milestones position by block offset instead of inline offset, and the vertical completed rail replaces the horizontal one.
2. The Today marker rotates a quarter turn with it and its label moves out of the rail's path.
3. Collision avoidance is replaced by showing every label: labels hidden on a wide rail are shown on the vertical one.
4. The layout is chosen by container width, not viewport width, so the same artifact is correct inside the owner's panel and inside the phone preview. No viewport media query may decide layout.
5. The existing keyboard contract is unchanged: Left/Right between milestones, Home/End to jump, Enter or Space to select, Escape to close, with a roving tab index, aria-expanded, aria-controls and a polite live region.
6. prefers-reduced-motion: reduce disables the rail, label, point, metric and Today marker animation.
7. Zero critical and zero serious axe violations, and no horizontal document overflow, at 390, 768, 1280 and 1440.
8. Every assertion above runs in CI.

**Evidence**
- _wt-wp14/src/modules/timeline/components/artifact/timeline-artifact.module.css:967-1145 — The vertical tier, verified line for line against the audit's claim. progressGeometry becomes width:2px/height:auto (1053-1058); todayMarker becomes width:2rem/height:1px (1079-1085) against the base rule's width:1px/height:2rem (427-433); milestones position by inset-block-start (1104-1107); completedRail display:none and completedRailVertical display:block (1070-1077); hidden labels become opacity:1/pointer-events:auto (1121-1125).
- _wt-wp14/src/modules/timeline/components/artifact/timeline-artifact.module.css:12-13 — container-name: timeline-artifact; container-type: inline-size. The finding the audit did not record: layout is driven by CONTAINER width, which is why the same artifact is correct in the owner panel and the phone preview. The new test collects every @media (…width…) in the file and asserts the list is empty.
- _wt-wp14/src/modules/timeline/components/artifact/timeline-artifact-contract.test.mjs — Was NOT registered in package.json (grep -c returned 0) and was RED when run by hand: 7 pass, 2 fail. Now 13 pass, 0 fail, registered. Four new tests pin the breakpoint identity by reading the shipped CSS rather than a snapshot of it.
- studio/docs/execution/venue-edition-and-films/evidence/E06.09-E06.10-timeline-layouts.md — Criterion-by-criterion for both tasks, with the audit re-verification table and one correction to the audit's desktop line reference (three container tiers, not two).
- _wt-wp14/experience/browser-contract.json — The four viewport projects: mobile 390x844, tablet 768x1024, desktop 1280x900, wide 1440x960. Also records accessMode 'demo' and reducedMotion 'reduce', which bound what the evidence proves.
- _wt-wp14/src/modules/timeline/components/artifact/timeline-artifact.module.css — EXISTS, 1256 lines. Every line reference in the evidence doc resolves exactly. Vertical tier opens 967, closes 1145. progressGeometry width:2px/height:auto 1053-1058. completedRail display:none 1070-1072, completedRailVertical display:block;width:2px 1074-1077. todayMarker width:2rem/height:1px 1079-1085 against base width:1px/height:2rem at 431-432. milestone inset-block-start:var(--timeline-position) 1104-1107. data-labelled false and extra get opacity:1/pointer-events:auto 1121-1125. todayMar
- _wt-wp14/package.json:36 — Registration CONFIRMED in the test script, in the node --test .mjs segment. ci.yml:58 runs pnpm test with NO path filter, so the contract test runs on every PR. C8 is met for the contract test.
- _wt-wp14/.github/workflows/design-quality.yml:5-44 — DEFECT. The paths filter lists src/app, src/components, src/ds, src/lib, src/server, src/proxy.ts, src/env.ts, src/instrumentation*, src/preview, public, next.config, experience, package.json, pnpm-lock, scripts/ds, scripts/experience. It does NOT list src/modules. The artifact lives at src/modules/timeline/components/artifact/. A PR touching only that CSS never triggers pnpm experience:test (line 89), which is where the axe and overflow assertions live.
- _wt-wp14/experience/output/evidence-attestation.json — EXISTS (gitignored via .gitignore:16). Matches the claimed identifiers byte for byte: runId tasks-playwright-5501ae463b99182192baa7cc, rawArtifactSha256 670f570acf683405c5990991b688bce1f526860e4326f6e3fae887e822ee4617, projects mobile/tablet/desktop/wide 32 tests each, testCount 128, passedCount 128, unexpectedCount 0, startedAt 2026-08-03T21:19:09.930Z.
- _wt-wp14/experience/tests/critical-experiences.spec.ts:124-146,373-374 — Mechanism CONFIRMED. expectNoBlockingAccessibilityIssues filters axe to critical+serious and asserts toEqual([]); expectNoDocumentOverflow asserts scrollWidth-clientWidth <= 1. Both are called from auditCurrentSurface for every fixture case, and the config maps one Playwright project per viewport, so both run 4x. tasks.page.s-by-token has 2 cases; app-timeline and app-timeline-audience-by-publication-id 1 each.
- _wt-wp14/src/modules/timeline/components/artifact/timeline-artifact.tsx — Keyboard contract present and UNCHANGED by this package: ArrowLeft/ArrowRight/Home/End, Escape at :455, roving tabIndex at :573, aria-expanded :576, aria-controls :577, aria-live polite at :262/:301/:612. The only diff in this file is E06.04's detailNote copy rewrite (14 lines), which shifted these by 8 and made the evidence doc's line refs (:568-569, :604-607, :447-450) stale.
- _wt-wp14 git diff — Diff matches the claims. Only three files relevant to E06.09 changed: package.json (registration), timeline-artifact-contract.test.mjs (+82/-3), timeline-artifact.tsx (+14/-7, E06.04 copy). timeline-artifact.module.css diff is empty. No layout code was written; the deliverable is evidence and CI registration, which is what the task spec and the audit-first rule say.
- Specification: `studio/docs/execution/venue-edition-and-films/tasks/E06.09.md`

### `E06.10` Refine the desktop editorial Timeline shown in the Mara and Finn concept.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. The artifact has three container tiers and the widest one is what reveals collision-avoided labels.
2. The horizontal rail's affordances (horizontal scrolling with hidden scrollbars) belong only to the wide layout and never below the breakpoint.
3. The owner surfaces embed the exact same artifact rather than a variant, and the owner's app shell suppresses the artifact's own wordmark so two wordmarks never stack.
4. Reduced motion is honoured and the motion preference is hydration-safe: server and first client render choose the same subtree.
5. Zero critical and zero serious axe violations, and no horizontal document overflow, at 390, 768, 1280 and 1440.
6. The contract file asserting all of the above runs in CI.
7. Anything found that contradicts a ratified decision is reported with its file and line, whether or not it is in this task's scope to fix.

**Evidence**
- _wt-wp14/src/modules/timeline/components/artifact/timeline-artifact.module.css:622 — @container timeline-artifact (min-width: 980px) — the widest editorial tier, and the only place milestone[data-labelled="extra"] labels become visible. The mid tier is at 953 and the vertical tier at 967, so three tiers, not the two the audit's line reference implied.
- _wt-wp14/src/modules/timeline/app/plan/[projectSlug]/page.tsx:295 — <TimelineArtifact timeline={timeline} embedded showProductHeader={false} /> — a deliberate improvement that stops the artifact's wordmark stacking under the app shell's identity. A literal-string assertion in the contract test made this correct change look like a regression; the assertion now matches on the embedded prop regardless of order and additionally asserts showProductHeader={false} so the improvement is protected.
- _wt-wp14/src/modules/timeline/components/artifact/timeline-artifact.tsx:70 — The second stale assertion: doesNotMatch(/StudioBar/i) was firing on a JSDoc line reading 'the StudioBar already provides identity, so the owner view suppresses it rather than stacking two wordmarks' — the rule being honoured, described. The assertion now runs against a comment-stripped copy. Neither the component nor the CSS was changed.
- _wt-wp14/src/modules/timeline/components/artifact/timeline-artifact.tsx:279-281 — DEFECT, referred not fixed. A 'timeline' wordmark with an accent dot renders above the couple's names on the shared page, against D-011 point 2 ('One line in the footer. No logo, no badge, no powered by in the viewport'). The correct footer line already exists at :678-681. critical-experiences.spec.ts asserts the wordmark's exact text, so a passing test locks in what the ratified decision forbids.
- _wt-wp14/src/modules/timeline/components/artifact/timeline-artifact.tsx:320-324 — DEFECT, referred. detailNote returns 'This is the next point on the shared journey' and 'This milestone comes later on the shared journey'. Front-facing copy on a couple's wedding page, and 'journey' as a noun is on the banned list.
- _wt-wp14/src/modules/timeline/components/artifact/timeline-artifact.module.css:375 — EXISTS. '.stageViewport { ... overflow-x: auto; }' at :375-377 with scrollbar-width: none at :380 and ::-webkit-scrollbar at :384; overridden to 'overflow-x: hidden' at :1039-1041, inside the max-width:620px block that opens at 967 and closes before 1183. C2 MET as written.
- _wt-wp14/package.json — C6 MET, VERIFIED. 'git show HEAD:package.json | grep -c timeline-artifact-contract.test.mjs' returns 0; the working tree returns 1, in the 'node --test ... .mjs' segment. .github/workflows/ci.yml:58 runs 'pnpm test'. So the file went from never running in CI to running in CI. Also checked all eight new test files in this tree - provision-slug, viewer-count, couple-artifact-boundary, timeline-provisioning-contract, public-surface-analytics-contract, published-surface-revalidation, image/metadata,
- _wt-wp14/src/modules/timeline/components/artifact/timeline-artifact.tsx:56-60 — C4 MET. useArtifactReducedMotion returns 'hydrated && Boolean(prefersReducedMotion)'; useHydrated (src/lib/use-hydrated.ts) is useSyncExternalStore with a server snapshot of false, so SSR and first client render both compute reduceMotion=false. All 30 motion call sites read the single wrapper - the contract test counts useReducedMotion() occurrences and fails at two. No conditional subtree swap; only motion props change. Pre-existing code, newly pinned.
- _wt-wp14/experience/output/evidence-attestation.json — C5 MET, REPRODUCED. My own run: testCount 128, passedCount 128, unexpectedCount 0, projects mobile/tablet/desktop/wide at 32 each, runId tasks-playwright-5501ae463b99182192baa7cc, canonicalEvidenceSha256 5501ae46...4a775 - the SAME run id the build agent reported, so the canonical evidence hash reproduced byte-for-byte on an independent run. browser-contract.json projects are 390x844, 768x1024, 1280x900, 1440x960. Every routeCase runs auditCurrentSurface (spec :373-374), which calls expectNoDocu
- _wt-wp14/src/app/app/timeline/audience/[publicationId]/page.tsx:13 — NEW FINDING, not in the evidence. 'await requireAppAccess()' - the allowlist-only gate. Same at src/app/app/timeline/[projectSlug]/page.tsx:11, timeline/page.tsx:13, timeline/audience/page.tsx:14, notes/page.tsx:21, signal/page.tsx:12, signal/onboarding/page.tsx:8, signal/settings/notifications/page.tsx:11, and components/app/app-access-gate.tsx:22. See R-WP14-01.
- _wt-wp14/src/app/p/[slug]/page.tsx:21-58 — NEW FINDING, contradicts D-033. generateMetadata returns title, description, openGraph and twitter - and no 'robots' key. grep for noindex / 'index: false' / X-Robots-Tag across src and next.config.ts shows /embed, /s and /share all set robots index:false; /p sets nothing, and no header rule covers it. See R-WP14-02.
- Specification: `studio/docs/execution/venue-edition-and-films/tasks/E06.10.md`

### `E06.11` Implement the post-wedding read-only Keepsake state and the agreed downloadable export.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. The term is max(redemption + 548 days, wedding date + 90 days), never a flat eighteen months, and the R-015 case (books far ahead, redeems immediately) provably keeps access past the wedding.
2. The result names which half of the max() decided it, and no couple-facing sentence claims a wedding-derived date unless the wedding half won.
3. A postponement extends access automatically; a wedding date moved earlier never shortens a term already granted.
4. A couple is not 'after the day' on the morning of their wedding: the wedding date is a day, not an instant.
5. The term ending produces a read-only Keepsake, not a lockout. Export, unpublish and delete survive it; every other write intent does not.
6. The export opens with no network access of any kind, asserted as a property of the document rather than as a list of strings.
7. The export escapes the couple's own words: a supplier called Rónán & Co and a task titled 'Order the cake <topper>' cannot break the file, and accented names survive intact.
8. Attribution is the venue's name only, once, in the footer. No logo, no badge, no venue-written message. No venue means no attribution line at all.
9. No phase of the couple-facing wording, and no line of the export, says forever, for life, in perpetuity, a flat eighteen months, guarantee, GDPR or compliant, and none carries an em dash or an exclamation mark.
10. Every criterion is covered by a test registered in package.json's test script.
11. What is not built is stated as not built.

**Evidence**
- _wt-wp14/src/lib/keepsake/lifecycle.ts — PLANNING_DAYS_FROM_REDEMPTION 548 and GRACE_DAYS_AFTER_WEDDING 90 as named constants, endsAt = max of the two, a 'basis' discriminator naming which half won, a three-phase model, a twelve-intent read-only write policy with three stated exceptions, and every couple-facing sentence about the term in one function so one test can police all of them.
- _wt-wp14/src/lib/keepsake/export.ts — buildKeepsakeExport returns keepsake.html (one self-contained document, inline CSS, no script/link/img/iframe/src/absolute-URL/@import/url()) and keepsake.json. findExternalReferences is exported so the no-network property is asserted on the produced document, not on a remembered list.
- _wt-wp14/src/lib/keepsake/keepsake.test.ts — 16 tests. The R-015 scenario is driven concretely: books 2027-03-10, marries 2028-09-16, redeems the day they sign; the term lands on 2028-12-15T23:59:59.999Z, strictly later than redemption + 548 days. Under a flat term this couple loses the product on top of their wedding.
- studio/docs/execution/venue-edition-and-films/evidence/E06.11-keepsake.md — Includes the pre-task grep proving Keepsake did not exist (two matches, both comments saying it does not exist), the criterion-by-criterion record, and section 3 on the ds:check failure and how it was resolved without touching the grandfather manifest.
- _wt-wp14/src/ds/tokens.css:48-67 — The export's palette is the actual light-mode token values materialised as rgb() with each one named in a comment, rather than the beige guess it started as. ds:check went from '2 drift failures' to 'clean — no drift' without adding the first entry to a founder-owned exception manifest.
- _wt-wp14/package.json:36 — CONFIRMED REGISTERED. `src/lib/keepsake/keepsake.test.ts` appears in the `test` script's third `node --import tsx --test` group. NOT the unregistered-test-file failure mode. I additionally confirmed it EXECUTES inside the chain, not just that the path is present — see the verification field.
- evidence/E06.11-keepsake.md — EXISTS, 314 lines, read in full. §0 pre-task grep verified by me independently: `grep -rni keepsake src/ drizzle/ scripts/` returns only billing.tsx:101-102 (two comments saying it does not exist) plus a robots.ts comment this branch added. §3's ds:check narrative verified. §C11 is unusually candid and names the zero-caller gap itself. ONE INACCURACY FOUND: §C9 overstates the export test's banned-word coverage by four terms.
- _wt-wp14 (git) — Branch claude/wp14-timeline, HEAD 88e8c5a on top of 5717b75 (WP-08 E05) as stated. 36 working-tree entries; the three keepsake files are untracked additions and package.json is the only diff attributable to this package (test-script registration). No GENERATED file touched — templates.generated.ts is absent from the diff; only the source src/lib/templates.ts is modified, by other tasks in the branch. Tree left exactly as found (36 entries before and after); both probe scripts removed.
- _wt-wp14/src/components/app/settings/sections/billing.tsx:109 — THE COUNTER-EVIDENCE. The only couple-facing sentence about the access term that actually ships lives here, not in keepsakeCopy, and no test file references it. This is what breaks the 'one function, one test' claim.
- _wt-wp14/src/components/redeem/redeem-result-card.tsx:89 — COUNTER-EVIDENCE for C1 at product level: renders 'until {expiresLabel}' from the minted expiresAt, which is still the flat 548-day value while R-015 is open.
- Specification: `studio/docs/execution/venue-edition-and-films/tasks/E06.11.md`

## Not ready — cannot be approved

| Task | Title | Missing |
|---|---|---|
| `E06.01` | Define the public artifact content model and what can never be published from the private workspace. | status is "blocked", not founder_review |
| `E06.08` | Define restrained venue attribution and Signal Studio attribution across shared artifacts. | status is "done", not founder_review |
| `E06.12` | Complete unpublish, delete, broken-link, performance, accessibility, social-preview and cross-browser QA. | status is "blocked", not founder_review |

## Your decision

Approve all of the above in one command:

```bash
node studio/docs/execution/venue-edition-and-films/tools/project-control.mjs approve-batch "your approval note" E06.02 E06.03 E06.04 E06.05 E06.06 E06.07 E06.09 E06.10 E06.11
```

Or push back on any single one:

```bash
node studio/docs/execution/venue-edition-and-films/tools/project-control.mjs reject E06.02 "what is wrong"
```


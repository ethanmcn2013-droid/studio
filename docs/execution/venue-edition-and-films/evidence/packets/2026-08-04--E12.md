<!-- honest-state-banner -->
> ## Read this before approving
>
> The heading below says "ready to approve". That is the tool's way of saying every task
> has acceptance criteria and evidence recorded. **It does not mean the work is complete.**
>
> **4 of 4 tasks in this packet are PARTIAL.** Every one was
> independently re-verified by an adversarial agent whose default was to refute, and the
> verifier's read overrides the build agent's self-assessment wherever they disagreed. Where
> a task was claimed complete and the verifier disagreed, it is recorded below as partial.
>
> | Task | Verified state | Criteria | **Not met** | Evidence |
> |---|---|---|---|---|
> | `E12.10` | **PARTIAL** | 14 | 2 | 12 |
> | `E12.11` | **PARTIAL** | 13 | 1 | 19 |
> | `E12.12` | **PARTIAL** | 14 | 1 | 14 |
> | `E12.13` | **PARTIAL** | 18 | 1 | 13 |
>
> Approving a partial task is a legitimate choice — it means you accept the gap as recorded
> and want the task closed anyway. It should be a decision, not an accident. The unmet
> criteria for each task are in its own section, and the reasoning is in the session record.

# Founder review packet — E12

Generated 2026-08-04T08:23:03.038Z from PROJECT_STATE.json. 14 task(s).

**4 ready to approve · 10 not ready.**

Approving sets these tasks to Done and is the only thing that can. Nothing here is Done yet.

## Ready to approve

### `E12.10` Produce the detailed Venue Edition sales deck.

Executor: claude_code · in review since 2026-08-03

**Acceptance criteria**
1. The deck exists as a rendered artefact at a gated route, not a document describing one, and prints one slide per A4 landscape page
2. No ratified sentence is restated; every price, term and promise is imported from src/lib/venue-invitation/copy.ts
3. Every price statement carries 'including VAT at the prevailing rate', proved over rendered prose rather than source
4. Every slide traces to at least one ratified decision, and the trace is visible on the slide
5. Price appears after the privacy boundary, the launch-scope sentence and the couple's access term, enforced by test
6. The launch-scope sentence appears verbatim, as its own slide
7. The survival sentence appears unprompted
8. A slide states what is not built, naming the Keepsake, adoption figures and the absence of a venue login
9. No prohibited claim, banned programme term, em dash or exclamation mark, in the filled deck or the unfilled one
10. The deck ends on exactly one ask and it is the walkthrough
11. No numeric entitlement, no seat language, no places-left figure
12. An unfilled deck says so rather than looking prepared
13. Registered in the HQ room registry and the experience registry, adding no new experience:validate failure
14. The test is registered in package.json so CI runs it

**Evidence**
- _wt-wp16/src/lib/venue-sales-deck.ts — Twenty slides as data. Imports COMMERCIAL_SUMMARY_ROWS, COUPLE_TERM_SENTENCE, ENTITLEMENT_SENTENCE, FOUNDING_MEANING, OFFER_PARAGRAPH, PRICE_FORMS, PRIVACY_BLOCK, SCALE_SENTENCE, STANDARD_RATE_SENTENCE, SURVIVAL_SENTENCE, TEAM_ASK_SENTENCE from venue-invitation/copy.ts. No second copy of any price.
- _wt-wp16/src/lib/venue-sales-deck.test.ts — 21 assertions, all pass. Includes the ladder ordering test (price index > what-you-never-see, how-long and at-launch), the VAT test over rendered prose, and the sweep run against both the filled and the unfilled deck.
- _wt-wp16/src/app/hq/venue-deck/page.tsx — Gated route, ?venue= fills the one slot, robots noindex on its own account. Present in the build route table as dynamic.
- public/brand/collateral/venue/venue-deck-screen.pdf via /hq/venue-kit:20 — AUDIT FINDING. The only pre-existing venue deck. Its own caption reads 'the demo → €1,500 → ... → the pilot ask'. Wrong on the founding price, the retired programme term and an ask the ratified offer does not contain. Recorded as defect C-3, not deleted.
- evidence/E12.10-E12.13-verification-run.txt sections 1, 4, 9 — pnpm exec tsx --test src/lib/venue-sales-deck.test.ts: 21 tests, 21 pass, exit 0. pnpm run build: exit 0 with /hq/venue-deck in the table (after two exit-1 attempts caused by a concurrent session holding the next build lock, recorded rather than re-run quietly).
- _wt-wp16/src/components/venue/venue-sales-deck.css — EXISTS. @media print carries @page { size: A4 landscape; margin: 12mm } and .venue-deck-slide { break-after: page; break-inside: avoid }, with :last-child exempt. The rule is correct as written. It has never been printed or measured, so the print half of criterion 1 is asserted, not proved.
- _wt-wp16/public/brand/collateral/venue/venue-deck-screen.pdf — EXISTS, 210,525 bytes. I read the linking caption myself at src/app/hq/venue-kit/page.tsx:20. It currently reads 'Cover -> the 19,898 problem -> four views -> the couple -> the team -> the demo -> €1,500 -> the Founding 25 -> the standard -> the pilot ask.' The audit finding is real and is recorded as defect C-3. See risksFound for what the fix left behind.
- deck probe run in C:/Users/ethan/signal-studio-workspace/_wt-wp16 (tsx over the built deck) — MY OWN MEASUREMENT, not the builder's. slides: 20. em-dash present: false. exclamation present: false. price lines: 4, and all four carry 'including VAT at the prevailing rate' verbatim (€1,000 founding x2, €1,500 standard x2). bare 18-month lines: 0 — the access term always travels as '18 months from when they open it, or 3 months past the wedding, whichever is later'. Criteria 3 and 9 CONFIRMED.
- _wt-wp16/scripts/check-venue-copy.mjs — EXISTS, registered in the package.json 'test' chain. I ran it: exit 0, '16 rules, 92 patterns, 29 surfaces swept'. I then enumerated its 29 surfaces from prohibited-claims.v1.json: NOT ONE of them is a WP-16 artefact. venue-sales-deck.ts, venue-sales-deck.tsx and hq/venue-deck/page.tsx are all outside it. See risksFound R1.
- _wt-wp16/src/lib/hq/rooms.ts:158 and experience/registry.json:6014 — Both registrations present and read directly. I re-ran pnpm exec tsx --test src/lib/hq/rooms.test.ts: 6 pass, exit 0. I re-ran pnpm run experience:validate -- --product=studio: exit 1 with 8 failures. I checked all 8 by name — two __design-lab/delight routes, hq-venue-proposal, v/[token], v/[token] not-found, venues-privacy, venues-questions, venues-what-you-see. None is a WP-16 route. Criterion 13 CONFIRMED: this work adds zero failures.
- _wt-wp16/package.json — git diff confirms 'src/lib/venue-sales-deck.test.ts' was added to the 'test' script, and 'node scripts/check-venue-copy.mjs' added to the chain. I ran pnpm test end to end: exit 0. Criterion 14 CONFIRMED.
- app/src (Keepsake and export claims, verified against the app repo) — Slide 18 says the Keepsake is not built, adoption figures are not built, and there is no venue login. I checked each. grep -rni keepsake app/src returns exactly one hit, a code comment in timeline-artifact.tsx:800 using the ordinary English word — so there is genuinely no Keepsake mode, route or component. app/src/app/api/account/export/route.ts EXISTS, so the deck's 'what exists today is an account-level export' is true. R-043 independently confirms there is no venue-authenticated route. Criter
- Specification: `tasks/E12.10.md`

### `E12.11` Redesign the Founding Venue certificate as 01/25 through 25/25 with the €1,000 founding rate.

Executor: claude_code · in review since 2026-08-03

**Acceptance criteria**
1. A certificate cannot be produced without a cleared payment date, and the refusal says a number is assigned on cleared payment and never on signature
2. A certificate cannot be produced for a venue not on the founding plan
3. A certificate cannot be produced without a valid number in 01 to 25, refused in both directions
4. No refused state renders a blank number, a number shape, or any field a number could be written into
5. The number renders zero-padded as NN/25
6. The record block carries the date the payment cleared and no signature date
7. The rate never appears without the VAT wording, and the lock condition and change-of-control rule appear in the same block
8. No permanence word appears anywhere on the artefact
9. The certificate states that it is not the agreement and does not grant access on its own
10. No prohibited claim, retired programme term, em dash or exclamation mark, on the certificate or in any refusal reason
11. Nothing implies review, accreditation, approval, verification or an award
12. Registered in both registries, adding no new experience:validate failure
13. The test is registered in package.json so CI runs it

**Evidence**
- _wt-wp16/src/lib/founding-certificate.ts — buildFoundingCertificate returns {state:'refused', reason} for empty input, missing cleared-payment date, non-founding plan, and any number outside 01-25. It delegates to foundingNumberRefusal in venue-founding-number.ts (E02.13) rather than restating the rules. There is no code path producing a blank number.
- _wt-wp16/src/lib/founding-certificate.test.ts — 19 assertions, all pass. Includes 'there is no code path that renders a blank number' (asserts the refused prose contains neither '__' nor '/25'), a nine-word permanence ban, and the sweep run over every refusal reason as well as the issued certificate.
- src/app/hq/partner-card/page.tsx (defect C-1) — AUDIT FINDING, recorded not fixed. The existing physical exploration states 'the number is still written by hand, in front of the venue, at signing' and 'the number field stays blank until a real venue signs'. Both are assignment at signature, which D-009.6 forbids. A concurrent WP-16 stream corrected that page's programme wording on the same day; the assignment-event defect is a different thing and is untouched by that sweep.
- _wt-wp16/src/components/venue/founding-certificate-sheet.tsx — Renders the refusal with a visual treatment rather than an empty frame, because a blank number field is the failure mode this task exists to remove.
- evidence/E12.10-E12.13-verification-run.txt sections 1, 9 — pnpm exec tsx --test src/lib/founding-certificate.test.ts: 19 tests, 19 pass, exit 0.
- _wt-wp16/src/app/hq/partner-card/page.tsx — DEFECT C-1 CONFIRMED AND STILL PRESENT. Line 82: 'the number field stays blank until a real venue signs.' Line 84: 'The number is still written by hand, in front of the venue, at signing.' Line 25: 'The number on the front is still written by hand at signing.' All three are assignment at signature, which D-009.6 forbids. The evidence entry's account is exactly accurate: the concurrent stream DID correct the programme wording (title is now 'The Founding 25 Card', eyebrow 'Signal HQ · The Founding
- VERIFIER RE-RUN: pnpm exec tsx --test src/lib/founding-certificate.test.ts — 19 tests, 19 pass, 0 fail, exit 0. Claim confirmed exactly.
- VERIFIER RE-RUN: pnpm run typecheck — tsc --noEmit, exit 0. Claim confirmed.
- VERIFIER RE-RUN: pnpm test — exit 0. Full suite green. I confirmed by reading the diff of package.json that `src/lib/founding-certificate.test.ts` and `node scripts/check-venue-copy.mjs` are both registered inside the `test` script, so CI runs them. Criterion 13 confirmed.
- VERIFIER RE-RUN: pnpm run build — exit 0. I then read .next/server/app-paths-manifest.json directly: "/hq/founding-certificate/page" is present. Note the build leaves a stale .next/lock that blocks the next build with 'Another next build process is already running'; I removed it. The evidence file already records the same lock trap at its section on attempts 1-3.
- VERIFIER RE-RUN: pnpm run experience:validate -- --product=studio — exit 1, 8 failures. I read all eight: two __design-lab/delight routes, hq-venue-proposal, v/[token], v/[token] not-found, venues-privacy, venues-questions, venues-what-you-see. `studio.page.hq-founding-certificate` is NOT among them. The route IS registered — src/lib/hq/rooms.ts:192-193 and experience/registry.json:4801-4806. Criterion 12 confirmed: both registries, zero new failures.
- VERIFIER RE-RUN: pnpm run lint — exit 1. ONE error: src/app/hq/account-review/account-review.tsx:86 react-hooks/set-state-in-effect. NOT attributable to E12.11 — that file is unmodified in git status, last touched by commit 90908f3, and `git diff 16df762 -- src/app/hq/account-review/` is empty, so it is inherited from the stacked WP-13 base. The evidence document discloses this failure honestly rather than omitting it.
- VERIFIER BANNED-TERM SWEEP over E12.11's touched files — I wrote and ran my own sweep (19 rules: partner, member, investor, exclusive, guaranteed, certified, for life, forever, in perpetuity, lifetime, countdown, plus/ex VAT, seat count, allotment, GDPR compliant, solicitor, accountant). 10 hits total across founding-certificate.ts, .test.ts, the sheet, the CSS, the route and venue-copy-refusals.ts. EVERY ONE is the ban list existing rather than a use: three in the ban-list definition comment, three in the regex array itself, three quoting the permane
- VERIFIER BANNED-TERM SWEEP over the whole of public/brand and src/app — 148 raw hits, NONE introduced by E12.11. 'founding partner' now survives in ZERO venue-facing HTML or TSX — the market-entry deck and src/app/design/page.tsx are clean, which is R-042's two headline surfaces cleared. Surviving occurrences are CHANGELOG.md, docs/content-truth-audit.md, the RAID entry, the prohibited-claims JSON defining the ban, and the test asserting absence. The remaining src/app 'partner' hits are unrelated (school design-partner pilot, partnership conversations, a 'Northline 
- VERIFIER PLANTED-VIOLATION TEST 1 — the standing repository check — I appended `The founding partner variant, locked in for life, guaranteed.` to public/brand/market-entry-deck-2026.html (pinned at 0) and ran `node scripts/check-venue-copy.mjs`. IT FAILED, exit 1, naming all three: [p1-permanence] "for life", [p1-permanence] "guaranteed", [d033-programme-terms] "partner". I restored the file and re-ran: exit 0. The check is real, it catches the retired term, and it is registered in `pnpm test` which is a blocking CI step.
- VERIFIER PLANTED-VIOLATION TEST 2 — the certificate's own test — I replaced the lock-condition line in TERMS_LINES with `Your founding partner rate is locked in for life, guaranteed forever.` and re-ran the suite. IT FAILED, exit 1, 4 of 19 tests red: the VAT/lock test, the permanence test, the sweep test, and the Founding-25-naming test. I restored the file and re-ran: 19/19, exit 0. Confirmed: this suite does not pass on a planted violation.
- RATIFIED-CLAIM CHECK against the certificate's actual strings — Both prices VAT-INCLUSIVE and in one string: 'The founding rate is €1,000 a year, prepaid, including VAT at the prevailing rate. The standard rate is €1,500 a year on the same basis.' Lock on CONTINUOUS RENEWAL: 'holds for as long as the agreement renews continuously without lapse.' Change of control present in the same block. 01/25 on CLEARED PAYMENT: the statement reads 'assigned when the first payment cleared', the record label is 'Payment cleared', the refusal reads 'never on signature'. NO 
- UNBUILT-CAPABILITY CHECK on E12.11's artefact — The certificate promises nothing the product does not have. It makes exactly one claim of fact (a place was assigned when payment cleared), states the rate with its conditions, and says outright 'This certificate records an event. It is not the agreement, and it does not grant access on its own.' The number is backed by real code, not a document: `pnpm test` ran assignFoundingNumber's 38 assertions green, including 'refuses before the payment clears', 'refuses a venue that is not on the founding
- GIT DIFF CHECK — The diff matches the claims. E12.11 ships five new untracked files (founding-certificate.ts, .test.ts, founding-certificate-sheet.tsx, founding-certificate.css, app/hq/founding-certificate/page.tsx) plus registry edits in src/lib/hq/rooms.ts and experience/registry.json and the test registration in package.json. Nothing outside the declared column. C:/Users/ethan/signal-studio-workspace/studio was not touched by me and no project-control.mjs write command was run. My two planted violations were 
- Specification: `tasks/E12.11.md`

### `E12.12` Produce the pre-booking venue sales kit for brochures, proposals, websites and coordinator conversations.

Executor: claude_code · in review since 2026-08-03

**Acceptance criteria**
1. Five channels exist covering brochure, website, written proposal, show-around conversation and wedding fair, each with three lengths
2. Every channel states its moment of use and the one rule that makes it different, and carries a never-list of at least three items
3. No couple-facing block carries a figure, a currency, a value word, 'free', 'complimentary' or 'sponsored'
4. Every channel states what it costs the couple, using the permitted phrasing
5. The privacy sentence appears in the short and full block of every channel, unchanged from the copy pack
6. No couple-facing block claims a Keepsake, a one-click export, a download or a read-only mode
7. The one after-the-wedding claim is true today and makes no claim about the end of the term
8. Nothing states or implies a venue can see, receive or check a couple's plan
9. Nothing implies a venue's logo, colour or own words reach a couple
10. The team notes carry the launch-scope sentence verbatim
11. The kit points at the one prohibited-claims list rather than reprinting it
12. No prohibited claim, retired term, em dash or exclamation mark in any block; the never-lists are still held to the retired term and to punctuation
13. Registered in both registries, adding no new experience:validate failure
14. The test is registered in package.json so CI runs it

**Evidence**
- _wt-wp16/src/lib/venue-prebooking-kit.ts — Five channels, three lengths each, a per-channel never-list, KIT_TEAM_NOTES carrying the launch-scope sentence verbatim, and MARKETING_UNDERTAKING as R-020's mitigation in plain words. PROHIBITED_SHEET points at E09.11-venue-copy-pack.md section 4 and prohibited-claims.v1.json rather than reprinting either.
- _wt-wp16/src/lib/venue-prebooking-kit.test.ts — 16 assertions, all pass. The couple-facing sweep refuses currency, 'free', 'complimentary', 'worth', 'sponsored', 'discount', and separately refuses 'export', 'download', 'read-only', 'keepsake' and 'archive'.
- evidence/E09.11-venue-copy-pack.md section 1, third sentence (defect C-2) — AUDIT FINDING, recorded not fixed. The approved pack tells a venue to say today: 'After the wedding it stays open, read-only, and they can download everything in one click.' E09.11 section 7 sets the rule that forbids it, and none of it is built. This is R-020's exact shape: an approved sentence, repeated by a venue, folding into the venue's own contract with the couple. Replacement supplied and used in this kit.
- _wt-wp16/src/lib/venue-copy-refusals.ts — The refusal set once, 49 absolute expressions plus the conditional P7 bare-term rule, shared by all four artefacts. Counted at runtime, not by eye. The neverHere lists are excluded structurally rather than by a citation heuristic, because a heuristic of that kind once suppressed two real defects on the live venues page (E09.11 section 12.5).
- evidence/E12.10-E12.13-verification-run.txt sections 1, 9 — pnpm exec tsx --test src/lib/venue-prebooking-kit.test.ts: 16 tests, 16 pass, exit 0.
- BANNED-TERM SWEEP I ran myself over public/brand and src/app (all .html/.tsx/.ts/.txt/.md/.json/.css) — 'founding partner': ZERO hits. D-033's retirement holds across both trees for the compound term. Residual single-word hits are all other contexts and none is a Founding 25 programme term: partner 19 (school design-partner pilot, motion brief, lender-pack cohorts, 'Partnership conversations' on /contact, internal HQ route names), investor 7 (all internal HQ, meaning actual lenders), seat 20 (includes the known pre-existing venues/page.tsx:469 'No seats', pinned defect owned by E12.04), 'ex VAT' 1
- _wt-wp16/public/brand/collateral/social/alt-text.txt lines 26-33 — ONE SURVIVING OCCURRENCE, disclosed not hidden, and NOT E12.12's. The four s4-founding-sp01 PNGs were renamed from s4-partner-sp01 but their PIXELS still read 'Founding Limerick Partner' and 'Glen House' rather than Glenmara House. The file says so and says 'Do not publish S.4 until the images are re-authored.' Belongs to the concurrent WP-16 R-042 stream, not this task. It does not refute E12.12, which never claimed the repository-wide sweep.
- _wt-wp16/scripts/check-venue-copy.mjs and package.json — REGISTERED AND IT WORKS. package.json 'test' runs it fifth in the chain, and .github/workflows/ci.yml:53 runs 'pnpm test'. I ran it: exit 0, 16 rules, 92 patterns, 29 surfaces, 30 pinned hits, 21 citations, 6 exceptions. I then PLANTED '> Your founding partner place is guaranteed forever.' into the enforced surface E09.11-venue-copy-pack.md: exit 1, 3 hits reported (p1-permanence forever, p1-permanence guaranteed, d033-programme-terms partner), pinned at 0, owner named. Restored; re-run exit 0 w
- MY RE-RUNS: pnpm exec tsx --test src/lib/venue-prebooking-kit.test.ts -> 16/16, exit 0. pnpm run typecheck -> exit 0. pnpm test -> exit 0 (38 tests in the final suite, 0 fail). pnpm run build -> exit 0, 'Compiled successfully in 14.7s', 51/51 static pages, and the route table carries 'f /hq/venue-prebooking-kit'. pnpm run experience:validate -- --product=studio -> exit 1, 8 failures. — Every claimed command reproduces exactly. The 8 experience failures are: two __design-lab/delight routes, hq-venue-proposal, v/[token], v/[token] not-found, venues-privacy, venues-questions (all unregistered discoveries from the stacked WP-13 base) and venues-what-you-see (changed coverage). NONE names venue-prebooking-kit. E12.12 adds no new failure.
- pnpm run lint -> EXIT 1 (I ran it; the claimed verification omitted it, the evidence document does not) — One error: src/app/hq/account-review/account-review.tsx:86 react-hooks/set-state-in-effect. That file is not in git status and is untouched by WP-16, so the failure is pre-existing on the stacked branch. The evidence document section 7.2 states this correctly and notes CI runs lint as informational and non-blocking. Reported because it is red.
- _wt-wp16/experience/registry.json and src/lib/hq/rooms.ts:175-176 — Registered in BOTH. registry.json carries studio.page.hq-venue-prebooking-kit -> /hq/venue-prebooking-kit -> studio/src/app/hq/venue-prebooking-kit/page.tsx. rooms.ts carries slug venue-prebooking-kit, group 'sell', kind 'artifact', lifecycle 'active'.
- THE KEEPSAKE CHECK (the prompt's highest-value item): git -C app grep -n -i -E 'keepsake|one-click export' -- src — CONFIRMS THE FINDING. Only two hits in the whole of app/src, both CSS/JSX comments in timeline-artifact about a visual 'keepsake' metaphor. There is no Keepsake mode, route, state or export. The kit correctly claims none of it: I verified venuePrebookingCoupleFacingProse() contains no 'export', 'download', 'read-only', 'keepsake', 'archive' or 'one-click'. The companion welcome kit (E12.13, src/lib/couple-welcome-kit.ts) records the same verification in its header at lines 22-31 and its ONBOARDI
- THE CRITERION-5 MEASUREMENT: I ran a throwaway probe importing the module and testing block.text.includes(PREBOOKING_PRIVACY_SENTENCE) per block, then deleted it — brochure short TRUE, brochure full TRUE, website short FALSE, website full FALSE, proposal short FALSE, proposal full FALSE, show-around short TRUE, show-around full TRUE, wedding-fair short FALSE, wedding-fair full FALSE. Website and proposal reword the connector; wedding-fair omits 'We can see that you opened it' altogether. The task spec at tasks/E12.12.md criterion 5 says 'unchanged from the copy pack', and its Inputs section says the pack is 'the source of the privacy sentence used unchange
- git status and git diff in _wt-wp16 — THE DIFF MATCHES THE CLAIMS. package.json adds check-venue-copy.mjs to the test chain, adds the four new test files including venue-prebooking-kit.test.ts, and adds a check:venue-copy script. registry.json adds four new route entries plus ten R-042 re-baselines belonging to the concurrent stream, exactly as section 7.1 of the evidence document discloses. The E12.12 artefacts are untracked new files. WORKING TREE LEFT AS FOUND: 50 status entries before and after, kit SHA256 identical to backup, c
- Specification: `tasks/E12.12.md`

### `E12.13` Produce the post-booking couple welcome kit, approved email wording and printable welcome object.

Executor: claude_code · in review since 2026-08-03

**Acceptance criteria**
1. The kit produces a print-ready welcome card, email wording and an onboarding sequence from one module, with four slots and an explicit missing-slot state
2. No couple-facing string carries a figure, currency, value word, 'free', 'complimentary', 'sponsored' or 'gift'
3. The only cost statement to a couple is that there is nothing to pay
4. The card names the venue exactly once, as 'Compliments of <venue>', matching the product's own first-open string
5. The email names the venue exactly once, in the sign-off
6. Nothing carries a venue logo, colour, mark or venue-written message
7. No couple-facing string promises a Keepsake, an export, a download, an archive or a read-only mode
8. The one after-the-wedding claim is true today and does not describe the end of the term
9. Both the card and the email carry the privacy sentence
10. Nothing suggests the venue can be shown the plan through a setting
11. The email carries no tracked link, image or button, and one subject
12. Every onboarding step names an actor, a state of built, by hand or not built, and the file or route the claim came from
13. The sequence marks issuing and adoption evidence as done by hand, and states there is no login for the venue's team
14. The last step is marked not built and its evidence names the Keepsake
15. The editing rules forbid a coordinator adding a term, a value or a leaflet
16. No prohibited claim, retired term, em dash or exclamation mark, in the filled kit, the unfilled kit, or the venue-facing half
17. Registered in both registries, adding no new experience:validate failure
18. The test is registered in package.json so CI runs it

**Evidence**
- _wt-wp16/src/lib/couple-welcome-kit.ts — Card, email and an eight-step sequence from one module, four slots, explicit missing-slot reporting. The card's attribution is 'Compliments of <venue>', which is the exact string the product already shows on first open, so paper and screen agree.
- _wt-wp16/src/lib/couple-welcome-kit.test.ts — 23 assertions, all pass. Includes a venue-name-appears-exactly-once test on the card and again on the email, a price sweep over all couple-facing prose, and refusals for keepsake/export/download/read-only/archive.
- KEEPSAKE VERIFICATION, and a correction to my own first reading — grep -rn -i keepsake app/src returns TWO hits, not zero as my first grep in this session reported. Both are code comments in modules/timeline/components/artifact/timeline-artifact.tsx:800 and its stylesheet at :1526, using 'keepsake' as an ordinary English word for a printed timeline. Neither is the Venue Edition Keepsake. Two narrower checks confirm the substantive claim: no Keepsake mode/state/route/component exists, and grep for readOnlyReason|isReadOnly|expiredWorkspace|workspaceExpired retu
- app/src/server/actions/planning.ts:945 recomputeSponsoredAccessTermQuietly — CORRECTS A NATURAL ASSUMPTION. D-022 point 1 says capture the wedding date at redemption. The code does not: the recompute fires on completing the planning onboarding inside the product. The behaviour is right because the rule only moves access later, but a kit that told a venue the date is asked for at the code page would describe a flow that does not exist. Step 6 of the sequence says so.
- evidence/E12.10-E12.13-verification-run.txt sections 1, 7, 8, 9 — pnpm exec tsx --test src/lib/couple-welcome-kit.test.ts: 23 tests, 23 pass, exit 0. Sections 7 and 8 carry the Keepsake greps and the wedding-date trace verbatim.
- PLANTED-VIOLATION TEST — the check is real, not decorative — I edited couple-welcome-kit.ts to replace the after-the-wedding line with 'It is yours forever, and your founding partner venue guaranteed it.' The suite went to pass 20 / fail 3, exit 1, failing on the couple sweep, the unfilled-kit sweep and the after-the-wedding test. File restored byte-for-byte from backup; re-run returned 23/23 pass. I separately planted 'A founding partner card, guaranteed for life.' into public/brand/collateral/social/alt-text.txt and `node scripts/check-venue-copy.mjs` e
- _wt-wp16/package.json:13 — CONFIRMED registered in CI. The `test` script diff adds both `node scripts/check-venue-copy.mjs` and `src/lib/couple-welcome-kit.test.ts`. I ran `pnpm test` end to end: exit 0.
- app/src/components/welcome/venue-welcome-card.tsx:66 — INDEPENDENTLY CONFIRMS THE HARDEST CLAIM. The product's own first-open string is `Compliments of {sponsorName}`. The printed card and the screen say the same four words. This was the one claim most likely to be asserted rather than checked, and it holds.
- KEEPSAKE — I re-ran the greps rather than trusting the correction — `grep -rn -i keepsake app/src` returns exactly two hits, both code comments in the Timeline print renderer (timeline-artifact.tsx:800, timeline-artifact.module.css:1526) using 'keepsake' as ordinary English. `find app/src -iname '*keepsake*'` returns nothing. `grep -rnE 'readOnlyReason|isReadOnly|expiredWorkspace|workspaceExpired' app/src` returns nothing. The only export route is app/src/app/api/account/export/route.ts. The evidence document records its own first wrong reading rather than repla
- app/src/server/actions/planning.ts:945 and app/src/server/db/sponsored-access-term.ts — EXIST. recomputeSponsoredAccessTermQuietly is imported at :58 and called at :945 on completing planning onboarding, not at redemption — the kit's step 6 describes it correctly. I went one step further than the evidence did: planning.ts:875 refuses a wedding_season period without a primaryDate, so the built path always has a date, which is what makes the card's 'It does not switch off the day after the wedding' true rather than conditional.
- evidence/E12.10-E12.13-verification-run.txt — EXISTS in the canonical VEF evidence directory (not in the worktree). Sections 1, 7, 8 and 9 read verbatim. Every number in it matches what I got on my own re-run, including the 8-failure experience:validate baseline and the build that went red twice on a concurrent next-build lock before passing.
- _wt-wp16/src/lib/hq/rooms.ts and experience/registry.json:4088 — BOTH REGISTRIES CONFIRMED. rooms.ts gains a `couple-welcome-kit` room; registry.json carries `studio.page.hq-couple-welcome-kit` with route /hq/couple-welcome-kit. `pnpm run experience:validate -- --product=studio` returns 8 failures and none of them names any of the four new WP-16 routes — all 8 are on committed, pre-existing sources (two __design-lab routes, hq-venue-proposal, v/[token], venues/privacy, venues/questions, venues/what-you-see).
- _wt-wp16/src/components/venue/venue-kit-print.css — DEFECT. The sheet heading and the module comment both say the card is 'A5, one side'. The print stylesheet sets `@page { size: A4 }` for the whole document, and the A5 heading itself is `print:hidden`. A venue printing from the browser gets the card alone on an A4 sheet. Small, but it is the one place the artefact does not do what it says.
- Specification: `tasks/E12.13.md`

## Not ready — cannot be approved

| Task | Title | Missing |
|---|---|---|
| `E12.01` | Build the public Venue Edition landing page around the sponsored couple experience. | status is "done", not founder_review |
| `E12.02` | Build the private Founding 25 invitation page. | status is "done", not founder_review |
| `E12.03` | Build a parameterised private proposal page for each venue. | status is "done", not founder_review |
| `E12.04` | Present the €1,500 standard price, €1,000 founding rate, founding conditions and annual prepayment clearly. | status is "done", not founder_review |
| `E12.05` | Add the branded couple-experience preview. | status is "done", not founder_review |
| `E12.06` | Add the Venue Portal trust-and-renewal preview. | status is "done", not founder_review |
| `E12.07` | Add the privacy explanation covering exactly what the venue sees and never sees. | status is "done", not founder_review |
| `E12.08` | Add the complete commercial, product, support, entitlement, Keepsake and renewal FAQ. | status is "done", not founder_review |
| `E12.09` | Produce the concise one-page commercial proposal. | status is "done", not founder_review |
| `E12.14` | Complete analytics, conversion, responsive, accessibility, performance, copy and visual QA across every commercial page. | status is "done", not founder_review |

## Your decision

Approve all of the above in one command:

```bash
node studio/docs/execution/venue-edition-and-films/tools/project-control.mjs approve-batch "your approval note" E12.10 E12.11 E12.12 E12.13
```

Or push back on any single one:

```bash
node studio/docs/execution/venue-edition-and-films/tools/project-control.mjs reject E12.10 "what is wrong"
```


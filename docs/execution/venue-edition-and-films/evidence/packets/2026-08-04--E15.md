<!-- honest-state-banner -->
> ## Read this before approving
>
> The heading below says "ready to approve". That is the tool's way of saying every task
> has acceptance criteria and evidence recorded. **It does not mean the work is complete.**
>
> **21 of 21 tasks in this packet are PARTIAL.** Every one was
> independently re-verified by an adversarial agent whose default was to refute, and the
> verifier's read overrides the build agent's self-assessment wherever they disagreed. Where
> a task was claimed complete and the verifier disagreed, it is recorded below as partial.
>
> | Task | Verified state | Criteria | **Not met** | Evidence |
> |---|---|---|---|---|
> | `E02.04` | **PARTIAL** | 8 | 0 | 11 |
> | `E02.06` | **PARTIAL** | 8 | 0 | 10 |
> | `E02.07` | **PARTIAL** | 9 | 3 | 12 |
> | `E15.01` | **PARTIAL** | 7 | 2 | 14 |
> | `E15.02` | **BLOCKED** | 9 | 8 | 9 |
> | `E15.03` | **BLOCKED** | 10 | 10 | 6 |
> | `E15.04` | **PARTIAL** | 9 | 8 | 12 |
> | `E15.05` | **PARTIAL** | 9 | 7 | 15 |
> | `E15.06` | **DOCUMENTED-ONLY** | 9 | 8 | 9 |
> | `E15.07` | **DOCUMENTED-ONLY** | 8 | 8 | 9 |
> | `E15.08` | **PARTIAL** | 7 | 6 | 8 |
> | `E15.09` | **DOCUMENTED-ONLY** | 7 | 5 | 10 |
> | `E15.10` | **DOCUMENTED-ONLY** | 7 | 5 | 10 |
> | `E15.11` | **DOCUMENTED-ONLY** | 7 | 7 | 15 |
> | `E15.12` | **DOCUMENTED-ONLY** | 7 | 7 | 12 |
> | `E15.13` | **DOCUMENTED-ONLY** | 7 | 5 | 8 |
> | `E15.14` | **DOCUMENTED-ONLY** | 8 | 4 | 6 |
> | `E15.15` | **PARTIAL** | 7 | 3 | 7 |
> | `E15.16` | **BLOCKED** | 8 | 7 | 6 |
> | `E15.17` | **DOCUMENTED-ONLY** | 8 | 8 | 9 |
> | `E15.18` | **DOCUMENTED-ONLY** | 7 | 6 | 12 |
>
> Approving a partial task is a legitimate choice — it means you accept the gap as recorded
> and want the task closed anyway. It should be a decision, not an accident. The unmet
> criteria for each task are in its own section, and the reasoning is in the session record.

# Founder review packet — E15

Generated 2026-08-04T11:42:45.907Z from PROJECT_STATE.json. 18 task(s).

**14 ready to approve · 4 not ready.**

Approving sets these tasks to Done and is the only thing that can. Nothing here is Done yet.

## Ready to approve

### `E15.01` Run the formal launch go/no-go review against commercial, legal, product, data, creative and sales gates.

Executor: founder · critical path · in review since 2026-08-04

**Acceptance criteria**
1. Every one of the 75 gate exit criteria carries a verdict of met, not met, or waived, with a repository path or re-runnable command behind every 'met'
2. The review explicitly names R-031, R-043, the absence of a Keepsake product surface, the unexercised Blob store, and the fact that no renewal falls due before 1 September
3. The review distinguishes what is true in the integration branches from what is true on deployed production, and states which the decision is taken against
4. Every gate marked passed has a founder waiver per unmet criterion, each naming the exposure accepted
5. The decision is recorded as go, no-go, or conditional-go, dated, with conditions listed and owned
6. No line states or implies legal approval, solicitor review, accountant verification or unqualified GDPR compliance (D-016)
7. The film position is stated honestly against the creative gate given E13 is 4/18 and E14 is 1/18

**Evidence**
- studio/docs/execution/venue-edition-and-films/evidence/E15.01-launch-go-no-go.md — Executable go/no-go script with the 2026-08-04 run recorded: gate ledger, both test suites, the production comparison, and the five must-name items
- studio/docs/execution/venue-edition-and-films/evidence/E15-release-readiness-assessment.md — Consolidated assessment across all eighteen tasks
- studio/docs/execution/venue-edition-and-films/PROJECT_STATE.json — RE-RAN read-only. EXACT MATCH: 6 gates all not_started, criteria 13/12/12/12/13/13 = 75, met=0, all passedAt=-, legal blocker CR-001, milestones M1-M6 open.
- _wt-integrate-studio @ 64b3b91 — RE-RAN `pnpm test`. EXACT MATCH: exit 0, 70+616+56 = 742 tests, 0 fail, and the final line reproduced verbatim: '[venue-copy] ok — 16 rules, 92 patterns, 29 surfaces swept (0 cross-repo, not checked here), 30 pinned hit(s), 21 citation(s), 6 recorded exception(s).'
- _wt-integrate @ a78f3c3 — RE-RAN `pnpm test`. EXACT MATCH: exit 0, eleven batches summing to 1,263 tests, 0 fail. All twelve named assertions present and passing, including '/p is disallowed in robots.txt as well as noindexed' and 'gives three months past the wedding when that is later'.
- https://app.signalstudio.ie/robots.txt — RE-RAN curl. CONFIRMED: HTTP 200, /p is NOT disallowed in production. Sitemap line points at the legacy origin tasks.signalstudio.ie. The R-031 finding is real and undeployed.
- https://signalstudio.ie/venues — RE-RAN curl. EXACT MATCH to the byte: HTTP 200, 172,524 bytes, 'Founding 25' x8, '€1,000' x8, '€1,500' x2, 'twenty-five' x5. Zero banned terms (no 'founding partner', 'for life', 'forever', 'in perpetuity'). I-002's 'prepared but not deployed' claim is correctly identified as stale.
- _wt-wp22/src/lib/account/live/project-venue-access.ts:330 — RE-RAN grep. EXACT MATCH: '330: members: [],'. R-043 correctly characterised.
- _wt-integrate/src/lib/keepsake/ — RE-RAN grep for resolveCoupleAccess|buildKeepsakeExport outside lib/keepsake. CONFIRMED zero production callers — every hit is the definition or keepsake.test.ts. The Keepsake absence is correctly named.
- _wt-integrate/src/server/db/schema.ts + src/server/db/couple-access-term.ts:122 + src/server/actions/comp.ts:326 — RE-RAN grep. CONFIRMED and this is the document's best finding: wedding_date/weddingDate absent from schema.ts entirely; primaryDate at line 308; weddingDateMsForWorkspace has exactly one real production caller (comp.ts:326). So the migrated production column entitlements.wedding_date is written by nothing and read by nothing.
- _wt-integrate/src/app/p/[slug]/page.tsx:49-56 and src/server/db/schema.ts — RE-RAN grep. CONFIRMED the R-031 opt-in genuinely does not exist: no optIn/allowIndex/searchOptIn column in schema, and the source comment matches E15.01's quote verbatim ('Until that opt-in exists, the default is the whole behaviour.').
- studio/content/hq/operator-todos/apply-venue-edition-terms-migration.md — REFUTES E15.01 Step 6. Canonical studio tree at HEAD 1fc7a3d (2026-08-04) has this COMMITTED as 'status: open — REOPENED. Verified by direct query 2026-08-03 — the first pass is live, the second is not. founding_number and founding_number_assigned_at are absent from entitlements-prod.' E15.01 read the superseded Wave-1 copy (d17f27f) inside its own worktree.
- _wt-integrate-studio/src/lib/account/instrumentation/{daily-metrics.ts,retention.ts,suppression.ts} — REFUTES E15.01's data-gate row. presentRate now has real production callers (daily-metrics.ts:238,241; retention.ts:96,99; fixtures.ts:38) and metricRateLabel is deleted with a test asserting it 'must not exist'. R-028 is CLOSED in branch, not open.
- https://signalstudio.ie/venues/{questions,what-you-see,privacy,proposal} — RE-RAN curl. All four return 404 in production while /venues returns 200. E15.01's Step 4 production comparison does not test these at all. (Separately: the companion assessment's claim that 'every page it links to is a 404' is overstated — the live page references only /venues/opengraph-image, none of the four.)
- Specification: `studio/docs/execution/venue-edition-and-films/tasks/E15.01.md`

### `E15.04` Complete live Venue Portal, Usage, PDF, CSV and renewal-report tests.

Executor: founder · in review since 2026-08-04

**Acceptance criteria**
1. A venue with fewer than 3 eligible sponsored workspaces gets no behavioural count and no surface reveals the population size
2. A behavioural count is withheld when the count is below 3 or when the complement is below 3; 39 of 40 is withheld for the same reason as 1 of 40
3. No surface renders 'fewer than 3' or any equivalent; that phrasing is itself the disclosure
4. Every percentage shown to a venue comes from the rate value type and is withheld below 5 eligible activations; no formatter constructs a percentage from two loose numbers
5. The CSV opens in a spreadsheet, carries the same withheld states as the screen, and contains no couple name, email, workspace slug or identifier
6. The report download opens and its content type, extension and every venue-facing description use the same word for what it is
7. members is empty on the live venue snapshot and no surface offers a venue an invite-a-member action that does nothing
8. The renewal worklist runs without error and correctly reports that no venue has a live term
9. The delivery model is written down and matches the venue pack: Signal HQ produces and sends, the venue does not log in, and the HQ password is never given to a venue

**Evidence**
- studio/docs/execution/venue-edition-and-films/evidence/E15.04-venue-portal-and-reports-test.md — Nine-step script written against Option C (Signal HQ administers, venue receives evidence) because no venue login exists; steps 1-4 run
- studio/docs/execution/venue-edition-and-films/evidence/E07.16-venue-identity-lab.md — The four venue-identity options and their costs; Option C is the recommended launch position
- studio/docs/execution/venue-edition-and-films/evidence/R-027-R-028-suppression-fix.md — Existing suppression-fix evidence
- _wt-wp22/src/lib/account/instrumentation/suppression.ts:61-64 — RE-RAN grep myself. isSmallCell is two-sided exactly as claimed: `if (value < BEHAVIOURAL_MIN_WORKSPACES) return true; return population - value < BEHAVIOURAL_MIN_WORKSPACES;`. Claim CONFIRMED.
- _wt-wp22/src/lib/account/instrumentation/suppression.ts:112-126 — presentRate applies ONLY `denominator < RATE_MIN_WORKSPACES` (5). It never calls isSmallCell. This is the defect Step 3 did not test.
- _wt-wp22/src/lib/account/instrumentation/daily-metrics.ts:238,241 — RE-RAN grep. presentRate is on the production path as claimed. Claim CONFIRMED. But continuedAfter30Days returns presentRate(returned, sealed.length) with no small-cell guard on either side.
- _wt-wp22/src/lib/account/format.ts:48,70 — RE-RAN grep. metricRateLabel survives only as a comment — claim CONFIRMED. But formatRateAccessibleLabel at :70 publishes `${rate.numerator} of ${rate.denominator}` and is live at components/metric.tsx:50.
- _wt-wp22/src/lib/account/csv.ts:20-32,96-97 — Column set is account_name, edition, period_start, period_end, coverage_state, data_through, metric_key, value_state, value, denominator, unit, definition_version, withheld_reason — no couple name, email or slug. But the rate branch writes value=<numerator>, denominator=<population> unguarded.
- _wt-wp22/src/lib/account/live/project-venue-access.ts:330 — RE-RAN grep: `330: members: [],` unconditional. Claim CONFIRMED. R-043 stands.
- _wt-wp22/src/app/hq/account-review/download/route.ts:23-61 — Read in full. Live path accepts csv and html only (404 otherwise); report branch returns Content-Type text/html; charset=utf-8 with a .html filename via snapshotToReportHtml. Claim CONFIRMED. pdfFilename IS used, but only on the fixture/sample path at :82-84 (application/pdf) — the claim said 'never emitted on the live path' and that wording is accurate.
- _wt-wp22/src/app/hq/account-review/panels/account-panel.tsx:10-23,77-90 — 'Invite member' button still present, onClick sets a local notice string only; SUPPORT_HISTORY still a hardcoded two-row array. Criterion 7's second clause fails.
- _wt-wp22/src/app/hq/account-review/account-review.tsx:209 and panels/reports-panel.tsx:40,44 — CAPABILITY CHECK PASSED. `<option value="live">Live access (HQ)</option>` exists, and reports-panel builds exactly the two download URLs Steps 6 and 7 give. Steps 5-8 target surfaces that exist.
- Specification: `studio/docs/execution/venue-edition-and-films/tasks/E15.04.md`

### `E15.05` Complete live shared Timeline, viewer-count, unpublish, export and Keepsake tests.

Executor: founder · in review since 2026-08-04

**Acceptance criteria**
1. A published wedding workspace is served noindex, nofollow and /p is disallowed in the deployed robots.txt, verified against production
2. A token-bound share is unreachable once revoked, on the next request, with no cached render surviving
3. Unpublishing removes the workspace from every route that can render it including /p and /embed, visible without waiting for a revalidation window
4. The viewer count counts qualified views only and cannot be inflated by the couple's own loads or by a crawler
5. An export downloads, opens, and is readable without Signal Studio
6. The Keepsake position is stated honestly in one paragraph matching what the product does today, which is that the library exists and the couple-facing state does not
7. No Keepsake copy uses 'forever', 'for life', 'in perpetuity' or any storage guarantee
8. /embed/[slug] behaviour is decided and recorded; today it renders a published workspace with no token on any third-party site
9. Third-party analytics on couple-facing surfaces is absent or consent-gated, and the privacy page says whatever is true

**Evidence**
- studio/docs/execution/venue-edition-and-films/evidence/E15.05-shared-timeline-and-keepsake-test.md — Eight-step script; step 1 PASS, step 2 FAIL against production, Keepsake recorded as having zero production callers
- studio/docs/execution/venue-edition-and-films/evidence/E06.11-keepsake.md — Existing Keepsake design evidence
- _wt-integrate @ a78f3c3 · pnpm test — RE-RAN BY ME. Exit 0. Summed across the sub-suites: tests 1263, pass 1263, fail 0. Matches the claim exactly.
- _wt-integrate · grep -rn 'resolveCoupleAccess/|buildKeepsakeExport' src/ | grep -v lib/keepsake — RE-RAN BY ME. No output, exit 1. Keepsake genuinely has zero production callers. Full-tree grep -rni keepsake returns only robots.ts, billing.tsx, and the three lib/keepsake files.
- curl -s https://app.signalstudio.ie/robots.txt — RE-RAN BY ME. Disallow list is /app /s /share /redeem /welcome /api. /p is NOT present. The step-2 production FAIL is real and correctly reported.
- _wt-integrate/src/app/robots.ts — Branch DOES disallow /p, with a comment recording the residual URL-only-listing gap. Undeployed, as the evidence says.
- _wt-integrate/src/app/p/published-wedding-noindex.test.mjs — All four named assertions pass. BUT the file states in its own header: 'Asserted against source text on purpose... It does not prove Next emits the header.' These are regexes over page.tsx and robots.ts, not behavioural checks.
- _wt-integrate/src/lib/public-analytics-boundary.ts — CONTRADICTS THE EVIDENCE. /p, /s, /share, /embed are excluded from GA4 'entirely and unconditionally' under R-032, decided as Option A in D-033. This file exists on the exact commit the evidence was written against.
- _wt-integrate/src/components/analytics/google-tag.tsx — isAnalyticsExcludedPath(pathname) returns null BEFORE the enabled check. The comment the evidence quotes says 'No consent gate on the marketing surfaces yet' and is immediately followed by 'The couple-facing public routes are excluded unconditionally and are not waiting on a consent gate.' The evidence read the first half and missed the second.
- _wt-integrate/src/server/public-surface-analytics-contract.test.mjs — Passing, and inside the 1,263-test run the evidence itself cites as green. Pins the GA exclusion on all four couple-facing routes.
- _wt-integrate/src/modules/timeline/lib/viewer-count.ts:34 — VIEWER_COUNT_FLOOR = 5. presentViewerCount returns {state:'withheld'} with a note and no number below the floor. artifact-studio.tsx:25 calls it with no floor override. A passing test asserts 'no owner surface renders a raw qualified view count'.
- _wt-integrate/src/app/api/account/export/route.ts — An account export DOES exist and IS wired: authed GET returning JSON with Content-Disposition attachment, reached from a 'Download export' link at src/components/app/settings/sections/privacy.tsx:55. Distinct from buildKeepsakeExport, which has no caller.
- _wt-integrate/src/app/embed/[slug]/page.tsx — Confirms R-033 as the evidence describes: tokenless, resolves through getPublishedWorkspaceBySlug, force-dynamic (so unpublish reaches it on the next request), statically index:false.
- _wt-integrate/src/app/privacy/page.tsx:151-160 — NOT ADDRESSED BY THE EVIDENCE AT ALL. The Cookies section states 'never a third-party tracker. No advertising cookies.' while GoogleTag loads googletagmanager.com in production on every non-excluded route. Criterion 9's second half is unexamined and, on its face, false.
- _wt-integrate/src/components/app/settings/sections/billing.tsx:101-104 — Confirms the evidence's claim of a stale comment: it asserts 'grep -rni keepsake src returns nothing', which is no longer true.
- Specification: `studio/docs/execution/venue-edition-and-films/tasks/E15.05.md`

### `E15.06` Confirm that contracts, privacy notices, analytics, support channels, monitoring and incident procedures are live.

Executor: founder · in review since 2026-08-04

**Acceptance criteria**
1. Every venue-facing and couple-facing route in the commercial and legal set returns 200 in production; any 404 is named with an owner and a date
2. The privacy notice a couple reaches describes the analytics that actually run on the surface they are on
3. The support address is live, monitored, and carries a stated response time the founder can meet alone
4. Monitoring is confirmed by an error deliberately raised and observed arriving, not by the presence of a DSN
5. The health endpoint returns 200 and something checks it on a schedule with an owner for a failure
6. The incident procedure is rehearsed once end to end and the rehearsal records what broke
7. The rollback procedure is rehearsed once against a real deployment
8. No document, page or reply states or implies legal approval, solicitor review, accountant verification or unqualified GDPR compliance
9. The venue agreement carries a marketing-controls clause and a prohibited-claims list, and the venue pack contains the approved copy pack (R-020)
10. Every venue-facing and couple-facing route in the commercial and legal set returns 200 in production; any 404 is named with an owner and a date
11. The privacy notice a couple reaches describes the analytics that actually run on the surface they are on
12. The support address is live, monitored, and carries a stated response time the founder can meet alone
13. Monitoring is confirmed by an error deliberately raised and observed arriving, not by the presence of a DSN
14. The health endpoint returns 200 and something checks it on a schedule with an owner for a failure
15. The incident procedure is rehearsed once end to end and the rehearsal records what broke
16. The rollback procedure is rehearsed once against a real deployment
17. No document, page or reply states or implies legal approval, solicitor review, accountant verification or unqualified GDPR compliance
18. The venue agreement carries a marketing-controls clause and a prohibited-claims list, and the venue pack contains the approved copy pack (R-020)

**Evidence**
- studio/docs/execution/venue-edition-and-films/evidence/E15.06-operational-readiness-confirmation.md — Nine-step script with the production HTTP sweep and its four failures recorded
- studio/docs/execution/venue-edition-and-films/evidence/E08.12-incident-response-runbook.md — The runbook that exists and has never been exercised
- studio/docs/execution/venue-edition-and-films/tasks/E15.06.md — Task spec exists.
- studio/docs/execution/venue-edition-and-films/evidence/E03.02-annual-agreement-and-order-form.md — EXISTS (50648 bytes, 2026-08-04 12:10 — SEVENTEEN MINUTES AFTER E15.06 was written). Carries clause 12 marketing controls, clause 12.2 approved copy pack, clause 12.3 prohibited-claims list. E15.06 step 8's basis ('E03.02 is open ... has not produced one') is stale, not false at time of writing.
- _wt-wp22/src/lib/venue-prebooking-kit.ts:269-285 — The venue pack references evidence/copy/prohibited-claims.v1.json rather than restating it, deliberately. Criterion 9 is now substantially testable; E15.06 never tested it.
- _wt-wp22/src/app/venues/ — Contains couple-preview, demo, page.tsx, privacy, questions, what-you-see. It does NOT contain proposal. `find src/app -type d -name proposal` returns nothing.
- _wt-wp22/src/app/venues/page.tsx:24 — The only /venues/* link the venues page emits is withTracking('/venues/demo', ...). It links to none of the 404ing routes.
- app/src/lib/product-urls.ts:67 — CONTACT_EMAIL = 'hello@signalstudio.ie'. The claim cites 'src/lib/product-urls.ts:66'; line 66 of the studio worktree copy is TIMELINE_PUBLIC_ORIGIN and that file contains no email at all. Substance true, citation wrong in both repo and line.
- app/src/instrumentation-client.ts:14 — Sentry init is guarded by `if (process.env.NEXT_PUBLIC_SENTRY_DSN && !isBearerTimeline)` and the file's own header says 'No-ops when DSN is unset.' Presence of the file proves nothing about production.
- studio/docs/execution/venue-edition-and-films/evidence/E15.06-operational-readiness-confirmation.md — Nine-step script with the production HTTP sweep and its four failures recorded
- studio/docs/execution/venue-edition-and-films/evidence/E08.12-incident-response-runbook.md — The runbook that exists and has never been exercised
- studio/docs/execution/venue-edition-and-films/tasks/E15.06.md — Task spec exists.
- studio/docs/execution/venue-edition-and-films/evidence/E03.02-annual-agreement-and-order-form.md — EXISTS (50648 bytes, 2026-08-04 12:10 — SEVENTEEN MINUTES AFTER E15.06 was written). Carries clause 12 marketing controls, clause 12.2 approved copy pack, clause 12.3 prohibited-claims list. E15.06 step 8's basis ('E03.02 is open ... has not produced one') is stale, not false at time of writing.
- _wt-wp22/src/lib/venue-prebooking-kit.ts:269-285 — The venue pack references evidence/copy/prohibited-claims.v1.json rather than restating it, deliberately. Criterion 9 is now substantially testable; E15.06 never tested it.
- _wt-wp22/src/app/venues/ — Contains couple-preview, demo, page.tsx, privacy, questions, what-you-see. It does NOT contain proposal. `find src/app -type d -name proposal` returns nothing.
- _wt-wp22/src/app/venues/page.tsx:24 — The only /venues/* link the venues page emits is withTracking('/venues/demo', ...). It links to none of the 404ing routes.
- app/src/lib/product-urls.ts:67 — CONTACT_EMAIL = 'hello@signalstudio.ie'. The claim cites 'src/lib/product-urls.ts:66'; line 66 of the studio worktree copy is TIMELINE_PUBLIC_ORIGIN and that file contains no email at all. Substance true, citation wrong in both repo and line.
- app/src/instrumentation-client.ts:14 — Sentry init is guarded by `if (process.env.NEXT_PUBLIC_SENTRY_DSN && !isBearerTimeline)` and the file's own header says 'No-ops when DSN is unset.' Presence of the file proves nothing about production.
- Specification: `studio/docs/execution/venue-edition-and-films/tasks/E15.06.md`

### `E15.08` Monitor product, email, landing-page, booking, film and portal performance during the first release window.

Executor: founder · in review since 2026-08-04

**Acceptance criteria**
1. Six surfaces each have a named check, a frequency, a threshold and an action on failure
2. The health endpoint is checked on a schedule and a failure reaches a person
3. Email delivery is measured by delivery and reply, never by opens; 'opened' means a first-party load of the redemption page (D-026 R4)
4. Landing and proposal pages are checked for HTTP status daily during the window
5. Error monitoring is confirmed live by an event that arrived, not by a DSN
6. Every measurement of couple-facing behaviour respects the suppression rules; no venue-attributable count is produced below the floors
7. The close-out names what broke; a window with nothing recorded is a window nobody watched

**Evidence**
- studio/docs/execution/venue-edition-and-films/evidence/E15.08-release-window-monitoring.md — The six-row watch sheet plus three automatable checks, two of which were run
- studio/docs/execution/venue-edition-and-films/tasks/E15.08.md — EXISTS. Spec written 2026-08-04. Deliverables 2 (daily records) and 3 (close-out) are unticked, consistent with the evidence.
- studio/docs/execution/venue-edition-and-films/evidence/E09.04-outreach-measurement.md — EXISTS. Cited as an input. Its section 10 is the refutation of watch-sheet row 5: 'Nothing in this document is instrumented. No route, no table, no emitter, no test.'
- studio/docs/execution/venue-edition-and-films/evidence/E08.08-E08.09-observability-and-continuity.md — EXISTS. Cited as an input; not relied on by any claim I tested.
- app/src/app/api/health/digest/route.ts — THE MOST SERIOUS FINDING. Source comment: 'Returns 200 always so the status-page probe reads "OK".' Body returns {ok:true, pipeline:'daily-digest', configured:{cronSecret, resend, database}} from three Boolean(process.env...) reads. No liveness check, no query, no failure path.
- app/vercel.json — One cron only: {path:'/api/cron/digest?send=1', schedule:'0 9 * * *'}. No health poller exists, so 'checked on a schedule' has no implementation.
- app/src/app/redeem/[code]/page.tsx — The redemption page. No instrumentation of any kind; unauthenticated visitors are redirect()ed to /sign-up before render. The D-026 R4 'opened' fact has no producer.
- _wt-wp22/src/lib/account/instrumentation/suppression.ts — CLEARS criterion 6 in the branch: isSmallCell (lines 61-64) implements the two-sided rule (value < floor OR population - value < floor) and presentBehavioural (73-81) applies it. R-027's defect is fixed here. E15.08's statement is consistent with the code, though it cites no code.
- Specification: `studio/docs/execution/venue-edition-and-films/tasks/E15.08.md`

### `E15.09` Complete signed-venue handoff from sales into onboarding.

Executor: founder · in review since 2026-08-04

**Acceptance criteria**
1. A handoff record exists per signed venue with signature date, cleared-payment date, plan, founding number if assigned, named contacts and annual wedding count
2. Anything promised outside the standard offer is written verbatim and either honoured or corrected with the venue before configuration
3. The venue is told, unprompted, the survival sentence from D-020 point 2
4. The release rule from D-020 point 3 is stated, including venue name and branding coming off within 24 hours
5. The annual wedding count is recorded with the exact D-020 point 4 framing and used only to set the fair-use ceiling
6. Nothing in the handoff creates a commitment that is not in the agreement
7. The handoff is rehearsed once end to end before the first real venue (R-024)

**Evidence**
- studio/docs/execution/venue-edition-and-films/evidence/E15.09-signed-venue-handoff.md — The one-page handoff template, the four sentences said out loud, the verbatim column, and the R-024 rehearsal script
- studio/docs/execution/venue-edition-and-films/tasks/E15.09.md — EXISTS. Status: backlog. Executor: founder. Founder sign-off block empty, all four boxes unticked. Deliverable 3 is 'A named list of anything promised in the sale that the product cannot do' — the evidence delivers a list of two, and both omissions below are in scope of it.
- app/src/modules/timeline/components/artifact/timeline-artifact.tsx, app/src/modules/timeline/components/artifact/timeline-artifact.module.css — THE KEEPSAKE DOES NOT EXIST. Re-ran the brief's check myself: case-insensitive search for 'keepsake' across app/src returns 2 occurrences in 2 files, and both are prose comments about a printed timeline page ('the keepsake keeps its content'). Zero product implementation — no read-only mode, no export, no state. Yet E15.09 section 3 sentence 2 scripts the founder to say, unprompted, at the table: 'Any workspace already redeemed keeps its full term plus Keepsake, whatever happens to the licence.'
- studio/src/lib/venue-lifecycle.ts — The only Keepsake implementation anywhere is a pure state-machine model in the STUDIO repo (states 'active'/'keepsake', sponsorship 'released', brandingRemovalDeadlineMs at :249). Verified its importers: venue-lifecycle.test.ts, venue-billing.ts and venue-billing.test.ts, plus one doc comment in src/app/venues/privacy/page.tsx. It is a specification library on the marketing/HQ side. It is not wired to the app where couples' workspaces actually live.
- studio/src/lib/account/live/project-venue-access.ts:330 — R-043 RE-VERIFIED BY ME. 'members: []' sits inside the returned live-snapshot object literal with no conditional around it. The risk register cites :326; in this worktree it is :330 (harmless drift). The document's own claim on this point is correct — there is no venue-authenticated route. The problem is that section 3 sentence 3 then tells the venue 'you can release the workspace'.
- studio/scripts/mark-venue-paid.ts:129, :87-91, :100-108 — THE CLEARED-PAYMENT DATE HAS NOWHERE TO GO. Read the arg parser directly: it accepts --vat-rate, --note and a no-op --founding, and fail()s on every other -- flag. paidAtMs = Date.now() at :129. Its own usage text says 'Run on cleared payment, not on signature' — which only works if the operator runs it the same day. E15.09 makes the cleared-payment date a first-class handoff field and a stop rule, and never names this command or this constraint.
- studio/content/hq/operator-todos/apply-venue-edition-terms-migration.md — CHECKED IN THE VENUE'S FAVOUR — status: done. sponsors.founding_number and founding_number_assigned_at plus the unique index are live on production entitlements as of 2026-08-03. So the template's 'read from the database, never from memory' is NOT a phantom capability. This one survives scrutiny.
- studio/docs/execution/venue-edition-and-films/evidence/E15.09-signed-venue-handoff.md (D-016 sweep) — CLEAN on D-016. Searched and read the full text: no statement or implication of solicitor review, accountant verification, legal approval or GDPR compliance. Section 6's stop rule correctly points at the R-020 marketing-controls clause without claiming anyone reviewed it. No finding.
- studio/docs/execution/venue-edition-and-films/evidence/E15.09-signed-venue-handoff.md (D-021 / VAT sweep) — CLEAN on the VAT check. The document states no price at all, so it cannot have been written to the retired VAT-exclusive rule. Separately confirmed mark-venue-paid.ts:89,102-103 states both prices as 'inclusive of VAT at the prevailing rate', matching D-021. No finding.
- studio/docs/execution/venue-edition-and-films/evidence/E15.09-signed-venue-handoff.md §3.1 and §3.4 — D-020 point 1 gap. Sentence 1 says 'No seat count, no per-couple maths' and sentence 4, spoken moments later, says 'This sets your issuance ceiling'. The doc never carries D-020 point 1 — fair use notifies, never blocks. Searched the file for 'notifies', 'never block', 'fair use': zero matches. A venue that hears 'unlimited' then 'ceiling' will ask what happens at the ceiling, and the script has no answer.
- Specification: `studio/docs/execution/venue-edition-and-films/tasks/E15.09.md`

### `E15.10` Collect venue branding, configure the account, invite venue members and approve the couple experience.

Executor: founder · in review since 2026-08-04

**Acceptance criteria**
1. The venue record carries the correct plan and a fair-use ceiling computed from the annual wedding count, and no seat count is presented to the venue anywhere
2. The venue's name renders in a real couple workspace exactly once, as one quiet line, with no logo and no venue-written message
3. The couple preview shown to the venue is the real render, not a mockup, and the approval is recorded in writing with a date
4. 'Invite venue members' is explained in the terms that are true: Signal HQ issues invitations, the venue does not log in, and no surface offers a member invitation that does nothing
5. The HQ password is not given to the venue in any circumstance
6. The onboarding form does not silently apply a default allotment
7. Nothing collected from the venue is data the product cannot use

**Evidence**
- studio/docs/execution/venue-edition-and-films/evidence/E15.10-venue-configuration-and-approval.md — Six-step script; the two launch boundaries (name only, no venue login) stated up front with the code that proves them
- studio/docs/execution/venue-edition-and-films/tasks/E15.10.md — EXISTS (4,034 bytes). Task spec present.
- _wt-wp22/src/lib/account/live/project-venue-access.ts:330 — RE-RAN AND CONFIRMED. `members: []` is at line 330 exactly, and `grep -n members` over the whole file returns that one line only, inside the single snapshot return. Venue login is genuinely absent, exactly as claimed.
- _wt-wp22/src/lib/hq/auth.ts:1-77 — RE-READ AND CONFIRMED. Header names the ten rooms (Today, Money, Company, Vault, Atlas, Decks, Founders Circle, Data Room, Access, Account Review). HQ_SESSION_VERSION = "v2", token `v2.<issuedAtMs>.<hmac>`, KEY_ITERATIONS = 150_000, KEY_SALT = "signal-hq-session-key:v2", SESSION_TTL_MS server-side. Source states plainly: "no per-person identity, no venue account, no revocation of one holder without rotating for everyone" and "A venue must not be given this password". Claim accurate.
- _wt-wp22/scripts/check-entitlements-integrity.test.mjs:366 — RE-RAN MYSELF. `node --test scripts/check-entitlements-integrity.test.mjs` -> tests 21, pass 21, fail 0. Includes `✔ warns, without failing the run, when an unlimited venue still carries a seat count (405.2292ms)`. NOTE: the worktree had NO node_modules, so the author could not have run this here; I linked studio/node_modules temporarily and removed the link afterwards (git status clean, junction deleted).
- _wt-wp22/src/app/hq/entitlements/OnboardVenueForm.tsx:17-20,33,92-104,136-142 — REFUTES THE EVIDENCE FILE. The form has NO defaultValue={10}. Issuance is a two-option select defaulting to "unlimited" (useState("unlimited")); the numeric allotment input only appears in the "limited" branch; unlimited collects `annualWeddingCount` and renders `fairUseCeilingFor(parsedCount)`. `git log` shows this landed in commit d17f27f (Wave 1), i.e. BEFORE the 2026-08-04 evidence was written on this same branch.
- _wt-wp22/src/lib/venue-allotment.ts:95-108 and src/lib/account/live/project-venue-access.ts:143-213 — RE-RAN MYSELF. `npx tsx --test src/lib/venue-allotment.test.ts src/lib/account/live/project-venue-access.test.ts` -> tests 16, pass 16, fail 0, including `✔ R-016: an unlimited venue is never shown a seat count`, `✔ R-016: the headroom warning is suppressed, not merely zero`, `✔ nothing in an unlimited snapshot reads as a seat number`, `✔ the fair-use ceiling doubles the venue's own annual figure`. Step 2's 'surface unchecked' is stale.
- app/src/app/p/[slug]/, app/src/app/s/[token]/, app/src/app/share/[token]/, app/src/app/embed/[slug]/ — REFUTES STEP 4. `grep -rni "sponsor|compliments|venue"` across all four public render routes returns ZERO matches. No public couple-facing surface carries a venue name at all, and `/p/[slug]/page.tsx` contains no `sponsor` reference in its metadata or openGraph block.
- app/src/components/welcome/venue-welcome-card.tsx:66 and app/src/app/app/tasks/page.tsx:4,45 — The ONLY place the venue name renders is `Compliments of {sponsorName}` in VenueWelcomeCard, mounted on the authenticated `/app/tasks` page. It is not a public workspace surface and cannot be reached by curl.
- _wt-wp22/src/app/hq/account-review/panels/account-panel.tsx:77-91 — The dead control the criteria forbid still exists. Line 89 renders an "Invite member" button whose onClick sets `setNotice("Invite recorded in this review fixture. No email is sent.")` and nothing else. The evidence file wrote a conditional ("Remove it or wire it") instead of checking; it is still there.
- Specification: `studio/docs/execution/venue-edition-and-films/tasks/E15.10.md`

### `E15.11` Train the venue and support it through its first real couple invitation and redemption.

Executor: founder · in review since 2026-08-04

**Acceptance criteria**
1. The venue can request an invitation through a route it can actually use and the request reaches Signal HQ reliably
2. The invitation is issued and the couple redeems it, in production, for real
3. The couple's access term written to the database matches D-022 for that couple's wedding date; read the database, not the screen
4. The venue receives the approved copy pack and a one-page prohibited-claims sheet before it speaks to a couple
5. The venue is told what to say when a couple asks how long they keep it, and that answer matches the agreement
6. Every point of friction is recorded in the venue's words, including the venue's own confusions
7. Support requests arising in the first week are logged with their resolution

**Evidence**
- studio/docs/execution/venue-edition-and-films/evidence/E15.11-venue-training-and-first-couple.md — Six-step script including the verbatim prohibited-claims list and the exact answer to the question every couple asks
- studio/docs/execution/venue-edition-and-films/tasks/E15.11.md — EXISTS (3,549 bytes). Founder sign-off block is empty and unticked. Status backlog, executor founder. No price or VAT text anywhere in the task or its evidence, so the D-021 VAT-inclusive check is not applicable here.
- studio/docs/execution/venue-edition-and-films/evidence/E09.11-venue-copy-pack.md — EXISTS. Referenced input resolves. Section 4 'Never say these' is the prohibited-claims content, generated from evidence/copy/prohibited-claims.v1.json (also exists). Section 2's answer to 'How long do I have it?' — '18 months from when you open it, or 3 months past your wedding, whichever is later. If you move the date, it moves with you' — matches E15.11 step 5 in substance. No conflict between the two answers.
- studio/docs/execution/venue-edition-and-films/evidence/R-024-name-only-copy-register.md — EXISTS. Referenced input resolves.
- studio/docs/execution/venue-edition-and-films/evidence/E15.05-shared-timeline-and-keepsake-test.md — EXISTS. Referenced input resolves.
- studio/docs/execution/venue-edition-and-films/evidence/E15.03-invitation-and-redemption-test.md — EXISTS. This is the source of the 'nine passing tests' phrase — it lists nine named results by name. E15.11 cites the number without citing the source, which reads as if the whole fix is nine tests.
- app/src/server/actions/comp.ts (origin/main, lines 215-216) — I READ THE DEPLOYED BRANCH. `const expiresAt = new Date(Date.now() + row.durationDays * 24 * 60 * 60 * 1000)`. No wedding date reaches it. Production writes redemption + durationDays, flat. The evidence's central claim is TRUE.
- app/src/server/db/sponsored-access-term.ts — `git cat-file -e origin/main:src/server/db/sponsored-access-term.ts` returned: fatal: path exists on disk, but not in 'origin/main'. The file is UNTRACKED in the dirty app working tree. Confirms the fix is not deployed. Note: this is a SECOND untracked implementation of D-022 sitting alongside the _wt-integrate one — two implementations exist in the workspace, as E15.03 already recorded.
- _wt-integrate/src/lib/venue-access-term.test.ts + _wt-integrate/src/server/db/couple-access-term.test.ts — I RAN THEM. `node --import tsx --test` → 'tests 48, suites 14, pass 48, fail 0, duration_ms 3572'. Eight of the nine named results are in these two files and all passed. The ninth ('a sponsored wedding workspace has no member cap, so no seat count exists') I located in _wt-integrate/src/server/venue-never-a-collaborator.test.ts but did not execute. Fix commit is f6331d6 'R-015 · apply the ratified access term in the app redemption path' on branch claude/integrate-app.
- app/src/server/db/schema.ts:605-627 — The SQL in step 4 is runnable against production. Columns id, workspace_id, started_at, expires_at, notes all exist on the entitlements table on the deployed branch, and the `comp:<code>` notes convention is real (app/src/server/actions/comp.ts:255 writes ``notes: `comp:${code}` ``). This is the one step in the document that would actually execute.
- app/src (keepsake grep) — I RAN IT. `grep -rni keepsake app/src` returns exactly 2 hits, both prose comments in timeline-artifact.tsx:800 and timeline-artifact.module.css:1526 ('the keepsake keeps its content'). No Keepsake state, no product surface. The evidence's claim is TRUE.
- studio/src/lib/venue-lifecycle.ts — I RAN IT. The keepsake state machine exists here (COUPLE_ACCESS_STATES includes 'keepsake'), but `grep -rn 'venue-lifecycle' studio/src` excluding the file itself returns ZERO results, and there are no callers of nextCoupleAccessState or COUPLE_ACCESS_STATES outside it. Zero production callers CONFIRMED — it is a library plus its own test.
- studio/src/app/hq/account-review/actions.ts:5,46 — THE FINDING THAT MATTERS. The only writer of the sponsor_requests table anywhere is this HQ file, behind the single shared HQ password (R-043). `grep -rn sponsorRequests studio/src` outside the schema returns only src/lib/entitlements-db/requests.ts and two src/app/hq/account-review/* files. There is no venue-reachable route in studio/src/app. Acceptance criterion 1 cannot be met by anything that exists.
- studio/docs/execution/venue-edition-and-films/evidence/E15.10-venue-configuration-and-approval.md — The sibling document written by the same package states it plainly: 'There is no venue login. Verified 2026-08-04: project-venue-access.ts:330 returns members: [] unconditionally.' E15.11 omits this, which is why its step 3 reads as a testable step.
- app/src/app/api/account/export/route.ts (origin/main) — Checked in the venue's favour. The 'you can export everything at any time' half of the step 5 answer IS supported in production — account export exists and ships. Only the Keepsake half is unbuilt, which is exactly what the document says.
- Specification: `studio/docs/execution/venue-edition-and-films/tasks/E15.11.md`

### `E15.12` Run seven-day and 30-day venue reviews covering adoption, friction, support and next action.

Executor: founder · in review since 2026-08-04

**Acceptance criteria**
1. Both reviews happen within three days of their due date, or the slip is recorded with a reason
2. Adoption evidence shown to the venue passes the two-sided small-cell test and the rate floor, and any withheld metric is presented as withheld without disclosing the population
3. Friction is recorded in the venue's words and every item gets a disposition: fixed, on the roadmap, or a stated no
4. Support volume and resolution time are recorded per venue
5. Exactly one next action leaves each review, with an owner and a date
6. A cross-venue rollup exists once more than one venue has completed both reviews and names anything two or more venues raised
7. Nothing in a review commits to building for one venue

**Evidence**
- studio/docs/execution/venue-edition-and-films/evidence/E15.12-venue-seven-and-thirty-day-reviews.md — The fifteen-minute review script plus the disclosure rules and the code that enforces them
- studio/docs/execution/venue-edition-and-films/tasks/E15.12.md — EXISTS. Executor is 'founder'. Its Validation plan says only 'See evidence/E15.12-...md'; the evidence doc contains no validation plan for criteria 1, 3, 4 or 6.
- _wt-wp22/src/lib/account/instrumentation/suppression.ts:61-64 — CONFIRMED. isSmallCell is present and byte-identical to the block quoted in the evidence doc. Both quoted comments are verbatim: lines 52-59 ('publishing 3 of 5 also publishes 2 of 5 did not, and the second sentence is the one that identifies people') and lines 24-26 ('A message that says fewer than three is itself the disclosure the floor exists to prevent').
- _wt-wp22/src/lib/account/instrumentation/daily-metrics.ts:238,241 — CONFIRMED exactly. presentRate(null, null, absentReason) at 238 and presentRate(returned, sealed.length, absentReason) at 241. Also live at retention.ts:96,99. metricRateLabel survives only as a comment at format.ts:48 and as a test at suppression.test.ts:257 asserting it must not exist.
- _wt-integrate-studio (branch claude/integrate-studio) — pnpm test — RE-RUN BY ME. EXIT=0. Summed across the three runners: tests=742, pass=742, fail=0 (70 + 616 + 56). The numbers reproduce exactly. Suppression suite ran: 'R-027: the complement is withheld too', 'the small-cell test refuses an impossible input', 'a zero is withheld like any other small cell', 'a day count keeps the population floor and drops the complement rule' — all passing.
- studio/src/lib/account/instrumentation/suppression.ts — REFUTES THE COMMAND AS WRITTEN. The tree literally named in the verification record ('studio pnpm test') does NOT contain the fix. isSmallCell is ABSENT. grep returns only BEHAVIOURAL_MIN_WORKSPACES at lines 13, 43, 73, 102 — the one-sided shipped behaviour R-027 describes.
- studio/src/lib/account/format.ts:44 — REFUTES 'metricRateLabel is gone', unqualified. In studio/ the two-argument metricRateLabel(numerator, denominator) still exists and is still CALLED at studio/src/app/hq/account-review/components/report-preview.tsx:30. studio/src/lib/account/instrumentation/suppression.test.ts contains no isSmallCell, complement, or metricRateLabel test at all.
- _wt-wp22/src/lib/account/instrumentation/ingest.ts:22,45 — R-029 CONFIRMED OPEN (main half). Comment: 'Events carry a salted hash; redemptions.user_clerk_id is raw. Hash at read'. subjectIdHash: hashIdentity(row.userClerkId, salt). No workspace-ownership check. grep for workspace_sponsorships across studio/src/lib/account returns nothing.
- app/src/server/auth.ts:150-161 (UNCOMMITTED) — R-029's SECOND HALF IS STALE. getInstrumentationSubjectId() returns the Clerk id, and app/src/server/actions/tasks.ts:104,146,365,487 now call it. Both files are ' M' on app main; git log -S shows the function has NEVER been committed. At HEAD, tasks.ts passes `me` (internal user) at lines 124,167,190,430,523 — so the claim is true against HEAD and false against the live tree. The evidence doc names no baseline.
- _wt-wp22/src/lib/account/format.ts:64-75 and suppression.ts:121 — NEW FINDING, against criterion 2. formatRateAccessibleLabel renders a withheld rate as 'withheld for small-group privacy protection'. presentRate returns withheld ONLY when denominator < RATE_MIN_WORKSPACES (line 121), so that sentence tells the reader the eligible cohort is under 5. Criterion 2 requires a withheld metric be presented 'without disclosing the population'.
- grep -ni 'keepsake|venue portal|login|dashboard|wedding date|opt-in|/p' on the E15.12 evidence doc — PASSES the absent-capability check. Returns NONE. The doc does not send the founder to a screen that does not exist. grep for 'legal|solicitor|accountant|GDPR compliant|reviewed by' also returns NONE, so D-016 is clean.
- grep -ni 'R-043|D-027|invitation administration' on the E15.12 evidence doc — RETURNS NONE — the converse omission. The doc assumes adoption evidence gets 'shown to the venue' at day 7 and day 30 without stating that R-043 means there is no venue-authenticated route, or that D-027 point 4 puts aggregate adoption evidence out of launch scope ('the Venue Portal at launch is invitation administration only. Aggregate adoption evidence follows after 1 September').
- Specification: `studio/docs/execution/venue-edition-and-films/tasks/E15.12.md`

### `E15.13` Operate the direct founder channel and structured founding-request intake without creating uncontrolled custom scope.

Executor: founder · in review since 2026-08-04

**Acceptance criteria**
1. The founder-access commitment appears in the Benefits Charter in the exact ratified shape: one 30-minute call per venue per year plus a named email route, and not standing access
2. Every founding request is written to sponsor_requests with a sponsor, a state and a created date; nothing lives only in an inbox
3. Every request reaches a terminal state with an answer the venue receives; silence is not a state
4. The standing answer for a request that will not be built is written once and reused
5. A monthly rollup names what was asked for and how many venues asked, and feeds the roadmap rather than the backlog directly
6. No request results in venue-specific code, configuration or copy
7. The call is offered and its use is recorded, so the commitment can be evidenced at renewal

**Evidence**
- studio/docs/execution/venue-edition-and-films/evidence/E15.13-founder-channel-and-request-intake.md — The five request states with response times, the standing answer written verbatim, and the two stop rules
- _wt-wp22/src/lib/entitlements-db/schema.ts — Lines 715-752. Table is real and well indexed. States are open|approved|declined|fulfilled|canceled. Kinds are more_codes|report|support|profile_change. Neither set matches the procedure the evidence document writes.
- _wt-wp22/src/lib/entitlements-db/requests.ts — Lines 28-73 insert only; 75-143 read only. No UPDATE anywhere in the module. listOpenSponsorRequests filters state == 'open' and returns free-text note, which cannot be grouped by what was asked for.
- _wt-wp22/src/lib/entitlements-db/requests.test.ts — Lines 5-19 deep-equal-assert both constant arrays. The procedure's states would break this test, so they cannot be adopted without a contract change nobody has raised.
- _wt-wp22/src/app/hq/account-review/actions.ts — Lines 33-55. The only production writer. Hardcoded kind 'more_codes', gated by the shared HQ password. A venue cannot file a request; only an HQ operator can, and only for more codes.
- studio/docs/strategy/FOUNDING_25_BENEFITS_CHARTER.md — Lines 54-90. Founder-approved 2026-08-03 under E02.08. Carries the ratified founder-access shape and a standing answer. This is the artifact the evidence document says does not yet exist.
- _wt-wp22/src/lib/venue-invitation/copy.ts — Line 107. A third standing-answer variant already shipping in venue-facing copy. With the charter and the evidence document there are now three.
- _wt-wp22/scripts/migrate-account-requests.mjs — EXISTS. Idempotent additive migration for sponsor_requests. Never run against any database in this work; nothing was operated.
- Specification: `studio/docs/execution/venue-edition-and-films/tasks/E15.13.md`

### `E15.14` Secure permissioned venue evidence, couple evidence, referrals and public map/name approval.

Executor: founder · in review since 2026-08-04

**Acceptance criteria**
1. A permission register exists with one row per permission: giver, exact use, date, expiry or revocation route, and the artefact where it appears
2. No venue name appears on the public map or in any founding-programme surface without a row in the register
3. No couple material is used publicly without a specific, separate, dated opt-in from that couple; a general consent in an account flow does not satisfy this
4. Permission is specific to a use, not general; 'marketing' is not a use
5. Every permission has a stated revocation route the giver can actually use, and revocation removes the material within a stated period
6. A referral request is made only where the venue has agreed to be asked
7. No published material implies a venue's logo or its own words appear in a couple's workspace
8. Nothing published states or implies legal approval or GDPR compliance

**Evidence**
- studio/docs/execution/venue-edition-and-films/evidence/E15.14-permissioned-evidence-and-referrals.md — The eight-column register design, the four permission types with their stop rules, and the R-017 warning about what is inside a couple's workspace
- studio/docs/execution/venue-edition-and-films/tasks/E15.14.md — EXISTS (4,028 bytes). Task spec with the eight acceptance criteria, executor 'founder', dependency E15.11 open and not waived. Founder sign-off block correctly left blank.
- app/src/app/robots.ts — Line 14: disallow: ["/app", "/s", "/share", "/redeem", "/welcome", "/api"]. '/p' is absent from the list and no noindex is set on that route. Source matches production exactly.
- studio/docs/execution/venue-edition-and-films/venue-universe/counts-only.md — Lines 3-6 reproduced verbatim as quoted in the claim: 'GENERATED by venue-export.mjs. Counts only, by design. No venue is named here.' and 'Venue names are published as participants only with recorded consent (E10.14, E15.16).' The tooling-discipline claim is accurate.
- app/src/server/actions/planning.ts — Line 1182-1193: the only in-product consent that exists. Gated on sponsor.consentGranted, then mints consentReceiptId: `consent-${randomUUID()}`. Its scope is stated at line 1183: 'Consent is required before sharing activation metadata' — i.e. activation label and primary date shared with the venue. It is not permission to publish material.
- app/src/server/db/schema.ts — Lines 334-352: workspace_sponsorships carries consentedMetadata, consentReceiptId and consentedAt. The comment at 329-333 says private content must never be copied into consentedMetadata. This is a sponsorship-activation consent, which is precisely the 'general consent in an account flow' that criterion 3 rejects.
- Specification: `studio/docs/execution/venue-edition-and-films/tasks/E15.14.md`

### `E15.15` Release Cohorts 2, 3, 4 and reserve accounts as necessary until 25 venues are signed and paid.

Executor: founder · critical path · in review since 2026-08-04

**Acceptance criteria**
1. Each cohort is built under the same three rules: one property per operator per cohort, nothing unconfirmed in a cohort, and a short cohort reported as short
2. No cohort contains an account from outside the ratified ring
3. The running count of signed and paid venues is derived from the entitlements database and matches sponsors.founding_number assignments
4. Held reserve accounts are verified before release rather than assumed, with the result recorded either way
5. When the pool inside the ring is exhausted before 25 signatures, that is reported as a fact with the number reached, and the next move is a founder decision under change control
6. No venue is contacted twice across cohorts
7. Every cohort release is preceded by a check that the ratified position and the live surfaces still agree

**Evidence**
- studio/docs/execution/venue-edition-and-films/evidence/E15.15-cohorts-2-3-4-release.md — The six-step per-cohort script plus the pool arithmetic said out loud for the first time
- studio/docs/execution/venue-edition-and-films/venue-universe/counts-only.md — 219 researched, 70 in ring, 50 in ring and eligible, 28 near the boundary
- studio/docs/execution/venue-edition-and-films/venue-universe/05-COHORTS.md — Not listed as evidence but is the load-bearing source. Lines 9-11 confirm the cohort statement verbatim. Lines 69 and 85-88 give the number the evidence doc should have used: Cohort 2 fills to 16, two contactable accounts remain, "from a total of 43".
- studio/docs/execution/venue-edition-and-films/tools/venue-universe.test.mjs — I RAN IT: 25 tests, 25 pass, 0 fail. Four tests directly enforce criteria 1, 2 and the short-cohort half of 5. This is real mechanised evidence the doc did not execute.
- studio/src/lib/db/schema.ts — prospectsTable lines 218-274. Has stage/last_contacted_at/outcome but NO account_id. This is why criterion 6 has no executable mechanism.
- studio/src/lib/entitlements-db/schema.ts — foundingNumber and foundingNumberAssignedAt at lines 193-194. Criterion 3's column exists in schema — production application unverified.
- studio/docs/execution/venue-edition-and-films/venue-universe/BUILD.md — Carries the exact runnable rebuild command Step 1 needed and omitted.
- Specification: `studio/docs/execution/venue-edition-and-films/tasks/E15.15.md`

### `E15.17` Close the founding offer at 25 paid agreements, switch all new commercial surfaces to €1,500 and prepare the first renewal cycle.

Executor: founder · critical path · in review since 2026-08-04

**Acceptance criteria**
1. The offer closes on the date the twenty-fifth cleared payment is recorded, not on the twenty-fifth signature
2. Every forward-facing commercial surface states EUR 1,500 inclusive of VAT at the prevailing rate, swept by the full-workspace inventory method rather than the sixteen-file method
3. check-venue-edition-contract.mjs and check-venue-copy.mjs pass against the post-closure position
4. Every existing founding venue's price history still reads EUR 1,000 for every past term and its next renewal is priced at EUR 1,000, demonstrated by query
5. The rate lock survives rebrand and ownership change of the property and does not transfer to additional properties a new owner brings
6. The renewal worklist produces a correct list for the first venues approaching renewal, each with an action
7. No surface says the lock is forever, for life, or in perpetuity
8. The closure is recorded as a dated decision

**Evidence**
- studio/docs/execution/venue-edition-and-films/evidence/E15.17-founding-offer-close.md — Nine-step script; the sweep risk from I-002 named as the real hazard, with section C's named files listed
- studio/scripts/check-venue-edition-contract.mjs — RE-RAN in _wt-wp22: '[venue-edition-contract] ok', exit 0. MATCHES the claim. But lines 179-197 are requireText pins on the PRE-closure position — VENUE_EDITION_FOUNDING_ANNUAL_PRICE_EUR = 1_000, "cohortSize": 25, "annualAmountCents": 100000. Grep for 1500/150000 across all 359 lines: zero matches.
- _wt-wp22/scripts/check-venue-copy.mjs — RE-RAN --report: summary line byte-identical to the claim — '[venue-copy] ok — 16 rules, 92 patterns, 29 surfaces swept (0 cross-repo, not checked here), 30 pinned hit(s), 21 citation(s), 6 recorded exception(s).' exit 0. Note this script exists ONLY in the worktree, not in studio/scripts.
- _wt-wp22/scripts/check-venue-copy.mjs:181,289 — REFUTES the doc's headline caveat. Line 289 prints `${surfaces.length - skipped} surfaces swept (${skipped} cross-repo, not checked here)`. `skipped` counts surfaces skipped BECAUSE the app repo is absent (line 181: 'present only when the app repository is checked out alongside studio'). 0 = none skipped = all were checked.
- app/src/app/the-wedding/page.tsx — Proof the cross-repo caveat is wrong: this couple-facing app file WAS swept, reporting 'the-wedding/page.tsx:26 [p8-special-category] "dietary"'. Four more app surfaces swept alongside it: src/lib/templates.generated.ts:108, src/server/demo/tasks-demo.ts:89,99, src/modules/notes/server/demo/notes-demo.ts:63, src/components/welcome/venue-welcome-card.tsx.
- studio/src/lib/entitlements-db/schema.ts:193 — CAPABILITY AUDIT PASS. `foundingNumber: integer("founding_number")` exists, with foundingNumberAssignedAt at :194 and a unique partial index at scripts/migrate-venue-edition-terms.mjs:118-119. Step 1's SQL `SELECT COUNT(*) FROM sponsors WHERE founding_number IS NOT NULL` is real, not aspirational.
- studio/src/lib/entitlements-db/venue-billing.ts:123,411 — CAPABILITY AUDIT PASS. recordAnnualPrepayment (:123) and venueRenewalWorklist (:411) both exist and are exported. sponsor_price_agreements is present in the entitlements schema. Every capability E15.17 names is real.
- studio/contracts/commercial-terms.v2.json:65-68 — Evidence E15.17 had available and did not cite: lockFollows: "property", lockTransfersToNewProperties: false (criterion 5), forbiddenWording: ["for life","forever","in perpetuity","lifetime"] (criterion 7). The doc restates D-009 prose instead of pointing at the machine contract that already encodes both.
- _wt-wp22/public/brand/pitch-deck-2026.html:558 — Pinned p1-permanence hit 'FOREVER'. I read lines 548-570: it is the Free tier cadence ('Free / €0 / FOREVER'), NOT the founding lock. Criterion 7 is not violated by it. Recorded so the finding is not overstated.
- Specification: `studio/docs/execution/venue-edition-and-films/tasks/E15.17.md`

### `E15.18` Complete the project postmortem, archive final assets, update the business plan and market-entry documents, and transfer ongoing work into operations.

Executor: founder · in review since 2026-08-04

**Acceptance criteria**
1. The postmortem names what went wrong with dates and consequences, at minimum addressing the two concurrent implementations of the same ratified rule, the branch-versus-production gap at the go/no-go, the Keepsake promise shipping ahead of the Keepsake product, and the film schedule
2. Every superseded position is traceable: what it was, what replaced it, and when
3. The business plan and market-entry documents state the actual outcome including the conversion rate against the eligible pool
4. Every ongoing obligation has a named owner and a cadence, including annual renewals, the rate lock, the annual founder call, the request intake, couple access terms running to 2028 and beyond, and the Keepsake tail which has no end date
5. The asset archive has an index a person can use a year later without asking anyone
6. The record shows what was judged at approval, not only that approval happened, for every decision taken after D-026 flagged the gap
7. Nothing in any updated document states or implies legal approval or GDPR compliance

**Evidence**
- studio/docs/execution/venue-edition-and-films/evidence/E15.18-postmortem-and-operations-transfer.md — The four-section postmortem structure, four candidate findings recorded now while they are visible, and the operations-transfer table written for the first time
- studio/docs/execution/venue-edition-and-films/tasks/E15.18.md — EXISTS. Carries the seven acceptance criteria verbatim as claimed. DEFECT: its 'Validation plan' section says only 'See evidence/E15.18-postmortem-and-operations-transfer.md' — and that evidence file has no validation plan in it. The spec points at a plan that was never written.
- PROJECT_STATE.json (tasks.E15.18) — E15.18 is `status: backlog`, `acceptanceCriteria: []`, `evidence: []`, `founderSignOff.state: not_requested`, `specPath: null`. Nothing from this work is recorded in state. Whatever the orchestrator replays has to carry all seven criteria, the evidence ref and the spec path or the work is invisible.
- _wt-integrate (app integration branch) `npm test` — RE-RAN BY ME. Summed node:test totals: TESTS=1263 PASS=1263 FAIL=0. Exactly matches the '1,263 tests, zero failures' cited in section 3. CONFIRMED.
- _wt-integrate-studio (studio integration branch) `npm test` — RE-RAN BY ME. Summed node:test totals: TESTS=742 PASS=742 FAIL=0. Exactly matches the '742' cited in section 3. CONFIRMED.
- app/src/lib/venue-edition-term.ts + app/src/server/db/sponsored-access-term.ts vs _wt-integrate/src/lib/venue-access-term.ts + _wt-integrate/src/server/db/couple-access-term.ts — CONFIRMED, file by file. All four exist; the app pair shows as `??` untracked in `git status`; the integrate pair is committed. Both are imported from `src/server/actions/comp.ts` and `src/server/actions/planning.ts` in their own tree, under different export names (`coupleAccessExpiryForRedemption`/`recomputeSponsoredAccessTermQuietly` vs `extendCoupleAccessForWeddingDate`). Two live implementations of D-022 in one workspace. One wording nit: the document says 'wired into different call sites'; 
- _wt-integrate/src/lib/keepsake/ (export.ts, lifecycle.ts, keepsake.test.ts) — RE-RAN BY ME: `node --import tsx --test src/lib/keepsake/keepsake.test.ts` → tests 16, pass 16, fail 0. Production callers: zero. `grep -rn 'lib/keepsake' _wt-integrate/src` outside the directory itself returns nothing. The only other keepsake strings in the tree are two source comments. CONFIRMED exactly as section 3 states.
- studio/src/app/venues/ (main) vs _wt-integrate-studio/src/app/venues/ — CONFIRMED. The branch has five sub-paths (couple-preview, demo, privacy, questions, what-you-see); studio main has only `demo` and `page.tsx`. Four of five absent from the shipping tree, consistent with the '404' claim. Verified structurally against the source trees, not against the live site — I did not fetch production.
- app/src/app/robots.ts (main, dirty) vs _wt-integrate/src/app/p/published-wedding-noindex.test.mjs — CONFIRMED. App main's robots.ts line 14 disallows /app, /s, /share, /redeem, /welcome, /api — /p is still absent, so the R-031 fix is branch-only. The noindex regression test exists only in the integration branch.
- studio/content/hq/operator-todos/apply-venue-edition-terms-migration.md — Checked against the section-3 claim that `entitlements.wedding_date` was applied to production on 2026-08-03. The operator todo, verified by direct query of `entitlements-prod`, records `entitlements.wedding_date` as PRESENT. The first half of the claim holds. (`drizzle-entitlements/README.md` marking migration 0001 'NOT YET APPLIED' is a different, unapplied drift-closure set and does not contradict it — I checked before reporting.)
- studio/src/lib/entitlements-db/codes.ts and schema.ts — REFUTES the second half of section 3's wedding_date finding. `schema.ts:127` declares `weddingDate: integer("wedding_date")`; `codes.ts:396` writes it on redeem; `codes.ts:610` selects it and `codes.ts:650` updates it inside `setCoupleWeddingDate`. So 'a production migration that no code reads' is false as written. The true statement is narrower: the only functions that touch the column, `redeemLicenseCode` and `setCoupleWeddingDate`, have zero production callers — `grep` for either outside code
- studio/docs/execution/venue-edition-and-films/venue-universe/counts-only.md — CONFIRMED. Line 10: 'Inside the ring and eligible: 50'. The denominator cited in section 6.2 is real.
- Specification: `studio/docs/execution/venue-edition-and-films/tasks/E15.18.md`

## Not ready — cannot be approved

| Task | Title | Missing |
|---|---|---|
| `E15.02` | Complete a real production billing and founding-rate test. | status is "blocked", not founder_review |
| `E15.03` | Complete a real invitation, redemption, account creation and branded workspace test. | status is "blocked", not founder_review |
| `E15.07` | Release Cohort 1 and record every personalised touch and response. | status is "blocked", not founder_review |
| `E15.16` | Maintain the authoritative founding-place counter, assign 01/25–25/25 numbers and publish only permissioned venue names. | status is "blocked", not founder_review |

## Your decision

Approve all of the above in one command:

```bash
node studio/docs/execution/venue-edition-and-films/tools/project-control.mjs approve-batch "your approval note" E15.01 E15.04 E15.05 E15.06 E15.08 E15.09 E15.10 E15.11 E15.12 E15.13 E15.14 E15.15 E15.17 E15.18
```

Or push back on any single one:

```bash
node studio/docs/execution/venue-edition-and-films/tools/project-control.mjs reject E15.01 "what is wrong"
```


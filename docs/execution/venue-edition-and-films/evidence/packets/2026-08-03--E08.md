<!-- honest-state-banner -->
> ## Read this before approving
>
> The heading below says "ready to approve". That is the tool's way of saying every task
> has acceptance criteria and evidence recorded. **It does not mean the work is complete.**
>
> **12 of 12 tasks in this packet are PARTIAL.** Every one was
> independently re-verified by an adversarial agent whose default was to refute, and the
> verifier's read overrides the build agent's self-assessment wherever they disagreed. Where
> a task was claimed complete and the verifier disagreed, it is recorded below as partial.
>
> | Task | Verified state | Criteria | **Not met** | Evidence |
> |---|---|---|---|---|
> | `E08.01` | **PARTIAL** | 10 | 12 | 13 |
> | `E08.02` | **PARTIAL** | 10 | 6 | 9 |
> | `E08.03` | **PARTIAL** | 9 | 8 | 9 |
> | `E08.04` | **PARTIAL** | 9 | 8 | 12 |
> | `E08.05` | **PARTIAL** | 9 | 8 | 10 |
> | `E08.06` | **PARTIAL** | 10 | 7 | 15 |
> | `E08.07` | **PARTIAL** | 9 | 7 | 9 |
> | `E08.08` | **PARTIAL** | 8 | 8 | 11 |
> | `E08.09` | **PARTIAL** | 9 | 11 | 11 |
> | `E08.10` | **PARTIAL** | 8 | 9 | 9 |
> | `E08.11` | **PARTIAL** | 8 | 8 | 11 |
> | `E08.12` | **PARTIAL** | 8 | 10 | 10 |
>
> Approving a partial task is a legitimate choice — it means you accept the gap as recorded
> and want the task closed anyway. It should be a decision, not an accident. The unmet
> criteria for each task are in its own section, and the reasoning is in the session record.

# Founder review packet — E08

Generated 2026-08-03T15:26:12.132Z from PROJECT_STATE.json. 12 task(s).

**12 ready to approve · 0 not ready.**

Approving sets these tasks to Done and is the only thing that can. Nothing here is Done yet.

## Ready to approve

### `E08.01` Implement annual prepaid billing for standard and founding agreements.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. A venue's annual charge is derivable in code from one place: plan (founding|paid) to amount in cents, matching contracts/commercial-terms.v2.json (100000 / 150000), with a test that fails if the two drift.
2. Every venue-facing money string renders VAT-inclusive with the exact D-021 wording 'inclusive of VAT at the prevailing rate', never VAT-added and never a separate VAT line implying it is on top.
3. A prepaid annual term records termStartsAt, termEndsAt, paidAt and the amount actually received, and is individually identifiable for retrospective VAT correction (R-014, R-022).
4. The recording path refuses the standard price against a founding plan and the reverse, proven by test.
5. There is exactly one live writer that records a cleared payment, and it carries every guard. A second unguarded path does not exist.
6. The payment capture route is stated honestly: either a built path exists end to end, or the documented answer is that venues are invoiced out of band and recorded by CLI, with no surface claiming otherwise.
7. No surface produced states or implies legal or accounting approval, or that VAT treatment has been confirmed by Revenue (D-016, R-013).
8. D-022's couple-term rule is not reimplemented here; billing links to venue-lifecycle.ts as the single source.
9. Money is constructed from gross only: net is always derived, never an input, and net + VAT reconciles to gross exactly for every amount and rate.
10. An undetermined VAT rate is recorded as null, never as zero, so the treatment can be corrected retrospectively.

**Evidence**
- _wt-wp07s/src/lib/venue-money.ts — vatInclusive() is the only constructor and takes gross. net = round(gross*10000/(10000+rate)), vat = gross - net, so net+vat===gross by construction. No constructor accepts a net amount. Null rate means 'not determined', never zero-rated.
- _wt-wp07s/src/lib/entitlements-db/venue-billing.ts — recordAnnualPrepayment(): the single guarded writer. Gross comes from the plan and the caller's figure is checked then discarded, so an operator typo cannot become the price of record. One transaction writes the append-only term, refreshes the sponsors cache and appends the audit line.
- _wt-wp07s/scripts/mark-venue-paid.ts — REWRITTEN THIS SESSION. Previously wrote five mutable columns itself, kept no history, wrote no audit line, was not idempotent, never assigned the founding number, and swallowed a failed shared-DB mirror in a console.warn while exiting 0. Now records through recordAnnualPrepayment and exits non-zero if the HQ Traction mirror fails.
- _wt-wp07s/src/lib/venue-money.test.ts — Includes 'gross is the input and net is derived, never the other way round', 'net plus VAT equals gross exactly, for every amount and rate', 'no code path reconstructs gross from net', 'no surface built here claims a confirmed VAT position'.
- _wt-wp07s/docs/strategy/VENUE_RENEWAL_AND_LAPSE_RUNBOOK.md — WRITTEN THIS SESSION. The checkpoint's code referenced this path and the file did not exist. Its first table lists ten process steps with five marked 'Not built': invoicing, payment capture, both sends, and dunning.
- _wt-wp07s/scripts/check-venue-edition-contract.mjs — Updated: the requirement that mark-venue-paid.ts contains the literal line 'const founding = plan === "founding";' would have forced the weaker pre-rewrite version back, so it now requires the recordAnnualPrepayment call by name. The runbook was added to currentCommercialSources so it is string-checked.
- evidence/E08-venue-billing-and-renewal.md — Evidence document, criterion to proof, including the six checkpoint defects with the commands that proved each was real.
- _wt-wp07s/src/lib/entitlements-db/venues.ts — REFUTES CRITERION 5. onboardVenue() lines 115-177 writes paidAt, termStartsAt, termEndsAt, annualAmountCents and foundingLocked directly on `sponsors`, on both the insert and the update branch (`paidAt: isPaidPlan ? now : null`). `grep -n 'prepaymentRefusal|recordAnnualPrepayment|venue-billing' src/app/hq/entitlements/actions.ts src/lib/entitlements-db/venues.ts` returns NOTHING. No sponsor_price_agreements row, no payment audit line, no founding-number assignment, no amount check.
- _wt-wp07s/src/app/hq/entitlements/actions.ts — Proves the second writer is LIVE, not dead code. Line 19 imports onboardVenue; line 264 `export async function onboardVenueAction` in a 'use server' file; line 295 calls onboardVenue. OnboardVenueForm.tsx line 4 binds the action and line 67 sets `defaultValue="founding"` on the plan select, so the DEFAULT submission of the live HQ onboarding form stamps paidAt=now, foundingLocked=1 and annualAmountCents=100000 with no money received.
- _wt-wp07s/src/lib/entitlements-db/founding-numbers.ts — Consequence of the second writer. foundingNumberRefusal() at line 92 gates only on `sponsor.paidAt` being non-null. Because onboardVenue sets paidAt on plan selection, a console-onboarded venue that never paid satisfies the 'assigned on cleared payment' guard. D-009 point 6 ('on payment, not on signature') is defeated on that path.
- _wt-wp07s/src/app/venues/page.tsx — REFUTES CRITERION 2. Quotes euro prices at lines 11, 102, 468, 473, 495 and 509 ("€1,500 per venue, per year, prepaid", "€1,000", "€1,500"). `grep -rn 'inclusive of VAT' src/app src/emails` returns ZERO hits across the entire app and email trees. src/emails/registry.tsx:186 quotes "€1,500 a year" the same way. No venue-facing surface renders through formatVenuePrice().
- _wt-wp07s/src/lib/venue-edition.ts — Criterion 1 GENUINELY MET. venueEditionAnnualAmountCents() at line 34 is the single resolver; VENUE_EDITION_FOUNDING_ANNUAL_PRICE_CENTS = 100000, VENUE_EDITION_ANNUAL_PRICE_CENTS = 150000. The drift test is real: src/lib/venue-money.test.ts:245-259 asserts the resolver equals COMMERCIAL_TERMS.plans.venue.founding.annualAmountCents and .annualAmountCents, which src/lib/commercial-terms.ts:1 loads from contracts/commercial-terms.v2.json (verified 100000 / 150000 at lines 52 and 56 of the JSON).
- _wt-wp07s/scripts/venue-billing-ddl.mjs — No statement-breakpoint trap. The DDL is a list of discrete statements executed one at a time (migrate-venue-billing.mjs lines 98, 115, 124), never a .sql file split on '--> statement-breakpoint'. `git status` shows no .sql or drizzle migration file touched. vat_rate_basis_points is present in CREATE_VENUE_BILLING_TABLE, and only one CREATE TABLE for this table exists in the repo.
- Specification: `studio/docs/execution/venue-edition-and-films/tasks/E08.01.md`

### `E08.02` Implement an immutable founding-rate flag and historical price record for each qualifying venue.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. A founding venue carries an immutable record of the rate it locked, and no code path can lower or raise it while the agreement renews continuously, proven by a test that attempts the downgrade.
2. A founding number 01/25 to 25/25 is assigned only when payment clears, is unique at the database level, and is never reused after a lapse.
3. The number-assignment path is actually called by whatever records payment; assignment is not left as an uncalled library function.
4. A historical price survives a renewal: the amount charged in each term is retrievable, not overwritten, proven with a term written at a hypothetical future EUR 1,800.
5. A recorded term cannot be edited or deleted, and that is a property of the database rather than a convention in the writer.
6. A payment reversal withdraws the number and returns the place to the pool with date and reason, in the audit ledger.
7. 'Unlimited' is representable and the founding record carries no seat count (D-020, R-016).
8. No surface states the lock as 'for life', 'forever', 'in perpetuity' or 'lifetime' (D-009 point 3, D-021).
9. A test proves two venues cannot hold the same founding number at the index level, not only in arithmetic.
10. The drizzle table the application queries and the DDL the migration executes declare the same columns, proven by a check that fails on drift.

**Evidence**
- _wt-wp07s/src/lib/venue-billing.ts — planChangeRefusal() ADDED THIS SESSION — the guard that actually makes the rate immutable. Append-only rows protect the past; nothing stopped the NEXT term being recorded as 'paid' at EUR 1,500 for a venue holding 01/25, with every historical row intact, every amount matching its plan, and the lock ended with no lapse on record. Also foundingRateOnReturn(), which returns an explicit unsettled marker rather than inventing what a returning venue is offered.
- _wt-wp07s/src/lib/entitlements-db/venue-billing.test.ts — 'refuses to renew a held founding lock onto the standard plan' uses its own seeded venue on purpose: the preceding fixture leaves v-founding carrying a directly-inserted future standard term, which is exactly the state that would have made this test pass for the wrong reason. Also 'survives a price change, per term' asserting history [100000, 100000, 180000].
- _wt-wp07s/scripts/venue-billing-ddl.mjs — CREATED THIS SESSION. The ledger table was defined three times by hand — in the migration, in schema.ts, and inline in the test file behind a comment claiming they were 'kept identical on purpose'. The migration and the tests now read the same strings from here.
- _wt-wp07s/scripts/venue-billing-ddl.test.mjs — CREATED THIS SESSION. Parses schema.ts as text and compares its column set to the DDL the migration executes, both directions. Also asserts the VAT columns stay nullable (null means not determined; NOT NULL would force a zero, which is a claim) and that no DDL statement carries a semicolon outside a trigger body.
- _wt-wp07s/scripts/migrate-venue-billing.mjs — probeAppendOnly() writes a real probe row, attempts an UPDATE and a DELETE, and rolls back — it proves the triggers FIRE rather than checking sqlite_master and assuming. A trigger that exists and does not fire is worse than none, because the table name says append-only and a reviewer stops looking.
- _wt-wp07s/src/lib/entitlements-db/founding-numbers.ts — assignFoundingNumber / withdrawFoundingNumber. Pre-existing and genuinely correct: idempotent, payment-gated, audit-writing, with a partial UNIQUE index. The gap was that nothing called it; recordAnnualPrepayment now does.
- _wt-wp07s/src/lib/entitlements-db/venue-billing.ts — EXISTS. The guard is wired at :176-194, before any write. Verified genuinely load-bearing: the price of record comes from the plan constant at :200 ('gross = venueEditionAnnualAmountCents(input.venuePlan)') and the caller's amount is checked then discarded, so an operator typo cannot become the rate. assignFoundingNumber is called at :299 for founding plans only. No other code path in src/ or scripts/ writes sponsors.annualAmountCents, venuePlan or foundingLocked.
- _wt-wp07s/scripts/mark-venue-paid.ts — Verified independently as the ONLY caller of recordAnnualPrepayment in the entire repo (:122-146). This makes criterion 3 genuinely met — the thing that records a cleared venue payment does call the assigner. It also means the whole payment path is one operator CLI; there is no Stripe webhook in studio.
- _wt-wp07s/drizzle-entitlements/0000_init.sql — The only checked-in migration. Contains none of the columns or tables this task depends on. This is the load-bearing defect.
- Specification: `studio/docs/execution/venue-edition-and-films/tasks/E08.02.md`

### `E08.03` Implement renewal invoices, renewal reminders, failed-payment handling, grace periods and lapse behaviour.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. A renewal date exists per venue and is derived from the recorded term without a manual calculation.
2. Grace and lapse are named states with defined boundaries, visible to the operator, never guessed from absence of data.
3. A lapse is derived from the term and the grace window and cannot be declared by hand.
4. D-020 point 2 is provably preserved: a lapse stops new issuance only, and every already-redeemed couple workspace keeps its full term plus Keepsake, proven by a test that lapses a venue and asserts couple access is unchanged.
5. Branding removal on lapse is expressed as a checkable value, not a promise.
6. A lapsed founding venue keeps its number historically and its place shows as closed, not open.
7. No copy anywhere implies a couple can lose access because a venue did not pay.
8. The notice lead time and grace window are marked unratified and cannot reach an agreement, order form, renewal email or the portal while they are.
9. Whatever is not built by 1 September is stated as not built, in the runbook, rather than described as automatic.

**Evidence**
- _wt-wp07s/src/lib/venue-billing.ts — deriveVenueBillingState() — seven states, with never_paid, term_end_unknown and not_a_paying_plan all distinguished from a lapse so absence of data is never read as one. VENUE_RENEWAL_POLICY carries ratified:false and renewalPolicyPublicationRefusal() names agreements, order forms, renewal emails and the portal. The machine takes the notice and grace windows as parameters, so ratifying them is a one-line change and no rework.
- _wt-wp07s/src/lib/entitlements-db/venue-billing.ts — recordVenueLapse() is defined by what it refuses to touch: it does not write to entitlements, does not shorten any expiry and does not withdraw a founding number. It refuses a lapse the dates do not support, and refuses without a reason. It writes one audit line recording entitlementsTouched: 0.
- _wt-wp07s/src/lib/entitlements-db/venue-billing.test.ts — 'does not shorten one couple's access, and does not take the founding number' inserts a sponsored entitlement expiring 900 days past term end, lapses the venue, then asserts expires_at is unchanged and status is still active, the founding number is still 01/25, and the audit line says entitlementsTouched: 0. This is the single most important assertion in the package.
- _wt-wp07s/src/lib/venue-billing.test.ts — 'walks current, renewal due, grace, lapsed at the right boundaries' pins the exact instants, including that the instant the term ends is grace and not lapse. 'a lapse stops new issuance and moves no couple's access' exercises accessAfterSponsorshipEvent from venue-lifecycle.ts rather than a local copy, so D-020 has one source.
- _wt-wp07s/docs/strategy/VENUE_RENEWAL_AND_LAPSE_RUNBOOK.md — WRITTEN THIS SESSION — the checkpoint's code pointed at this path and no such file existed. Opens with a ten-row table of which process steps are built and which are not, five marked Not built. Includes the weekly worklist procedure, the state-to-action table, and the five settled consequences of a lapse.
- _wt-wp07s/src/lib/venue-lifecycle.ts — Pre-existing and the strongest asset for this task: accessAfterSponsorshipEvent returns access unchanged for every sponsorship event, canIssueNewSponsorship is false once ended, BRANDING_REMOVAL_DEADLINE_MS is 24h. Linked, never reimplemented.
- _wt-wp07s/scripts/check-venue-edition-contract.mjs — VERIFIED: the runbook is at line 44 of currentCommercialSources, so it is now genuinely covered by the retired-price, retired-cohort and permanence pattern checks. The script exits 0. Note the checks it applies are pricing, cohort size and permanence only — it enforces nothing about the unratified grace window, so criterion 8 gains no protection from it.
- _wt-wp07s/package.json — VERIFIED the 'absent at the checkpoint' claim by parsing `git show HEAD:package.json`: src/lib/venue-billing.test.ts present at HEAD = false, entitlements-db/venue-billing.test.ts present at HEAD = false. Both were genuinely added to the suite this session. This claim was true and it is the kind of claim worth checking, since a test that runs nowhere is the failure mode this programme keeps hitting.
- _wt-wp07/src/server/stripe.ts — VERIFIED no venue price id. PRICE_IDS at line 35-39 carries workspace, studio, wedding, event. ANNUAL_PRICE_IDS at 63-64 carries workspace only. Webhook at app/src/app/api/webhooks/stripe/route.ts handles exactly three events: checkout.session.completed (113), customer.subscription.updated (134), customer.subscription.deleted (159). The build agent's description was accurate.
- Specification: `studio/docs/execution/venue-edition-and-films/tasks/E08.03.md`

### `E08.04` Complete multi-tenant data isolation and database row-level security for venues and couple workspaces.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. Every workspace-scoped read in the app carries a tenant predicate, proven by a static gate that fails CI when a scoped table is read without one.
2. The gate is itself proven to detect a violation — a deliberately unscoped read turns it red — rather than only ever reporting green against a repository that already passes.
3. The tenant boundary has a runtime guard that throws rather than degrading to an unscoped or empty-string-scoped query.
4. The absence of database row-level security is stated explicitly in the evidence with the compensating control named, and no surface implies it exists.
5. A cross-tenant negative test exists per sensitive object (tasks, comments, attachments, share links, invites, entitlements, notifications, activities, sponsorships) proving an id from workspace A returns nothing in workspace B, against a real database rather than by source inspection.
6. The negative suite carries a control case proving the unscoped read would have leaked, so the assertions cannot pass vacuously.
7. No venue-scoped read can reach Notes, Tasks, Timeline or Signal content, enforced at the capability, module-reachability and projection layers.
8. Every tenant-keyed table is governed, and the governed list cannot drift from the schema in either direction.
9. The evidence names, by file, every place a tenant predicate is enforced, so a reviewer can audit the list rather than trust it.

**Evidence**
- _wt-wp07/src/server/tenant-scope-rules.mjs — NEW. Detection rules extracted as pure functions so their sensitivity can be tested. Closes the verified false negative by splitting scope tokens by strength: strong tokens (workspaceId, userId, sponsorId, workspaceSlug) count anywhere after .from(; weak bare-column tokens (.id, .token, .slug, .code, .email) count only inside .where(.
- _wt-wp07/src/server/tenant-scope-rules.test.mjs — NEW, 16 cases. Proves the detector actually detects, over synthetic statements with known answers. Includes the join-ON-clause false negative pinned as a permanent regression test, and an explicit test recording the accepted substring-matching limitation.
- _wt-wp07/src/server/cross-tenant-negative.test.ts — NEW, 5 tests. The first BEHAVIOURAL isolation test in the repo — real in-memory libSQL, two workspaces, nine sensitive tables. Asserts a scoped read returns only the caller's rows, and returns nothing when handed the other tenant's primary key. Carries a control case asserting the unscoped read returns both tenants' rows, so the negatives cannot pass vacuously.
- _wt-wp07/src/server/tenant-scope.test.mjs — Rewritten to consume the rules module. Keeps its three invariants; MIN_EXPECTED_READS raised 170 to 200 against a measured 247 reads across four surfaces.
- _wt-wp07/src/server/db/tenant.ts — Pre-existing and correct, kept unchanged. byWorkspace() throws on an empty workspace id and states the no-RLS position plainly. Now also asserted behaviourally in cross-tenant-negative.test.ts.
- _wt-wp07/package.json — Both new suites wired into `pnpm test`. Verified they execute in the full run.
- evidence/E08.04-E08.05-tenancy-and-access-control.md — Evidence document: criterion-to-proof table, the before/after mutation table, the four known limitations of the control, and the full enforcement file list.
- tasks/E08.04.md — Task specification written to the TASK.md template.
- _wt-wp07/src/server/db/daily-digest.ts:90-104 — REFUTING. Live production read of the tenant-keyed activities table with NO tenant predicate in its WHERE (only activities.kind and activities.createdAt), limit 50, then filtered in JavaScript by whether the snippet contains @handle. I ran the shipped classifier against this exact statement: it returns scoped=true, evidence=userId - GREEN purely because userId appears in the leftJoin ON clause at line 96. Lines 113-116 then read a task title by eq(tasks.id, a.taskId) with no workspace predicate,
- _wt-wp07/src/app/app/inbox/page.tsx:82 — REACHABILITY. compileDailyDigest(me, ws) renders the signed-in inbox page, so the unscoped read above is on a live user-facing surface. Also reachable at src/app/api/cron/digest/route.ts:130, which emails the digest. This is not dead code.
- _wt-wp07/src/server/tenant-scope-rules.mjs:58-89 — ROOT CAUSE. STRONG_SCOPE_TOKENS is commented 'Tokens that name a tenant column outright' but contains taskId, task_id, parentTaskId, noteId, publicationId and clerkId, none of which is a tenant column. Strong tokens count anywhere after .from(, joins included. So the fix closed one instance of the join-ON false negative (the weak .id case) and left the class open.
- _wt-wp07/src/server/db/tenant.ts:14-18 — OVERSTATED. The docstring claims 'Routing every workspace-scoped collection read through it gives ... a RUNTIME guard'. byWorkspace() has exactly 6 production call sites, all in src/server/db/queries.ts, against 239-247 governed reads. The two other files that mention the name (src/modules/signal/lib/data/source.ts, src/server/actions/project-overview.ts) do so as a local Map variable and a comment, not calls. The guard is real and throws; it covers roughly 2.5% of scoped reads. Not listed among
- Specification: `tasks/E08.04.md`

### `E08.05` Complete role-based authentication and authorisation across venue owners, managers, viewers and couples.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. The role set is enumerated in one place per surface — venue roles in studio, workspace roles in the app — with no third undocumented set, and any divergence between documentation and code is stated explicitly rather than reconciled silently.
2. Every role check runs server-side; no capability is enforced only by hiding a control in the UI.
3. Every cell of the venue capability matrix is asserted against the shipped module, not against the test's own copy, and a capability added to the module without a recorded decision fails the build.
4. No venue role may hold a capability naming couple content or couple identity, proven by a mutation that adds one and turns the suite red.
5. The venue-facing read path cannot reach the app database, and the venue projector cannot fetch anything at all.
6. The Signal HQ session token is not derivable from the password, carries a server-side expiry, cannot be forged by editing its issue time, and does not survive password rotation — each proven by a test written to fail against the previous implementation.
7. Every /hq surface is gated or waived in writing, and the session token is minted in exactly one place.
8. Whatever is not implemented by 1 September is stated as not implemented, with the interim operating model named (D-016, R-043).
9. The evidence cites the guard by file for each role check, not a description of one.

**Evidence**
- _wt-wp07s/src/lib/account/roles.ts — FIXED. AccountCapability was a bare union type, which cannot be enumerated at runtime — the root cause of the guard failure below. Now ACCOUNT_CAPABILITIES is a runtime `as const` array with the type derived from it, plus ACCOUNT_ROLES derived from ROLE_CAPABILITIES' keys (which TypeScript already forces to be exhaustive).
- _wt-wp07s/src/lib/account/role-matrix.test.ts — FIXED on two counts. (1) It was NEVER WIRED INTO `pnpm test` — it had not run in CI once, while roles.ts cited it in its own header as the enforcement. (2) It iterated its own hand-written copy of the capability list, so it could not see a new capability at all. Now reads ACCOUNT_CAPABILITIES/ACCOUNT_ROLES from the shipped module and adds a suite asserting the recorded matrix matches the module in both directions.
- _wt-wp07s/package.json — role-matrix.test.ts wired into the studio test script. Confirmed it now executes in the full run.
- _wt-wp07s/src/lib/hq/auth.ts — AUDITED AND KEPT unchanged — the predecessor's v2 rewrite is correct. v1 cookie was sha256('signal-hq-session:v1:'+password): derivable offline, an offline oracle for the password itself, and no server-side expiry. v2 is `v2.<issuedAt>.<HMAC>` over a PBKDF2-150k-derived key. Its header states plainly that this is still a shared password and that a venue must never be given it.
- _wt-wp07s/src/lib/hq/auth.test.ts — AUDITED AND KEPT. Twelve malformed-input cases fail closed; each property test is written to fail against v1 and does. Wired into CI by the checkpoint.
- _wt-wp07s/src/lib/hq/hq-authorization.test.mjs — AUDITED AND KEPT, 5 tests passing. Pins that proxy.ts verifies rather than recomputing, that every /hq page and route handler is gated or waived in writing, that only auth.ts may mint a token, that no venue-facing module may reach TASKS_DATABASE_URL, and that the venue projector cannot fetch.
- evidence/E08.04-E08.05-tenancy-and-access-control.md — Section 3 carries the full role and permission matrix: venue owner/manager/viewer with the enforcing file per capability, plus couple, collaborator, guest (a token, not a role), venue (no identity exists) and HQ operator. Also records the unreconciled four-role-doc vs three-role-code disagreement.
- tasks/E08.05.md — Task specification written to the TASK.md template.
- _wt-wp07s/src/app/hq/account-review/actions.ts — DEFECT I FOUND, not in the agent's evidence. `recordMoreAccessRequestAction` (lines 33-55) is a real server action that writes a row via createSponsorRequest. Its only guard is requireHqAccountReview() -> verifyHqToken, i.e. the shared HQ password. It performs NO AccountRole check. The `request_access` capability it implements is gated only client-side.
- _wt-wp07s/src/app/hq/account-review/account-review.tsx — DEFECT I FOUND. Line 60: `const [role, setRole] = useState<AccountRole>("owner")`. The role is client state chosen from a picker. There is no server-side notion of which role a caller holds anywhere in the product, so no venue role check can run server-side by construction.
- Specification: `tasks/E08.05.md`

### `E08.06` Secure invitation tokens against guessing, reuse, unintended forwarding, replay and duplicate redemption.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. A newly minted couple invitation code carries at least 48 bits of entropy, with the arithmetic computed in a test rather than asserted in prose, and the test fails if the length or alphabet is reduced.
2. Exactly one invitation-code generator exists per repository; a source-contract test fails if a local re-implementation reappears.
3. Code characters are drawn uniformly. The test proving it carries a control that runs the previous biased draw and must fail the same bound, so a bound too loose to detect the defect cannot pass as a green test.
4. share_links stores sha256 under a UNIQUE index and never the secret; proven by dumping the whole table after a mint and asserting the secret is absent, against a database built from the checked-in migration 0027 SQL.
5. Neither the stored hash nor the public row id opens a share link; both are proven to resolve to null.
6. Every share link minted before migration 0027 still resolves, and a backfilled row cannot be opened by its public id through the legacy branch.
7. Share-link resolution never crosses a tenant boundary and never revives an archived workspace, proven on both branches of the dual-read resolver.
8. The redemption path consults its attempt budget before it looks the code up, proven by a source-contract test on statement order, so no unmetered 'does this code exist' oracle exists.
9. Every test above runs in pnpm test. A security test that is not registered is not a control.
10. The limiter's failure mode (open, and unenforced until Upstash is provisioned) is stated in the source file, not only in a document.

**Evidence**
- evidence/E08.06-invitation-and-share-token-security.md — Full criterion-to-proof trace, the audit of the interrupted checkpoint, the two-live-redemption-writers finding, and the real command output including the two failures seen and how they were attributed.
- _wt-wp07s/src/lib/invitation-code.ts — The single studio generator. 31-symbol alphabet, 10-character body = 31^10 ≈ 2^49.5, drawn with crypto randomInt (unbiased). Replaces generateCode(prefix,5) in scripts/issue-codes.ts (31^5 ≈ 2^24.8) and genLicenseCode() in hq/entitlements/actions.ts (31^8 ≈ 2^39.6).
- _wt-wp07/src/lib/comp-code.ts — The app-side generator, extracted out of the 'use server' comp.ts so a test can load it. Replaces newCode(), which was 7 hex chars off a UUID = 2^28 with a Math.random() fallback. Rejection sampling; throws rather than falling back when no CSPRNG exists.
- _wt-wp07/src/server/invitation-code-security.test.ts — 13 tests. The uniformity test carries its own control: it runs the old randomBytes%31 draw in the same run and asserts it EXCEEDS the bound the new draw must sit under, so a bound too loose to detect the defect cannot pass as green.
- _wt-wp07/src/server/share-token-security.test.ts — 23 tests, all now passing. Before this session it had NEVER RUN: it imported resolveShareLinkRowWith from @/server/db/queries (server-only, and no such export) so it threw on load, and it was not registered in pnpm test.
- _wt-wp07/src/server/db/share-link-resolver.ts — Rewired to import nothing server-only and take its database as an argument (the Timeline recordQualifiedViewWith pattern). The checkpoint's version imported db from ./index, which pulled server-only back in transitively and defeated its own stated purpose.
- _wt-wp07/src/server/actions/comp.ts — redeemCompCodeImpl now resolves identity and spends redeemWithinAttemptLimits BEFORE the comp_codes lookup. The limiter's fail-open behaviour and the unprovisioned Upstash dependency are stated in the file and asserted by a test.
- _wt-wp07/src/server/share-revocation-contract.test.mjs — Was RED on main: it asserts the archived-workspace guard inside queries.ts, and the checkpoint moved that guard out. Repointed at the resolver and strengthened to count both branches of the dual read.
- _wt-wp07s/scripts/issue-codes.ts — The script whose own comments call the Tasks comp_codes row 'runtime redemption' and the studio license_codes row 'sponsor audit'. This is the proof that the guarded path is the audit copy and the unguarded one is what couples reach.
- tasks/E08.06.md — EXISTS, 7,560 bytes. Status 'Founder Review', sign-off block empty. Non-goals section names recipient locking, revocation and the unmerged writers explicitly. Scanned for banned wording: no VAT statement, no seat count, no permanence/'for life'/'forever', no 'partner'/'guaranteed'/'certified', no legal-approval implication. Clean against D-009/D-016/D-020/D-021.
- _wt-wp07s/src/app/hq/entitlements/actions.ts — EXISTS. Line 25 imports generateInvitationCode; line 235 is its only mint site. genLicenseCode is gone (grepped both repos: zero occurrences outside the assertion that it is absent).
- _wt-wp07/src/server/db/queries.ts — PRODUCTION CALLER CONFIRMED. Line 27 imports resolveShareLinkRowWith; line 715 binds it to the live `db`. Grepped: this is the only non-test caller, so the tested control is the one that runs. This is the failure mode the programme keeps hitting (a control tested but never called) and it does NOT apply here.
- _wt-wp07/drizzle/0027_share_link_token_hash.sql — EXISTS. I checked the breakpoint trap specifically: the first segment before `--> statement-breakpoint` contains the header comment AND `ALTER TABLE share_links ADD COLUMN token_hash TEXT;`. It is NOT a comment-only first segment. All three statements run. Journal entry idx 27 present; ledger carries sha256 a4d68de2… bound to receipt drizzle/receipts/share-link-token-hash-2026-08-03.json.
- _wt-wp07/src/server/actions/share.ts — MINT PATH VERIFIED. createShareLinkAction writes token=generateSharePublicId(), tokenHash=hashShareToken(secret), tokenScheme='sha256', and returns the secret once. I also chased the UX regression the spec flags: share-button.tsx builds `/share/${token}` only from the freshly-minted secret (line 116); the manage-links rows use link.token solely as a revoke id, so no dead share URL is rendered to owners.
- _wt-wp07/src/lib/ratelimit.ts — FAIL-OPEN CONFIRMED IN CODE, not just in prose: `if (!url || !token) return true;` plus `if (!res.ok) return true;` plus `catch { return true; }`. Matches what comp.ts tells the reader.
- Specification: `studio/docs/execution/venue-edition-and-films/tasks/E08.06.md`

### `E08.07` Secure photograph and attachment uploads with file validation, malware controls, size limits and private storage.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. An upload is refused unless its declared type is on an allowlist; types nobody enumerated are refused by absence, proven for ten types including SVG, HTML, XML and executables.
2. A file whose bytes disagree with its declared type is refused, proven by the exact bypass the previous denylist had: HTML content declared image/png.
3. The MIME type written to attachments.mime_type is the validated type, never the raw client string, proven by a source-contract test.
4. Validation runs before the metadata row is claimed, so a refused file never reaches the database.
5. Formats with no dependable magic number are marked as unverified rather than reported as checked.
6. The download route does not redirect to a blob URL. Private blobs are streamed through the route, after the workspace check, proven by a source contract; put and get are proven to agree on access: 'private'.
7. The security headers (nosniff, sandbox CSP, private no-store) apply to every backend, with exactly one header block so a future branch cannot skip them.
8. A cross-tenant attachment id still returns 404, never 403.
9. The production storage backend is reported as it actually is, and the absence of malware scanning is stated with its cost and its trigger.

**Evidence**
- evidence/E08.07-attachment-upload-security.md — Criterion-to-proof trace, the four-row audit of what the task title claims against what existed, the malware-scanning cost table with a recommendation, and an explicit list of what the evidence does NOT claim.
- _wt-wp07/src/lib/upload-validation.ts — The allowlist with byte signatures. Replaces an 8-entry DENYLIST checked against the client-supplied file.type string — two compounded failures: anything not on the list passed, and the label could simply be changed. SVG/HTML/XML/executables are refused by ABSENCE, which is the point.
- _wt-wp07/src/server/attachment-upload-security.test.ts — 16 tests. Includes the exact old bypass — HTML content declared image/png — plus MZ-executable-as-PDF, SVG-as-PNG, JPEG-as-PNG and RIFF/AVI-as-WebP (which checks the WebP wildcard bytes have not made every RIFF file a WebP).
- _wt-wp07/src/server/storage.ts — chooseBackend() returns 'vercel-no-token' and putBytes THROWS when BLOB_READ_WRITE_TOKEN is absent. New openBlobStream() reads via get(url, {access:'private'}) with the store token, because a private blob URL is not fetchable by a browser.
- _wt-wp07/src/app/api/attachments/[id]/route.ts — Response.redirect(url, 302) to a PRIVATE blob is removed. That redirect sent the browser to a URL requiring an Authorization header it does not have; it had never fired only because the store is unprovisioned, so it would have fired on the day the token was added. Security headers now cover both backends from one block.
- studio/content/hq/operator-todos/premium-blob-storage.md — status: open, priority: P1, blocking: true. This is the proof that BLOB_READ_WRITE_TOKEN is unprovisioned and therefore that attachment upload is broken in production today.
- _wt-wp07/node_modules/.pnpm/@vercel+blob@2.6.1/node_modules/@vercel/blob/dist/index.d.ts — The SDK's own type declarations, lines 128-132 and 202-212: "'private': The blob requires authentication to access" and get()'s note that "The authorization header is automatically set". This is the static proof that the 302 could not have worked.
- _wt-wp07/src/server/actions/attachments.ts — Not in the submitted evidence list but it is where the change lands. `validateUpload(` at line 181, `.insert(attachments)` at line 214 — C4 verified by reading the file, not only by the offset test. `const mimeType = verdict.mimeType` at line 195 verifies C3. `verdict.sniffed` is never read.
- upload-path completeness check (my own, not the builder's) — I grepped for every byte-writing path: `putBytes`, `arrayBuffer()`, `instanceof File`, `insert(attachments)`, `insert(resources)`. `uploadAttachmentAction` is the ONLY one. `addLinkResourceAction` inserts links, never bytes. So there is no second, unvalidated upload route — the coverage claim is genuine.
- Specification: `studio/docs/execution/venue-edition-and-films/tasks/E08.07.md`

### `E08.08` Complete audit logging, operational logging, error monitoring and alert escalation.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. The audit log and the operational log are defined in one place in code, naming both audit tables (workspace_events, entitlement_events), and a test fails if that definition is removed.
2. No server-scope console call may log an email address, a rendered email body, a task or workspace title, guest or dietary data, or a URL that can be a bearer token, proven by a test shown to fire on each of those seven shapes and shown NOT to fire on four ordinary error logs.
3. A new server-scope logging site cannot be added without a change to a recorded list, proven by a ratchet test whose comparison direction is itself asserted, plus a guard that fails if the scan finds nothing.
4. The two identified leaks are closed at source: the CSP collector no longer writes /share/<token> to the log, and sendEmail no longer writes the recipient or a body preview outside NODE_ENV development.
5. On a database built from the checked-in entitlements baseline, UPDATE and DELETE against entitlement_events are refused after apply-audit-ledger-triggers.mjs runs, and demonstrably NOT refused before it runs.
6. A command reports the absence of append-only enforcement and exits non-zero; the integrity checker treats absence as a failing run rather than a clean one.
7. No source comment claims an enforcement the schema does not deliver, held by a test that fails if the retired wording returns.
8. The state of error monitoring and alerting is recorded as fact, including naming what is not provisioned, and no surface implies monitoring exists that does not (D-016).

**Evidence**
- _wt-wp07/src/server/operational-log.ts — NEW. The written distinction plus the only sanctioned console sink. opLog takes scalars only, so spreading a database row into a log call does not type-check. Exports redactForLog/redactEmail/safeError, reusing sentry-scrub's redactSensitiveUrl so the console path applies the same rule Sentry already applied.
- _wt-wp07/src/server/operational-log-contract.test.mjs — NEW, 8/8 pass, wired into pnpm test. RULE 1 ratchet over 51 recorded logging sites across 31 files (unrecorded file fails, grown count fails, stale entry fails). RULE 2 denylist proven to fire on document-uri, args.to, args.html, task.title, guest list, row.dietary, distinctId, and proven not to fire on four ordinary error logs.
- _wt-wp07/src/app/api/csp-report/route.ts — LEAK CLOSED. logViolation logged the browser-supplied document-uri verbatim; on a shared workspace that URL is /share/<token>, a bearer credential, written into the platform function log. sentry-scrub already redacted exactly that shape, so the same string was safe when thrown and unsafe when logged.
- _wt-wp07/src/server/email.ts — LEAK CLOSED. sendEmail logged recipient address, subject and the first 120 chars of the body whenever RESEND_API_KEY was unset. That is a configuration state, not an environment, so a production deploy that lost the key would have logged every couple's address and a slice of every wedding email. Full payload now gated on NODE_ENV development with an op-log-safe waiver; every other environment gets the recipient domain only.
- _wt-wp07s/scripts/apply-audit-ledger-triggers.test.mjs — 9/9 pass, now wired into pnpm test (it never ran before). Includes the case 'DEFECT: a database built from the baseline lets an audit line be edited and deleted', which passes because that is the true state of a fresh database. I added the ninth case: a source-contract test that fails if audit.ts re-states the retired enforcement claim.
- _wt-wp07s/src/lib/entitlements-db/audit.ts — FALSE CLAIM CORRECTED. Header said the physical append-only guarantee 'is enforced by SQLite triggers from migrate-access.mjs'. True of one hand-run database, false of every database built since the 2026-07-31 reset: grep -c 'CREATE TRIGGER' drizzle-entitlements/0000_init.sql returns 0 because drizzle cannot express a trigger. Now states plainly that the hash chain DETECTS and the triggers PREVENT, and names the two triggers and the commands.
- _wt-wp07/src/instrumentation.ts — MONITORING FACT. Line 17 returns early when SENTRY_DSN is unset. studio/docs/INFRASTRUCTURE.md records Sentry, PostHog, Upstash and production Anthropic as 'integrated in app code, none provisioned'. An unhandled production error is currently observed by nobody, and there is no alerting mechanism of any kind in either repository.
- _wt-wp07/src/server/db/schema.ts — AUDIT GAP, STATED NOT CLOSED. workspace_events (line 988) is written from exactly two sites in src/server/actions/settings.ts, kinds inviteSent and inviteAccepted. Member removal, role change, share-link create/revoke, publish and account deletion are unaudited. Those writers sit in files other groups are editing concurrently this wave.
- studio/docs/execution/venue-edition-and-films/evidence/E08.08-E08.09-observability-and-continuity.md — Evidence document, criterion to proof, covering both tasks, including the five checkpoint defects and the pre-existing studio lint red.
- _wt-wp07s/scripts/apply-audit-ledger-triggers.mjs — EXISTS (8620 bytes). Read in full. Not a drizzle migration, so the `--> statement-breakpoint` comment-first-segment trap does not apply; DDL is executed statement by statement via client.execute. --verify path exits 1 on missing triggers. I ran all three modes against a real database.
- evidence/E08.08-E08.09-observability-and-continuity.md — EXISTS (17398 bytes). Criterion-to-proof structure is genuine. Section C3 is titled 'A new logging site cannot appear unreviewed', which is the overstatement my probe refutes, and it repeats the incorrect 51-sites figure. Sections C4-C7 are accurate. Em dashes present (21) but this is an internal evidence doc, not front-facing copy, so the brand-voice rule does not bind it.
- Specification: `studio/docs/execution/venue-edition-and-films/tasks/E08.08.md`

### `E08.09` Complete backups, restore testing, disaster-recovery procedures and data-integrity verification.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. A backup of a real database can be taken and then restored into a throwaway database, with per-table row counts and content hashes recomputed FROM THE RESTORED DATABASE and compared against the manifest, executed at least once with the elapsed time recorded.
2. The restore path refuses any target that is not a local file, so a rehearsal cannot become a production overwrite.
3. A restore that loses rows, alters a value, loses an index, or loses a trigger is each detected, proven by a test per failure mode.
4. A recovery drill exercises the real entitlements schema, carries sponsors, license codes, redemptions and the audit ledger, and proves append-only enforcement on the RESTORED copy rather than on the source.
5. Every module database can be backed up by one command, and a run that covered only some of them exits non-zero.
6. Data-integrity invariants exist over both databases, are SELECT-only by contract, and each one is proven to fire by a test that plants exactly one violation and requires that check, and only that check, to report it.
7. An integrity check whose table is absent is reported as skipped, never as passing.
8. No database dump is committed to a repository, and the backup output directory is git-ignored.
9. The runbook states the actual provider, the actual tier, the actual backup guarantee, and the data-loss window as a number, and names what is not built as not built.

**Evidence**
- _wt-wp07/scripts/db/restore-verify.mjs — AUDITED AND KEPT from the interrupted checkpoint. Correct in the ways that matter: it re-reads the restored database rather than the dump, sorts rows before hashing so the hash describes a set not an engine's return order, installs triggers LAST (an append-only trigger installed before the rows would refuse its own restore), and throws on any non-file: target (lines 45-50). I fixed one doc reference that pointed at studio/docs/RECOVERY.md as if it were in the app repo.
- _wt-wp07s/scripts/recovery-drill.mjs — NEW, replacing scripts/run-recovery-drill.mjs which I DELETED. The old one could not fail: it created a two-row table called recovery_probe that exists nowhere in this product, JSON.stringify'd it, compared the string to itself, then wrote {"release":"v1"} to a file and read it back. It was wired to pnpm recovery:drill while docs/RECOVERY.md listed 'rehearse one restore' as the open Critical action, so a green drill read like that action was closed. The new one builds the real entitlements schem
- _wt-wp07s/scripts/recovery-drill.test.mjs — NEW, 7/7 pass, wired into pnpm test. Four mutation cases prove the drill can fail: a lost row (count differs), an altered value at the same count (hash differs - the mutation is deliberately a founding venue's annual_amount_cents rewritten 100000 to 150000), a lost index, and the important one: every row replayed with the triggers dropped, where the data compares byte-identical and the drill still fails because the restored ledger accepts an UPDATE.
- _wt-wp07s/scripts/check-entitlements-integrity.test.mjs — WAS RED, NOW 21/21. The checkpoint's version shared ONE database across every case, so paid-venue-without-term reported 4 violations against 1 expected and paid-venue-without-price-agreement reported 2 against 1. It had never been run. Rewritten with a fresh database per case and a strictly stronger assertion: the planted violation must be the ONLY check that fires. Also stopped hand-copying the sponsor_price_agreements DDL (a fourth copy of a money table) and now imports it from Group A's scrip
- _wt-wp07/scripts/db/backup-all.mjs — NEW. Backs up every module from studio/docs/INFRASTRUCTURE.md in one command, restore-verifies each, writes into the existing workspace db-archive/<date>/ convention, and EXITS NON-ZERO on partial coverage. Exercised: with one module credentialed it reported '1 backed up, 5 skipped' and exit 1; with --modules=tasks it reported full coverage and exit 0.
- _wt-wp07/scripts/db/integrity-check.test.mjs — AUDITED AND KEPT, 27/27, now wired into pnpm test (it never ran before). Builds its fixture from the real drizzle migration SQL from baseline 0014 forward, splitting on '--> statement-breakpoint' and never on semicolons - the migration trap this workspace has paid for twice, handled correctly by the predecessor.
- _wt-wp07/.gitignore — DEFECT FIXED. The checkpoint committed a 307KB database dump (.db-backups/tasks-local-2026-08-03T07-30-04-141Z.jsonl, 462 rows) and .db-backups/ was not ignored. Content was synthetic (person0@example.test) so nothing personal leaked, but the next run against a real database would have committed real couple data. Removed from index and tree; directory now ignored. The bytes remain in the checkpoint commit's history.
- _wt-wp07s/docs/RECOVERY.md — REWRITTEN against 2026-08-03 truth. Turso account ethan387 is FREE tier so PITR (the 2026-06-19 runbook's top recommendation) is unavailable and the budget is zero (D-015 Q5); nothing takes a backup on a schedule; the only backup of production is db-archive/2026-07-31/, a manual single copy on one laptop covering the PREVIOUS generation of databases, never restored; the data-loss window is four days.
- _wt-wp07/scripts/db/backup.mjs — EXISTS. tableHash at lines 96-101 DOES sort before hashing. I proved it: same row set in reversed order hashes equal. This is the correct implementation and the studio drill's copy of it is not (see unmetCriteria).
- _wt-wp07/scripts/db/integrity-check.mjs — EXISTS. 16 checks, SELECT-only guard real at lines 226-236. I ran it against the real tasks.db: 16 checks, 0 failing, 0 warning, 0 rows implicated, exit 0 - matches the claim. BUT it has no skip concept whatsoever: against a database missing the schema it dies on the FIRST check with 'db:integrity: SQLITE_ERROR: no such table: workspaces' and the other 15 never run.
- _wt-wp07s/scripts/check-entitlements-integrity.mjs — EXISTS. Skip logic is real (lines 243-253 push {skipped: 'table(s) absent: ...'}). Against an empty database it printed 16 [SKIP] lines and '0 checks run, 16 skipped, 0 failing, 0 warning' - but EXIT=0. Count is 16 checks, not the 17 RECOVERY.md states twice.
- Specification: `studio/docs/execution/venue-edition-and-films/tasks/E08.09.md`

### `E08.10` Set and test performance budgets across portal, couple workspace, Timeline images and public artifact.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. Every budget in the contract carries a measured value, the population that value was computed over, the date it was measured, and the basis of both its target and its ceiling. No budget is a bare number.
2. A measured value above its ceiling fails the run with a non-zero exit code, proven by deliberately breaching a ceiling and observing the failure, not by reading the code.
3. A budget whose population is zero never reports 'ok'. It either fails with exit 3, or, where the emptiness is a known fact, reports 'no data (declared)' against a reason recorded in the contract.
4. A missing production build, a missing imageRoot, a changed build-manifest shape, or a contract naming a budget the script cannot produce each fail with exit 3 rather than reporting a measurement of zero.
5. The check runs in CI in both repositories, after pnpm build, on every pull request and every push to main.
6. The checker's own rules are exercised by --self-test, which runs in CI and needs no build, so the enforcement logic is proven and not merely present.
7. Every budget the check does NOT measure (LCP, INP, CLS, server p75, per-route first-load JS, uploaded-image delivery, queries per page) is named in the contract with the reason it cannot be measured today and the instrument that would be needed. None is reported as passing.
8. No surface produced by this task claims a web-vitals, device or throttled-connection measurement has been taken. None has.

**Evidence**
- _wt-wp07/scripts/check-performance-budgets.mjs — Rewritten from the checkpoint version. The ratchet logic is extracted into a pure evaluate(contract, measured, populations) so --self-test can prove it without a build. Every number now carries its population; population 0 exits 3 unless the contract declares it, and then prints 'no data (declared)', never 'ok'. A missing imageRoot now exits 3 instead of walking to zero.
- _wt-wp07/contracts/venue-surface-performance-budgets.v1.json — App budgets. repo_images now carries emptyPopulation:{expected:true,reason:...} stating that app/public holds five SVGs and no raster image at all. The checkpoint's basis text ('no image exceeds 200 KB') was true of an empty set and read as a passing measurement.
- _wt-wp07s/contracts/venue-surface-performance-budgets.v1.json — Studio budgets. repo_images ceiling 657 KB against a 200 KB target: public/brand/collateral/venue/venue-deck-preview.png is 656.2 KB and is venue-facing.
- _wt-wp07s/scripts/measure-performance-baseline.mjs — Retired. It walked five sibling repositories, four merged into `app` on 2026-07-31, printed built:false/gzipBytes:0 for each and exited 0. Now prints why, names pnpm perf:budgets, and exits 3. Verified exit 3 at close. NOTE: this file was briefly reverted by a concurrent lane mid-session.
- _wt-wp07/.github/workflows/verify.yml — Both repositories now run perf:budgets:self-test then perf:budgets after pnpm build, plus the two journey-coverage steps.
- evidence/E08.10-performance-budgets.md — Evidence: the measured numbers from real builds in both repositories, the breach proof with exit codes, the self-test output, the unmeasured list, and the statement that the E08.10 gate criterion cannot be satisfied as written.
- _wt-wp07s/scripts/check-performance-budgets.mjs — EXISTS, tracked. diff --strip-trailing-cr against the app copy returns IDENTICAL, so 'same checker' is true and not two drifting forks.
- _wt-wp07s/.github/workflows/verify.yml — EXISTS, tracked. perf steps at lines 44 and 46 after pnpm build at line 28. Triggers: pull_request and push to [main, feat/remediation-program]. grep confirms budgets:check and performance:measure appear in NO workflow in either repo.
- tasks/E08.10.md — EXISTS, 8293 bytes. This is the stale artefact: deliverable 6 unchecked and the Risks section asserts performance:measure 'is still live and still exits 0 measuring nothing', which my run refutes.
- Specification: `studio/docs/execution/venue-edition-and-films/tasks/E08.10.md`

### `E08.11` Complete unit, integration, end-to-end, browser, responsive and device test coverage for the entire sponsored journey.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. The real current pass/fail state of both suites is established by running them, with actual counts recorded, not inferred, and not reported as passing without a run.
2. Every segment of the sponsored journey is enumerated: venue invited, venue pays, founding number assigned, venue issues an invitation, couple redeems, couple plans, couple publishes, couple leaves, venue reads metrics.
3. Every segment is either mapped to named test files or declared uncovered with a reason and a named owner. A segment that is neither fails the check.
4. A named covering test file that exists but is not executed by pnpm test fails the check. Proven by the check's own self-test, not asserted.
5. Both suites pass end to end with the exact commands from package.json, and the recorded output is the real output.
6. The privacy boundary of the venue axis is pinned by a test that runs: the only venue-directed read in the app repository projects four aggregate numbers and can reach no couple content.
7. The check and its self-test run in CI in both repositories.
8. What is not covered is stated as not covered. No segment is described as tested because a test file exists.

**Evidence**
- _wt-wp07/scripts/check-journey-coverage.mjs — Expands `pnpm test` — following pretest and any chained `pnpm <script>` — into the set of files CI actually runs, then fails when a file named as covering a segment is missing OR is not in that set. --self-test proves 11 rules including 'an ORPHANED covering test fails'.
- _wt-wp07/contracts/sponsored-journey-coverage.v1.json — App segment map: 6 covered (couple_redeems, couple_workspace_is_private, couple_plans, couple_publishes, venue_reads_metrics, couple_leaves), 2 declared uncovered (browser_responsive_device, attribution_is_correct).
- _wt-wp07s/contracts/sponsored-journey-coverage.v1.json — Studio segment map: 7 covered, 2 declared uncovered (renewal_and_lapse_are_operated — nothing exists to test; browser_responsive_device).
- _wt-wp07/src/app/api/internal/partner-stats/partner-stats-boundary.test.mjs — THE ORPHAN I WIRED. 193 lines, 6/6 passing, executed by nothing until this session. It pins that the only venue-directed read in the app projects four aggregate numbers, reaches no couple-content table, fails closed when the shared secret is unset, and that its missing D-011 floor is acknowledged in the route rather than forgotten.
- _wt-wp07/src/server/share-token-security.test.ts — THE ORPHAN THAT COULD NOT RUN. 444 lines. Imported resolveShareLinkRowWith from @/server/db/queries, which begins `import "server-only"` — unresolvable by the Node test runner: 'Error: Cannot find module server-only'. Its source-contract block also sliced queries.ts from a function that does not live there, yielding an empty string every assertion would match against. Repaired by its owning group; now runs.
- evidence/E08.11-sponsored-journey-test-coverage.md — Evidence: the red-suite finding, the seven orphans with line counts and fates, real suite counts, the segment maps, the observed flake, and the four declared gaps.
- _wt-wp07/.github/workflows/verify.yml — Triggers on pull_request and push to main. Journey-coverage self-test and check both present — but positioned after `pnpm build`.
- _wt-wp07s/.github/workflows/verify.yml — Triggers on pull_request. Both coverage steps present, after remediation:check, recovery:drill, typecheck, test and build. Note studio's verify.yml runs no lint at all, and ci.yml runs lint with continue-on-error: true — so the builder's statement that the lint error 'gates nothing today' is verified true.
- _wt-wp07/src/app/api/internal/partner-stats/route.ts — Read in full. Carries the R-027 acknowledgement and the 'not venue-facing' wording the test pins. The projection really is two timestamps and the response really is four numbers TODAY — the defect is that the test would not stop that changing.
- C:/Users/ethan/AppData/Local/Temp/.../scratchpad/studio-test.log — My run 1 of studio pnpm test: STUDIO TEST EXIT: 1, group 3 `tests 57 / pass 56 / fail 1`, founding-numbers.test.ts file-level failure with all 16 subtests green.
- C:/Users/ethan/AppData/Local/Temp/.../scratchpad/mut/ — Mutation harness — a scratchpad COPY of route.ts + the boundary test. Both worktrees verified untouched afterwards (git status on the partner-stats directory is empty).
- Specification: `studio/docs/execution/venue-edition-and-films/tasks/E08.11.md`

### `E08.12` Complete security review, production-readiness review, release checklist, rollback process and incident-response runbook.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. The review contains findings, each verified against current code by file and line, with a severity, an exploitation path in concrete terms, and a state of closed, reduced or open. A finding with no failure scenario is not a finding.
2. R-027, R-028, R-031, R-032 and R-033 each appear with their true current state, and no fix is described as landed in production when it has only landed in a branch.
3. The production-readiness verdict is allowed to be, and is, negative where the evidence is negative, and names the specific conditions that must be met before the first venue is contacted.
4. The release checklist is executable by one person: every step is a command or an observation with a stated pass condition, and every step requiring a founder decision says so rather than assuming it.
5. The rollback procedure covers code, configuration and schema separately, states plainly which migrations cannot be rolled back and what is done instead, and gives a time budget per step.
6. The incident runbook names a person, a channel and a clock for each of the five scenarios the data gate lists, records the 72-hour Article 33 clock and where the DPC is contacted, and records the absence of paid on-call as an accepted limitation of the zero budget.
7. Nothing in any of the four documents states or implies legal approval, solicitor review, accountant verification or unqualified GDPR compliance.
8. Every command in every document is real, runnable in this stack, and taken from package.json, DEPLOY.md or the Vercel and Turso CLIs, not invented.

**Evidence**
- evidence/E08.12-security-and-production-readiness.md — Ten findings F-01 to F-10, each verified in code by file and line, each with a concrete failure scenario and a state. Verdict: NOT READY to contact a venue, with four named conditions. Includes a table restating every open register entry against its verified state — R-027 and R-028 would read as closed to anyone checking the branch and are recorded as not closed, because nothing is closed until it is in production.
- evidence/E08.12-release-checklist.md — T-7 preconditions (four open founder decisions plus the two gate items), T-1 verification with the exact package.json commands and expected counts, the receipt-backed db:migrate sequence from DEPLOY.md §4 with the backup-and-restore-verify step first, the both-repositories redeploy requirement from the launch operator-todo, and an outside-in smoke test from a phone in incognito.
- evidence/E08.12-rollback-procedure.md — Opens on the one thing that matters — code rolls back in a minute, data does not roll back at all — then a two-minute decision tree, vercel promote for code, a configuration table (four variables, each needing a redeploy), and the schema section stating there is no down migration and why the answer is a forward fix. §6 records that the rollback has never been executed against a staging deploy.
- evidence/E08.12-incident-response-runbook.md — Five scenarios, each with person, channel and clock. Article 33's 72 hours and dataprotection.ie recorded. §0 states plainly that there is one person, no rota, no paid on-call and no second contact, as an accepted limitation of D-015 Q5. §6 covers the wrongly issued or wrongly expired entitlement including the rule that a couple's access is never shortened to fix an administrative error.
- _wt-wp07/drizzle/0027_share_link_token_hash.sql — F-04 verification. The `--> statement-breakpoint` trap did NOT recur: applying the file as the runner does (split on the marker, never on semicolons) produced 3 segments and all three objects — columns token_hash and token_scheme present, uq_share_links_token_hash unique. Proven against a real libSQL database with pragma_table_info and pragma_index_list, not reasoned about.
- _wt-wp07/src/lib/ratelimit.ts — F-06. Lines 14-21 state the design: with UPSTASH_REDIS_REST_URL/_TOKEN unset the limiter is a no-op that ALLOWS, and on a Redis error it ALLOWS rather than 500s. Bounded by the code entropy arithmetic: 10 chars over a 31-symbol alphabet = 49.5 bits, with a 48-bit floor asserted in a test that fails the build.
- _wt-wp07/next.config.ts — F-03 and F-07. Line 20: enforceCsp is gated on SIGNAL_ENFORCE_CSP === 'true', so the CSP is Report-Only by default. Lines 136-149: /embed is excluded from the global X-Frame-Options DENY by negative lookahead and gets frame-ancestors * — R-033's redistribution exposure confirmed at the header level, not just the route.
- BRANCH-STATE CHECK (my own, not claimed by the build agent) — git log confirms commit 7cb50d5 (R-027/R-028 suppression floor and branded rate value, and the hq/auth.ts v2 rewrite) is contained ONLY by claude/wp07-engineering-studio, not by main. So the review's 'closed in the studio branch, not deployed' framing for R-027/R-028 is factually correct. Criterion 2 MET.
- CONTROL-HAS-A-PRODUCTION-CALLER CHECK (my own) — Chased the programme's recurring failure mode. R-028's fix is genuine: presentRate has real production callers at daily-metrics.ts:241 and retention.ts:99, formatRateValue is consumed in 8 production UI files, and metricRateLabel is deleted (surviving only in a comment and in a test asserting it must not exist). R-027's two-sided rule reaches production through isSmallCell at daily-metrics.ts:108 and :151. NOTE: presentBehavioural itself has ZERO production callers (test-only), the same shape as
- BANNED-CLAIMS SWEEP (my own regex, all four docs plus the task spec) — Criterion 7 MET. Four hits total, all negations or the criterion text itself: 'No solicitor reviewed them (D-016)', 'It is not a legal opinion. No solicitor reviewed it. No accountant verified it', and the two occurrences inside the acceptance criteria. Separately swept for the programme's other traps: no VAT-exclusive statement anywhere (the only VAT line is 'inclusive of VAT at the prevailing rate', correct under D-021); no founding-lock permanence wording ('for life'/'forever'/'in perpetuity'
- Specification: `studio/docs/execution/venue-edition-and-films/tasks/E08.12.md`

## Your decision

Approve all of the above in one command:

```bash
node studio/docs/execution/venue-edition-and-films/tools/project-control.mjs approve-batch "your approval note" E08.01 E08.02 E08.03 E08.04 E08.05 E08.06 E08.07 E08.08 E08.09 E08.10 E08.11 E08.12
```

Or push back on any single one:

```bash
node studio/docs/execution/venue-edition-and-films/tools/project-control.mjs reject E08.01 "what is wrong"
```


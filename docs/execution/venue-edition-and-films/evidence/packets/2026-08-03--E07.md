# Founder review packet — E07

Generated 2026-08-03T05:15:31.869Z from PROJECT_STATE.json. 18 task(s).

**18 ready to approve · 0 not ready.**

Approving sets these tasks to Done and is the only thing that can. Nothing here is Done yet.

## Ready to approve

### `E07.01` Lock the portal information architecture and the primary buyer job of administering and proving the sponsored benefit.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. One named document is the sole declaration of the portal navigation set — an ordered list of top-level sections, exactly one label per section. No second document declares a conflicting set.
2. The fifth navigation item ('Venue settings' vs 'Account') resolves to one label, with a recorded rationale, used identically in both contract docs and in the implementation.
3. The primary buyer job is stated in one sentence and is invitation administration (D-027 pt 4); the document marks which sections are 1 September launch scope and which are the post-launch adoption-evidence layer.
4. The coverage-state enum has exactly one definition (the 3-vs-4-state disagreement is resolved) and the TypeScript union matches it exactly.
5. Every section names exactly one primary action; Overview's is 'Invite a couple'.
6. The IA forbids in terms any venue-facing section or field carrying couple-private content, and names the runtime assertion that enforces it on every payload path.
7. Founder is given named IA options with a recommendation and rationale, not a silent pick (decisionClass founder_choice).
8. Every file in studio/docs/venue-portal/ carries a title making it unambiguous that it specifies the Signal Studio Account (the surface E07 calls the Venue Portal), citing D-015 Q4, with the folder path unchanged and every pre-existing `##` anchor intact.
9. The two document sets agree with each other and with the shipped `/hq/account-review` surface on the fifth navigation item, and the correction is recorded rather than made silently.
10. The two sets agree on the coverage-state enum and the MetricValue discriminants, matching the shipped types.

**Evidence**
- studio/docs/venue-portal/PRODUCT_CONTRACT.md:23-31 — Nav declaration #1, fifth item 'Venue settings'
- studio/docs/account/PRODUCT_CONTRACT.md:27 — Nav declaration #2, fifth item 'Account'
- studio/docs/account/VOCABULARY.md:14,22 — 'Venue settings' → 'Account' mapping already ratified in the vocabulary
- studio/src/lib/account/vocabulary.ts:10 — Runtime nav is the four-state-contract 'Account' variant; single source for the tablist
- studio/docs/venue-portal/PRODUCT_CONTRACT.md:98-106 vs studio/docs/account/PRODUCT_CONTRACT.md:41-45 vs studio/src/lib/account/types.ts:10-14 — Coverage enum is 3 states, 4 states and 4 states respectively
- studio/docs/architecture/ADR-007-venue-portal-phase-a.md:3,98-103 — ADR still 'proposed for founder sign-off' while its own release gate blocks Phase B and C
- studio/docs/venue-portal/PRODUCT_CONTRACT.md:111-124 and ADR-007:41-45 — Couple-private content is forbidden in terms — the passing half of criterion 6
- browser check, http://localhost:3000/hq/account-review, 2026-08-03 — Rendered tablist is Overview Access Usage Reports Account; Overview main region holds one control, 'Open corresponding report'
- studio/src/app/hq/account-review/account-review.tsx (tab set) and studio/src/lib/account/types.ts:10-14, :25-37 — The shipped surface is the authority used for both reconciliations. Tabs are Overview, Access, Usage, Reports, Account. CoverageState has 4 values including `suppressed`; MetricValue has 5 discriminants including `unlimited`. The documents were changed to match this, not the reverse.
- node ~/.claude/skills/brand-voice/voice-check.mjs studio/docs/venue-portal studio/docs/account — 49 findings before the copy pass, 5 after. The 5 are pre-existing `banned:sprint` hits on internal engineering-plan lines that the skill's own scope guard excludes. No exclamation marks, no em dashes, no SaaS-fluff or AI-marketing terms remain in either tree.
- git status --porcelain studio/docs/venue-portal studio/docs/account — Exactly 17 modified files, all inside WP-05's column. The 15 other changes under studio/docs/ (PROJECT_STATE.json plus untracked $VEF evidence and tools) belong to the orchestrator and other work packages and were not touched. Nothing was committed, pushed or deployed.

### `E07.02` Redesign Overview so “Invite a couple” is the principal action.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. Overview renders exactly one visually primary action meaning 'invite a couple', and it is the first interactive control in DOM order in the main content region.
2. That control initiates the invitation flow (issues or reveals an invitation), not a link to documentation. Verified by following it.
3. Zero hits on Overview for 'seats', 'allotment', 'codes remaining', 'licences allotted', 'low allotment', 'X of your 60', 'for life', 'forever'.
4. Invitation administration is above the adoption-evidence material in DOM order and on screen; usage is subordinate.
5. Missing metrics show as missing, never 0. Unlimited sponsors render no seat count and no headroom copy.
6. No couple-private content on Overview; the privacy assertion runs on every payload path feeding it, including the export/download path.
7. The wireframes doc and the product contract both name the same Overview primary action as the code does.

**Evidence**
- browser check, Overview tab, #account-main interactive elements, 2026-08-03 — Exactly one control on Overview: 'Open corresponding report'. No invitation action exists.
- browser check, Overview heading order, 2026-08-03 — Adoption evidence is the first section; invitation administration is absent
- studio/src/app/hq/account-review/concepts/account-brief.tsx:78 + account-review.tsx:348 — The Overview button calls setTab('Reports'), not an invitation flow
- studio/src/app/hq/account-review/concepts/account-brief.tsx:10,57 — Banned vocabulary rendered on Overview: 'Allotted' and 'From allotted access to continued use'
- studio/src/lib/account/live/project-venue-access.ts:187 — `sponsor.codeAllotment ?? 0` — the R-016 legacy-NULL coercion that puts a fabricated 0 on Overview
- npx tsx --test on a throwaway probe of projectVenueAccessSnapshot, 2026-08-03 (file removed, read-only phase) — codeAllotment:null + allotmentMode:'limited' yields allotted/available = {state:'exact',value:0} and nextAction 'Allotment headroom is exhausted.'
- studio/src/lib/account/format.ts:11-14 and studio/src/lib/account/live/project-venue-access.ts:199-200,210 — The unlimited case is correctly suppressed and never zeroed — R-016's fixed half, recorded as passing evidence
- studio/src/app/hq/account-review/panels/access-panel.tsx:34-38,141-181 — Approved invitation copy and a distribution kit already exist, on the Access tab

### `E07.03` Complete account-standing, current-term, founding-status, renewal-date and support-status presentation.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. All five facts render with a named label each: account standing, current term, founding status, renewal date, support status.
2. Founding status derives from a real founding-number column on sponsors, assigned on cleared payment (D-027 pt 2) — never from row order, insertion order or a hard-coded list.
3. Each of the five has a defined missing/unknown presentation that shows missing, never a fabricated, defaulted or zero value.
4. Renewal/term copy never says 'for life' or 'forever'; the founding rate is expressed as holding for as long as the agreement renews without lapse (D-009 pt 3).
5. Support status has one enumerated state set defined once; the code union matches the contract document.
6. Tests cover the missing/unknown branch of all five, not only the happy path, and the named test actually runs.
7. Venue branding on this surface is the venue's name only — no logo slot, no venue-written message (D-027 pt 3).

**Evidence**
- studio/src/lib/entitlements-db/schema.ts:180-194 — founding_number is a real column assigned on cleared payment, uniquely indexed, never reused — D-027 pt 2 satisfied at the data layer
- studio/src/lib/venue-founding-number.ts:32-58 — NN/25 formatting, validity and next-place allocation exist as a pure, client-safe library
- package.json:13 — venue-founding-number.test.ts and entitlements-db/founding-numbers.test.ts are in the npm test chain, so the founding-number rules are actually tested
- studio/src/lib/account/live/project-venue-access.ts:39-51 and studio/src/lib/account/types.ts:39-55 — foundingNumber is never threaded into the snapshot; AccountSnapshot has no founding field
- studio/src/lib/account/live/load-venue-access.ts:59-62,76-88 — The sponsors select-all already returns foundingNumber; it is dropped at the mapping step — the gap is cheap
- studio/src/app/hq/account-review/panels/account-panel.tsx:10-23,151-159 — SUPPORT_HISTORY is a module constant of two fabricated rows rendered in live mode as well as fixture mode
- studio/src/lib/account/types.ts:48-55 and studio/src/lib/account/live/project-venue-access.ts:125-140,257-264 — Account standing, current term and renewal date are modelled and derived from live entitlements
- browser check, Account tab, 2026-08-03 — Rendered: 'Account active', 'TERM 2026-04-01 → 2027-03-31', 'Renewal 2027-03-31', 'SUPPORT / Request history' with the two fabricated rows, and no founding number anywhere
- grep for logo|brandMeta|welcome message across the account surface and its docs, 2026-08-03 — Zero hits — venue branding is the name only, D-027 pt 3 met

### `E07.04` Ratify every adoption-funnel definition from invitation through meaningful first action and continued use.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. A single named document proposes a definition for every funnel stage from invitation issued through meaningful first action to continued use, each with a name, a computable rule and a stated observation window.
2. Every stage names its source of truth (event name, or table/column) and that source either exists in the repo or is explicitly marked not-yet-instrumented.
3. Each stage states its privacy treatment: aggregate only, minimum cohort size before a rate is shown, no person-level identification.
4. The proposal states that missing telemetry renders as missing, never zero, and that page loads are not use.
5. Every founder call left open is enumerated, numbered, and phrased as a closed question the founder can answer without further research.
6. Each open call carries a recommendation, a reason, and the consequence of the alternatives, so all can be answered in one pass.
7. No definition counts a page load, a session, or a visibility-removal event as use.
8. Stage names as rendered to a venue comply with the D-020 vocabulary ban (no allotment, seats, codes remaining).
9. The invitations-issued / invitations-redeemed denominator rule and the prohibition on booking-attributed denominators are documented in both contract sets.
10. E09.02's §9.1 to §9.10 are marked as proposed with a named decision owner, and none is implemented as decided.
11. The `not_instrumented` suppression reason required by E09.02 §3 is documented as the fourth value.
12. The not_instrumented suppression reason required by E09.02 section 3 is documented as the fourth value.

**Evidence**
- studio/docs/execution/venue-edition-and-films/evidence/E09.02-metric-definitions.md:416-483 — Exactly ten open founder calls, each with a recommendation and named alternatives — AC5 and AC6 met
- studio/docs/execution/venue-edition-and-films/evidence/E09.02-metric-definitions.md:56-66 vs :333-336 — The §2 'any qualifying event of any kind' rule admits timeline_visibility_changed, which §7 forbids — internal contradiction, AC7 failed
- studio/src/lib/account/instrumentation/event-schema.ts:17-33 — timeline_visibility_changed:29 and briefing_acknowledged:32 are still allowlisted Tier 1 kinds; timeline_curated:28 and timeline_published:30 exist as E09.02 claims
- studio/docs/venue-portal/METRIC_DICTIONARY.md (venue-metrics.v1 header, Access and activation table) — A competing live dictionary names venue-facing metrics 'Licences allotted', 'Codes issued', 'Codes remaining' and defines day-7/30/90 retention — un-retired by E09.02
- studio/docs/planning-period-analytics.md:36 — The four-condition wedding activation bundle named in E09.02 §9.3 as the competing Timeline-creation definition
- studio/src/lib/account/instrumentation/coverage.ts:110-112, :157, :171, :176 — `not_instrumented` is already emitted by shipped code, so adding it as the fourth suppression_reason in both dictionaries records what runs rather than proposing something new.

### `E07.05` Implement an evidence-backed next-action system for stale invitations, low redemption, incomplete setup and renewal preparation.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. Four named next-action generators exist — stale invitation, low redemption, incomplete setup, renewal preparation — each reachable from the live projection.
2. Every emitted next action carries the evidence that produced it — observed value, threshold and window — as structured data, not only prose.
3. Every threshold is one exported named constant imported by the generator, not a literal duplicated at the decision site.
4. When the underlying evidence is missing or unknown, no action is emitted; the suppression is explicit and tested. Missing is never coerced to zero.
5. No next-action string uses banned vocabulary and none names a person or couple.
6. Output is deterministic and ordered by a stated rule.
7. The renderer shows the evidence alongside the action.
8. The legacy limited-sponsor NULL allotment case emits no false 'request more access' action (R-016 remaining work).

**Evidence**
- studio/src/lib/account/live/project-venue-access.ts:147-175 — The only next-action generator; three variants, none of the four required classes
- studio/src/lib/account/live/project-venue-access.ts:187 with :168-174 — NULL allotment coerced to 0, producing a false request-more action — reproduced by running the projection under npx tsx
- studio/src/lib/venue-allotment.ts:55-59 — remainingAllotment returns null for limited+NULL, the behaviour the projection contradicts; confirmed returning null in the same run
- studio/src/lib/account/live/load-venue-access.ts:68-75 — deliveredAt and expiresAt are never selected, so stale-invitation evidence cannot be computed on live data
- studio/src/lib/account/fixtures.ts:235-240 rendered at http://localhost:3000/hq/account-review Overview tab — The 'EVIDENCE-BACKED NEXT STEP' section is driven by a hand-written constant, not a generator
- studio/src/lib/account/instrumentation/daily-metrics.ts:175 vs suppression.ts:14 and retention.ts:26 — The threshold-as-literal failure mode already exists in this subsystem and must not be repeated
- npx tsx --test over six studio/src/lib/account suites — 56 pass, 0 fail — Verified baseline; the NULL-allotment defect is not caught by any existing test

### `E07.06` Replace product-centric metrics with understandable customer outcomes while retaining detailed product reach where useful.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. The venue-facing primary metrics are customer outcomes, not per-product counts.
2. Per-product reach, if retained, is secondary and labelled as reach.
3. No venue-facing surface renders the four-product number row as the primary measure.
4. Each outcome metric traces to a definition in E09.02 and the definition is available to the venue at the point of display.
5. Missing telemetry renders as missing for outcome metrics, never zero.
6. Every venue-facing surface agrees — screen, PDF, CSV, wireframes, claims doc.
7. Aggregate only. No person ranking, no biggest user.

**Evidence**
- http://localhost:3000/hq/account-review — Overview and Usage tabs, browser check — Product reach renders as four independent numbers (Notes 8, Tasks 10, Timeline 6, Signal 5) on the landing surface, and 30-day continuation renders as a bare 9 with no denominator
- studio/src/lib/account/fixtures.ts:174-192 vs docs/venue-portal/VENUE_FACING_CLAIMS.md:11 and WIREFRAMES.md:31,71-74 — The shipped fixture, the claims doc and the wireframes all carry the identical four-product row E09.02 §5 names as the metric to remove
- studio/src/lib/account/format.ts:5-6 and :22-24 vs report-preview.tsx:27-36 — The denominator is dropped by the shared formatter and restored only in one surface, against E09.02 §4:186-188
- studio/src/lib/account/types.ts:101-109 with a zero-hit grep for timelineCreat/timelineShar across studio/src — E09.02 §6 and §7 outcomes have no field in the snapshot contract; daysWithSponsoredUse has no E09.02 definition
- studio/src/lib/account/privacy.ts:40-48 enforced at load-venue-access.ts:96-102, 56 tests passing — Aggregate-only and missing-never-zero criteria are genuinely met and machine-enforced

### `E07.07` Replace the old 40/80 allotment language with the final eligible-booking or fair-use entitlement model.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. `unlimited` is a first-class representable state end to end — type, DB column, mint, presenter, panel, operator list, CSV — not a null, sentinel or 0.
2. A LIMITED sponsor with a NULL code_allotment never renders a numeric 0 and never emits a 'headroom exhausted' next action; it renders as missing/unknown, consistent with remainingAllotment returning null for that case.
3. Fair use notifies, never blocks (D-020 correction 1). No numeric pause, refusal or ceiling is visible on any venue-facing surface.
4. Zero venue-facing occurrences of allotment / allotted / headroom / seats / codes remaining / 'X of your N' across WP-05-owned code and docs; operator-only text that keeps a number is explicitly labelled internal.
5. No '40/80' numeric entitlement language survives on any WP-05-owned surface. Per-wedding arithmetic is founder sales language, never a product string.
6. The documentation set states the D-020 model in venue words: every couple who books gets a workspace, for as long as the licence is current.
7. The founder_only decision question ('Replace the 40/80 allotment language with what?') is closed in writing: what D-020 answers, and precisely what still needs a founder call.
8. A limited sponsor with a NULL code_allotment renders allotted and available as unavailable, not as exact 0 and not as unlimited.
9. That venue's next action does not claim headroom is exhausted and carries no banned D-020 vocabulary.
10. The projection agrees with remainingAllotment (venue-allotment.ts:55-59) on the same input.
11. csv.ts renders the unavailable state correctly for the access metrics.
12. A fifth fixture exercises the unlimited branch through CSV, PDF and the fixture contract.
13. A sixth fixture covers the NULL-allotment case and fits the fixture contract.
14. The SAMPLE watermark survives rendering for every fixture, as generate-samples.ts:36-38 requires.
15. Zero UNMARKED occurrences of "allotment", "seat", "codes remaining" and "licences allotted" across both trees. Every surviving occurrence must sit inside an explicit supersession context, because nothing may be superseded silently.
16. studio/docs/venue-portal/phase-a-wireframes.html — the only artifact in these two trees that renders on screen and could be seen by a founder on a prospect screen share — contains zero occurrences of the retired vocabulary.
17. The unlimited entitlement is representable in the written contract: on-screen rendering, the legacy-null case, and the CSV consumer contract for value_state='unlimited' stating that a blank cell means no number exists, not missing data.
18. Every venue-facing sentence written into these documents passes the brand-voice and typography rules.
19. The committed commercial-gate copy sweep passes for these files without the sweep being disabled when it first trips.

**Evidence**
- studio/src/lib/account/live/project-venue-access.ts:187,190,168-174,210 — The NULL-allotment coercion, the derived zero, the 'headroom exhausted' next action, and the attention guard that is correct while the metrics are not.
- studio/src/lib/venue-allotment.ts:55-59 — remainingAllotment returns null for a limited sponsor with a null cap — the contract the presenter breaks.
- studio/src/lib/entitlements-db/codes.ts:179-183, 63-103 — Unlimited escape inside the atomic conditional UPDATE; fair use alerts and proceeds with no refusal path.
- studio/src/lib/account/csv.ts:70-79 — Export writes value_state=unlimited with a blank value cell, never 0.
- studio/scripts/migrate-venue-edition-terms.mjs:229-235,281 — Zero sponsors were switched to unlimited; the ratified D-020 model is representable but currently applied to no venue.
- studio/docs/venue-portal/WIREFRAMES.md:22,58 — The literal 40-allotted/remaining figures E07.07's title names, still in the spec set and rendering on the review surface.
- studio/docs/execution/venue-edition-and-films/evidence/gates/commercial.json item 5 and item 11 — The commercial gate names E07.07 and requires a committed string-sweep script plus file list and output; no such script exists under studio/scripts/.
- studio/src/app/hq/entitlements/OnboardVenueForm.tsx:33 — Onboarding defaults to unlimited while venue-allotment.ts:30 and schema.ts:207 default to limited.
- Fail-before run, pre-fix `?? 0` coercion restored: 'ℹ tests 16 / ℹ pass 10 / ℹ fail 6' with AssertionErrors — allotted actual 'exact' expected 'unavailable'; nextAction actual 'request-more'; attention item actual false expected true. — All six new R-016 tests genuinely fail against the code as it stood, so they are not tautological.
- Post-fix run of src/lib/account/live/project-venue-access.test.ts: 'ℹ tests 16 / ℹ pass 16 / ℹ fail 0'. — The fix closes exactly those six failures with no collateral damage to the seven pre-existing R-016 and drift tests in the same file.
- Probe via npx tsx against projectVenueAccessSnapshot with {allotmentMode:'limited', codeAllotment:null, codesIssued:0}: allotted/available both unavailable; nextAction 'confirm-access-record'; attention 'access-record-missing'; remainingAllotment on the same input = null; CSV rows show value_state=unavailable with blank value cells. — The same reproduction the recon used to demonstrate the defect now produces the corrected output end to end, including the export path.
- private/account-samples/unlimited.csv lines 5-6: 'Glenmara House,venue,…,access.allotted,unlimited,,,codes,account-metrics.v2,' and the same for access.available. — A deterministic committed artefact now exercises the unlimited branch through the CSV exporter with blank value cells, never a zero.
- private/account-samples/unrecorded.csv lines 5-6: value_state=unavailable, blank value, withheld_reason='Signal HQ Access holds no issuing record for this venue'; lines 7-8 keep access.issued=3 and access.redeemed=1 as exact. — The NULL-allotment case is now covered by a deterministic artefact, and the fix does not suppress facts that were genuinely measured.
- `ls -la private/account-samples` after generation: the four pre-existing artefacts still carry their Jul 27 15:26 timestamps; only unlimited.* and unrecorded.* are new. — The two new samples were added without regenerating or churning the four committed binaries.
- C:/Users/ethan/AppData/Local/Temp/claude/C--Users-ethan-signal-studio-workspace/9b00cbfc-8d47-4963-b57f-a7b4fd24073e/scratchpad/sweep.mjs run against git HEAD and against the working tree — Before/after for the four named terms. BEFORE unmarked: allotment 25, seat 0, codes remaining 2, licences allotted 2 (plus allotted 6, headroom 1) — every occurrence unmarked. AFTER unmarked: 0 for all six terms, exit code 0. The 'before' tree was extracted with `git archive HEAD docs/venue-portal docs/account`, so it is the real pre-change state and not a reconstruction.
- studio/docs/venue-portal/phase-a-wireframes.html — The only rendered artifact in these trees is now clean: grep -ic returns 0 for allot, seat, headroom, remaining and 'codes remaining'. Previously it rendered 'licences allotted', 'codes remaining', '14 codes remain.', a bare '69.2%' and 'venue active days' twice.
- studio/scripts/check-venue-edition-contract.mjs --copy-report, and its contract at studio/docs/execution/venue-edition-and-films/evidence/copy/prohibited-claims.v1.json — A committed gate sweep already exists (the audit finding that no such script exists is now out of date — the parallel E09.11 work delivered it). It sweeps 22 surfaces against 15 rules / 85 patterns. Only 2 of my 17 contract files are in its list. Both are `tracked` not `enforced`. VENUE_FACING_CLAIMS.md reads 12 hits against pin 0, METRIC_DICTIONARY.md 9 against pin 5, every hit being the document
- Blockquote-scope simulation using the sweep's own rules from prohibited-claims.v1.json applied to only the '>' lines of all 16 markdown files — Under the sweep's existing blockquote scope, 15 of 16 files return 0 hits. The single exception is studio/docs/venue-portal/PRODUCT_CONTRACT.md:42, the verbatim D-020 sentence quoted from the ratified decision, matching rule p5-numeric-entitlement on 'seat count'. This is the exact case the sweep's own citation mechanism was written for.
- studio/src/lib/account/csv.ts (the `unlimited` case of metricFields) — The CSV consumer contract documented in both PRODUCT_CONTRACTs describes actual shipped behaviour: value_state=unlimited with a blank value cell and blank denominator, never 0.

### `E07.08` Complete invitation creation, copying, sending, resending, revoking, replacing, expiring and redemption status.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. A venue can CREATE an invitation from the portal surface and COPY its code/link; creation persists and the new invitation appears in the list.
2. SEND and RESEND exist as actions; each writes a real timestamp to the activation lifecycle record, and the surface distinguishes not-sent from sent.
3. REVOKE and REPLACE exist; revoke makes the code unredeemable, replace issues a fresh code against the same activation with the old one dead.
4. EXPIRE is represented from a stored expiry column, never defaulted or computed when the column is null; an expired invitation cannot be redeemed and reads as expired.
5. REDEMPTION STATUS shows the full ladder (not sent / sent / accepted / declined / expired / revoked) from real data, degrading to unknown rather than to a wrong state when columns are unbackfilled.
6. The fair-use ceiling notifies HQ and never blocks creation (D-020 pt 1). No venue-facing numeric refusal on create.
7. Every mutation is authorised to the acting sponsor's own scope, and no invitation payload carries notes/task/project/briefing/comment/attachment/collaborator or private Timeline content.
8. The live loader selects deliveredAt and expiresAt so the code-state ladder can reach 'issued' and 'expired'.
9. A single substrate decision is made and recorded: which table carries the invitation, which carries redemption truth, and which wins when they disagree.
10. sponsor_activations is wired for the first time and joined to the code ledger.
11. Create, copy, send, resend, revoke and expire exist as server actions with per-sponsor authorisation.
12. Replace is a single operation that withdraws the old invitation and issues its replacement.
13. Every mutation writes an auditable row.
14. Each action fails closed without a named operator, the way lib/hq/operator-identity.ts intends.
15. Destructive actions carry type-to-confirm friction enforced server-side, as hq/entitlements/actions.ts:163 does.
16. A redemption on license_codes moves an invitation to accepted.
17. deliveredAt and expiresAt are actually selected so states stop degrading.
18. Every new payload goes through assertSnapshotPrivacy.
19. No message leaves the system; 'send' means marking an invitation issued and producing wording for a human.

**Evidence**
- studio/src/app/hq/account-review/actions.ts:1-56 — The entire server-action surface: one loader, one request-recorder. No create/send/resend/revoke/replace/expire.
- studio/src/lib/entitlements-db/schema.ts:254-263, 289-298 — SPONSOR_INVITATION_STATES and the invitation/activation timestamp columns already model the full lifecycle the task asks for.
- studio/src/lib/entitlements-db/index.ts:31 — sponsorActivations is only re-exported; grep across src finds no production read or write, so the lifecycle model is entirely inert.
- studio/src/app/hq/account-review/panels/access-panel.tsx:33, 179, 248, 254-261 — Copy is a single generic welcome link; the per-row copy emits a masked code into a query string and is disabled in live mode; the UI states no email is sent.
- studio/src/lib/account/live/load-venue-access.ts:68-78 — deliveredAt/expiresAt are not selected, so sent/expired states degrade on live data.
- studio/src/lib/account/privacy.ts:52 and load-venue-access.ts:96 — assertSnapshotPrivacy has exactly one call site — new invitation payload paths would be unchecked by default.
- studio/src/app/hq/account-review/actions.ts:33-55 — recordMoreAccessRequestAction accepts a caller-supplied sponsorId with no per-sponsor authorisation check — the pattern any new mutation must not copy.
- Fail-before run with the inline five-column select restored: 'ℹ tests 14 / ℹ pass 11 / ℹ fail 3', including 'The input did not match the regular expression /\.select\(LIVE_ACCESS_CODE_COLUMNS\)/'. After restore: 14/14 pass. — The delivery-column tests are real regression guards, and the byte-identical-with-all-nulls property survived the change.
- npx tsx --import ./src/test/register-server-only.mjs --test src/lib/account/invitations/store.test.ts → tests 21, pass 21, fail 0 — The store works against a real SQLite engine: sha256 join, transactional state+audit, cross-tenant refusal, server-side confirm word, system-only transitions refused, create-pairs-oldest-then-refuses.
- Browser, localhost:3007 live mode, audit ledger query: three rows — grant/invitation.mark_sent, revoke/invitation.revoke, export/invitation.reveal_link — all actor_id=wp05-verify, all chained (prev_hash and row_hash present), after_json carrying masked codes; SELECT COUNT(*) WHERE after_json LIKE '%SIG-%' returned 0. — Every mutation taken through the real UI wrote an attributed, chained audit row, and no plaintext code entered the ledger.
- Browser: clicking 'Mark as sent' on SI-••••-HB returned the notice 'Recorded as sent. Signal Studio sent nothing; this is your note that you passed it on.' and the row moved to 'With the couple' with sent date 2026-08-03. — The full client → server action → store → DB → reload path works, and the honest register is preserved at the point of action.
- Browser: 'Withdraw' on SI-••••-ZP opened a dialog reading 'Type REVOKE to confirm.'; confirm button disabled with 'revoke', enabled with 'REVOKE'; after confirming, the row read 'Withdrawn' with only 'Get link' remaining. — Type-to-confirm friction works in the UI and the lifecycle library correctly removes all further venue actions from a terminal state.
- store.test.ts 'stamps revoked_at when the revoke creates the activation row itself' — a bug I found in the browser, not in the tests, because every earlier test revoked a row that already existed. — The insert branch was writing invitation_state='revoked' with revoked_at NULL and state='pending'. Fixed and now covered.

### `E07.09` Complete access search, filters, pagination, stale-invitation alerts and masked-code handling.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. Text search over the access list matches across the WHOLE set, not only the rows already rendered.
2. State filters exist, combine with search, show active state and can be cleared.
3. Pagination (or equivalent) covers the full set. No silent truncation: the total is shown and rows are never dropped without saying so.
4. Stale-invitation alerts are COMPUTED from real timestamps against a stated threshold; missing timestamps read as unknown, never as 'not stale'.
5. Masked-code handling: codes masked by default with deliberate reveal, masking applied server/presenter side so full codes are absent from the payload when masked.
6. The list carries no forbidden private content and no per-person ranking.
7. Empty result is distinguishable from not-loaded / missing data.
8. Null continues to mean 'not tracked' and is never manufactured into a default.
9. A text search runs against the full set rather than the rendered rows.
10. Pagination replaces the silent truncation at project-venue-access.ts:226.
11. Staleness is computed from real timestamps against a stated threshold, returning unknown when deliveredAt is null rather than 'not stale'.
12. The 14-day fixture literals are no longer mistaken for an implementation.
13. The table has an empty-state branch.
14. Masked-code handling: no plaintext code reaches the list payload or the clipboard by the old route.

**Evidence**
- studio/src/lib/account/live/project-venue-access.ts:223-226 — Silent truncation to 40 rows with no total carried into the snapshot.
- studio/src/app/hq/account-review/panels/access-panel.tsx:216 — '{rows.length} masked rows' counts surviving rows, not the true total, so the truncation is invisible to the reader.
- studio/src/lib/account/fixtures.ts:82,160,161,238,378,400,401 — Every stale-invitation string is a hardcoded fixture literal; the alert renders in review but does not exist on live data.
- studio/src/lib/account/live/project-venue-access.ts:202-217 — The live attention list contains only allotment-drift and no-remaining — no stale computation exists.
- studio/src/lib/account/live/mask-code.ts:5-11 and project-venue-access.ts:230 — Masking is applied presenter-side, so plaintext codes are absent from the snapshot payload.
- studio/src/lib/account/live/project-venue-access.test.ts:138 — assertSnapshotPrivacy returns no errors for the projected snapshot; passed in the test run.
- studio/src/app/hq/account-review/panels/access-panel.tsx:194-215, 233-273 — Six filter chips with aria-pressed exist; the table body has no empty-state branch.
- store.test.ts 'pages over the full set and reports the true total': 45 seeded codes → page 0 returns 40 rows with total 45, page 1 returns the 5 the old slice(0,40) dropped, and the two pages do not overlap. — Silent truncation is replaced by real server-side paging that reports the account's total, not the page's.
- store.test.ts 'search runs over every row, not only the current page': searching '44' finds SIG-MANY-0044, the oldest row, which sorts onto page 2. — Search runs against the full set before paging, which is the defect the criterion names.
- Browser fixture mode: stale alert '1 invitation has been with a couple for over 14 days with nothing back'; row detail 'Waiting 21 days.' computed from issuedOn 2026-07-03 against the fixture's dataThrough 2026-07-24. — Staleness is computed, not asserted by a fixture literal, and is deterministic for a review fixture.
- lifecycle.test.ts 'an invitation sent with no send date is unknown staleness, never fresh and never zero' asserts sentAt stays null and the verdict carries no daysSinceSent property at all. — Missing telemetry shows as missing. The unknown verdict cannot carry a hidden number.

### `E07.10` Complete the distribution kit with approved email wording, welcome link and printable welcome card.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. A named distribution-kit artefact exists in an owned path covering all three deliverables, each marked approved or not.
2. Invitation email wording exists as definitive subject and body strings the operator or portal actually uses, not prose describing an email.
3. The welcome link is a concrete URL shape resolving to a real route in the repo.
4. A printable welcome card exists as a print-producing artefact carrying venue name, code, and how to redeem.
5. Banned-vocabulary sweep over every kit string returns zero for allotment, seats, codes remaining, licences allotted, for life, forever.
6. Venue branding in the kit is the venue NAME ONLY, with no logo slot and no venue-written welcome message field (D-027 point 3).
7. The wording carries a recorded founder approval, since the title says "approved".
8. The welcome link resolves to a working redeem page rather than the invalid-code page.
9. The approved email wording has a subject as well as a body and lives in a copy source of truth.
10. The printable welcome card produces a printable artefact, not clipboard text.
11. The copy passes the D-020 banned-vocabulary sweep the commercial gate requires.
12. D-027 point 3: venue branding is the venue's name only, with no logo and no venue-written message.
13. The kit is documented as a kit document.
14. The control no longer asserts 'Approved' against a task whose founder sign-off is not_requested.

**Evidence**
- studio/src/app/hq/account-review/panels/access-panel.tsx:141-181 — The distribution kit exists as a UI section with three clipboard actions and nothing more
- studio/src/app/hq/account-review/panels/access-panel.tsx:33 — The welcome link is the literal placeholder /redeem/venue-welcome
- studio/src/lib/redeem/lookup.ts:44-46 with studio/src/app/redeem/[code]/page.tsx:195-207 — That link renders the invalid-code page to a couple
- studio/src/app/hq/account-review/panels/access-panel.tsx:161 — Venue branding in the kit is the name only, satisfying D-027 point 3
- Browser check, http://localhost:3000/hq/account-review Overview tab, 2026-08-03 — "Allotted 40" and "From allotted access to continued use" render live in venue-facing copy
- Browser live run, kit sheet: link https://signalstudio.ie/redeem/SIG-WN2C-6FHB, subject 'Your wedding planning workspace, from Glenmara House', body as generated, iframe srcdoc 2407 chars, controls [Copy link, Copy subject and wording, Copy card text, Print welcome card]. — All three named deliverables exist and are produced from real data through the real server action.
- npx tsx --test src/lib/account/invitations/lifecycle.test.ts src/lib/account/invitations/copy.test.ts → tests 35, pass 35, fail 0 — The lifecycle rules and every front-facing string pass their decision sweeps (D-009, D-010, D-016, D-020, D-021, D-027 pt 3) and BRAND.md §3 glyph rules.
- Rendered welcome card delivered to the founder as welcome-card.html. — The printable artefact is real and inspectable, since browser screenshots were unavailable.

### `E07.11` Complete Usage with first useful action, recent use, continued use, product reach and measurement definitions.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. All four metrics exist as named fields in the venue-facing usage payload with E09.02 semantics.
2. Measurement definitions are visible on the venue surface itself, sourced from E09.02, with no parallel definition live in the repo.
3. Recent use always displays data_through next to the number (E09.02 §3).
4. Product reach's four counts are never summed, and the venue-facing form is the collapsed sentence of E09.02 §5, or the open §9.5 choice is surfaced rather than silently picked.
5. Continuation always renders its denominator with the share ("9 of 12", never "75%" alone).
6. Absent telemetry renders unavailable, never 0, on the production path, guarded by an assertion that actually runs.
7. No metric derives from reached_board_at, timeline_visibility_changed or briefing_acknowledged (E09.02 §9.0 items 1, 4, 5).

**Evidence**
- studio/src/lib/account/types.ts:101-115 — All four named metrics plus product reach exist as first-class payload fields
- studio/src/lib/account/instrumentation/sealing.ts:23 — The continuation band is implemented at days 25-35 exactly as E09.02 §4 defines
- studio/src/lib/account/format.ts:3-16 with components/metric.tsx:20-22 — Every metric renders without its denominator because formatMetricValue never reads the field
- studio/src/lib/account/live/project-venue-access.ts:268-270 — dataThrough is set but the live coverage.detail carries no date, so live venues see no data-through
- Browser check, http://localhost:3000/hq/account-review Usage tab, 2026-08-03 — "30-day continuation 9" with no denominator, and per-product reach 8/10/6/5, render live
- npx tsx --test over the eight studio/src/lib/account suites — 71 pass, 0 fail — The absent-never-zero and suppression behaviours hold on the tested paths

### `E07.12` Implement the ratified small-cohort suppression rule and the “Use, without surveillance” privacy receipt.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. One exported source of truth for both thresholds, imported by every suppression site, with zero hard-coded 3 or 5 literals in metric computation.
2. The exported suppression helpers are invoked on the production path, not dead code.
3. Suppression holds on screen, CSV, PDF and the download route — every egress.
4. Suppressed values do not leak via tooltip, aria-label or other accessibility text.
5. A venue-facing "Use, without surveillance" privacy receipt exists as a rendered artefact stating what is measured, what never is, and why numbers are withheld.
6. The access-versus-usage suppression asymmetry is either governed by a recorded decision or raised as an open question.
7. Boundary tests cover n=2 vs 3 and n=4 vs 5, and fail if the declared constant changes without behaviour changing.
8. The rate threshold is imported rather than hard-coded, and changing the constant changes the behaviour.
9. suppression.ts's four exported functions are no longer dead in production.
10. The usage overlay re-runs assertSnapshotPrivacy on the snapshot it builds.
11. The 3 and 5 suppression thresholds are not edited, because D-011 point 3 cites these documents as its own source.

**Evidence**
- studio/src/lib/account/instrumentation/daily-metrics.ts:175 — The rate threshold is hard-coded as 5 rather than importing RATE_MIN_WORKSPACES
- studio/src/lib/account/instrumentation/suppression.ts:34-72 with suppression.test.ts:14-75 — All four exported helpers are called only from their own test file
- studio/src/lib/account/privacy.ts:12-24 invoked at live/load-venue-access.ts:96 — A withheld or unavailable metric structurally cannot carry a hidden number on the production path
- studio/src/lib/account/csv.ts:56-68 — CSV egress writes blank value and denominator for withheld and unavailable, leaking nothing
- studio/src/lib/account/fixtures.ts:225-226 — The privacy receipt's withheldRule states only the three-workspace threshold, never the ratified five
- studio/docs/execution/venue-edition-and-films/evidence/E09.02-metric-definitions.md:42 and :4 — The only rationale for unsuppressed access metrics sits in a document marked as proposed, not ratified
- Mutation run with RATE_MIN_WORKSPACES temporarily set to 6. Pre-fix daily-metrics: 'ℹ pass 19 / ℹ fail 2' — the rate-boundary test and the source-contract test both fail. Fixed daily-metrics, same mutated constant: those two pass and instead the two legacy tests that hard-code 'five' fail. — Behaviour now follows the declared constant. This is the precise property the E07.12 criterion asks for and the one no previous test could catch.
- Repo grep after the change: `grep -n "BEHAVIOURAL_MIN_WORKSPACES\|RATE_MIN_WORKSPACES\|< 5\|< 3" src/lib/account/instrumentation/daily-metrics.ts` returns nothing but the (now removed) import line. — No numeric suppression threshold survives in daily-metrics.ts; the thresholds live only in suppression.ts.
- `diff suppression.ORIG.ts src/lib/account/instrumentation/suppression.ts` → identical. — The dead-code defect was fixed entirely on the consumer side. suppression.ts, which is the ratified rule, was not edited at all.
- git diff studio/docs/venue-portal/PRIVACY_AND_RETENTION.md filtered to threshold patterns — No line containing 'below 3', 'below 5', 'least three', 'least five', '3 eligible' or '5 eligible' was added or removed. D-011 point 3's source document is intact.

### `E07.13` Refine lifecycle visualisation and ensure it does not imply surveillance or expose private behavioural detail.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. A lifecycle visualisation exists showing invitation states in order.
2. Wedding dates shown only where the couple redeemed a code from that venue (D-011 point 1).
3. Date changes are never shown — no previous date, no change entry, no postponement event.
4. No per-couple behavioural detail in the lifecycle: no last-seen, no activity trail, no per-couple usage marks.
5. Reads as administration, not monitoring: no live or online affordance, no progress bar implying a couple is behind, no negative disengaged state.
6. States are never inferred from absence — a missing delivery timestamp renders unknown, not "not delivered".
7. Both D-011 point 1 rules are written down in a document in the E07 set.
8. D-011 point 1 is stated where an implementer would actually read it, and the "date changes are never shown" rule, which appeared in no document at all, now exists.

**Evidence**
- Browser check, http://localhost:3000/hq/account-review Overview tab, 2026-08-03 — The adoption story renders as a six-step funnel from Allotted 40 down to Continued after 30 days 9
- studio/src/app/hq/account-review/panels/usage-panel.tsx:141-158 — Three metrics with different denominators are plotted as one bar series with an arbitrary value*6 height
- studio/src/lib/account/live/project-venue-access.ts:219-221 and :108-123 — Delivery state is never inferred from absence; untracked delivery is labelled as untracked
- studio/src/lib/account/privacy.ts:73-84 — Identifier patterns and unmasked codes are rejected from the snapshot in production
- studio/docs/venue-portal/PRIVACY_AND_RETENTION.md:39 against DECISIONS.md D-011 point 1 — The live privacy document forbids wedding dates that the ratified decision permits

### `E07.14` Complete monthly, access-term and renewal-report generation.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. Three named report kinds exist as distinct typed definitions (monthly, access-term, renewal), each with a period resolver computing start/end from real inputs (calendar month; agreement or access-term dates), not a reader-chosen control with no backing.
2. Generation is invocable end-to-end from a non-test caller (route, job, script or UI control) and produces a persisted or downloadable report for venue + kind + period.
3. Each generated report carries the reconciliation header on its own face: data-through date, completeness state, definition version and generated-at.
4. Every report body is aggregate-only, enforced by a privacy assertion that runs ON the generation path, with a test proving a seeded forbidden field is rejected.
5. Missing telemetry renders as missing (never 0), suppressed renders with a reason, unlimited renders with no number and no allotment vocabulary; a sweep of report copy for allotment/seat vocabulary returns zero.
6. Reproducible: the same venue+kind+period generated twice returns identical numbers because it reads sealed/frozen data rather than re-querying, and the seal/freeze writer has a production caller.
7. Renewal report copy states a renewing term and never says 'for life', 'forever' or 'lifetime' (D-009 point 3, D-001 point 16, R-008).

**Evidence**
- studio/src/app/hq/account-review/panels/reports-panel.tsx:30,147-155,170 — The three report kinds are a local useState union and the cadence control persists nothing; the panel's own copy states 'Nothing is scheduled. No email, no delivery, no schedule.'
- Browser check 2026-08-03, http://localhost:3000/hq/account-review, Reports tab, HQ cookie signal_hq_access derived per verify-hq.mjs:62-63 — Rendered text returned two hard-coded monthly library rows ('July 2026 account review', 'June 2026 account review') and the three cadence labels Monthly / Access term / Renewal pack with no generation control.
- grep -rn 'freezeReport|readFrozenReport|sealCandidate|assessCadence|sponsorReportSnapshots' over studio/src and studio/scripts — Zero non-test callers for the freeze and sealing engines; zero callers of any kind for the sponsor_report_snapshots table.
- studio/src/lib/account/instrumentation/freeze.ts:90-128 — freezeReport content-hashes the payload at :124 without calling assertSnapshotPrivacy — the generation path has no privacy gate.
- studio/src/lib/account/live/project-venue-access.ts:242,271-273,313-325 — The single live report is labelled 'Access term' while its coverage period is last-30-days computed from Date.now().
- studio/src/lib/account/pdf-html.ts:33 and studio/docs/venue-portal/METRIC_DICTIONARY.md:12,15 — Prohibited allotment vocabulary is on the report face and in the shipped metric dictionary, against D-020 and E09.02:380-381.
- npx tsx --test on csv/fixtures/samples/freeze/sealing test files, 2026-08-03: 40 tests, 40 pass, 0 fail — The freeze and sealing components are correct as built and are genuine candidate evidence, independent of being unwired.

### `E07.15` Complete reconciled PDF and CSV exports with data-through dates, completeness states and definition versions.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. Both formats are produced by the live path from the same snapshot, and the PDF is generated at request time from live data rather than read from a pre-generated fixture on disk.
2. Reconciled: PDF, CSV and the on-screen panel carry the same values for the same venue and period, asserted field-by-field by one test.
3. Every export carries a data-through date, in the body and in a machine-readable position (CSV column or header, PDF header).
4. Completeness state is carried per value (present / suppressed / missing / unlimited), not as a single document-level flag.
5. The definition version carried is the current dictionary (account-metrics.v2), and no export path, wireframe, fixture or storage default pins the retired venue-metrics.v1.
6. value_state='unlimited' writes a blank value cell and is covered by a documented consumer contract enumerating every legal value_state, shipped with or referenced by the export, so a reader parses it correctly without prior knowledge.
7. The export route re-runs the privacy assertion on the exact payload it serialises, not on an upstream object.
8. The download route re-checks privacy before serialised bytes leave the server.
9. The live snapshot no longer advertises a format the server will not serve.

**Evidence**
- curl -s -o /dev/null -w '%{http_code}' --cookie signal_hq_access=... 'http://localhost:3000/hq/account-review/download?fixture=complete&format=pdf' → 404 'Sample not generated. Run npm run account:samples.' — The only PDF path in the system 404s under the project's documented launch config, while studio/private/account-samples/complete.pdf exists at 62082 bytes — a process.cwd() resolution bug at samples.ts:31-37 that CI cannot see.
- curl '...?source=live&venue=glenmara-house&format=pdf' → 404 'Unknown live format.' — Live mode has no PDF at all (route.ts:28-30), contradicting the formats: ['pdf','csv'] the live snapshot advertises at project-venue-access.ts:322.
- curl '...?source=live&venue=glenmara-house&format=csv' → 404 body containing the full SQL SELECT column list and bound params — download/route.ts:33-34 returns load-venue-access.ts:105-110's raw Error message to the client — internal schema disclosure through an export route.
- studio/src/lib/account/csv.ts:70-79 with studio/docs/account/PRODUCT_CONTRACT.md:34-37 — The unlimited blank cell is written correctly but the only shipped consumer contract enumerates four states and omits unlimited; grep -rn value_state across studio finds no documentation anywhere.
- grep -c unlimited studio/private/account-samples/*.csv → 0 for all four, and csv.test.ts:8-27 covering only withheld and unavailable — The unlimited export path has zero test coverage and zero sample coverage.
- studio/src/lib/entitlements-db/schema.ts:673,889-891 and studio/drizzle-entitlements/0000_init.sql:190 — The frozen-report table's metric_dictionary_version defaults to the retired venue-metrics.v1 — a second stale pin beyond the sponsor_usage_daily one at :226 that recon named.
- studio/docs/venue-portal/METRIC_DICTIONARY.md:3,12,15 — The dictionary the exports' definition_version names is still versioned venue-metrics.v1 and defines 'Licences allotted' and 'Codes remaining', both prohibited by E09.02-metric-definitions.md:380-381 under D-020.
- studio/src/lib/account/csv.ts:9,31,94 and studio/private/account-samples/complete.csv line 4; studio/src/lib/account/pdf-html.ts:147,170 — Data-through is carried in both exports in a machine-readable position — criterion 3 genuinely met.
- studio/src/lib/account/csv.ts:35-79 — Completeness state is per value across all five variants with numeric cells blanked, not a document-level flag — criterion 4 genuinely met.
- private/account-samples/unlimited.pdf.txt lines 12-13 read 'Unlimited' then 'Allotted'. — The pdf-html.ts:33 tile label defect the recon predicted is now demonstrable in a committed artefact: a founding venue's report would print 'Unlimited / Allotted'. Recorded, not fixed — see open questions.

### `E07.16` Complete Account, organisation settings, members, roles, support history and reporting preferences.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. A venue user can authenticate as themselves: a venue-scoped identity exists and the account surface is reachable by it, not by a single shared operator secret.
2. The members list on the live path returns the real administrators of that venue from a persisted store, and is non-empty for a venue with at least one admin.
3. Invite member performs a persisted server-side mutation that survives a reload, not a client-side notice string.
4. roles.ts governs the live path: a capability is checked server-side against the authenticated venue user's role before a mutating action runs, and a role lacking it is refused.
5. Organisation settings are persisted per venue and tenant-scoped so venue A cannot read or write venue B, and branding stays venue-name-only per D-027 point 3.
6. Support history is that venue's real record from a data source, or is explicitly presented as unavailable; never a hardcoded array presented as fact.
7. Reporting preferences persist per venue and their payload carries no couple-private content and no per-person ranking.
8. AC-2 Organisation identity shows venue name, edition, term start and end, renewal date and standing, all read from `sponsors`, none hard-coded.
9. AC-3 Venue branding on every Account surface is the venue's name only — no logo, no venue-written message, no free-text field (D-027 point 3).
10. AC-4 Members and roles are either live and audited, or absent by decision with the surface stating Signal Studio administers the account. A role selector that governs nothing satisfies neither.
11. AC-5 The capability matrix in docs/venue-portal/ROLES_AND_PERMISSIONS.md and the shipped list in lib/account/roles.ts agree, or the divergence is recorded as a dated decision.
12. AC-6 Support and request history is either real sponsor_requests rows or an explicit nothing-recorded state; illustrative rows never render on live data.
13. AC-7 Reporting preferences are persisted and honoured, or the control is absent.
14. AC-8 No Account payload, export or page contains private content or a plaintext licence code, proved by a test over the payload.
15. AC-9 Every mutation reachable from the Account writes an audit event naming a real actor; attribution to 'whoever held the credential' fails.
16. Conflicts that no ratified decision settles are recorded, not silently reconciled (WORKFLOWS §8).
17. AC-1 A named principal can reach the Account surface for exactly one venue, resolved server-side from the session and never from a client-supplied identifier.

**Evidence**
- studio/src/lib/hq/auth.ts:15-25 — One shared password derives one token for all operators; no per-venue credential exists.
- studio/src/lib/hq/access-guard.ts:7-12 — The sole route guard checks only the shared token, then redirects to /hq/access.
- studio/src/lib/account/live/project-venue-access.ts:326 — members: [] is returned unconditionally on every live snapshot.
- studio/src/app/hq/account-review/panels/account-panel.tsx:10-23,82-87,121-123,151-159 — Hardcoded SUPPORT_HISTORY rendered as fact; invite and preference are setNotice calls only.
- studio/src/app/hq/account-review/account-review.tsx:60,251-265 — Role is client state chosen from a dropdown; browser-verified the Role select offers Owner/Manager/Viewer freely.
- studio/src/app/hq/account-review/actions.ts:22-27,33-55 — loadLiveVenueSnapshotAction accepts any slug with no per-venue authorisation; recordMoreAccessRequestAction runs no capability check.
- studio/src/lib/entitlements-db/schema.ts:641-642 — The schema comment records that portal membership does not yet exist.
- studio/docs/venue-portal/ROLES_AND_PERMISSIONS.md:37-48 — The sponsor_members projection is fully specified and explicitly labelled 'future'; the audit requirement is stated.
- studio/src/lib/account/roles.ts:11-28,35-54 — The capability matrix and denial copy are genuinely built and tested — candidate evidence for the eventual server-side gate.
- studio/src/lib/hq/auth.ts:3,15-25 + studio/src/proxy.ts:13,38-57 — Every Account surface is gated by one shared password on a cookie, with no per-principal identity. /hq also holds ~50 routes including cap-table, financial-model, loan-pack, incorporation, data-room, crm, vault and founders-circle, so that single password is the whole company's back office. No venue may hold it and no venue-facing route may sit under /hq.
- studio/package.json dependencies (11 packages, none @clerk/*) + studio/src/proxy.ts:84-87,110-112 — Studio has no Clerk SDK. It reads the shared __session cookie as a raw string only to decide a marketing redirect. Option B's cost includes adding Clerk to studio with new env keys across three environments and a new proxy layer that must not disturb hqGate.
- studio/src/emails/templates/ (17 templates) with no Resend client; app/src/server/email.ts holds the only mailer — Studio renders email and cannot send it. Any option requiring member-invitation delivery (B) needs a mailer that does not exist in this repo.
- app/src/app/api/webhooks/clerk/route.ts:98 — organization.* events are explicitly ignored. Clerk Organizations are not in use in either repository, so Option B cannot lean on them as an off-the-shelf membership model.
- studio/docs/account/LIVE_HQ_PREVIEW.md:3,5,49 — The HQ gating is documented deferral, not oversight: line 3 says 'complete for this cycle', line 5 lists 'sponsor auth' under Still closed, line 49 schedules sponsor_members + Clerk for Phase C. Worth recording as a determination so it is not rediscovered as a defect a third time.
- studio/src/lib/account/invitations/lifecycle.ts (617 lines) + store.ts (638 lines) + copy.ts (304 lines) + actions.ts:95-101 — E07.08, E07.09 and E07.10 are substantially built: six states, nine actions, a documented substrate decision that the ledger outranks the record, sha256(code) as the join key, server-side search and paging over the venue's whole set, and one reviewable copy source bound to D-027 point 3, D-020, D-009 point 3, D-016, D-021 and D-010. Built for an operator, not a venue. Candidate evidence, never fou
- studio/src/app/hq/account-review/account-review.tsx:246-254 — Recon finding F-2 (the picker listing live venues by name on screen shares) is fixed: venues are labelled by founding place or opaque id because consent to public naming is unknown for every account.
- studio/src/lib/account/live/project-venue-access.ts:183-191,237-246 — R-016's legacy limited-sponsor-with-NULL-allotment case is fixed: allotted and available render as unavailable rather than zero, and that case gets its own next action instead of 'Allotment headroom is exhausted'. The remaining WP-05 R-016 work named in the wave context is done.
- studio/src/app/hq/account-review/download/route.ts:48,67 — The recon finding that the download route re-serialises the snapshot without a privacy re-check is no longer true. assertExportPayloadSafe now runs over the serialised body on both CSV and HTML before the response is returned.
- studio/src/app/venues/page.tsx:126,361,560,566 + DECISIONS.md D-020 venue-facing sentence — The ratified and live commercial position already describes Option C: 'Nothing for your team to run', 'Light by design. We come to you, not the other way around', 'we send the codes and a short note you can pass to couples', and the activation register disclosed as 'in development' rather than promised as a login. D-020's own venue-facing sentence is 'no seat count, no per-couple maths, nothing fo
- studio/docs/execution/venue-edition-and-films/evidence/gates/product.json item 11 — The product gate requires 'a single authenticated session' reaching invitation administration, adoption evidence, account standing, renewal date and support history without a second login. It does not say whose session. As written, an operator session on a screen share satisfies it, which is why the ambiguity is returned as an open question rather than resolved unilaterally.
- grep for sponsor_members across studio: 6 documentation hits, 0 source files — The sponsor_members lifecycle at docs/venue-portal/ROLES_AND_PERMISSIONS.md:37-61 has no implementation. ADR-008:71 and :128-134 record the same conclusion independently.
- Specification: `docs/execution/venue-edition-and-films/evidence/E07.16-venue-identity-lab.md`

### `E07.17` Add the live branded couple-experience preview, branding controls and appropriate upcoming wedding-date view.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. The preview renders from the same source of truth as the live couple experience, not a parallel mock that can drift.
2. Branding is the venue's name only: no logo, no venue-authored welcome message, and no control anywhere that would let a venue supply either (D-027 point 3).
3. sponsors.brandMeta renders no venue-authored branding on any path; if it is dead, that is recorded rather than left as a latent D-027 violation.
4. The upcoming wedding-date view satisfies D-011 point 1 and never shows date changes: no prior date, no delta, no 'moved from', no change indicator.
5. The wedding-date view carries no couple-private content: no notes, tasks, projects, briefings, comments, attachments, collaborators or private Timeline.
6. Missing shows as missing: no upcoming weddings, or a wedding with no date, renders an explicit empty or unavailable state — never 0, never a placeholder date.
7. The preview is read-only and cannot mutate the couple's workspace.

**Evidence**
- app/src/components/welcome/venue-welcome-card.tsx:66 — The shipped couple-facing venue branding is the venue name only, matching D-027 point 3. Also the source of truth a preview must share — and outside WP-05's column.
- studio/src/lib/entitlements-db/schema.ts:120-127,147 — wedding_date exists and is indexed, but is documented as deliberately absent from SPONSOR_DEFAULT_FIELDS and readable only via the consent projection.
- studio/src/lib/entitlements-db/sponsorship-policy.ts:13-29,37-41,44-53 — The default/consent/forbidden field split: the date is consent-gated; couple-private content is never representable.
- studio/src/lib/entitlements-db/sponsorship-policy.ts:245 — projectSponsorActivation, the only sanctioned way to build a venue-facing activation payload, has zero production callers in studio.
- studio/src/lib/redeem/lookup.ts:33-40,88 — brandMeta is parsed by an unschema'd JSON.parse and carried on the redeem DTO, though nothing renders it.
- studio/src/app/hq/account-review/account-review.tsx:314-328 — The tab set is Overview/Access/Usage/Reports/Account — browser-confirmed; no preview or wedding-date surface exists.
- studio/src/lib/account/live/project-venue-access.ts:301-311 — Missing behavioural values render as unavailable, never zero — the pattern a wedding-date view must reuse.

### `E07.18` Complete portal permissions, audit history, empty/loading/error states, responsive design, accessibility and end-to-end data reconciliation.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. Every venue-facing route and every mutating handler is guarded server-side by a venue-scoped capability check before any data is read.
2. Audit history: every administrative action writes an append-only record with actor, action, target and timestamp, and a venue sees its own history and only its own.
3. Route-level loading.tsx, error.tsx and not-found.tsx exist for the portal segments, and each panel has a defined empty state.
4. Errors and gaps are honest: a failed load says unavailable and missing telemetry shows missing — never coerced to 0, never a silent empty success.
5. Responsive: usable at 375px, 768px and 1280px with no horizontal page scroll and no clipped or unreachable controls.
6. Accessibility: keyboard-reachable and operable controls with visible focus, accessible names on all controls, one logical heading hierarchy, 4.5:1 body and control contrast.
7. End-to-end reconciliation against the underlying store by a reproducible check, and the privacy assertion runs on every venue-facing payload including export and overlay-built snapshots.
8. The export route no longer returns raw database error text to the client.
9. The venue picker leak is fixed.
10. Finding F-2: the venue picker must not name other founding venues.
11. AC-G1 Each role is exercised against each capability by an authorisation test asserting a request outcome, not a pure function's return value.
12. AC-G2 A request naming another venue's identifier returns not found, and a client-supplied venue identifier cannot change the server-resolved venue context.
13. AC-G3 Withdrawing a principal's access ends its existing sessions, proved by a test.
14. AC-G4 The venue-visible audit history is a real read of entitlement_events scoped to that venue, with no operator-only and no other venue's rows.

**Evidence**
- Browser check, http://localhost:3000/hq/account-review at 375x812, 768x1024, 1280x900 — No horizontal page scroll and zero overflowing elements at any of the three widths.
- Browser check, computed-style sweep of every text node on the Account tab — 37 controls, zero without an accessible name; H1 then four H2s; exactly two contrast failures, both in the review chrome ('Not a sponsor route' 4.40:1, decorative '/' 1.34:1).
- Browser check, Data select switched to 'Live access (HQ)' with the query failing — Fixture figures 40/26/18/15/11 remain on screen as the account's numbers, and the raw SQL including the contact_email column is printed into the error banner.
- studio/src/app/hq/account-review/account-review.tsx:78-79,266-270 — The silent fixture fallback and the verbatim driver-error banner, in code.
- Executed: projectVenueAccessSnapshot with codeAllotment null, allotmentMode limited — Returns Allotted 0 / Available 0 and 'Allotment headroom is exhausted', while venue-allotment.ts:55-59 returns null for the same input. Coercion at project-venue-access.ts:187.
- find studio/src/app -name 'loading.tsx' -o -name 'error.tsx' -o -name 'not-found.tsx' — Returns only ./error.tsx and ./not-found.tsx at the app root — no loading.tsx anywhere; error and not-found do cover the segment by inheritance.
- node studio/scripts/check-loading-contract.mjs → '[loading-contract] ok (studio)' — The loading gate passes vacuously; its header at lines 21-23 states studio is a no-op.
- studio/src/lib/entitlements-db/audit.ts:53-55,82; venues.ts:182 — A real hash-chained append-only audit ledger exists for HQ mutations — candidate evidence for the venue-visible history.
- grep for assertSnapshotPrivacy and scanTextForPrivacyLeaks across studio/src — assertSnapshotPrivacy has one production caller (load-venue-access.ts:96); scanTextForPrivacyLeaks has zero, leaving download/route.ts:39,51 and project-venue-usage.ts:75-104 unchecked.
- studio/src/app/hq/account-review/download/route.ts:17,39,51 — The export route runs only the shared-password guard, then emits CSV and HTML with no capability check and no text privacy scan.
- `npx eslint src/lib/account src/app/hq/account-review/download` → exit 0. `npx eslint src/lib/account src/app/hq/account-review` → 1 error, react-hooks/set-state-in-effect at account-review.tsx:86, a file this package did not touch. — Every file changed here is lint-clean; the one lint error in the tree is pre-existing and sits in the next agent's file.
- Browser live run: venue picker options were 'glenmara-house|Venue 07/25' and 'rivermill-hall|Venue db8e6e'; a page-text check for the non-selected venue's name returned false. — F-2 is closed. Note I first shipped a slice-based token that rendered 'Venue ermill' from a name-derived id; I caught it in this same run and replaced it with a sha256 token so the label's safety does not depend on the id format.
- Dev-server log: '[account-review listLiveVenueOptions] failed: ... no such column: "founding_number"' appears server-side only, while the page rendered the fixed string 'Live venues could not be reached.' — The companion leak is fixed: the raw driver message, which contains the full sponsors SELECT including contact_email, no longer reaches the client.

## Your decision

Approve all of the above in one command:

```bash
node studio/docs/execution/venue-edition-and-films/tools/project-control.mjs approve-batch "your approval note" E07.01 E07.02 E07.03 E07.04 E07.05 E07.06 E07.07 E07.08 E07.09 E07.10 E07.11 E07.12 E07.13 E07.14 E07.15 E07.16 E07.17 E07.18
```

Or push back on any single one:

```bash
node studio/docs/execution/venue-edition-and-films/tools/project-control.mjs reject E07.01 "what is wrong"
```


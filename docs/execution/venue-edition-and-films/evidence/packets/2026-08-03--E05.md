<!-- honest-state-banner -->
> ## Read this before approving
>
> The heading below says "ready to approve". That is the tool's way of saying every task
> has acceptance criteria and evidence recorded. **It does not mean the work is complete.**
>
> **9 of 9 tasks in this packet are PARTIAL.** Every one was
> independently re-verified by an adversarial agent whose default was to refute, and the
> verifier's read overrides the build agent's self-assessment wherever they disagreed. Where
> a task was claimed complete and the verifier disagreed, it is recorded below as partial.
>
> | Task | Verified state | Criteria | **Not met** | Evidence |
> |---|---|---|---|---|
> | `E05.01` | **PARTIAL** | 8 | 8 | 26 |
> | `E05.03` | **PARTIAL** | 10 | 7 | 23 |
> | `E05.04` | **PARTIAL** | 11 | 9 | 15 |
> | `E05.05` | **PARTIAL** | 10 | 7 | 9 |
> | `E05.06` | **PARTIAL** | 8 | 5 | 35 |
> | `E05.07` | **PARTIAL** | 8 | 4 | 25 |
> | `E05.08` | **PARTIAL** | 7 | 6 | 26 |
> | `E05.09` | **PARTIAL** | 8 | 4 | 22 |
> | `E05.10` | **PARTIAL** | 9 | 8 | 16 |
>
> Approving a partial task is a legitimate choice — it means you accept the gap as recorded
> and want the task closed anyway. It should be a decision, not an accident. The unmet
> criteria for each task are in its own section, and the reasoning is in the session record.

# Founder review packet — E05

Generated 2026-08-03T14:47:39.294Z from PROJECT_STATE.json. 12 task(s).

**9 ready to approve · 3 not ready.**

Approving sets these tasks to Done and is the only thing that can. Nothing here is Done yet.

## Ready to approve

### `E05.01` Map the complete couple journey from venue invitation through first useful action, active planning, wedding day and Keepsake mode.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. Every route from invitation to first useful action is walked in order, each step naming the file and line, marked exists / partial / absent.
2. The access term is stated as max(redemption + 548 days, wedding date + 90 days) per D-022, with the code path named and its reachability stated. No flat 'eighteen months' claim appears anywhere.
3. Every production break is named as a break, with file and line, and is never described as working.
4. For each of the four products (Notes, Tasks, Timeline, Signal), the map states whether a redeemed couple reaches it and what they see.
5. The map states what the venue can see of private planning content at each stage, with every egress path enumerated, and asserts nothing the code does not support.
6. No seat count, allotment or numeric per-couple entitlement is stated as fact anywhere in the document (D-020).
7. Keepsake and post-wedding stages are described as not built where they are not built, with grep evidence.
8. Every claim is traceable to a file and line in the repository, or is explicitly labelled as needing a live check.

**Evidence**
- studio/docs/execution/venue-edition-and-films/evidence/E05.01-couple-journey-map.md — The deliverable. Section 9 traces all eight acceptance criteria to their proof.
- src/app/sign-up/[[...sign-up]]/page.tsx:33-34,72-73 — welcomeUrl = buildWelcomeUrl(preselected); both fallbackRedirectUrl and forceRedirectUrl are set to it. sp.redirect_url is read only at :24-31 to render a sponsor card. The redeem path is never handed to Clerk.
- src/app/welcome/page.tsx:58 with src/server/db/venue-welcome.ts:39-55 — /welcome has no redeem fallback. detectVenueWelcome queries entitlements for a row redemption would have created; with no redemption it is null and the venue branch at :69-86 never runs.
- src/server/actions/comp.ts:215-217 — expiresAt = new Date(Date.now() + row.durationDays * 24*60*60*1000). Flat. This is the shipped path. D-022 is not applied.
- studio/src/lib/entitlements-db/codes.ts:29-34,252-267,285 — D-022 is correctly implemented here and the header itself says the public redeem write lives in the Tasks repo. grep for redeemLicenseCode|setCoupleWeddingDate across studio/src and studio/scripts returns only codes.test.ts and one re-export at index.ts:101.
- studio/scripts/issue-codes.ts:121-129 — Mint still fails on any venue_edition duration other than exactly 548. D-022 point 4 said to relax this guard. Not done.
- src/app/app/layout.tsx:11,16 with src/server/require-app-access.ts:21-34 and src/server/app-access.ts:29-62 — Blocker 1 re-confirmed. The outer allowlist-only gate fires before the membership-aware requireAppAccessTasks called at src/components/app/tasks-runtime-shell.tsx:67. A redeemed couple already holds a workspace_members owner row from comp.ts:206-207, so the inner gate would admit them.
- src/modules/timeline/server/actions/workspaces.ts:93 — createWorkspaceAction has zero callers across src, e2e, scripts and experience. Blocker 2 re-confirmed. Empty state at src/modules/timeline/app/page.tsx:48-71 advises 'Create a workspace in Tasks first', which does nothing.
- src/modules/timeline/server/sync/tasks-milestone-source.ts:120-141 and src/modules/signal/lib/briefing/tasks-db-source.ts:114 — NEW second Timeline blocker not in the prior audit. Milestones require is_milestone=1 and order by due_at; Signal reads due_at. The wedding template sets neither, so even a provisioned Timeline renders empty.
- src/components/app/settings/sections/billing.tsx:90-104 — NEW. Four defects on one couple-facing card: price 'Via venue partner' (banned term, R-042), 'Twelve months' (wrong term, contradicts 548 days and D-022), 'Up to 6 collaborators' (a seat count against D-020, and fiction since getMemberCapacity returns unlimited), '12 months of editing for the planner' (wrong term, planner voice).
- src/server/db/membership.ts:95-115 with src/components/app/settings/sections/members.tsx:96 — D-020 verified HELD in the runtime: wedding tier returns max:null and the seat counter is suppressed. The violation is copy-only.
- src/server/db/schema.ts:308 with src/components/welcome/contextual-onboarding.tsx:383 and src/lib/planning/flags.ts:33 — The wedding date is already asked for and already has a column, on a path whose flag defaults to NODE_ENV !== production, so it is off in production and wired to nothing.
- studio/docs/execution/venue-edition-and-films/tasks/E05.01.md — EXISTS. Well-formed against the template, status founder_review, sign-off block empty and unfilled. Eight acceptance criteria match the claimed set verbatim.
- _wt-wp08/src/server/actions/comp.ts:215-217 — CONFIRMED verbatim: 'const expiresAt = new Date(Date.now() + row.durationDays * 24 * 60 * 60 * 1000,);'. Flat multiply, shipped path, D-022 not applied. Also confirmed the atomic conditional UPDATE at :227-233 as the real over-issue guard, and ensureUserProvisioned at :206.
- _wt-wp08/src/server/db/ensure-user.ts:108-109 — CONFIRMED the map's load-bearing assumption for the blocker-1 fix: 'INSERT OR IGNORE INTO workspace_members (workspace_id, user_id, role) VALUES (..., 'owner')'. A redeemed couple does hold a membership row, so requireAppAccessTasks would admit them.
- _wt-wp08/src/app/app/layout.tsx:11,16 + src/server/require-app-access.ts:21-34 + src/server/app-access.ts:29-62 + src/components/app/tasks-runtime-shell.tsx:67 — BLOCKER 1 CONFIRMED at every cited line. Both gates are production-only (isProductionMode() === getAccessMode() === 'production'), so the bounce is a production behaviour, not a local artefact. The outer SharedAppGate wraps the shell that calls the membership-aware gate.
- studio/src/lib/entitlements-db/codes.ts:29-34,252-267,285 + studio/src/lib/venue-edition.ts:100-121 + studio/scripts/issue-codes.ts:121-129 — D-022 IMPLEMENTED-AND-UNREACHABLE CONFIRMED at every cited line. The header at :30-31 does say the public redeem write lives in the Tasks repo. grep for redeemLicenseCode|setCoupleWeddingDate across studio/src and studio/scripts returns only codes.ts itself, codes.test.ts, and the index.ts:101 re-export. venue-lifecycle.ts's only importer is venue-lifecycle.test.ts. issue-codes.ts:124-128 still fails any venue_edition duration other than 548 — D-022 point 4 is not done.
- _wt-wp08/src/app/api/internal/partner-stats/route.ts — PRIVACY: read in full. Bearer PARTNER_STATS_SECRET with timingSafeEqual, reads comp_codes by json_extract sponsor_slug and entitlements by notes IN (...), returns exactly four scalars. No title, tag, name, date or workspace identifier. The map's 'no small-cell floor' gap is real and correctly attributed elsewhere.
- _wt-wp08/src/server/planning/queries.ts:225-245 — PRIVACY: listSponsorActivationDTOs allowlist verified field by field — activationId, sponsorReference, status, metadata, metadataVersion, consentReceiptId, consentedAt, revokedAt. No workspace name, task, note, member or link. Zero production callers; the only other references are assertions inside venue-never-a-collaborator.test.ts.
- _wt-wp08/src/lib/account/instrumentation/event-schema.ts:7,64-79 — PRIVACY, attacked independently: the sponsored-use event schema carries a denylist covering title, name, body, content, note, filename and states 'It carries no content, no identity, and no destination'. The sink at sink.ts:36 is a noop. No content egress via instrumentation.
- studio/src/lib/hq/product-analytics.ts:93,103-199 + studio/src/server/today/aggregate.ts:80,274-300 — PRIVACY, attacked independently: these are the only studio paths that open TASKS_DATABASE_URL outside issue-codes and the migration script. I read the SQL. Every statement is COUNT(*) / COUNT(DISTINCT) or a timezone/cadence preference. No task title, note body or workspace name crosses. They are also not sponsor-scoped. No refutation found.
- _wt-wp08/src/server/venue-never-a-collaborator.test.ts — 8 tests, 8 pass under npx tsx --test. Test at :225 'the sponsor-facing read path carries no membership field' pins the DTO allowlist against regression. This CONTRADICTS section 7's closing claim that no such test exists, and it strengthens the privacy verdict rather than weakening it.
- _wt-wp08/src/lib/wedding-template-contract.test.ts — 6 tests, 6 pass. 'audit pin (E05.03): the shipped wedding template declares ZERO milestones' and 'no task carries a wedding-relative due offset' both pass, so the map's second Timeline blocker is CONFIRMED and pinned. Cross-checked: git diff shows apply-template.ts now writes dueAt and isMilestone, but templates.generated.ts contains zero occurrences of either 'milestone' or 'dueOffsetDays', so the mechanism is live and the data is still inert. The claim holds.
- _wt-wp08/src/components/app/settings/sections/billing.tsx:93,94,97,98,175 — All four copy defects CONFIRMED present, but two of the four cited line numbers are wrong (:93 not :92, :97 not :96). Line 175 is an unenumerated second call to redeemCompCodeAction in the same file the map audits.
- studio/scripts/partner-digest.ts:16-20,60-103 — UNENUMERATED VENUE-FACING EGRESS. Usage text: prints a summary 'suitable for forwarding to the venue contact'. buildDigest emits ungated small-cell counts in prose: ':82 N couples have redeemed so far', ':94 N couples havent reached the workspace yet'. Counts only, no content, so the boundary holds, but criterion 5's enumeration is incomplete.
- _wt-wp08 git diff src/app/invite/[token]/page.tsx — MISATTRIBUTION PROOF. The diff adds a comment beginning 'E05.10: this page is the first thing a couple's spouse, planner or family member sees'. E05.10 is WP-08's own task and its evidence doc was written by this session at 08:18. The experience:validate failure is this package's, not a sibling's.
- studio/docs/execution/venue-edition-and-films/evidence/E05-production-blocker-app-gate.md — The first of the two production blockers the audit found, now fixed and verified: the shared /app gate no longer bounces invited and redeemed users to /waitlist. Full suite exit 0. Also records three privacy tests that had never run in CI, now registered.
- Specification: `studio/docs/execution/venue-edition-and-films/tasks/E05.01.md`

### `E05.03` Build the default wedding workspace template with restrained milestones, decisions, tasks and example Notes.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. A written audit states how many tasks, milestones, decisions and example Notes the shipped template produces, each with a file and line.
2. The audit answers plainly whether the current template is restrained, against a stated criterion.
3. The canonical source and the build artifact are identified, the generator's resolved source path is recorded, and content changes are shown to belong in the canonical source.
4. A template task can declare a Timeline milestone, and applying the template writes tasks.is_milestone accordingly, proven by a test.
5. A template task can declare a due date relative to the wedding date, and applying the template writes a real tasks.due_at when the workspace has an anchor date, proven by a test.
6. When no anchor date exists, no due_at is written and no date is invented, proven by a test.
7. A machine check fails when a new task instructing collection of special-category guest data is added to the wedding template, proven by a fail-before run.
8. R-017 is neither silently removed nor redesigned; handling options with trade-offs are put to the founder.
9. The template seeds example Notes for the couple.
10. The template seeds decisions.

**Evidence**
- studio/docs/execution/venue-edition-and-films/evidence/E05.03-wedding-template-audit.md — The audit. Section 1 the source/artifact trap, section 2 what ships, section 2a the restraint verdict, section 3 the build, section 4 R-017, section 6 criterion-to-proof, section 7 verification.
- scripts/sync-templates.ts:20 — resolve(__dirname, '../../studio/src/lib/templates') resolves from _wt-wp08/scripts to the LIVE studio working tree, which this package may not edit and which carries another lane's uncommitted work. This is why template CONTENT could not be changed here.
- src/lib/templates.generated.ts:1 vs scripts/sync-templates.ts:62 — The committed artifact says '// AUTO-GENERATED, do not edit by hand.' while the generator emits '// AUTO-GENERATED — do not edit by hand.' with an em dash. Proof that the artifact has been hand-edited and that re-running the generator would revert hand edits.
- _wt-wp08/src/lib/template-anchor.ts — New pure module. resolveTemplateDueAt returns null with no anchor, deliberately, rather than falling back to days-from-now.
- _wt-wp08/src/server/db/apply-template.ts — Now reads workspaces.primary_date as the anchor and writes dueAt: resolveTemplateDueAt(...) and isMilestone: t.milestone === true. Optional options.anchorDate lets the redemption path supply the wedding date directly once it is captured.
- _wt-wp08/src/lib/wedding-template-contract.test.ts — R-017 forward guard plus four audit pins. The ledger holds one entry, marked open, with a comment stating that adding a title to clear the test is not the fix.
- src/lib/templates.generated.ts:108 — The ONLY production occurrence of 'Collect final dietary notes' in the whole app repo. Canonical source is studio/src/lib/templates/wedding-planning-workspace/tasks.ts:69.
- src/server/suite-navigation-contract.test.mjs:464,495 — CORRECTS the prior audit. This asserts 'Confirm the final dietary list before the venue team locks the service notes', which is Notes/Tasks DEMO FIXTURE copy, not the template string. The two were conflated. Removing the template task breaks no test.
- studio/src/lib/templates/wedding-planning-workspace/meta.ts:15 — audience: 'wedding-planner'. The template was written for the planner, not the sponsored couple.
- studio/src/lib/templates/wedding-planning-workspace/notes.ts:9-14 with scripts/sync-templates.ts:51-58 — Criterion 9 UNMET. The canonical note seed exists; the generator copies only id, name, description, icon, domain and tasks, so it is dropped at the repo boundary. grep for NoteSeed or template.notes across src/ returns zero.
- _wt-wp08/src/server/db/schema.ts — Criterion 10 UNMET. No decision table or object exists anywhere. A 'decision' tag on three template tasks is the whole of it.
- evidence/E05.03-wedding-template-audit.md — EXISTS, 18217 bytes. All 12 evidence paths in the claim exist; not one dead path. I read the whole document and the task spec.
- _wt-wp08/src/lib/templates.generated.ts — CONFIRMS the generated file was NOT hand-edited by this package. Absent from git status; git diff --stat empty; md5 37f9b3c35dccd0c240c7af207accf0de before my mutation and byte-identical after restore. The recurring failure mode 'an edit to a GENERATED file a build step will overwrite' does not apply.
- _wt-wp08/src/app/welcome/page.tsx:73 — THE DEFECT. applyTemplateToWorkspace(VENUE_TEMPLATE_ID, ws) is called with no options, and the UPDATE at :74-81 does not set primary_date. VENUE_TEMPLATE_ID is 'wedding-planning-workspace' (:23), so the guard does target the right template, but the anchor is null at apply time.
- _wt-wp08/src/app/welcome/page.tsx:60-61 — COMPOUNDS THE DEFECT. When planningFlags.contextualOnboarding is on, a venue arrival redirects to /welcome/plan and line 73 is never reached, so no template is applied at all. src/lib/planning/flags.ts:33 sets defaultEnabled = env.NODE_ENV !== 'production', so that is the default everywhere except production.
- _wt-wp08/src/app/welcome/plan/page.tsx — grep -c for 'applyTemplate|TEMPLATES' returns 0. The only branch that captures the wedding date applies no template; the only branch that applies the template captures no date. The two halves of the mechanism never meet.
- _wt-wp08/src/server/actions/comp.ts:259 — Second applying caller, same shape: no options.anchorDate, and the UPDATE at :260-265 sets only template_id and active_domain.
- _wt-wp08/src/server/db/apply-template.ts:43-49,77-81 — The code is correct and reads workspaces.primary_date as the anchor, writing dueAt and isMilestone. Verified in the diff. The implementation is sound; only its reachability and its test coverage are not.
- _wt-wp08/src/server/suite-navigation-contract.test.mjs:464,495 — THE BUILD AGENT'S CORRECTION OF THE PRIOR AUDIT IS CORRECT. Those two lines assert 'Confirm the final dietary list before the venue team locks the service notes' - demo fixture copy. The prior audit cited :428 and :459, which assert an IdentityCell render and a sourceNoteId respectively. Repo-wide, 'Collect final dietary notes' appears in exactly two places: templates.generated.ts:108 and the new ledger at wedding-template-contract.test.ts:54. The prior audit's 'nine touchpoints, removing it fai
- _wt-wp08/package.json — GOOD. The diff adds src/lib/template-anchor.test.ts and src/lib/wedding-template-contract.test.ts to the pnpm test chain, so the R-017 guard runs in CI rather than being a control that exists only ad hoc.
- _wt-wp08/scripts/sync-templates.ts:20,57-62 — Criterion 3 verified. resolve(__dirname, '../../studio/src/lib/templates') is real at :20. The slice at :57-62 copies id, name, description, icon, domain, tasks - dropping notes, which is exactly why criterion 9 fails.
- _wt-wp08/src/modules/timeline/server/sync/tasks-milestone-source.ts:133 — WHERE t.is_milestone = 1 confirmed, and src/modules/signal/lib/briefing/tasks-db-source.ts:114 selects t.due_at. Both consumer claims that motivate the whole build are true, so the mechanism is the right thing to have built.
- _wt-wp08/src/modules/timeline/lib/templates.generated.ts — Trap 4 confirmed independently: it carries the AUTO-GENERATED banner and grep over scripts/ finds no generator that writes it. Hand-maintained under a generated file's banner.
- Specification: `studio/docs/execution/venue-edition-and-films/tasks/E05.03.md`

### `E05.04` Complete the wedding-specific Notes experience, including voice capture, manual capture and high-signal structured output.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. The audit states whether voice capture exists in the shipped build, with file and line, and does not claim a capability that is absent.
2. Before a couple's first dictation, the surface states plainly that the spoken words leave the device, names what receives them, and says typing does not. The statement is standing, not a dismissible one-time dialog.
3. Each dictation failure (permission refused, nothing heard, engine failure, no engine at all) produces a distinct plain-English message.
4. A browser with no speech engine explains that in words, rather than the control silently disappearing.
5. Notes selects a wedding register from data already crossing the Tasks boundary, with no new API field, no new route and no migration.
6. In the wedding register the capture placeholder, the privacy line and the empty state read in wedding-planning language; every other account is unchanged.
7. The wedding signal is presentation only. It grants no access, hides no control and scopes no query.
8. Every string added or changed passes the brand-voice mechanical lint with zero HARD findings.
9. Nothing this task changes alters what a venue can see. The venue still sees no private planning content.
10. pnpm typecheck clean and pnpm test green, with the new guards inside the CI chain rather than beside it.
11. NOT MET: the title also says 'high-signal structured output'. No wedding-shaped structured capture was built, deliberately, because R-017 is open.

**Evidence**
- evidence/E05.04-wedding-notes-and-voice-capture.md — Full criterion-to-proof evidence document with real command output, the fail-before run, and an explicit 'what is NOT done' section.
- src/modules/notes/app/notebook/hooks.ts:177 — useVoiceCapture — voice capture genuinely exists via the Web Speech API. This is the finding that most changed the task: the brief warned against claiming it or faking it, and it turned out to be real.
- src/modules/notes/app/page.tsx:50-51 — hybridNotebookEnabled = process.env.NOTES_LEGACY_NOTEBOOK_ENABLED !== '1'. HybridNotebook is the default; the legacy notebook needs an explicit opt-out. Establishes which surface 'shipped' means.
- src/server/db/ensure-user.ts:79-99 with src/server/actions/comp.ts:259-264 — CORRECTS a plausible wrong turn. A redeemed couple's workspace is provisioned with planning period context_type='general' and workspace context_type='project'; redemption then sets ONLY template_id and active_domain='wedding'. So contextType is NOT the wedding signal despite already reaching Notes in the workspace DTO. active_domain is the only signal.
- src/app/api/internal/workspace-personalization/route.ts:98 — The route already returns activeDomain. Notes' fetchTasksPersonalization had zero callers and dropped it. This is why criterion 5 (no new field/route/migration) is satisfiable at all.
- _wt-wp08/src/modules/notes/lib/notes-copy.ts — New. Two-register copy source. voice.disclosure is the string that closes the dictation privacy gap.
- _wt-wp08/src/modules/notes/lib/notes-copy.test.ts — New. 11 guards including 'dictation discloses that spoken words leave the device' and 'every dictation failure has its own plain-English message'.
- grep -rni 'speech|dictat|microphone' src/app/privacy/page.tsx src/app/terms docs — Zero disclosure anywhere before this task. The only hit was a code comment about brand 'voice'. This is the evidence for the new RAID candidate.
- _wt-wp08/src/modules/notes/app/notebook/hooks.ts — EXISTS, 305 lines. useVoiceCapture now at line 190 (HEAD 177). Diff adds only the VoiceEngineState tri-state and its plumbing. Voice capture is genuinely pre-existing, not fabricated.
- _wt-wp08/src/modules/notes/lib/notes-copy.ts:268 — DEFECT SOURCE. Wedding handoff.boundary ends 'Your venue never sees any of it.' That absolute promise is rendered above the control that moves the couple's words into Tasks, and approved words can reach an indexable public page. See failure_scenario.
- _wt-wp08/src/lib/public-task.ts:11 — toPublicTask publishes task.title on the unauthenticated allowlist. Combined with src/server/db/queries.ts:218 and src/app/p/[slug]/page.tsx ('Server-rendered for indexing', revalidate 60, /p absent from src/app/robots.ts disallow, no noindex), the approved wording is crawlable.
- _wt-wp08/src/modules/notes/app/Notebook.tsx:1144,1278,1300-1310 — The rollback notebook still calls useVoiceCapture, still gates the mic on `voice.supported &&` so it silently vanishes, still carries only three inline failure messages, shows no disclosure, and never receives activeDomain (page.tsx:174). Its whole diff is 4 lines of aria-label/copy.
- _wt-wp08/experience/feature-tests/notes-hybrid.spec.ts:88,152,168,170,172,175,254,259,262,307,309,312,315 — Eleven-plus assertions now reference strings this diff deleted. Not run by pnpm test, and not run by CI either: its config experience/notes-hybrid.playwright.config.ts has no npm script and no workflow step.
- _wt-wp08/src/app/api/internal/workspace-personalization/route.ts:75-82 — The wedding signal is read from a `.limit(1)` select with no ORDER BY. Pre-existing, but this task makes it load-bearing for which register a couple reads.
- _wt-wp08/package.json — notes-copy.test.ts IS registered in the `test` script's final node --import tsx segment. Verified in the diff and by the 413-test count. The guard is inside CI, not beside it.
- Specification: `tasks/E05.04.md`

### `E05.05` Complete the Notes-to-Tasks promotion flow using ordinary wedding-planning language.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. Every user-visible string in the hand-to-Tasks flow is defined in one module rather than inline in the component.
2. No user-visible string in the flow contains: extract, promote, approved wording, approved action, approved send, approved source, workspace ID, note ID, note identity, immutable, payload, receipt, destination.
3. The privacy sentence still names exactly what crosses and what does not, in words a couple reads, and appears in every state it appeared in before.
4. In the wedding register the boundary sentence states that the venue never sees any of it.
5. Every error state in the flow reads as something a couple can act on.
6. A test enforces criteria 2, 3 and 4 and fails if a banned term returns.
7. That test runs inside pnpm test, not only when invoked by hand.
8. The guard is demonstrated to fail against the pre-task strings.
9. Behaviour is unchanged: same states, same guards, same server actions.
10. pnpm typecheck clean, pnpm test green, brand-voice lint zero HARD findings in the module.

**Evidence**
- evidence/E05.05-notes-to-tasks-language.md — Criterion-to-proof document including a full before/after table of every rewritten string, the direct grep sweep, the fail-before run, and an explicit 'what is NOT done' section.
- _wt-wp08/src/modules/notes/lib/notes-copy.ts — New. Single source for every user-visible capture and hand-off string, in a generic and a wedding register, with BANNED_IN_COPY documenting why each term is forbidden and which of them actually shipped.
- _wt-wp08/src/modules/notes/lib/notes-copy.test.ts — New, 11 guards. Includes key-parity between registers so a wedding account can never fall through to a missing string, plus assertions that no string names a price or promises access forever (D-001 §5, D-009 pt 3, R-008).
- _wt-wp08/src/modules/notes/app/hybrid/HybridNotebook.tsx — The shipped surface. All hand-off strings now resolve through copy.*. A grep for the fourteen retired strings returns nothing; a broader sweep for approv|extract|receipt|payload|promot returns only the code comment explaining where the task id went.
- package.json — SHARED FILE, one line. Added src/modules/notes/lib/notes-copy.test.ts to the test chain so the guard runs in CI. Final test segment went 402 → 413. Declared for collision visibility.
- _wt-wp08/src/modules/notes/app/Notebook.tsx — Legacy rollback path. Only its two genuinely wrong strings fixed: aria-label='Promote note' and 'Signal Tasks', the latter violating docs/SUITE_URL_AND_NAMING_CONTRACT.md. Its remaining wording was already close and it is reachable only via NOTES_LEGACY_NOTEBOOK_ENABLED=1.
- _wt-wp08/package.json — EXISTS. One line changed, as declared. notes-copy.test.ts is in the final test segment. The same line also carries three sibling agents' additions (anchor-due, template-anchor, wedding-template-contract, assignee-roster-contract) — the declared collision surface is real.
- _wt-wp08/src/modules/notes/app/page.tsx:87-111,169 and src/app/api/internal/workspace-personalization/route.ts:78,90,98 — Checked specifically for the 'control that exists only in a test' failure mode. It is NOT one. The route genuinely returns activeDomain, page.tsx fetches it via fetchNotesWorkspaceDomain and passes it to the component. The wedding register is reachable in production.
- _wt-wp08/src/modules/notes/server/notes-cross-product-assertion.ts:64-65, 98-99 — noteId and workspaceId are claims in both the v1 and v2 signed envelopes, so they still cross to Tasks. The retired privacy sentence named them; the replacement does not.
- Specification: `tasks/E05.05.md`

### `E05.06` Complete the wedding Tasks experience for ownership, due dates, status, tags, priorities and clear next actions.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. AC1 — the people offered when assigning a task are the workspace's real members, resolved server-side, and no seed persona or design-lab fixture name can appear in a live workspace.
2. AC2 — a workspace with one member offers one person, and a workspace with no other members says so rather than offering anybody.
3. AC3 — the workspace's wedding date is a first-class stored field, and it is stated plainly in evidence whether a redeemed couple's workspace has one.
4. AC4 — where the workspace has a wedding date, a task's due date is shown against it in exact calendar days, with no rounding to weeks or months.
5. AC5 — where the workspace has no wedding date, no relation is shown and no date is invented.
6. AC6 — status, priority and tags are settable from the task detail and are recorded as already working, with file and line, rather than rebuilt.
7. AC7 — every string added is declarative plain English with no em dash and no exclamation mark.
8. AC8 — pnpm test passes, and the new behaviour is covered by tests that fail against the code as it stood.

**Evidence**
- evidence/E05.06-wedding-tasks-experience.md — Full criterion-to-proof trace with verbatim command output, the fail-before mutation result, and the scope correction.
- _wt-wp08/src/components/app/detail-panel/field-rows.tsx — AC1/AC2/AC4. Was line 26: the five-name seed-persona constant, rendered at line 291 as the whole assign menu. Now reads useWorkspaceMembers() and branches on members.length === 0. DueRow now renders the anchor relation.
- _wt-wp08/src/lib/members.ts:1-6 — AC1. The rule was already written in prose — 'the assign menu ... Resolved server-side from workspace_members ⋈ users' — and nothing enforced it. src/lib/domain-context.tsx says the same: 'never from design-lab fixtures'.
- _wt-wp08/src/components/hybrid/options/a/board-view.tsx:241 — AC1. The board already did this correctly via listPeople(); only the task detail was missed. This is why the fix is a wiring change, not a new system.
- _wt-wp08/src/components/hybrid/shared/task-inspector.tsx:108 — AC1, extension to the prior audit. Same class of defect (imports LAB_PEOPLE directly, bypassing listPeople) but it is NOT live: repo-wide grep finds no importer except suite-navigation-contract.test.mjs:35. Dead code. Recorded, not fixed.
- _wt-wp08/src/server/db/schema.ts:308-309 — AC3. workspaces.primary_date / primary_date_label — 'Calendar date, never an instant.' The wedding date IS a first-class field.
- _wt-wp08/src/components/welcome/contextual-onboarding.tsx:383 — AC3. The field is literally labelled 'Wedding date' and marked required when the context is a wedding; src/server/actions/planning.ts:875 refuses a wedding_season period without one.
- _wt-wp08/src/app/welcome/page.tsx:69-85 — AC3, the finding. The production branch applies the template and sets template_id, active_domain, primary_use_case and onboarding_completed_at — and never writes primary_date. The branch that DOES capture the date (:59-67) is gated on a flag that src/lib/planning/flags.ts:33 defaults OFF in production.
- _wt-wp08/src/server/actions/comp.ts — AC3. redeemCompCodeAction applies the wedding template and short-circuits to /app/tasks. It never writes primary_date. Combined with the above: a redeemed couple's workspace has primary_date = NULL in production, always.
- _wt-wp08/src/components/app/settings/sections/workspace.tsx — AC3. Zero matches for primaryDate, primary_date, 'Wedding date' or anchor. There is no field anywhere in the app to set or change the wedding date after the workspace exists.
- _wt-wp08/src/lib/tasks/anchor-due.ts — AC4/AC5/AC7. Pure module. relateDueToAnchor returns null for a missing or malformed date rather than guessing; counts are exact calendar days; the noun comes from primary_date_label so 'Examinations' works, and a label already carrying an article is not doubled.
- _wt-wp08/src/lib/tasks/anchor-due.test.ts — AC4/AC5/AC7/AC8. 14 tests: DST boundary, leap day, 2027-02-30 refused, 12/06/2027 refused, singular/plural, three tenses, and an explicit assertion that no generated line contains an em dash or an exclamation mark.
- _wt-wp08/src/components/app/detail-panel/assignee-roster-contract.test.mjs — AC1/AC2/AC8. Source-scan regression guard. Fail-before proven: restoring the ALL_USERS constant produced 'ℹ tests 4 / ℹ pass 3 / ℹ fail 1' on exactly the seed-persona test, then the mutation was reverted.
- _wt-wp08/src/lib/domain-context.tsx — AC4. anchorDate/anchorLabel added to DomainCtx with useWorkspaceAnchor(); both props optional and defaulted to null, so the single existing DomainProvider caller was the only one that had to change.
- _wt-wp08/src/lib/template-anchor.ts — AC3, independent corroboration. The concurrent E05.03 package in this same worktree reached workspaces.primary_date from the write side and made the same refusal (no anchor means no due date, never days-from-now). Two packages arriving at the same field independently.
- _wt-wp08/src/components/hybrid/fixtures.test.ts:83 — AC2, and a finding. The 'empty roster reads as empty, never eight fixture people' property was already asserted here — but grep -c 'hybrid/fixtures.test.ts' package.json returns 0. It was tested and never run. The new contract test IS in the chain.
- _wt-wp08 — all 20 claimed evidence paths — Checked every one with a file-existence loop. 20 of 20 EXIST. No dead paths.
- pnpm test in C:/Users/ethan/signal-studio-workspace/_wt-wp08 — PNPM_TEST_EXIT=0. Group totals extracted from the log: 13/50/70/20/31/20/90/45/239/413, every group 'ℹ fail 0'. This is an EXACT match to the claimed totals.
- node --import tsx --test src/lib/tasks/anchor-due.test.ts — ℹ tests 14 / ℹ pass 14 / ℹ fail 0. Reproduces the claim exactly. Includes the DST-boundary, leap-day, 2027-02-30-refused and 'no em dash or exclamation mark' assertions.
- node --test src/components/app/detail-panel/assignee-roster-contract.test.mjs — ℹ tests 4 / ℹ pass 4 / ℹ fail 0. Reproduces exactly.
- FAIL-BEFORE, re-run independently by me — I re-inserted `const ALL_USERS: UserId[] = ["chloe","david","alex","ada","marcus"]` into field-rows.tsx myself. Result: '✖ no seed persona id is hard-coded as an assignable person', AssertionError naming persona "chloe", exit 1 — exactly one failing test, the right one. I then restored from backup; git diff --stat returns to '68 +++, 60 insertions(+), 8 deletions(-)' and the test is 4/4 green. The contract test is NOT tautological.
- pnpm typecheck — TYPECHECK_EXIT=0. The disclosed mid-session TS2540 NODE_ENV failure in the concurrent package's unpublished-timeline-boundary.test.ts is genuinely resolved.
- pnpm ds:check — 'ds-check: clean — no drift.' DS_EXIT=0.
- npx eslint on all nine changed files — ESLINT_EXIT=0, zero problems. I included conversation-feed.tsx and task-detail.tsx, which the evidence table omits.
- _wt-wp08/package.json (git diff) — AC8. Both new test files are genuinely in the chain: assignee-roster-contract.test.mjs added to the node --test group, src/lib/tasks/anchor-due.test.ts added to the tsx group. Verified in the diff, not asserted.
- _wt-wp08/src/components/app/task-detail/metadata-rail.tsx:114-128 — AC6 DEFECT. Tags render as read-only <span> chips with one uniform neutral style (bg-bg-sunken/60, text-ink-soft), inside a `task.tags && task.tags.length > 0` guard. No button, no onClick, no updateTask. There is no per-tag colour lookup in this file at all.
- _wt-wp08 repo-wide grep for tag-setting controls — AC6 DEFECT, decisive. `grep -rn tags src/components/app/ --include=*.tsx | grep -E 'updateTask|setTags|onChange|onClick'` returns EMPTY. `grep -rln 'TagRow|TagPicker|TagEditor' src/` returns EMPTY. Tags cannot be added or removed from the task detail, or from anywhere in src/components/app/.
- _wt-wp08/src/lib/domain-context.tsx:171 useTagDefs() — AC6 DEFECT. useTagDefs() exists and tagDefs are threaded from the server through DomainProvider, but it has ZERO consumers in src/components/app/. The colour source is delivered to the client and never read, which is why the 'live colours' claim is false.
- _wt-wp08/src/app/app/your-work/page.tsx:13 and your-work-view.tsx:562-563,627,631 — AC3/AC4, extension the build agent did not record. The ONE surface in the app with a primaryDate input is the Your Work bulk-create form. Its route redirects to /app/tasks when the planningPeriods flag is off, and flags.ts:33 defaults it OFF in production, exactly like contextualOnboarding.
- _wt-wp08 grep for `primary_date/s*=` across src/ — AC3, my finding. Returns NOTHING. There is no UPDATE path for primary_date anywhere. Every write is an INSERT at workspace creation. The field is not merely unset for redeemed couples, it is unsettable after creation by any surface.
- _wt-wp08/src/components/app/detail-panel/field-rows.tsx nameFor() — AC1 DEFECT, narrow. `nameFor = (id) => members.find(m => m.id === id)?.name ?? USERS[id].name`. USERS (src/lib/data.ts:150) is a Proxy over SEEDED returning the canonical persona row for a known seed id, so an assigned-but-non-member seed id still renders a persona name in the avatar strip. Latent rather than live: apply-template.ts:74 writes `assignees: []` and the wedding template carries no persona ids. The contract test cannot catch it — it scans for quoted ids only.
- _wt-wp08 git status / git diff --stat — UNDECLARED SCOPE. conversation-feed.tsx and task-detail.tsx are modified and belong to this change (the mention pool now reads useWorkspaceMembers, and task-detail passes assigneeIds). `git show HEAD:...conversation-feed.tsx | grep useWorkspaceMembers` returns nothing, so the change is new. The evidence doc's Files touched table lists 8 files and omits both.
- PRIVACY BOUNDARY — checked, clean — No new wedding-date egress. src/lib/public-task.ts carries no primaryDate or anchor field. The only route exposing primaryDate, src/app/api/internal/workspaces/route.ts, is HMAC-signed with timingSafeEqual, jti replay tracking and membership-scoped rows, and was NOT touched by this change. The anchor renders only inside the authenticated app shell to workspace members.
- PROGRAMME FAILURE MODES — checked, clean — Grepped all added lines across the six changed source files for forever / for life / in perpetuity / seat / allotment / dietary / 18 month / eighteen month / guaranteed: zero hits. No generated-file edit (no 'generated' or 'do not edit' header on any E05.06 file). No invented dietary field, so R-017 is not touched. No flat eighteen-month access term and no seat count.
- _wt-wp08 grep -c 'hybrid/fixtures.test.ts' package.json → 0 — AC2. Independently confirms the build agent's own self-reported finding that fixtures.test.ts was written and never run. Honest disclosure, verified true.
- Specification: `tasks/E05.06.md`

### `E05.07` Complete the task-detail experience for comments, attachments, subtasks, decisions and private collaboration.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. AC1 — comments exist as durable, attributed, per-task records with a composer, proved by table and component rather than asserted.
2. AC2 — attachments exist with server-side size, type and quota enforcement and an authenticated download, proved by file and line.
3. AC3 — subtasks exist as real child tasks that stay out of the board, list, calendar and timeline views.
4. AC4 — a member of the workspace can be mentioned in a comment from the first comment onward, without having had to comment first.
5. AC5 — decisions are reported honestly as existing or not existing, with the search that establishes it.
6. AC6 — no comment, attachment, subtask or assignee reaches an unauthenticated surface, proved by the public projection allowlist and its test.
7. AC7 — nothing in this task adds or widens an upload path.
8. AC8 — pnpm test passes and the new behaviour is covered by a test that fails against the code as it stood.

**Evidence**
- evidence/E05.07-task-detail-experience.md — Full criterion-to-proof trace, including the three-ships / one-broken / one-absent verdict table.
- _wt-wp08/src/server/db/schema.ts:479-516 — AC1. comments table with cascade, author FK and both hot indexes, plus the compile-time contract that keeps the client Comment type covered.
- _wt-wp08/src/server/actions/attachments.ts:35-44 — AC2/AC7. BLOCKED_MIME_TYPES refuses text/html, image/svg+xml, xml and the executable shapes, with an honest comment: 'Not a full antivirus pass; enough to block the obvious executable + stored-XSS shapes.'
- _wt-wp08/src/server/actions/attachments.ts:182-235 — AC2. Active-workspace ownership check before any write, then claim-then-verify: the metadata row is inserted before the bytes, the usage SUM includes the claim, and an over-quota or failed byte write deletes the claim.
- _wt-wp08/src/app/api/attachments/[id]/route.ts — AC2. Authenticated download re-checks the workspace and returns 404 (not 403) for mismatched, unknown and missing; sends X-Content-Type-Options: nosniff, default-src 'none'; sandbox; frame-ancestors 'none', and Cache-Control: private, no-store.
- _wt-wp08/src/server/db/schema.ts:73-83 — AC3. parentTaskId with one level of nesting; getTasks filters parent_task_id IS NULL so subtasks stay out of board, list, timeline and calendar.
- _wt-wp08/src/components/app/detail-panel/subtasks-section.tsx — AC3. 372 lines: getSubtasksAction reads, addTaskAction/updateTaskAction write, ReorderList drag, and clicking a subtask swaps the panel's active id with a working back button.
- _wt-wp08/src/components/app/detail-panel/conversation-feed.tsx — AC4. Was: mention pool = assignees (never passed) plus past commenters. Now reads useWorkspaceMembers() and merges members, assignees and past speakers, with member names winning over the seed USERS proxy.
- _wt-wp08/src/components/app/task-detail/task-detail.tsx — AC4. PrimaryContent rendered ConversationFeed with no assigneeIds prop at all; it now passes task.assignees.
- _wt-wp08/src/lib/templates.generated.ts — AC5. The ONLY trace of decisions anywhere is a 'decision' tag on three wedding-template tasks. No decisions table in src/server/db/schema.ts, no decision route under src/app/app/.
- _wt-wp08/src/server/db/schema.ts:99-110 — AC5. The near-miss re-confirmed: tasks.source_note_extract_body already stores 'exact creator-approved wording' with a SHA-256 alongside. grep -rn sourceNoteExtractBody across src/components and src/app returns ZERO. The one field that already records a decision someone made is read by no component.
- _wt-wp08/src/lib/public-task.ts — AC6. Field-by-field allowlist with its reason written in: 'so adding a private column to Task cannot silently publish it through /p, /embed, or /share'. Comments, attachments, subtasks, assignees, description, contact and amount are all absent.
- _wt-wp08/src/lib/public-task.test.ts:33 — AC6. 'public task projection is a stable allowlist' — and it IS in the pnpm test chain, so the boundary is machine-enforced rather than documented.
- _wt-wp08/src/app/api/internal/partner-stats/route.ts:25-28 — AC6. The only venue-facing egress from the app returns four scalars — codesRedeemed, redeemed30d, reachedBoard, mostRecentRedemptionAt. No task, comment, attachment, subtask, name or title.
- _wt-wp08/src/components/app/detail-panel/assignee-roster-contract.test.mjs — AC4/AC8. Covers conversation-feed.tsx as well as field-rows.tsx: both must call useWorkspaceMembers(), neither may name a seed persona, neither may import the design-lab fixture roster.
- _wt-wp08/src/server/actions/comments.ts:38-50 — THE DEFECT. VALID_USER_IDS = new Set(Object.keys(USERS)) resolves to the six seed persona ids because USERS is a Proxy over SEEDED with no ownKeys trap. extractMentions fires notify() only for those. Real Clerk-backed members can never match.
- _wt-wp08/src/components/ui/mention-field.tsx:204 — const mention = `@${person.name}` — the composer inserts the display name, not an id and not a handle. Nothing downstream resolves a display name.
- _wt-wp08/src/server/db/schema.ts:181-183 — users.handle is declared as 'Mention slug, what @<handle> matches in the comment composer' and is read by no mention code anywhere. The intended design is documented in the schema and unbuilt.
- _wt-wp08/src/server/db/notifications.ts:22-56 — notify() inherits the workspace from the task but never checks that userId is a member of it, so a mention misrouted to a seed persona id writes a live but unreachable row inside the couple's workspace.
- evidence/E05.07-task-detail-experience.md:114-116 — The false load-bearing sentence: 'The downstream mechanism was already correct and was not touched.' Refuted by the reproduction above. The same claim is duplicated as a code comment in conversation-feed.tsx.
- _wt-wp08/src/lib/public-task.ts + src/server/db/queries.ts:218,744-745 — AC6 UPHELD after an attempt to break it. toPublicTask is the sole projection on all four unauthenticated route families (/p, /p opengraph, /embed, /share). No private field escapes.
- _wt-wp08/src/app/api/internal/partner-stats/route.ts:33-56 — AC6 UPHELD. The only venue-facing egress: bearer + length-guarded timingSafeEqual, returns four scalars. No task, comment, attachment, subtask or name.
- _wt-wp08/src/server/db/queries.ts:103,136,331,831 — AC3 UPHELD. isNull(tasks.parentTaskId) at all four read sites, so subtasks are genuinely absent from board, list, calendar and timeline.
- _wt-wp08/src/app/api/attachments/[id]/route.ts:34-35,67-70 — AC2 UPHELD. 404-not-403 on mismatch, plus nosniff, sandbox CSP and private/no-store, verbatim as claimed.
- git status --porcelain in C:/Users/ethan/signal-studio-workspace/_wt-wp08 — AC7 UPHELD. No file under src/server/actions/attachments.ts, src/server/storage.ts, src/app/api/attachments/** or resources-section.tsx is modified. No upload path added or widened.
- Specification: `tasks/E05.07.md`

### `E05.08` Complete the authenticated Timeline planning experience and its relationship with tasks and milestones.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. A publication in draft state never resolves for a bearer holding a valid, active, unexpired share token.
2. Moving a publication to published is the only state change that opens the boundary, and moving it to unpublished closes it again for the same token.
3. A revoked share cannot read a published publication.
4. The published projection carries presentation fields only: no source workspace id, no source relation, no digest, no workspace slug, no owner identity, no sponsor reference.
5. The relationship between Tasks and Timeline is stated exactly: which tasks become Timeline milestones, under what condition, and what a sponsored couple actually gets today.
6. Every venue-facing read path out of this app is enumerated, and none of them reads any Timeline table.
7. The reason a sponsored couple cannot reach the authenticated Timeline is proven independently, not inherited, and the fix is stated as options.

**Evidence**
- _wt-wp08/src/modules/timeline/server/unpublished-timeline-boundary.test.ts — 7/7 pass. Criteria 1-4 and 6 asserted behaviourally against a real database and the real resolver, not against source text.
- app/src/modules/timeline/server/audience-timeline.ts:755 — The publish gate. Criterion 1 and 2.
- app/src/modules/timeline/server/audience-timeline.ts:759-769 — The explicit five-column public projection. Criterion 4, asserted on a real serialised DTO and on the item key set.
- app/src/modules/timeline/server/audience-timeline.ts:587-595 — Unpublish revokes every active share transactionally. Criterion 2 and 3.
- app/src/modules/timeline/server/sync/tasks-milestone-source.ts:135 — A Tasks task becomes a Timeline milestone only when is_milestone = 1, ordered by due_at. Criterion 5.
- app/src/server/db/apply-template.ts:74-81 — CORRECTS THE RECON MAP. apply-template does set dueAt (via resolveTemplateDueAt) and isMilestone. The recon said it never sets dueAt. The mechanism is built; the template declares neither.
- app/src/lib/templates.generated.ts (wedding-planning-workspace) — Measured 2026-08-03T07:13:31Z: 18 tasks, 0 dueOffsetDays declared, 0 milestones declared. So the couple's Timeline would render empty even once reachable. Criterion 5.
- app/src/app/api/internal/partner-stats/route.ts — The only venue-facing egress. Asserted to contain none of timeline/Timeline/publication/audienceShares/workspaceSlug/tasks and all four returned scalars. Criterion 6.
- app/src/modules/timeline/server/actions/workspaces.ts:93 — createWorkspaceAction re-verified as having zero callers across src, e2e, scripts, experience. One definition, one internal call, one comment. Criterion 7.
- app/src/modules/timeline/server/audience-timeline.ts:49 — NEW, not in the prior audit: the entire audience publication surface is behind SIGNAL_AUDIENCE_TIMELINE_ENABLED. If unset in production, publishing is unavailable regardless of everything else.
- studio/docs/execution/venue-edition-and-films/evidence/E05.08-timeline-planning-boundary.md — Criterion-to-proof trace, the fail-before mutation output verbatim, and the R-031 statement that does not paper over /p.
- _wt-wp08/src/modules/timeline/server/audience-timeline.ts:755 — VERIFIED IN CODE. `if (!publication || publication.state !== "published") return { kind: "unpublished" };`. Criteria 1 and 2 hold.
- _wt-wp08/src/modules/timeline/server/audience-timeline.ts:759-769 — VERIFIED IN CODE. Five-column select (publicId, title, calendarDate, state, sortOrder) and the DTO mapping at :794-799 narrows further to four emitted keys. Criterion 4 holds.
- _wt-wp08/src/modules/timeline/server/audience-timeline.ts:587-595 — VERIFIED IN CODE. unpublishAudiencePublication sets state='unpublished' and, in the same db.transaction, updates every audienceShares row with state='active' to 'revoked'. Criteria 2 and 3 hold.
- MY OWN MUTATION A — delete the publish-gate term at :755 — MY RESULT, not the builder's: 'ℹ tests 7 / ℹ pass 5 / ℹ fail 2', failing exactly 'an unpublished planning Timeline never resolves' and 'unpublishing closes it again for the same token'. Matches the claim exactly. File restored; git diff on it empty.
- MY OWN MUTATION B — leak sourceRelation into the emitted DTO item (independent, not the builder's mutation) — MY RESULT: 'ℹ tests 7 / ℹ pass 4 / ℹ fail 3', red on 'the published projection carries presentation fields only' plus two others via the DTO validator. This is the check the builder did not run and it is the one that matters: criterion 4's test is NOT tautological — widening the projection is genuinely caught.
- _wt-wp08/src/lib/templates.generated.ts (wedding-planning-workspace, lines 15-175) — MEASUREMENT INDEPENDENTLY REPRODUCED AND EXACT: 18 "title" keys, 0 dueOffsetDays, 0 milestone. Four "due" string literals only (Today, Tomorrow, Fri x2). The file is UNMODIFIED in the worktree — no generated-file edit, so the recurring failure mode is clear.
- _wt-wp08/src/server/db/apply-template.ts — REFUTES THE EVIDENCE NOTE. Working tree sets dueAt/isMilestone; HEAD does not. `git show HEAD:` grep for dueAt|isMilestone|resolveTemplateDueAt returns nothing. src/lib/template-anchor.ts is untracked (another package's work).
- _wt-wp08/src/modules/timeline/server/actions/workspaces.ts:422,616,653 — THE PATH THE ENUMERATION MISSED. Three Timeline actions call recordSponsoredUse into the venue-facing instrumentation layer, passing workspaceSlug as objectKey and the Clerk id as subjectId.
- _wt-wp08/src/lib/account/instrumentation/emitter.ts:100-101 and sink.ts:23-36 — WHY IT IS NOT A LIVE LEAK. subjectId and workspaceId are salted SHA-256 hashed before the payload is built; the payload has no content field; sponsoredUseSink is a noop that increments a counter and posts nowhere. The omission is an enumeration defect, not a disclosure.
- _wt-wp08/src/modules/timeline/server/actions/workspaces.ts:93 — CRITERION 7 RE-DERIVED BY ME. Repo-wide grep for createWorkspaceAction returns exactly three lines: the definition at :93, a rate-limit comment at :183, and a doc comment at timeline-queries.ts:228. Zero call sites. The prior audit's blocker holds.
- Repo-wide grep for timelinePublications|timelinePublicationItems|audienceShares (7 files) — MY OWN SEARCH FOR A SECOND BEARER PATH — none exists. resolveAudienceTimeline is called only from src/app/s/[token]/page.tsx:16,45. The other readers are timeline-gdpr.ts (scoped to workspaces.ownerUserId = the couple's own clerkId, and it omits tokenHash), qualified-view-store.ts, the schema, and two tests. There is no route that reads publication content without the publish gate.
- _wt-wp08/src/app/s/[token]/page.tsx:29 — VERIFIED. `robots: { index: false, follow: false, noarchive: true, nosnippet: true }`. src/app/robots.ts disallows /app, /s, /share, /redeem, /welcome, /api — and, as R-031 says and this evidence correctly refuses to paper over, NOT /p.
- evidence/E05.08-timeline-planning-boundary.md — EXISTS (11,936 bytes), read in full. Honest in register: states criterion 7 as PROVEN NOT FIXED, states the partner-stats small-cell gap against itself, states the typecheck caveat, and does not describe the token-bound model as covering /p.
- tasks/E05.08.md — EXISTS. Founder sign-off block correctly left unticked. Deliverable 5 (Timeline provisioning) correctly marked unbuilt.
- RECURRING FAILURE-MODE SWEEP over both E05.08 documents — CLEAN. Regex sweep for '18 month|eighteen month|548|seat|forever|for life|in perpetuity|permanen|dietary|allerg|guaranteed|exclusive' returns ONE hit across both files: evidence line 160, 'which is null forever', describing a null return value. No flat 18-month term, no seat count, no permanence promise, no invented dietary field, no green gate that is red.
- Specification: `tasks/E05.08.md`

### `E05.09` Complete the wedding-specific Signal briefing showing only what needs the couple’s attention today.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. A quiet wedding workspace produces an empty briefing, not a list, and the empty state states its denominator.
2. Given thirty items of which three carry real deadlines, exactly those three surface.
3. The three-per-block cap holds under load, and work held back by the cap is counted as held back rather than as clear.
4. One task that crosses several rules is reported once, not once per rule.
5. A dismissal removes an item from the read without shrinking the denominator.
6. The scope the briefing reads is the couple's workspace membership, so an invited spouse's briefing covers the shared wedding.
7. What a sponsored couple's briefing contains on the day they redeem is measured, not asserted.
8. Whatever the wedding template contains, no seeded task can be surfaced by date while the workspace has no wedding date.

**Evidence**
- _wt-wp08/src/modules/signal/lib/briefing/wedding-briefing-selectivity.test.ts — 9/9 pass. Criteria 1-5 and 7-8.
- app/src/modules/signal/lib/briefing/build.ts:19,80-93,185,196 — BUCKET_CAP, per-task dedupe, readCount denominator, pre-cap triggeredCount. Criteria 1-4.
- app/src/modules/signal/lib/planning-periods/scope.ts:105-111 — CORRECTS THE RECON MAP. The catalog query joins workspace_members, so an invited spouse's briefing does cover the shared wedding. The recon recorded Signal as per-user; that is true only of the analytics_users preference row (signal-build-for-user.ts:208), not of the data. Criterion 6.
- app/src/modules/signal/lib/briefing/tasks-db-source.ts:126-128 — The DB source filters on workspace_id IN (SELECT workspace_id FROM workspace_members WHERE user_id = ?). Criterion 6.
- app/src/lib/template-anchor.ts:50-58 — resolveTemplateDueAt returns null when either the offset or the anchor is absent. Its header states the refusal deliberately: a fabricated deadline is worse than an absent one. Correct behaviour, recorded as such. Criterion 8.
- app/src/server/actions/comp.ts:259 — Redemption calls applyTemplateToWorkspace with no anchorDate, so it reads workspaces.primary_date, which redemption never sets. This is the missing input.
- app/src/lib/templates.generated.ts (wedding-planning-workspace) — Measured 2026-08-03T07:13:31Z: 18 tasks, 0 offsets, 0 milestones, lanes {done:2, doing:4, review:3, todo:9}. Fed through the real engine: zero due-soon, zero crowded-week, and exactly one row, synthetic:overload 'Seven items open at once'. Criterion 7.
- studio/docs/execution/venue-edition-and-films/evidence/E05.09-wedding-briefing-selectivity.md — Criterion-to-proof trace, the measurement command, and the note on why the template's counts are deliberately NOT pinned as test equalities while E05.03 rewrites it in the same worktree.
- _wt-wp08 — all 8 claimed evidence paths checked individually — NO DEAD PATHS. wedding-briefing-selectivity.test.ts, build.ts, planning-periods/scope.ts, tasks-db-source.ts, template-anchor.ts, comp.ts, templates.generated.ts, triggers.ts — all EXIST. The VEF evidence doc E05.09-wedding-briefing-selectivity.md exists (8237 bytes, Aug 3 08:17).
- node --import tsx --import ./src/test/register-server-only.mjs --test src/modules/signal/lib/briefing/wedding-briefing-selectivity.test.ts — RE-RAN MYSELF. Real output: 9 checkmarks then 'tests 9 / suites 0 / pass 9 / fail 0 / duration_ms 553.7056'. Matches the claim exactly.
- Independent probe of src/lib/templates.generated.ts (my own script, not the agent's) — REPRODUCES THE MEASUREMENT EXACTLY: 'tasks: 18 / offsets: 0 / milestones: 0 / lanes: {"done":2,"doing":4,"review":3,"todo":9}'. Identical to the claimed 2026-08-03T07:13:31Z measurement.
- Independent probe feeding the real template through buildBriefing at NOW=2026-08-03T09:00Z — REPRODUCES THE ENGINE RESULT EXACTLY: 'readCount: 18 triggeredCount: 0 isEmpty: false / needsAttention: [{id: synthetic:overload, trigger: overload, text: Seven items open at once}] / quietRisks: 0 movingWell: 0'. Zero due-soon, zero crowded-week, exactly one synthetic row. Criterion 7 is genuinely measured, not asserted.
- studio/src/lib/templates/wedding-planning-workspace/tasks.ts (canonical source) — THE '0 OFFSETS' MEASUREMENT IS NOT STALE — I checked the risk. grep -c dueOffsetDays = 0 and grep -c milestone = 0 in the CANONICAL studio source too, so running `pnpm sync:templates` would not change the generated file. The measurement survives regeneration.
- _wt-wp08 git status --short — NO GENERATED-FILE EDIT. src/lib/templates.generated.ts (header: 'AUTO-GENERATED, do not edit by hand') is NOT in the modified list. The worktree's template changes are confined to the hand-written scripts/sync-templates.ts and src/lib/templates.ts. The programme's 'edit to a generated file a build step will overwrite' failure mode is absent.
- grep -c wedding-briefing-selectivity _wt-wp08/package.json -> 0; siblings anchor-due/template-anchor/wedding-template-contract/notes-copy/assignee-roster all -> 1 — THE DEFECT. The new test is absent from the CI chain while five sibling new tests from the same worktree were added to it in the same diff. .github/workflows/ci.yml:58 and verify.yml:30 both run `pnpm test`; package.json has no glob-based discovery.
- src/modules/signal/lib/planning-periods/scope.ts:105-111 and src/modules/signal/lib/briefing/tasks-db-source.ts:126-128 — CRITERION 6 CORRECTION IS REAL — I read both queries. scope.ts: 'INNER JOIN workspaces w ON w.owner_user_id = cu.id OR EXISTS (SELECT 1 FROM workspace_members wm WHERE wm.workspace_id = w.id AND wm.user_id = cu.id)'. tasks-db-source.ts: 'WHERE t.workspace_id IN (SELECT workspace_id FROM workspace_members WHERE user_id = ?)'. The recon map's 'Signal is per-user' read is genuinely wrong about the data.
- src/server/db/ensure-user.ts:108-110 and src/modules/signal/lib/briefing/tasks-db-source.ts:78-93 — I CLOSED AN ASYMMETRY THE BUILD AGENT DID NOT MENTION. scope.ts admits owner-OR-member; tasks-db-source.ts admits member-only. An owner without a workspace_members row would be listed in the catalog yet read zero tasks. ensure-user.ts:108 saves it: 'INSERT OR IGNORE INTO workspace_members (workspace_id, user_id, role) VALUES (workspaceId, clerkUserId, owner)'. Id types also line up — tasksUserId resolves clerk_id -> users.id, and app-access.ts:55 records that ensureUserProvisioned writes users.i
- src/server/actions/comp.ts:259 plus grep for primary_date across comp.ts — THE CLAIMED GAP IS REAL. Line 259 is `await applyTemplateToWorkspace(VENUE_TEMPLATE_ID, ws);` with no options argument, and comp.ts contains ZERO references to primary_date/primaryDate. The sibling diff to src/server/db/apply-template.ts makes the no-options path read workspaces.primary_date, which redemption never sets. Missing input confirmed.
- pnpm ds:check and pnpm typecheck in _wt-wp08 — RAN BOTH MYSELF. ds:check -> 'ds-check: clean — no drift.' typecheck (tsc --noEmit --incremental false) -> no output, exit clean. Both claims true.
- node --import tsx --import ./src/test/register-server-only.mjs --test over six Signal briefing suites (triggers, build, signal-build-for-user, tasks-db-source, voice, calendar-time) — RAN MYSELF: 'tests 127 / suites 28 / pass 127 / fail 0'. Green. My set differs from the agent's claimed 117 (different suite selection), so the numbers are not comparable, but no regression exists either way.
- Failure-mode sweep: grep -niE '18 month|eighteen month|seat|forever|for life|in perpetuity|permanen|dietary|allerg|guaranteed' over the E05.09 evidence doc and the new test file — NO HITS on either file. No flat eighteen-month term, no seat count, no permanence wording, no invented dietary field (R-017 respected). No commercial claim is made anywhere in this task's artifacts.
- src/modules/signal/lib/analytics/ledger-adapters.ts:107 with voice.ts:96-112 — CRITERION 1's SECOND HALF VERIFIED IN CODE, not just in the data object. healthyEmptyState.body = readCountSentence(briefing.readCount, briefing.triggeredCount) ?? emptyStateBody ?? fallback — so the denominator sentence WINS over the wedding segment phrasing. The empty state does state its denominator.
- Specification: `tasks/E05.09.md`

### `E05.10` Complete spouse, planner, family-member and collaborator invitations with appropriate role boundaries.

Executor: claude_code · critical path · release-blocking · in review since 2026-08-03

**Acceptance criteria**
1. The four relationships the title names have a written access matrix stating what each should see and do.
2. That matrix is checked against shipped code, and every row is marked met, partly met, or not met with a file and line.
3. A sponsorship row confers no membership, and no membership confers a sponsorship.
4. Any role value other than owner reaching the accept path is clamped to member, including venue-shaped values.
5. A sponsor identifier or reference is not a usable invitation token.
6. No sponsor-facing read path can reach a membership row, a task, a note, a comment, an attachment or a share link.
7. No surface on the invitation path states a price, a plan or a seat count (D-020).
8. A sponsored wedding workspace has no member cap, and unlimited is represented as an absent cap rather than a large number.
9. The invitation surface tells the invitee truthfully what accepting grants, given that only one collaborator role exists.

**Evidence**
- _wt-wp08/src/server/venue-never-a-collaborator.test.ts — 8/8 pass. Criteria 3-8. Six role values (venue, sponsor, coordinator, planner, admin, empty string) driven through the real accept SQL all land as member; three sponsor references all fail as tokens.
- app/src/app/invite/[token]/page.tsx — THE FIX, and the FOURTH price surface in the product. The E05-E06 audit found three (Notes, billing settings, Timeline cap message) and did not reach the invitation path. Criterion 7 and 9.
- app/src/server/db/schema.ts:467,933 — workspace_members.role and pending_invites.role are typed to exactly owner|member. Criterion 4.
- app/src/server/actions/settings.ts:348-349,585 — Two independent clamps, on write and on accept. The second is why criterion 4 passes even against a hand-written database row. Criterion 4.
- app/src/server/planning/queries.ts:225-245 — listSponsorActivationDTOs asserted to reach none of workspaceMembers, pendingInvites, tasks, notes, comments, attachments, shareLinks. It has zero production callers, which is exactly why pinning it now is worth more than pinning it later. Criterion 6.
- app/src/server/db/membership.ts:105-114 — wedding is in the unlimited set and unlimited is max: null, never a large number. D-020 satisfied structurally. Criterion 8.
- app/src/server/actions/comp.ts:235-244 — Redemption writes workspaceId: ws. Had it written a user-level entitlement, getWorkspaceTier would return free and a sponsored couple would be capped at four and shown an upgrade prompt. One line from being false.
- app/src/modules/notes/server/actions/notes.ts:257 and app/src/modules/timeline/server/db/timeline-queries.ts:167 — Notes are per user and Timeline workspaces per owner. An invited spouse gets Tasks and Signal and NOTHING in Notes or Timeline, whatever role they are given. That is what 'invite your spouse' actually buys today.
- studio/docs/execution/venue-edition-and-films/evidence/E05.10-collaborator-access-matrix.md — The six-row matrix with verdicts, the cross-product delivery table, the fail-before output for the copy assertion, and the raised risk about the missing database constraint.
- app/src/server/email.ts:79-82 — THE REFUTATION, and it was not found by the build agent. inviteEmailHtml still renders the identical banned sentence. I applied the build agent's own forbidden list to the template body: 'VIOLATION: the invite email carries "editing guests" / "on Free" / "on Team" / "No card" / "trial" / total violations: 5'. Five hits versus four on the landing page, because the email's line wrapping makes 'editing guests' contiguous. `git diff --stat src/server/email.ts` returns nothing. Criterion 7 is refuted
- app/src/server/require-app-access.ts:31-33 with app/src/app/app/layout.tsx:16 and app/src/app/invite/[token]/accept-button.tsx:24,26 — THE SECOND REFUTATION. layout.tsx:16 awaits requireAppAccess() inside SharedAppGate, which wraps children at :45-47. require-app-access.ts:31-33 is `if (!isEmailAllowed(email)) redirect("/waitlist")`, and access-allowlist.ts:38-41 fails closed on any address not in SIGNAL_ALLOWLIST. accept-button.tsx toasts 'Open the board, you're in.' and pushes /app/tasks. requireAppAccessTasks (app-access.ts:29), the membership-aware gate built precisely for this, is wired at tasks-runtime-shell.tsx:67, insid
- app/src/app/api/internal/partner-stats/route.ts — The live sponsor-facing read path the evidence never opened. Consumed by studio/src/lib/partners/stats.ts:104 for /hq/partners. Returns codesRedeemed, redeemed30d, reachedBoard and mostRecentRedemptionAt per sponsor slug, with no suppression floor of any kind. Reaches comp_codes and entitlements only, so criterion 6's literal wording survives, but reachedBoard is an unsuppressed behavioural count and mostRecentRedemptionAt is an exact per-couple timestamp. R-027's shape, on a shipped path, in th
- app/src/server/db/schema.ts:467 and :933 — CONFIRMED at the exact cited lines. :467 `role: text("role").$type<"owner" | "member">().notNull().default("member")`. :933 `role: text("role").$type<"member" | "owner">().notNull().default("member")`. Criterion 4's structural half holds.
- app/src/server/actions/settings.ts:349, :584-585 — CONFIRMED. Both clamps read as claimed. Also found at :357-358, inside the same invite action, the string 'Free workspaces include three editing guests. Upgrade to Workspace to invite more.' It is gated by canAddMember and cannot fire for a wedding workspace (max is null), so it is not a live violation — but it is a fifth seat-count string on the invitation path that the evidence does not record, and the same test would not catch it if the gate ever changed.
- app/src/server/db/membership.ts:105-112 — CONFIRMED. `const isUnlimited = tier === "workspace" || tier === "studio" || tier === "wedding" || tier === "event"` and `max: isUnlimited ? null : FREE_WORKSPACE_MEMBER_CAP`. Unlimited is an absent cap, never a large number. Criterion 8 CONFIRMED, and the comp.ts:236 `workspaceId: ws` dependency the evidence flags is real and correctly identified.
- package.json (test script) — Programmatic check of the full chain: 'NOT IN CHAIN venue-never-a-collaborator' while 'IN CHAIN assignee-roster-contract / anchor-due / template-anchor / wedding-template-contract / notes-copy'. The same diff wired five sibling tests in and left this one out.
- Specification: `tasks/E05.10.md`

## Not ready — cannot be approved

| Task | Title | Missing |
|---|---|---|
| `E05.02` | Design the venue-branded welcome experience with “Compliments of [Venue]” and no visible price. | no acceptance criteria; no evidence; status is "backlog", not founder_review |
| `E05.11` | Complete responsive, mobile, accessibility, loading, empty, error, permission and tasteful motion passes across the couple journey. | no acceptance criteria; no evidence; status is "backlog", not founder_review |
| `E05.12` | Run a world-class design-system review and lock visual regression baselines for every captured product surface. | no acceptance criteria; no evidence; status is "backlog", not founder_review |

## Your decision

Approve all of the above in one command:

```bash
node studio/docs/execution/venue-edition-and-films/tools/project-control.mjs approve-batch "your approval note" E05.01 E05.03 E05.04 E05.05 E05.06 E05.07 E05.08 E05.09 E05.10
```

Or push back on any single one:

```bash
node studio/docs/execution/venue-edition-and-films/tools/project-control.mjs reject E05.01 "what is wrong"
```


# Founder review packet — E09.03, E09.04, E09.05, E09.07, E09.08, E09.09, E09.11, E09.12

Generated 2026-08-03T05:15:31.968Z from PROJECT_STATE.json. 8 task(s).

**8 ready to approve · 0 not ready.**

Approving sets these tasks to Done and is the only thing that can. Nothing here is Done yet.

## Ready to approve

### `E09.03` Instrument invitation, activation, meaningful-use, public-sharing and Keepsake transitions.

Executor: claude_code · critical path · in review since 2026-08-03

**Acceptance criteria**
1. Invitation transitions are read as Tier 2 durable rows (license_codes, redemptions, sponsors), never emitted as Tier 1 events. No `invitation_*` event kind exists.
2. The Tier 1 sink is durable: every live call site terminates in a stored row.
3. Rollup runs strictly before the 35-day retention sweep. Un-rolled-up raw events cannot be swept.
4. `SPONSOR_USAGE_EVENTS` and `SPONSOR_USAGE_HASH_SALT` are declared in `app/src/env.ts` and `.env.example`, with the salt length-validated at >=16.
5. The event validator rejects (never trims) an eighth key, rejects a kind that does not match its product, and rejects raw identifiers.
6. An ingest endpoint exists that accepts events, validates them, and stamps `hash_salt_epoch`.
7. Idempotency: a replayed `eventId` produces exactly one row.
8. The eligibility join event-hash to sponsored workspace to venue exists and closes on `redemptions.user_clerk_id`.
9. `sponsor_usage_daily` per-product counters are NULLABLE, so null means not instrumented and zero means instrumented and quiet.
10. A daily rollup actually writes `sponsor_usage_daily`.
11. A salt rotation across a reporting window renders a coverage break, not a changed number.
12. `timeline_created` is allowlisted and has a live post-commit call site.
13. `timeline_visibility_changed` is renamed `timeline_unpublished` and appears in no sharing computation.
14. Public sharing is split: "ever shared" from `timeline_published` events, "currently shared" from `timeline_publications.published_at` read as state at snapshot.
15. Access-term end is computed per D-010 as max(redemption+18mo, wedding+3mo), never a flat 548-day approximation presented as the ratified rule.
16. A Keepsake export event exists and fires on a real export; Keepsake entry is computed or rendered `unavailable`.
17. Every `venue-metrics.v1` pin is corrected to `account-metrics.v2`, including any SQL column DEFAULT.
18. `briefing_acknowledged` being unwired is carried as a coverage fact: Signal reports one of two kinds instrumented, not complete.
19. Unfiled Notes and unlinked Timelines are carried as coverage facts, never zeros.
20. Every metric renders with the full ten-field coverage envelope or does not render.
21. The E09.01 §6.2 forbidden-field additions (booking status, couple and wedding identity, planning content, relationship data, internal keys) are enforced, not only the §6.1 frozen list.
22. The stale `studio/docs/venue-portal/` documents carry a historical banner.
23. The rollup and the sealing step run ahead of the retention sweep, in one sequence, and the sweep cannot run if either failed.
24. A test exists that fails if the sweep runs before the rollup.
25. The MAX_SEALING_GAP_DAYS interlock is visible in the handler, not just in a doc.
26. An ingest endpoint in studio accepts venue-meaningful-action.v1 and calls ingestMeaningfulAction, authenticated the way the neighbouring api/internal/ routes are.
27. The endpoint rejects rather than strips a forbidden field, at every nesting depth.
28. A real sink replaces noopSink and posts to that endpoint, with the failure signal in the sink and not inside the call site's try.
29. The obsolete !sink.includes("fetch(") assertion is updated deliberately with the reason recorded, not deleted quietly.
30. SPONSOR_USAGE_EVENTS and SPONSOR_USAGE_HASH_SALT are declared, the salt stays fail-closed below 16 characters, and emission is off by default.
31. The variables are declared in .env.example.
32. The stale venue-metrics.v1 pins are corrected by a forward migration, without rewriting 0000.
33. timeline_visibility_changed appears in no sharing computation, and timeline_created exists.
34. briefing_acknowledged stays allowlisted, and Signal's mask reports 1 of 2 kinds rather than complete.
35. The two identity spaces are verified, and if they can differ the call site is fixed rather than the join.
36. Salt rotation is detected and surfaced as a coverage break rather than a change in the number.
37. The ten-field coverage envelope is complete.
38. Verified with real tests in both repos.
39. Nothing committed, pushed, deployed or sent. No task marked Done.

**Evidence**
- app/src/lib/account/instrumentation/sink.ts:21-24,36 — The sink is an in-memory counter; `sponsoredUseSink = noopSink`. No event has ever been stored.
- studio/src/app/api/cron/sponsored-use/route.ts:52-58 — The nightly job runs `sweepExpiredEvents` alone. Its own docblock at :14-20 states the required order and the code implements only step 3. This is the ordering hazard.
- studio/src/lib/account/instrumentation/rollup.ts (whole file) + grep for importers — The rollup is pure and complete but has no production importer. Only `daily-metrics.ts` imports two constants from it.
- grep -n SPONSOR_USAGE app/src/env.ts app/.env.example studio/src/env.ts studio/.env.example -> 0 matches — Neither emission gate is declared anywhere, so the flag cannot be turned on through the normal configuration path.
- npx tsx --test (studio instrumentation, 5 files) -> 56 pass, 0 fail — Event validation, forbidden-field walk, attribution join, idempotency, rollup nullability, first-action monotonicity and the 3/5 suppression thresholds are all proven against an in-memory SQLite engine.
- npx tsx --test (app instrumentation, 3 files) -> 35 pass; node --test src/server/instrumentation-call-site-contract.test.mjs -> 17 pass — Emitter gating, post-commit-only emission, day-bucketed ids, and the twelve wired call sites all behave as the contract states.
- studio/src/lib/venue-edition.ts:100-120 + entitlements-db/codes.ts:383,498,595-672 + schema.ts:127 — D-010/D-022 access-term computation is implemented and wired into redemption and postponement. This closes E09.01 §4.2 / R-015 in code and supersedes the recon brief.
- studio/drizzle-entitlements/0000_init.sql — grep -c wedding -> 0 — No migration creates `entitlements.wedding_date`, so the R-015 fix throws on any database built from the shipped migrations.
- app/src/modules/timeline/server/actions/workspaces.ts:640-658 — `timeline_visibility_changed` is emitted inside `unpublishWorkspaceAction` only, after `unpublishWorkspace`. Confirms E09.01 §3.4.
- app/src/server/actions/tasks.ts:98,124 + app/src/server/auth.ts:111-120 vs notes.ts:1258 / workspaces.ts:617 / signal-ledger-actions.ts:84 — Two identity spaces confirmed: Tasks passes `users.id` resolved by a lookup on `users.clerk_id`; the other three pass the raw Clerk id.
- app/src/server/instrumentation-call-site-contract.test.mjs:48-51 — The test named for the E09.01 §3.6 acceptance criterion asserts only `contract.subjectIdSource === "clerk-user-id"` — a JSON string, not any call site. It is false assurance.
- studio/src/lib/account/types.ts:57-70 — `ReportingCoverage` is missing `instrumentation_version` and `suppression_reason`, uses module COUNTS instead of module lists, and marks day coverage optional.
- studio/src/lib/entitlements-db/schema.ts:673,750-752,794,888-890 — `METRIC_DICTIONARY_VERSION = "venue-metrics.v1"` is the DEFAULT for both `sponsor_usage_daily` and `sponsor_report_snapshots`, and is part of `sponsor_usage_daily`'s composite primary key.
- studio/src/lib/account/instrumentation/daily-metrics.ts — 8 exports, 0 occurrences of published/shared — Four of the six E09.02 metrics have a pure implementation. Timeline creation (§6) and Timeline sharing (§7) have none.
- studio/docs/venue-portal/METRIC_DICTIONARY.md:1-6 — No historical banner. The retired dictionary still presents itself as current.
- Mutation proof: patched runNightlyMaintenance to call store.sweepExpiredEvents before the repair step, ran `npx tsx --test src/lib/account/instrumentation/nightly.test.ts` — 5 of 11 tests failed, including 'the sweep runs last, and the call log proves it', 'a failed rollup means nothing is deleted at all' and 'a failed sealing step also stops the sweep'. Reverted from backup; grep for the mutation marker returns 0; 11/11 pass. The ordering test genuinely catches the failure it exists for.
- C:/Users/ethan/signal-studio-workspace/studio/scripts/check-entitlements-migrations.mjs, first run — The drizzle-generated migration ABORTS on a real baseline database: 'LibsqlError: SQLITE_ERROR: no such index: entitlements_wedding_date_idx', after 13 DROP INDEX statements had already executed. Fixed by guarding all 44 drops with IF EXISTS.
- Same script, second run — A second, independent generator defect: 'LibsqlError: SQLITE_ERROR: no such column: wedding_date'. drizzle-kit emitted CREATE INDEX entitlements_wedding_date_idx before ALTER TABLE entitlements ADD wedding_date. Fixed by reordering into drop indexes, change defaults, add columns, rebuild indexes.
- Same script, final run: 'applied 0000_init (58 statements)', 'applied 0001_account_metrics_v2_and_drift_closure (96 statements)', 16 ok, 'entitlements migration set applies cleanly and asserts out.' — Both metric_dictionary_version defaults are account-metrics.v2 and neither retains the retired name; a row inserted without an explicit version is keyed under account-metrics.v2; entitlements.wedding_date and all five sponsors columns exist; five representative indexes survived the table rebuild.
- `grep -c 'wedding_date|allotment_mode' studio/drizzle-entitlements/0000_init.sql` returns 0, while schema.ts:127 declares weddingDate, schema.ts:207 declares allotmentMode, codes.ts writes weddingDate and venue-allotment.ts:46 reads `s.allotmentMode === "unlimited"` — The R-015 blocker the audit named is real and larger than stated. Six columns exist in schema.ts and in no migration, so a database built from the baseline cannot run the shipped code. sponsors.allotment_mode is the column the entire D-020 / R-016 'unlimited is representable' correction rests on, and it was not on any list.
- `grep -n 'venue-metrics.v1' studio/drizzle-entitlements/0000_init.sql` returns lines 190 and 226 — A fourth stale pin the brief did not list: sponsor_report_snapshots.metric_dictionary_version carried the same retired DEFAULT as sponsor_usage_daily. Both are corrected by 0001.
- app/src/server/db/schema.ts:171 comment on users.clerkId ('Null for legacy seeded users until they're claimed via webhook') plus app/src/server/auth.ts:111-120 returning row.id from a lookup on users.clerk_id, and DEV_FALLBACK_USER = 'david' — E09.01 §3.6 verified rather than assumed: the two identity spaces can and do differ for any claimed legacy row. That person's Tasks events hashed a different subject from their Notes, Timeline and Signal events, so their Tasks activity attributed to nobody and every distinct-workspace count split silently.
- `node scripts/check-sponsored-use-instrumentation-contract.mjs` — 24 checks, 'sponsored-use instrumentation contract holds.' — Ingest auth mechanism matches the neighbours and does not read STUDIO_OPS_SECRET; the body reaches validation untouched; the cron no longer references sweepExpiredEvents and the comment that justified the unguarded sweep is gone; app and studio event-schema.ts are sha256-identical and all four products' kinds match the JSON contract.
- studio `pnpm test`: 16 + 440 + 38 = 494 node:test assertions, 0 fail, plus 10 check scripts green. `npx tsc --noEmit` clean. eslint clean on all seven new or rewritten studio files. — The studio half integrates with the existing 137-test instrumentation suite and with a concurrent lane's in-flight edits to daily-metrics.ts and suppression.ts, which I did not touch.
- app suite run segment by segment: 24 commands green, 829 node:test assertions, 0 fail. `npx tsc --noEmit --incremental false` clean. `tsx scripts/sync-templates.ts --check` reports clean, 2 templates, all 4 parts in sync. — The app half holds across the whole existing suite, including the 23-test instrumentation call-site contract, the suite-navigation contract and the demo-determinism checks another lane added mid-session.
- `npx pnpm test` in app fails with ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY before running anything — A pre-existing environment condition, not a code fault: pnpm's dependency-status check wants to purge node_modules and cannot prompt in a non-TTY shell. I ran the pretest and test scripts segment by segment instead rather than let anything delete node_modules.

### `E09.04` Instrument personalised-film views, landing-page visits, booking actions, meetings, proposals and paid conversion.

Executor: claude_code · critical path · in review since 2026-08-03

**Acceptance criteria**
1. A versioned prospect-funnel taxonomy exists naming all six subjects, kept as a separate stream from `venue-meaningful-action.v1`.
2. Personalised-film views are instrumented, keyed on `account_id` rather than a venue name, deduplicated, with a defined view threshold.
3. A film load or autoplay impression is not counted as a view, and a landing-page load is not counted as a booking action.
4. Landing-page visits produce a first-party record joinable to a prospect account. GA4 pageviews alone do not satisfy this.
5. Booking actions (call or walkthrough booked) are recorded at commit and joined to the account.
6. Meetings held are recorded as a durable dated stage transition.
7. Proposals sent, and proposal views, are recorded and joinable.
8. Paid conversion is read from the commercial record rather than a hand-typed CRM field, with VAT-inclusive amounts.
9. The CRM stage vocabulary can express every stage the funnel measures, through to paid.
10. No third-party tracker fires without a consent gate on any funnel surface, and the published privacy disclosure matches what actually loads.
11. Funnel surfaces obey E09.10: no price in a film (D-014.1), no urgency or scarcity (P13), no "for life"/"forever" (P1).
12. Each of the six named subjects has an event or a named state read, with properties, trigger, emission site, retention and privacy position.
13. Bound to D-013 point 3: film link clicks only, no open pixels.
14. The consent problem is stated, with what must change first and in what order.
15. Does not contradict WP-12's link contract at E13.16.
16. No routes or emitters were built in E12 / WP-13's tree.

**Evidence**
- studio/src/lib/tracking.ts:1-70 — A typed six-key tracking vocabulary with a `venue` axis exists and is the natural foundation for E09.04, but `venue` is hardcoded to "unknown" in `VENUE_SITE_TRACKING` (:20-27).
- grep for normalizeTrackingParams/formatTrackingRef consumers across studio/src — The only reader is `studio/src/app/contact/page.tsx:77-85`, which formats the params into a display string. No capture endpoint, no durable row.
- find studio/src/app -ipath "*film*" -> studio/src/app/hq/demo-film/page.tsx only — No personalised film delivery surface exists in the repository.
- studio/src/lib/db/schema.ts:187-195 and studio/src/lib/hq/crm-utils.ts:211-217 — Both the stored stage enum and the rendered pipeline stop at `pilot_active`. The CRM cannot reach the project completion condition.
- studio/src/lib/db/schema.ts:218-270 — `prospectsTable` has no sponsor or account foreign key, so the CRM cannot be joined to the commercial ledger at all.
- studio/src/lib/hq/traction.ts:8,15-26,124-134,198-202 — Paid conversion exists as a commercial fact read from `sponsors.paid_at`, with an explicit refusal to count couples as revenue. It is the one E09.04 subject with a trustworthy source.
- studio/src/components/analytics/google-tag.tsx:7,14-16,21-35 and studio/src/app/layout.tsx:143-144 — GA4 loads on every studio page with a self-documented absence of any consent gate.
- app/src/lib/bare-artifact-path.ts:2 vs the existence of app/src/app/embed — The tracker exclusion predicate covers `/s` only, so GA4 fires on `/embed`.
- studio/docs/execution/venue-edition-and-films/evidence/E09.04-outreach-measurement.md — The specification deliverable: venue-outreach.v1, six subjects, five determinations, three blockers with ordering, retention table, privacy position, three open founder questions, ten acceptance criteria.
- studio/src/app/waitlist/actions.ts:36-37 with studio/src/lib/db/schema.ts:328 — Corrects the audit verdict. joinWaitlistAction writes referrer (from the `referer` header) and userAgent into waitlist_entries alongside the five tracking params. A capturing endpoint and a storing row both exist; neither header field is on the form or named in any notice.
- studio/src/components/analytics/google-tag.tsx with studio/src/app/layout.tsx:144 and studio/src/app/robots.ts:9 — The consent blocker, verified. gtag.js is rendered from the root layout on every production page with gtag('config') fired immediately, no Consent Mode and no banner (the component's own comment records it). robots.ts disallows /api/, /hq/, /redeem/, /review and not /v/, which E11.05-07 §7.2 requires.
- find studio/src/app -ipath '*film*' -o -ipath '*venue*' — No personalised-film route exists. Only /venues, /venues/demo and the HQ-gated /hq/demo-film. A film view has no surface to fire from, which is why E09.04's deliverable here is the specification.
- Specification: `docs/execution/venue-edition-and-films/evidence/E09.04-outreach-measurement.md`

### `E09.05` Join CRM, commercial, product and reporting data into one founder operating dashboard.

Executor: claude_code · critical path · in review since 2026-08-03

**Acceptance criteria**
1. One surface reads all four sources.
2. CRM source: live pipeline state per prospect account, with stages reaching paid.
3. Commercial source: agreements, payments, Founding-25 numbering assigned on payment, revenue against target, VAT-inclusive.
4. Product source: the six E09.02 adoption metrics rendered with the full ten-field coverage envelope and 3/5 suppression.
5. Reporting source: VEF project state — days to release, gates, founder-review queue, decisions ratified.
6. Founder-only and gated.
7. A missing source renders as missing, never as zero.
8. The dashboard can evaluate the project completion condition.
9. Every number names its source or shows a dash.
10. No per-couple row, no ranking, no leaderboard on any panel.
11. Venue-identifying data is not rendered in a form that leaks on a screen share to a prospect.
12. One founder operating dashboard is extended, not a second one built.
13. All four sources join on one surface: CRM, commercial ledger, product analytics, and VEF reporting state.
14. VEF reporting state is read from PROJECT_STATE.json, parsed against PROJECT_STATE.schema.json rather than guessed.
15. The register is never written by application code and no web request can mutate it.
16. The dashboard surfaces verified completion, release gates, Founding 25 counters, founder-review queue depth, blocked tasks and days to the 2026-09-01 release.
17. Honesty contract: a source that cannot be read renders nothing plus a health flag, never zero.
18. The surface can evaluate the project completion condition (25 venues signed, paid, configured, onboarded, issuing couple invitations).
19. The CRM-to-commercial gap is surfaced precisely, with its measurement derived from shipped code rather than hard-coded.
20. pilot_active no longer renders as won on the CRM board.
21. Verified by loading the page in a real browser, with a report of which panels could not read their source.

**Evidence**
- studio/src/app/hq/blueprint/page.tsx:20-26,52-53 — A gated founder dashboard exists and imports CRM, commercial, product and financial sources. `requireHqAccess()` runs before any fetch.
- studio/src/lib/hq/blueprint.ts:18-36 — Ten of eleven metrics are live-wired with named sources, and the one unsourced metric is labelled honestly rather than filled with a number. The missing-is-not-zero rule is already the shipped behaviour.
- grep -rn PROJECT_STATE studio/src app/src -> 0 matches — No code anywhere reads VEF programme state. The reporting corner of the four-way join is entirely absent.
- node project-control.mjs status — The reporting facts the dashboard would need are already machine-readable: 29 days remaining, 33.8% verified completion (71/210), commercial gate not_started, 0 tasks in founder review, and the completion condition stated verbatim.
- studio/src/lib/hq/crm-utils.ts:211-217 + studio/src/lib/db/schema.ts:218-270 — The CRM funnel stops at `pilot_active` and carries no sponsor key, so CRM cannot be joined to commercial and the completion condition is unevaluable.
- studio/src/lib/hq/traction.ts:15-26,124-134,198-202 — The commercial source is sound: it reads the sponsor ledger for paid cash and refuses to count couple-side entitlements as revenue.
- ls studio/src/app/hq -> ~45 route directories — The founder surface is fragmented across many pages; `/hq/blueprint` is one of them, not the single operating dashboard the task names.
- studio/src/components/hq/hq-marketing.tsx:665 + studio/src/lib/partners/stats.ts:20-23 — A founder surface in the same family renders `reached_board_at` as a funnel stat, which E09.02 §9.0.1 disqualifies as an activation signal.
- studio/src/lib/hq/vef-state.ts — The fourth source exists and is read-only. Parsed against PROJECT_STATE.schema.json; no write API; explicit VEF_CONTROL_ROOT is the sole root so a misconfigured override cannot silently substitute the repo register.
- studio/src/lib/hq/vef-state.test.ts — 14 of 14 pass under `npx tsx --import ./src/test/register-server-only.mjs --test`. The failure paths are pinned: missing file, non-JSON, wrong-shape JSON, absent counter, monotonicity break, counts drift, and no VEF metric renders 0 when the register is unread.
- studio/src/app/hq/blueprint/page.tsx section 09 rendered at http://localhost:3000/hq/blueprint — Live join of four sources. Reads on screen: 'Register schema 1.0.0 · last written 2026-08-03 · last validated 2026-08-03', '29 days to 2026-09-01', 'RELEASE GATES · 0 OF 6 PASSED', twelve-stage ladder with researched 219, 'Founder review · 0', 'Blocked · 0', 'In progress · 27', six freezes with days-to.
- scratchpad/shots/a-header-gates.png, d-reconciliation-full.png, e-metrics.png — Browser verification at 1440x1100 with the HQ gate cookie. Zero console errors, zero page errors, zero horizontal overflow. The four new VEF metrics render live; the six ledger and product metrics render '—' with their unread notes.
- studio/src/lib/hq/blueprint.ts VEF_STAGE_MAP and resolveCrmStageCoverage — The CRM-to-commercial finding is computed from shipped code, not asserted. Live output: 4 of 12 expressible, reachesCompletionCondition false, orphanCrmTokens ['pilot_active'].
- node studio/docs/execution/venue-edition-and-films/tools/project-control.mjs validate — Register unchanged and healthy after the work: 'OK — 212 tasks, 15 epics, 135 dependency edges, no cycles, no duplicate ids. Baseline approved.' Only the expected WIP-exception warning.

### `E09.07` Build a deterministic Glenmara House and Mara-and-Finn demonstration fixture across all four products and the portal.

Executor: claude_code · critical path · in review since 2026-08-03

**Acceptance criteria**
1. A single source of truth for the demonstration story (venue name, couple names, wedding date) exists, and every demonstration surface in all four products and the portal imports it rather than re-declaring it.
2. Running the demonstration reset twice from a clean state produces identical state; no demonstration path reads the wall clock.
3. A drift test exists and passes proving the Tasks pinned calendar frame, the Timeline review fixture and the frozen public projection agree on 'today'.
4. Exactly one pinned 'now' governs every product's demonstration clock, and a drift test asserts every product-level pinned clock equals it.
5. Notes, Tasks, Timeline and Signal each have a demonstration path wired to one centrally resolved access mode.
6. The Venue Portal has a demonstration path producing the venue-side view of the same story.
7. The venue is Glenmara House and the couple Mara and Finn on every demonstration surface, with zero live occurrences of the superseded venue name.
8. One wedding date across every demonstration surface.
9. Re-running the seed does not duplicate rows.
10. The fixture is built on the existing wedding template and fixtures rather than a parallel set.
11. No dietary, allergy, accessibility or health information about a named person appears in any demonstration fixture or on the public demonstration Timeline (E09.10 P8).
12. Demonstration actions emit no Tier 1 instrumentation events (E09.01 §3.2 rule 2), proven rather than assumed.
13. Any number the portal demonstration shows carries the full coverage envelope of E09.01 §7 or does not render.
14. A CI-runnable check fails when a demonstration fixture regains a wall-clock read or acquires a second pinned clock.
15. One documented command produces the fixture, and its output on two consecutive runs is recorded as evidence (data gate, criterion 11).
16. A single exported anchor constant exists, and every in-scope demo surface derives its pinned instant from it through an explicitly named offset rather than re-pinning a literal.
17. All four previously-independent pinned clocks are drift-tested, in both directions: a constant cannot move without its registry entry moving, and an entry cannot describe a value its module does not export.
18. The Tasks demo board is deterministic: no wall-clock read, and the same command produces the same state twice.
19. review-suite-fixture.ts reviewToday equals PINNED_REVIEW_CALENDAR_FRAME.today.
20. One venue name and one couple across every live application surface, per D-012 point 1.
21. One wedding date, recorded as a determination rather than a decision.
22. The wedding story survives either state of the planning-period feature flag.
23. The Signal analytics fixture is either joined to the story or excluded from the reset scope explicitly, with the choice stated.
24. sync-templates.ts carries all five canonical template parts to the product, and dropping a part again fails the build.
25. seed-wedding.ts is byte-identical across two consecutive runs, and its --i-understand-prod gate is not wrapped or bypassed.
26. A determinism check runs the demo state twice, diffs the rendered result, and is wired into the test chain.
27. A one-action reset command exists.
28. A venue-facing demo portal route exists with a demo access mode.
29. R-017 holds: no structured dietary or allergy field, no flow routing guest health data to a venue, no extension of the existing free-text echoes.

**Evidence**
- app/src/server/demo/tasks-demo.ts:38 — Tasks' demonstration board reads the wall clock, so the board is not deterministic
- app/src/modules/signal/lib/briefing/mock-source.ts:134,188 — Signal's demonstration briefing source defaults to Date.now()
- app/src/lib/calendar-frame.ts:73 · app/src/lib/review-suite-fixture.ts:58,59 · app/src/modules/notes/server/demo/notes-demo.ts:35 — Four distinct pinned 'now' values across the demonstration
- Command: node --import tsx --test src/modules/timeline/lib/owner-artifact.test.ts (run in app/) — 5 pass, 0 fail — The one drift-tested clock pair genuinely holds today
- Command: node --import tsx --test src/lib/calendar-frame.test.ts (run in app/) — 7 pass, 0 fail — The pinned review frame is asserted finite and equal to 2026-07-16
- studio/src/lib/account/fixtures.ts:118 vs app/src/lib/review-suite-fixture.ts:17-18 — Portal and product carry different venue names for the same demonstration
- studio/public/brand/collateral/venue/wedding-seed.md:9-11,19 — A third demonstration story exists in a public directory, with a synthetic guest health record and an instruction to substitute a real venue's name
- ls studio/src/app (no account or portal route) · studio/src/app/hq/account-review/account-review.tsx:77 — The portal has no demonstration path; the only account surface is HQ-gated
- project-control.mjs task E09.06 — status done, founder approved 2026-08-03, criterion 'Glenmara House, Mara and Finn' — The story is ratified, so the venue-name divergence is code that has not caught up, not an open question
- app's real test command: `pnpm test` (pretest runs test:truth). Final run exit code 0. Aggregate across all 52 suites: tests=829 pass=829 fail=0. — The full app test suite is green with every change in place, including the four new checks appended to the chain.
- Baseline before any change, same command: tests=800 pass=799 fail=1. The single failure was a file-level exit-code failure in src/modules/timeline/server/qualified-view-store.test.ts whose three individual tests all passed, and which passes in isolation. It did not reproduce on either post-change run. — There is a clean before/after comparison, and the pre-existing intermittent failure is characterised rather than attributed to this change.
- Regression-detection proof: temporarily restored `const now = () => Date.now()` in tasks-demo.ts and ran the determinism check. Output: 'the demo state is NOT deterministic … first divergence at line 244 … - pass 1: "updatedAt": "Date(2026-08-03T02:26:55.322Z)" / + pass 2: "updatedAt": "Date(2027-09-07T02:26:55.786Z)"'. Restored the fix; check returns clean, exit 0. — The determinism check catches the exact defect E09.07 named, and locates it to the field. It is not a check that passes because it tests nothing.
- `node scripts/experience/demo-determinism.mjs --self-test` → 'pass - a wall-clock read in the demo state is detected and located'. Wired into the test chain alongside the check itself. — The check is kept honest permanently, not only at the moment it was written.
- `node scripts/experience/demo-determinism.mjs` → 'clean - two runs under different clocks and time zones produced identical state (sha256:2897779e9267e65f, 1947 lines)'. — The whole demo state (Tasks board, four Notes fixtures, Timeline workspace/projects/tasks/effective nodes, both Signal sources, seed rows, pinned frames, clock registry) is invariant under both wall clock and time zone.
- `node --import tsx --import ./scripts/experience/server-only-shim.mjs --test scripts/experience/demo-clock-drift.test.ts` → 9 tests, 9 pass, 0 fail. — All four clocks, the registry in both directions, the reset scope's file existence and stated reasons, and the one-story/one-date facts are enforced.
- `npx tsx scripts/sync-templates.ts` followed by `git diff --stat src/modules/timeline/lib/templates.generated.ts` → empty diff. Then `--check` → 'clean - 2 template(s), all 4 parts in sync (meta + tasks, roadmap, notes, analytics)'. — The generator reproduces the previously hand-maintained roadmap file byte for byte, so making it genuinely generated changed no shipped data. src/lib/templates.generated.ts was also left untouched by the regeneration (the script only writes on difference), confirming the tasks slice round-trips exactly.
- `node scripts/ds/ds-check.mjs` → 'ds-check: clean — no drift.' after adding hex-to-rgb accent tokenisation to the generator. An intermediate run without it failed with 'raw hex count 2 exceeds grandfathered 0'. — Generating the roadmap file did not smuggle raw hex past the design-system gate, and the failure mode was found and closed rather than assumed away.
- `node scripts/check-module-boundaries.mjs` → '[module-boundaries] ok'. An earlier placement of the drift test at src/lib/demo-clock.test.ts produced three rule-2 failures; the test was moved to scripts/ rather than the gate being relaxed. — The cross-module clock inventory did not cost the module isolation guarantee.
- `npx tsc --noEmit --incremental false` reports errors only in src/env.test.ts, which is untracked (git status '??') and belongs to a concurrent session. A separate tsconfig run over the four changed/new scripts (which the app tsconfig excludes) reports zero errors after fixing one ConstructorParameters<typeof Date> literal-type issue. — No type regression was introduced, including in files the standard typecheck does not cover.
- `npx eslint` over all 13 changed and new source files → no output. — Lint clean.
- Baseline established in a detached git worktree at HEAD: `node scripts/experience/validate.mjs` → 'clean - 80 Tasks experiences'; `node scripts/experience/critical-fixtures.mjs` → already stale at HEAD. Worktree removed and pruned afterwards. — The experience-gate state before this change is measured rather than assumed, so the 20 validate failures now present can be attributed accurately (19 mine, 1 pre-existing).
- Final name sweep: grep -rn 'The Orchard|the-orchard|theorchard' across app/src, app/scripts, app/experience, app/DEMO_MODE.md and studio/src/lib/templates returns one hit, the explanatory comment at review-suite-fixture.ts:17. — The D-012.1 rename is complete inside WP-06's column, and the single remaining occurrence is the deliberate record of the change.
- R-017 sweep: grep -niE 'dietary|allergy|coeliac|vegan|nut |health|accessib' over both new generated files returns nothing. The seven pre-existing free-text echoes are enumerated and unchanged in substance. — No guest health data was normalised, added, routed or extended.

### `E09.08` Source and license all demonstration photographs and confirm that no unapproved real venue or couple material is used.

Executor: claude_code · critical path · in review since 2026-08-03

**Acceptance criteria**
1. An image-provenance register exists recording, per demonstration image: file path, generating model, prompt, source, generation date, and where it is used.
2. Every shipped demonstration image appears in the register and every register row resolves to a file (bidirectional completeness).
3. No unapproved real venue photograph and no real couple photograph is used in any demonstration surface.
4. No close-up generated human face appears in either film or on the public demonstration Timeline.
5. Budget zero is respected: no stock photography licence is purchased.
6. A per-image licence statement is recorded, including the generating tool's commercial-use terms, rather than assumed.
7. R-011's mitigations are encoded as a working rule: a named quality bar, a preference for environments and details over faces, and an explicit record of shots rejected because they could not be made to feel real.
8. A machine-checkable script asserts register completeness and is wired to the data gate.
9. Alt text for every demonstration image passes the E09.10 §1 never-list (alt text is explicitly in scope).
10. A named reviewer records a verdict that no unapproved real venue or couple material appears (data gate, criterion 11).
11. An image-provenance register exists, is committed, and records model, version, tool, seed, prompt, negative prompt, date, rights position, permitted surfaces, face flag, reviewer and verdict for every demonstration image.
12. A check script validates the register against the repository and fails on schema violation, missing file, hash mismatch, dimension mismatch, resolution below the shot minimum, a face on a forbidden surface, a face without a founder note, an unaccepted asset referenced from source, an unregistered raster in a demonstration directory, or a never-list term, em dash or exclamation mark in alt text or a caption.
13. The check script has been proven to fail, not only to pass: a deliberately malformed entry produces a non-zero exit and names each violation.
14. Every demonstration surface that needs imagery has a shot with a stated reason, a generation prompt and a negative prompt.
15. R-011's face rule is expressed as data, not prose: every surface carries a facesPolicy, and the shot list contains zero faces.
16. A named human reviewer has applied the fifteen acceptance tests to every generated image, and the rejects are recorded alongside the accepts.
17. An exhaustive audit of both repositories confirms no unapproved real venue or couple material appears on any demonstration surface, by enumeration of every raster asset and by testing every asset path and every alt, caption and aria string against all 219 real venue names.
18. Real material that does exist is registered with its rights position and its permitted surfaces, so the confirmation is a control rather than a snapshot.
19. R-011's mitigation that generated imagery is a first-class QA item at E05.12, E09.08 and E14.18 is written as a checklist that can actually be run.

**Evidence**
- DECISIONS.md D-012 point 2 — Imagery is AI-generated at zero budget; licensing is replaced by generation, so the register is the only rights artifact available
- studio/docs/execution/venue-edition-and-films/RAID.md:246-254 (R-011) — The faces mitigation and the 'cut the shot' rule are recorded as a risk but not implemented anywhere
- studio/docs/execution/venue-edition-and-films/evidence/gates/data.json, criterion 11 — The gate requires the reset diffed twice, the image provenance register, and a named reviewer verdict — all three absent
- find studio/public -type f (jpg|jpeg|png|webp|avif) → 378 files; find app/public → 0; find signal-motion/public → 0 — The scale of the unregistered asset set, and that no demonstration photography exists in the product or film trees
- find . -iname "*PROVENANCE*" → signal-motion/public/fonts/PROVENANCE.md only — No image-provenance register exists anywhere in the workspace
- studio/public/brand/collateral/social/alt-text.txt, S.4 entry — The one alt-text artifact is unenforced and already carries a never-list term ('Partner')
- grep -n image refs in studio/src/app/venues/page.tsx and weddings/page.tsx → no matches — The public venue-facing surfaces carry no photography, so the negative confirmation currently holds
- studio/docs/execution/venue-edition-and-films/evidence/E09.08-demonstration-imagery.md — The E09.08 specification: audit method and results, register design, 19-shot list with prompts, the 15-test acceptance test, the three-part QA gate, eight acceptance criteria with met/unmet status, three open founder questions.
- studio/src/lib/demonstration-imagery/register.json + registry.ts — The image-provenance register the data gate names as required evidence. 10 surfaces with a facesPolicy each, 19 shots with zero faces, 0 assets, 2 acknowledged real raster sets with pinned hashes and rights positions.
- studio/scripts/check-demonstration-imagery.ts — `npx tsx scripts/check-demonstration-imagery.ts` exits 0, reporting 10 surfaces / 19 shots (0 declare a face) / 0 assets / 2 acknowledged real rasters / 0 failures — The gate criterion can be evaluated mechanically. Before this, no script checked register completeness because no register existed.
- Negative test: a malformed asset entry produced 14 distinct failures and exit 1 — unknown key, empty reviewedBy, empty generator.model, empty prompt, empty rightsPosition, missing file, R-011 face on film-before-the-day, surface-policy on venue-account, and E09.10 hits for 'lifetime', 'seamless', an exclamation mark, 'forever' and an em dash — The check fails when it should, not only passes when it should. The register was then restored and re-verified to 0 findings.
- Audit enumeration: 378 rasters in studio/public, 0 in app/public, 0 in signal-motion; 8 of 378 carry any embedded metadata; 0 of 531 asset paths and 0 of 629 alt/caption/aria strings contain any of the 219 real venue names — No unapproved real venue or couple material is used on any demonstration surface, established by exhaustive enumeration rather than by assertion.
- studio/public/brand/assets/ethan-founder-color-20260715.jpg and ethan-headshot.jpg are byte-identical, sha256 cf8b9bc2903b1ee4b317f65454f756e30270bf9bd11516c1bfde157dac4a21fe, 1179x1745, EXIF Software=Picasa DateTimeOriginal=2026:06:10 16:43:49, referenced only from public/brand/business-loan-pack-2026.html:3073 and :3106 — The only real photographic material of a real person in either repo, with its provenance established and its permitted surface recorded. Two filenames, one file.
- studio/public/brand/assets/merchants/ holds ten third-party marks (apple, booking, irish-rail, netflix, one4all, ryanair, shamrock/Aer Lingus, starbucks, three, uber) plus two source sheets, with no licence recorded anywhere; referenced only by public/brand/assets/transactions/merchants.js, which no page loads — Real third-party trademark material served publicly with no recorded basis. Outside E09.08's own question, recorded as an adjacent finding, raised as OQ-E09.08-2.
- collateral/dist/social/alt-text.txt:26-27 carries 'Partner' three times ('S.4 The Partner', 'Founding Limerick Partner number four') and the file carries 24 em dashes; the reference sweep found no consumer — The existing alt-text file is unenforced and already breaks E09.10, which states the never-list binds alt text explicitly. The new register's check enforces both rules on every altText and caption it governs.
- Specification: `docs/execution/venue-edition-and-films/evidence/E09.08-demonstration-imagery.md`

### `E09.09` Reconcile every sample invitation count, adoption metric, report number, date and product-reach value.

Executor: claude_code · critical path · in review since 2026-08-03

**Acceptance criteria**
1. One canonical sample-numbers source exists and every surface carrying a sample number derives from it.
2. An inventory exists of every surface carrying a sample number, with file:line for each.
3. Arithmetic holds within each sample snapshot: issued ≥ redeemed, first-action ≤ redeemed, recent-use ≤ its denominator, and the continuation denominator ≤ the first-action cohort.
4. Suppression is respected in every sample: nothing below 3 behavioural or 5 rate is shown as a number, on screen, in CSV or in PDF.
5. Product-reach sample counts do not sum to a total, and a suppressed product is not inferable from its neighbours (E09.02 AC7).
6. Every sample metric carries the full coverage envelope of E09.01 §7, including data_through.
7. The denominator is labelled 'invitations redeemed', never 'your couples' (E09.02 §1, E09.10 §1.4, P9).
8. No sample presents a metric that E09.01 or E09.02 mark as retired, undefined or not computable as if it were measured.
9. Every date in the samples is consistent with one wedding date, one demonstration clock and one reporting window.
10. A reconciliation check re-derives every published sample number from the canonical source and fails on drift.
11. A string sweep over venue-facing copy returns zero hits for 'allotment', 'seats', 'codes remaining' and 'X of your N' (commercial gate, E09.10 P5 and P6).
12. Film numbers are reconciled: no invented metric on screen, and no price anywhere in Before the Day.
13. A harness walks every place a sample number appears, extracts the numbers, and reports every inconsistency.
14. The harness catches that the marketing surfaces are hand-written constants rather than fixture consumers, so they will drift the moment the fixture changes.
15. The harness reports the retired allotment model reaching a venue, per E09.01 section 4.1.
16. The harness identifies metrics with no E09.02 definition and E09.02 metrics with no implementation.
17. E09.09 is completed.
18. Every sample surface declares that the numbers are illustrative rather than measurements.

**Evidence**
- Command: node --import tsx --test src/lib/account/fixtures.test.ts (run in studio/) — 9 pass, 0 fail — The portal fixture's suppression and privacy behaviour genuinely holds and can be recorded as evidence
- studio/src/lib/account/fixtures.ts:147-148 and studio/src/lib/account/live/project-venue-access.ts:190 — The sample numbers and the live code both compute the retired 'codes remaining = max(0, allotted − issued)' figure
- studio/src/app/hq/account-review/panels/access-panel.tsx:122,127,137 · studio/src/lib/account/pdf-html.ts:33 · studio/src/lib/account/csv.ts:104 — 'Allotted' reaches the venue on screen, in the PDF and in the CSV, against the commercial gate's zero-hit sweep
- studio/src/lib/account/fixtures.ts:172 — daysWithSponsoredUse is a venue-facing metric with no definition in E09.02
- studio/src/lib/account/fixtures.ts:135,141-142 vs E09.01 §3.5 — The sample claims complete four-module coverage while the dictionary determines Signal's mask must read one of two kinds instrumented
- studio/src/app/venues/page.tsx:47-77 — A second, unlabelled sample table exists on a public page with no link to the portal fixture
- project-control.mjs task E09.09 — dependencies unmet: E09.07 — The dependency is real: reconciliation needs one story, one clock and one date set first
- studio/scripts/check-demo-number-reconciliation.mjs — exits 1 with 41 findings: RETIRED 21, UNDEFINED 1, MISSING 2, DRIFT 13, UNWIRED 4, UNKNOWN 0 across 17 surfaces and 54 claim keys — The harness runs, produces real output on the real tree, and would fail CI on drift. `--report` exits 0 for use before E09.07 lands.
- studio/docs/execution/venue-edition-and-films/evidence/E09.09-reconciliation-run.txt — The captured output of both scripts against the working tree at 2026-08-03, with the caveat that E09.07 is being built in parallel so the tree is moving.
- DRIFT story.venue-name: 'Glenmara Estate' at studio/src/app/venues/page.tsx:187 and 'Glen House' at studio/public/brand/collateral/venue/wedding-seed.md:10, against D-012.1 'Glenmara House' — Two of the three live venue-name variants recon named. The third, 'The Orchard', now survives only in a comment in app/src/lib/review-suite-fixture.ts, which the harness correctly ignores because it strips comments before story extraction.
- DRIFT story.wedding-date: '2026-09-12' at app/src/lib/calendar-frame.ts:88 and studio/src/components/marketing/heroes/timeline/fixture.ts:18, against DEMO_WEDDING_DATE 2026-10-03 — calendar-frame.ts:88 pins the 'Wedding season' planning period endDate to 2026-09-12 — the retired wedding date, and 21 days BEFORE the canonical one. Not named in recon; found by the harness.
- DRIFT prose.code-prefix: 'GM' at studio/src/app/venues/page.tsx:54,60,66,72 versus 'GH' at studio/docs/venue-portal/WIREFRAMES.md:88-91 — The demonstration invitation-code prefix disagrees across surfaces. Not named in recon; found by the harness.
- RETIRED E09.10 P7: 'entitlement: "18 months"' with no grace clause at studio/src/app/venues/page.tsx:55, :61, :67 and :73 — Four live P7 violations on the venues page, matching the four E09.10 section 7 recorded as drift. The harness checks for the grace clause within 160 characters rather than banning the figure, so the correct phrasing passes.

### `E09.11` Finalise offer, founding-rate, privacy, collaboration, Keepsake, CTA, objection and FAQ copy.

Executor: claude_code · critical path · in review since 2026-08-03

**Acceptance criteria**
1. A single copy source of record carries finalised copy for all eight named blocks (offer, founding-rate, privacy, collaboration, Keepsake, CTA, objection, FAQ), each labelled with its E09.10 §3 audience ladder and entering at rung 1.
2. Every price is VAT-inclusive and says so (D-021 / P12). Zero '+ VAT', 'ex VAT', 'plus VAT' or monthly equivalent front-facing.
3. Zero 'for life' / 'forever' / 'lifetime' / 'permanent' / 'perpetual' / 'in perpetuity' / 'never expires' / 'guaranteed' (D-009.3, D-001.16, P1); the founding-rate lock reads 'for as long as you stay'.
4. Keepsake block uses the D-010.3 ratified sentence and carries 'for as long as the service exists' wherever Keepsake is promised in a document, term or contract.
5. Nothing states or implies legal approval, solicitor review or accountant verification (D-016 / P2); the solicitor objection answer keeps the explicit 'No.'
6. No unqualified GDPR-compliance claim (P3); privacy copy names artifacts, and the GDPR objection answer is used in its INTERIM form until E03.01/E03.04/E03.05 exist and R-017 is decided.
7. Access length never appears as bare '18 months' (P7, D-010.2, D-022); every occurrence carries 'or 3 months past the wedding, whichever is later'.
8. Zero 'seats' / 'seat count' / 'allotment' / 'codes remaining' / 'licences allotted' / 'X of your 60' in venue-facing copy (D-020, E09.02 §8, commercial gate).
9. The P6 gate on the words 'unlimited' and 'no seats' is explicitly re-evaluated against current code and the outcome recorded, not assumed from the stale P6 text.
10. Collaboration copy never states or implies the venue sees collaborators, members, invitees or any per-couple row (E09.01 §6.2, P9).
11. Venue branding is the venue's NAME ONLY; no string implies a venue logo, badge or venue-written words in the couple's workspace or on a public Timeline (D-027.3, D-011.2, P11).
12. A named prohibited-claims list exists as a standalone artifact written for a venue reader, distributable to 25 untrained venues (R-020).
13. CTA copy carries no urgency or scarcity theatre (P13); the 14-day hold is stated once, plainly, as a fact.
14. No couple-facing string contains a price in any form, including 'free', 'complimentary', 'worth', 'value', 'sponsored' (P4, D-001.2, D-001.3).
15. No dietary, allergy, accessibility or health information about a named person in any example the copy references, and no copy says such data flows to the venue (P8, R-017).
16. No em dashes and no exclamation marks in finalised front-facing copy (E09.10 §2, BRAND.md §3).
17. Every number, term, date and promise traces to a cited ratified DECISIONS.md entry (E09.10 §10 check 6).
18. The negative half travels with the positive half: every claim about what a venue sees states, in the same block and at the same weight, what it does not (E09.10 §4).
19. The copy resolves none of the founder-open items (E09.02 §9.1–§9.10, E09.10 §9 OQ-1..OQ-3) by asserting an answer.
20. Metric sentences use only E09.02 §8 sanctioned forms and the 'invitations redeemed' denominator label, never 'your couples' (P9).
21. All eight named blocks have finalised copy, or a cited pointer to finalised copy in a Done document. No block is empty.
22. Every price string reads "including VAT at the prevailing rate", never "+ VAT", "ex VAT" or a monthly equivalent (D-021, P12).
23. Every founding-rate string is conditional on continuous renewal. No permanence word anywhere in the system or the pack (D-009.3, R-008).
24. No entitlement string carries a number, a seat, an allotment, a remaining count or per-couple maths the venue must supply (D-020, P5).
25. The D-020 survival sentence appears unprompted wherever the agreement's end is described.
26. No string states or implies a venue's logo, mark, colour or own words appear in the couple's workspace (D-027 point 3).
27. No string states or implies legal approval, solicitor review, accountant verification or unqualified GDPR compliance (D-016, P2, P3).
28. Every access-term string carries the grace rule. Bare "18 months" appears nowhere (P7).
29. The venue-facing list of what a venue can see contains exactly three items and no fourth (D-001.4).
30. A named prohibited-claims list exists, is venue-distributable, and is the same list the automated sweep enforces (R-020).
31. The sweep covers the venue-distributable copy pack, not only Signal Studio's own strings.
32. Every number, term, date and promise traces to a ratified decision.
33. No front-facing string contains an em dash or an exclamation mark (BRAND.md section 3).
34. Every block passes E09.10 section 10's six checks.

**Evidence**
- studio/src/app/venues/page.tsx:15,55,61,67,73,86 — P7 violated in four places on the live offer page: bare '18 months' in the OG description, in four rendered activation-register rows, and in a section title
- studio/src/app/venues/page.tsx:469 with studio/src/app/hq/entitlements/OnboardVenueForm.tsx:33 — 'No seats. No per-couple maths.' ships while the P6 gate is unresolved, and P6's stated basis (form defaults to 10) is stale — the form now defaults to 'unlimited'
- studio/src/app/venues/page.tsx:117-118 — The page promises 'Private couple work never appears unless the couple separately consents to a specific field' — a fourth visible thing beyond D-001.4's three, and a capability D-027 point 4 says stays unwired at launch
- studio/docs/execution/venue-edition-and-films/evidence/E09.10-copy-hierarchy.md:5 — The governing contract's own 'Blocks E09.11' list omits 'collaboration', and grep -i collaborat over the whole file returns zero hits
- studio/docs/strategy/VENUE_FAQ_OBJECTIONS.md:32 — The only FAQ artifact carries the banned word 'seamless' and answers the privacy question with a fourth visible thing ('the venue sees what the couple chooses to share: timeline, updates, and the artefacts')
- studio/docs/execution/venue-edition-and-films/RAID.md:378-384 — R-020 is open, names E09.11 in its affects list, and its mitigation requires a prohibited-claims sheet that does not exist
- node scripts/check-venue-edition-contract.mjs → '[venue-edition-contract] ok', exit 0 — The committed sweep passes, but its copy scope is only two files (lines 49-50: src/app/venues/page.tsx and src/components/hq/marketing-deck.tsx) out of 297 lines
- studio/docs/execution/venue-edition-and-films/evidence/E11.09-10-discovery-and-demo.md:856 — F-5 records that no Keepsake surface exists in app/src, so the Keepsake block of E09.11 has no demonstrable form
- studio/docs/execution/venue-edition-and-films/evidence/E09.11-sweep-run.txt — The extended sweep: 22 surfaces, 15 rules, 85 patterns, 25 hits, 4 citations, exit 0. Before the extension it was 2 copy surfaces, 4 patterns, 0 hits reported. Nothing was fixed to produce 25; the 25 were already there.
- studio/docs/execution/venue-edition-and-films/evidence/E09.11-copy-system.md section 2 — The audit table, one row per block, run before anything was written. Three of eight blocks empty or wrong, five already good.
- studio/docs/execution/venue-edition-and-films/evidence/E09.11-copy-system.md section 6 — The collaboration block, written from nothing. grep -i collaborat over the whole of E09.10 returns zero hits. Four refusals each grounded in verified product behaviour: the plaintext share token (E03.01:250-257), workspace-level publication (queries.ts:181 plus public-task.ts), /p being crawlable (robots.ts disallows /app /s /share /redeem /welcome /api but not /p, and /p/[slug] sets no noindex), 
- studio/docs/execution/venue-edition-and-films/evidence/E09.11-copy-system.md section 12.5 — The removed citation heuristic. A quoted match on a line carrying 'no' or 'never' was treated as a citation. It suppressed two real defects on the live venues page, one of them the fourth-visible-thing sentence at line 118. Replaced with four explicit citations keyed by substring, so a reworded line loses its exemption.
- studio/docs/execution/venue-edition-and-films/evidence/E09.11-venue-copy-pack.md — R-020's venue-distributable pack exists, with the named prohibited-claims list rendered from the same JSON the gate enforces. 31 blockquote lines, zero em dashes in the file.
- studio/scripts/check-venue-edition-contract.mjs (the [venue-copy] stage) — The extension is committed code, not a description. Two tiers, per-surface pinned counts, bidirectional ratchet (a count going down also fails, with a message saying to lower the pin), citation counts printed on every run. Exit 0.
- Specification: `docs/execution/venue-edition-and-films/evidence/E09.11-copy-system.md`

### `E09.12` Freeze the capture copy and build a one-action demo reset that restores the canonical state.

Executor: claude_code · critical path · in review since 2026-08-03

**Acceptance criteria**
1. A frozen capture-copy manifest enumerates every string visible in any capture surface, each with file:line, and states the freeze date 2026-08-21 (D-008).
2. Every frozen string passes all six checks of E09.10 §10, including the never-list sweep.
3. The demo venue is 'Glenmara House' and the couple are 'Mara and Finn' on every capture surface (D-012.1).
4. One wedding date across every capture surface; the fixture conflict is resolved to a single value and recorded as a determination.
5. One action restores the canonical demo state for all four products; the exact invocation is documented and runnable.
6. The reset is deterministic: two runs, or captures on two different days, produce byte-identical visible copy and identical relative dates. Every demo clock is pinned.
7. The reset restores the three in-memory fixture products by fixture-source state, not by seeding a database, and covers the DB-backed product by its approved path.
8. The reset does not wrap, bypass, weaken or re-implement the production gate in seed-wedding.ts.
9. The reset cannot mutate real user data and refuses to run against a non-demo database.
10. The existing contract and drift tests still pass: the suite-navigation-contract literal regex over notes-demo source, and reviewToday === PINNED_REVIEW_CALENDAR_FRAME.today.
11. E02.01, E02.03, E02.12 and E03.09 are verified Done with founder sign-off recorded in PROJECT_STATE.json.
12. No capture surface contains dietary, allergy or health information about a named person (P8, R-017).
13. No capture surface implies a venue logo or venue-written words inside the couple's workspace (D-027.3).
14. No capture surface shows seats, allotment, codes remaining, or any price on a couple-facing screen.
15. The freeze is enforced, not declared: a check fails if a frozen string changes without the manifest changing.
16. One command restores the canonical Glenmara House / Mara-and-Finn state.
17. The same command produces the same state every time.
18. The determinism proof is the reset run twice and diffed, with the real diff output reported.
19. The determinism check can fail, proven.
20. The reset covers all four products.
21. The reset covers the portal.
22. The demo/review safety invariant is not weakened; no real rows are written on the demo path.
23. seed-wedding.ts's --i-understand-prod gate is neither wrapped nor bypassed.
24. A manifest of every capture-copy string exists, with a hash.
25. A later change to any capture-copy string is detectable.
26. The freeze does not silently freeze a defect.
27. The freeze is not declared.

**Evidence**
- node project-control.mjs task E02.01 / E02.03 / E02.12 / E03.09 — All four commercial dependencies are status=done with founderSignOff approved dated 2026-08-03; E09.11 is the sole unmet dependency
- app/src/server/demo/tasks-demo.ts:38 and :198 — `const now = () => Date.now()` feeds updatedAt, so the Tasks demo board is not reproducible across days and defeats any byte-identical capture requirement
- app/scripts/seed-wedding.ts:32-36 — The production gate refuses turso.io/libsql:// unless both --i-understand-prod and an existing --checkpoint are supplied, and explicitly forbids the flag appearing in automation
- app/src/server/suite-navigation-contract.test.mjs:428 — The notes-demo body is pinned as a literal source regex including the dietary sentence, so the P8 fix and the test update must land in one change or the contract test fails
- studio/src/app/venues/page.tsx:187,389 vs app/src/lib/review-suite-fixture.ts:17 vs studio/src/lib/account/fixtures.ts — Three venue names live in one demo story: Glenmara Estate, The Orchard, Glenmara House, against D-012.1
- app/src/lib/calendar-frame.ts:74 · review-suite-fixture.ts reviewToday and lastUpdatedAt · notes-demo.ts:35 · mock-source.ts:134,188 — Four distinct pinned instants plus two live wall-clock reads, with only the calendar-frame/reviewToday pair drift-tested
- app/package.json:48 and listing of app/scripts/ — The only seed entry point is seed:wedding, which needs three arguments and covers Tasks alone; no reset script exists anywhere
- studio/src/lib/account/instrumentation/freeze.ts and studio/scripts/check-venue-edition-contract.mjs — A content-hash freeze pattern and a string-sweep harness already exist and can carry the E09.12 enforcement rather than being written from scratch
- studio/docs/execution/venue-edition-and-films/evidence/E09.12-reset-determinism-run.txt — The reset run twice and diffed, verbatim. Run A ambient clock TZ=UTC, run B clock +400 days TZ=Pacific/Kiritimati, diff produced no output. Plus two negative controls, one of which shows a single fixture edit moving four surfaces.
- studio/docs/execution/venue-edition-and-films/evidence/E09.12-freeze-readiness-run.txt — 11 blocked strings by surface, rule and line, text withheld. Includes the seventh P8 site at roadmap/demo-data.ts:201, previously uncatalogued.
- studio/docs/execution/venue-edition-and-films/evidence/copy/capture-copy-manifest.v1.json — 332 strings across 14 surfaces, hashed. Content addressing also proves the generated files still mirror their sources: hash ae354f0314f74bef appears at both templates.generated.ts:108 and templates/wedding-planning-workspace/tasks.ts:69, and 9da98d53a4f51469 at both roadmap/demo-data.ts:201 and templates/.../roadmap.ts:52.
- studio/docs/execution/venue-edition-and-films/evidence/E09.12-freeze-and-reset.md section 3.4 — Three filter mistakes recorded, all wrong in the same direction. A stopword requirement dropped 'Collect final dietary notes'. A 12-character floor dropped entitlement: "18 months", on screen four times in the portal mock. String-literal-only extraction missed 'Compliments of {sponsorName}', which would have frozen everything except the one line D-027 point 3 is about.
- Verification run, both repositories — studio: check-venue-edition-contract ok, check-capture-copy-freeze ok, check-venue-term-parity ok, check-content-truth ok, check-product-marketing-contract ok, check-suite-switcher-contract ok, check-chrome-contract ok, check-loading-contract ok, check-entitlements-migrations ok. app: demo-clock-drift 9/9 pass, demo-determinism clean plus self-test pass, sync:templates:check clean, demo-reset self
- Specification: `docs/execution/venue-edition-and-films/evidence/E09.12-freeze-and-reset.md`

## Your decision

Approve all of the above in one command:

```bash
node studio/docs/execution/venue-edition-and-films/tools/project-control.mjs approve-batch "your approval note" E09.03 E09.04 E09.05 E09.07 E09.08 E09.09 E09.11 E09.12
```

Or push back on any single one:

```bash
node studio/docs/execution/venue-edition-and-films/tools/project-control.mjs reject E09.03 "what is wrong"
```


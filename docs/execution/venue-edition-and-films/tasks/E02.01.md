# E02.01 — Ratify the single standard offer at €1,500 annually prepaid and the first 25 founding agreements at €1,000 annually prepaid.

**Task ID:** E02.01 · **Epic:** E02 — Commercial offer and Founding 25
**Status:** Founder Review · **Executor:** claude_code · **Approver:** Ethan McNamara
**Spec written:** 2026-08-03 (WP-10)

## Objective
Write the ratified two-tier commercial position into every place that records or publishes it, so the record and the product agree.

## Business reason
Twenty-five venues cannot be sold an offer the company's own systems contradict. Until this lands, the live page, the machine contract and the HQ decision record each state a different price.

## Intended outcome
One position, stated identically in the HQ decision record, the machine contract, the code constants, the strategy documents and the live page. I-002 closes.

## Deliverables
- [x] `content/hq/decisions/venue-edition-founding-25-2026-08-03.md`, status Active
- [x] `content/hq/decisions/venue-edition-fixed-price-2026-07-11.md` marked Superseded, body untouched
- [x] `contracts/commercial-terms.v2.json` + `app/src/lib/commercial-terms.v2.json` fixture
- [x] `src/lib/commercial-terms.ts` reads v2
- [x] `docs/strategy/VENUE_EDITION_STRATEGY.md` amended
- [x] `src/app/venues/page.tsx` prepared, NOT deployed
- [x] `scripts/check-venue-edition-contract.mjs` catches the retired position

## Scope
The commercial record and its machine representation. The price constants in `src/lib/venue-edition.ts` were handed to WP-01 to avoid two sessions editing one file.

## Non-goals
Contract drafting (WP-04). Outreach sequences (WP-11). The entitlement code representation (WP-01, R-016). Deploying the live page.

## Dependencies
| Dependency | State | Waived? |
|---|---|---|
| D-009, D-020, D-021 | approved | no |
| WP-01 price constants in venue-edition.ts | landed in working tree, uncommitted | no |

## Implementation approach
Supersede rather than rewrite. New dated HQ decision; new contract version alongside v1; targeted amendments to strategy docs that keep the reasoning and replace the facts.

## Acceptance criteria
1. Exactly one HQ pricing decision is Active, and it states €1,500 standard and €1,000 for the Founding 25, both VAT-inclusive.
2. The machine contract carries foundingCohortSize 25 and a €1,000 founding rate, and is byte-identical to the app fixture.
3. No file in the gated commercial-source list contains the retired 15-venue cohort or any permanence wording.
4. `pnpm typecheck`, `pnpm build` and `pnpm test` pass.

## Validation plan
`node scripts/check-venue-edition-contract.mjs`, `node scripts/check-suite-contract-consumers.mjs`, `node scripts/check-content-truth.mjs`, `npx tsc --noEmit`, `pnpm build`, `pnpm test`.

## Evidence required
Command output for each check, the /venues diff, and the rendered page text.

## Risks
R-022 (VAT-inclusive under a permanent lock) is accepted, not mitigated. R-008 (permanence wording) is now enforced mechanically.

## Decisions required
None. Ratified in D-009 and D-021.

---
## Founder sign-off
- [ ] Acceptance criteria met
- [ ] Evidence reviewed
- [ ] Verification passed
- [ ] Approved

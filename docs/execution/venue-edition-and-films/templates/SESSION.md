# Session <id> — <YYYY-MM-DD>

> Session records are normally written by
> `project-control.mjs session close`. This template is for a session that has
> to be recorded by hand — for example one that ended without a clean close.
>
> Filename: `sessions/YYYY-MM-DD--<short-session-id>.md`. Use the Claude session
> ID where available. **Append-only: never rewrite a closed session.**

**Objective:**
**Opened:**
**Closed:**

## Summary

What actually happened, in plain English.

## Tasks touched

- `<ID>` <title> — now **<status>**

## Files inspected

Paths that mattered, so the next session does not re-derive them.

## Decisions made

Decisions taken this session and their `DECISIONS.md` IDs. If a decision was
taken without a D-record, add the record now.

## Changes completed

## Tests and verification

Commands run and their results. Failures are recorded as failures.

## Evidence created

- `<ID>` <ref>

## Blockers found

## Status changes

- `<ID>` <from> → <to> (<by>) — <note>

## Founder review required

## Next action

One unambiguous action for the next session.

# Durable propagation outbox — 2026-07-11

Tasks now owns a durable `suite_outbox` table and reviewed migration. Events
are versioned, workspace-scoped, traceable, and idempotent by `event_id`. A
bounded cron worker signs delivery envelopes, retries failed rows on the next
run, and marks delivery only after every configured consumer acknowledges it.

Receipts:

- Outbox envelope tests: 2/2 pass.
- Worker contract test: 1/1 pass.
- Tasks typecheck: pass.
- Migrations: `0012_suite_outbox.sql`.

Timeline/Signal consumer endpoints, production delivery telemetry, and
provider cron configuration remain open.

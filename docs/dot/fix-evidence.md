# Focused review correction evidence

Corrections respond to F1–F4 in `finish-review.md`. The direction was retained. Browser checks use the local development route and are build-thread evidence, not independent reviewer observations.

- F1: computed desktop targets are 44 × 44 for play, replay, inspector/theme buttons and all three colour swatches; tabs and checkbox labels have 44 px height. The range hit box is 44 px high while the drawn track stays 4 px. Text/reaction buttons, search and file controls have a 44 px minimum. The Dot hit circle is clamped to at least 44 px even during miniature performances; a ResizeObserver supplies its diameter without per-frame layout reads. Mobile transport dimensions and overflow were checked again at 390 px.
- F2: current frame shows a zero-based frame plus count, e.g. `F0001 / 1845`. Neutral reset returned the player to Idle, paused, at `F0000 / 420`. Face and secondary effects have independent switches. After unchecking Face, the stage face group was absent; resetting restored it. The safe-area guide appears as a CSS overlay; stage SVG markup contains no guide, and the exporter receives no guide state. The face setting does enter SVG/PNG/sequence exports and preset metadata.
- F2 reference: the supplied source was selected through the local file chooser. Media source was a `blob:` URL, duration 30.75 s, paused. Stepping the film to frame 1 produced `F0001 / 1845` and reference `currentTime = 0.016666` s. No server upload route exists. The local comparison screenshot stays in the user's delivery packet, outside this public repository. Closing the inspector revokes the URL.
- F3: with the inspector open, Idle → wink → film showed full loop ends 420 → 144 → 1845. Each default Set loop action was exercised. Clip changes clear the prior status and reset the form, including a return to a previously edited clip; ordinary frame/playback updates preserve intentional form edits. Fractional frame entries are rejected.
- F4: the Good company specimen now includes its satellite layer. Its static SVG contains four circles (body plus three companions), with two companions visible beside the body at the poster phase. The same desktop/mobile performance shelf was recaptured.

Revised screenshots replace the same four review files: desktop, mobile, user-1280 and inspector. All are from scrollY 0. Source reference imagery is not included in those public screenshots. Test/typecheck/lint and standard Turbopack production build were rerun after these corrections. The original webpack attempt's unrelated CSS-module failure was bypassed by restoring normal independent dependencies and passing the standard build.
